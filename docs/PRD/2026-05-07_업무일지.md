# 업무일지

## 기본 정보
- 날짜: 2026-05-07 (수)
- 작성자: hexsera

## 진행 목표

JWT 인증 3단계 구현: 페이지 로드 시 여러 API 요청이 동시에 401을 받을 때 `/auth/refresh`가 중복 호출되지 않도록 동시 401 큐잉 처리 추가.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| 동시 401 큐잉 로직 추가 | `frontend/src/lib/api.js` | `isRefreshing` 플래그 + `failedQueue` 도입 |
| 프론트엔드 빌드 및 배포 | `frontend/dist/` → `nginx-server` 컨테이너 | `docker cp`로 nginx에 반영 |

**총 약 30분**

## 주요 변경 사항

### 1. frontend/src/lib/api.js 수정

**변경 전:**
```javascript
import axios from 'axios';
import { useAuthStore } from '../store/authStore';

const api = axios.create({ baseURL: '/api/v1' });

api.interceptors.request.use((config) => {
  const token = useAuthStore.getState().accessToken;
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

api.interceptors.response.use(
  (res) => res,
  async (error) => {
    const original = error.config;
    if (error.response?.status === 401 && !original._retry) {
      original._retry = true;
      try {
        const { data } = await axios.post('/api/v1/auth/refresh', {}, { withCredentials: true });
        useAuthStore.getState().setAccessToken(data.access_token);
        original.headers.Authorization = `Bearer ${data.access_token}`;
        return api(original);
      } catch {
        useAuthStore.getState().clearAccessToken();
        window.location.href = '/login';
      }
    }
    return Promise.reject(error);
  }
);

export default api;
```

**변경 후:**
```javascript
import axios from 'axios';
import { useAuthStore } from '../store/authStore';

const api = axios.create({ baseURL: '/api/v1' });

let isRefreshing = false;
let failedQueue = [];

const processQueue = (error, token = null) => {
  failedQueue.forEach((prom) => {
    if (error) prom.reject(error);
    else prom.resolve(token);
  });
  failedQueue = [];
};

api.interceptors.request.use((config) => {
  const token = useAuthStore.getState().accessToken;
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

api.interceptors.response.use(
  (res) => res,
  async (error) => {
    const original = error.config;
    if (error.response?.status !== 401 || original._retry) {
      return Promise.reject(error);
    }

    if (isRefreshing) {
      return new Promise((resolve, reject) => {
        failedQueue.push({ resolve, reject });
      })
        .then((token) => {
          original.headers.Authorization = `Bearer ${token}`;
          return api(original);
        })
        .catch((err) => Promise.reject(err));
    }

    original._retry = true;
    isRefreshing = true;

    try {
      const { data } = await axios.post('/api/v1/auth/refresh', {}, { withCredentials: true });
      useAuthStore.getState().setAccessToken(data.access_token);
      processQueue(null, data.access_token);
      original.headers.Authorization = `Bearer ${data.access_token}`;
      return api(original);
    } catch (refreshError) {
      processQueue(refreshError, null);
      useAuthStore.getState().clearAccessToken();
      window.location.href = '/login';
      return Promise.reject(refreshError);
    } finally {
      isRefreshing = false;
    }
  }
);

export default api;
```

**변경 의도:** 동시에 여러 401이 발생했을 때 `/auth/refresh`가 중복 호출되는 문제 방지. 첫 번째 401만 갱신을 수행하고 나머지는 큐에 대기시킨 후, 갱신 완료 시 새 토큰으로 일괄 재시도한다. 4단계 Refresh Token Rotation 도입의 전제 조건이기도 하다.

## 동시 401 처리 흐름

```
여러 요청이 동시에 401 수신
         │
         ▼
  첫 번째 401?
  ┌───────┴───────┐
  YES             NO (isRefreshing=true)
  │               │
  ▼               ▼
isRefreshing    failedQueue에 등록
= true          (Promise로 대기)
  │
  ▼
/auth/refresh 호출
  │
  ┌───────┴───────┐
 성공             실패
  │               │
  ▼               ▼
processQueue    processQueue
(새 토큰 전달)  (에러 전달)
  │               │
  ▼               ▼
큐 대기 요청    큐 대기 요청
일괄 재시도     전부 실패 처리
                  │
                  ▼
              /login 리다이렉트
```

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| API 요청 인증 (Access Token 자동 부착) | 변경 없음 (요청 인터셉터 그대로) | 정상 |
| 401 수신 시 자동 토큰 갱신 | 동시 401 큐잉 로직 추가 | 정상 |
| 갱신 실패 시 로그아웃 처리 | 큐에 대기 중인 요청들도 일괄 실패 처리 후 /login 이동 | 미확인 |

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- 4단계: Refresh Token Rotation (백엔드 — refresh 호출마다 새 Refresh 발급, 옛 Refresh 무효화)
- 5단계: 새로고침 대응 정리 (localStorage Access Token 의존 제거)
