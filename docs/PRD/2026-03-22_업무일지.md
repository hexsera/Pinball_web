# 업무일지

## 기본 정보
- 날짜: 2026-03-22 (일)
- 작성자: hexsera

## 진행 목표
Google OAuth 2.0 Authorization Code Flow를 사용하여 구글 계정으로 로그인하는 기능 추가. Access Token이 브라우저에 노출되지 않는 서버-서버 토큰 교환 방식으로 구현.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| @react-oauth/google 패키지 설치 | `frontend/package.json` | npm install로 설치 (v0.13.4) |
| GoogleOAuthProvider 앱 래핑 | `frontend/src/main.jsx` | 앱 최상위에 Provider 추가 |
| 구글 로그인 버튼 및 �핸들러 추가 | `frontend/src/pages/Login/Login.jsx` | 로그인 버튼과 회원가입 버튼 사이에 배치 |
| Authlib 패키지 추가 | `backend/requirements.txt` | Authlib==1.6.9 |
| Google OAuth 환경변수 설정 추가 | `backend/app/core/config.py` | GOOGLE_CLIENT_ID/SECRET/REDIRECT_URI |
| GoogleLoginRequest 스키마 추가 | `backend/app/schemas/user.py` | Authorization Code 수신용 |
| /auth/google 엔드포인트 추가 | `backend/app/api/v1/auth.py` | Token 교환 → userinfo 조회 → DB 확인 |
| Google OAuth 환경변수 입력 | `backend/.env` | CLIENT_ID, SECRET, REDIRECT_URI=postmessage |
| .env.example 업데이트 | `backend/.env.example` | Google OAuth 플레이스홀더 추가 |
| Docker 재빌드 및 테스트 | - | fastapi 컨테이너 재빌드 후 pytest 실행 |

**총 약 1시간**

## 주요 변경 사항

### 1. frontend/src/main.jsx 수정

**변경 전:**
```jsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.jsx';

const rootElement = document.getElementById('root');
const root = ReactDOM.createRoot(rootElement);

root.render(<App />);
```

**변경 후:**
```jsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import { GoogleOAuthProvider } from '@react-oauth/google';
import App from './App.jsx';

const rootElement = document.getElementById('root');
const root = ReactDOM.createRoot(rootElement);

root.render(
  <GoogleOAuthProvider clientId={import.meta.env.VITE_GOOGLE_CLIENT_ID}>
    <App />
  </GoogleOAuthProvider>
);
```

**변경 의도:** 앱 전체에서 Google OAuth를 사용할 수 있도록 최상위 컴포넌트를 Provider로 감싼다. `VITE_GOOGLE_CLIENT_ID`는 `frontend/.env`에 설정된 값.

---

### 2. frontend/src/pages/Login/Login.jsx 수정

**변경 전:**
```jsx
import { useState } from 'react';
import { Box, TextField, Button, Container, Typography, Paper, Alert, CardMedia} from '@mui/material';
import { useNavigate } from 'react-router-dom';
import { useAuth } from '../../contexts/AuthContext';
import axios from 'axios';
import Aurora from '../../components/Aurora/Aurora';

// ...

function Login() {
  const [username, setUsername] = useState('');
  const [password, setPassword] = useState('');
  const [loginError, setLoginError] = useState(false);
  const navigate = useNavigate();
  const { login } = useAuth();

  // 확인 버튼을 눌렀을 때 실행되는 함수
  const handleLogin = async () => { ... };

  return (
    // ...
    <Box sx={{ display: 'flex', justifyContent: 'center', mt: 3 }}>
      <Button
        variant="outlined"
        sx={{ width: '300px', borderColor: COLORS.text, color: COLORS.text }}
        onClick={()=>navigate('/Register')}
      >
        회원가입
      </Button>
    </Box>
    // ...
  );
}
```

**변경 후:**
```jsx
import { useState } from 'react';
import { Box, TextField, Button, Container, Typography, Paper, Alert, CardMedia} from '@mui/material';
import { useNavigate } from 'react-router-dom';
import { useAuth } from '../../contexts/AuthContext';
import { useGoogleLogin } from '@react-oauth/google';
import axios from 'axios';
import Aurora from '../../components/Aurora/Aurora';

// ...

function Login() {
  const [username, setUsername] = useState('');
  const [password, setPassword] = useState('');
  const [loginError, setLoginError] = useState(false);
  const navigate = useNavigate();
  const { login } = useAuth();

  const googleLogin = useGoogleLogin({
    flow: 'auth-code',
    onSuccess: async (codeResponse) => {
      try {
        const res = await axios.post('/api/v1/auth/google', {
          code: codeResponse.code,
        });
        if (res.status === 200) {
          login({
            id: res.data.user_id,
            name: res.data.nickname,
            role: res.data.role,
            email: res.data.email,
          });
          navigate(res.data.role === 'admin' ? '/admin' : '/');
        }
      } catch (error) {
        setLoginError(true);
        console.log('구글 로그인 실패:', error.response?.data?.detail);
      }
    },
    onError: () => {
      setLoginError(true);
    },
  });

  // 확인 버튼을 눌렀을 때 실행되는 함수
  const handleLogin = async () => { ... };

  return (
    // ...
    {/* 구글 로그인 버튼 - 로그인 버튼과 회원가입 버튼 사이에 추가 */}
    <Box sx={{ display: 'flex', justifyContent: 'center', mt: 2 }}>
      <Button
        variant="outlined"
        sx={{
          width: '300px',
          borderColor: '#4285F4',
          color: COLORS.text,
          textTransform: 'none',
          fontSize: '0.95rem',
        }}
        onClick={() => googleLogin()}
      >
        <img
          src="https://developers.google.com/identity/images/g-logo.png"
          alt="Google"
          style={{ width: 20, height: 20, marginRight: 10 }}
        />
        구글로 로그인
      </Button>
    </Box>
    <Box sx={{ display: 'flex', justifyContent: 'center', mt: 2 }}>
      <Button
        variant="outlined"
        sx={{ width: '300px', borderColor: COLORS.text, color: COLORS.text }}
        onClick={()=>navigate('/Register')}
      >
        회원가입
      </Button>
    </Box>
    // ...
  );
}
```

**변경 의도:** `flow: 'auth-code'` 설정으로 Authorization Code Flow를 사용. Access Token이 브라우저에 전달되지 않고 백엔드에서만 처리됨. 성공 시 기존 `login()` 함수를 재사용하여 localStorage 저장 및 페이지 이동 처리.

---

### 3. backend/requirements.txt 수정

**변경 전:**
```
pwdlib[bcrypt]
```

**변경 후:**
```
pwdlib[bcrypt]
Authlib==1.6.9
```

**변경 의도:** Authlib의 `AsyncOAuth2Client`로 구글과의 Token 교환 및 userinfo 조회를 처리하기 위해 추가.

---

### 4. backend/app/core/config.py 수정

**변경 전:**
```python
class Settings:
    """애플리케이션 설정"""
    DATABASE_URL: str = os.getenv("DATABASE_URL")
    API_KEY: str = os.getenv("API_KEY")
    ADMIN_EMAIL: str = os.getenv("ADMIN_EMAIL")
    ADMIN_NICKNAME: str = os.getenv("ADMIN_NICKNAME")
    ADMIN_PASSWORD: str = os.getenv("ADMIN_PASSWORD")
    ADMIN_BIRTH_DATE: str = os.getenv("ADMIN_BIRTH_DATE")
    GEMINI_API_KEY: str = os.getenv("GEMINI_API_KEY")

settings = Settings()
```

**변경 후:**
```python
class Settings:
    """애플리케이션 설정"""
    DATABASE_URL: str = os.getenv("DATABASE_URL")
    API_KEY: str = os.getenv("API_KEY")
    ADMIN_EMAIL: str = os.getenv("ADMIN_EMAIL")
    ADMIN_NICKNAME: str = os.getenv("ADMIN_NICKNAME")
    ADMIN_PASSWORD: str = os.getenv("ADMIN_PASSWORD")
    ADMIN_BIRTH_DATE: str = os.getenv("ADMIN_BIRTH_DATE")
    GEMINI_API_KEY: str = os.getenv("GEMINI_API_KEY")

    # Google OAuth
    GOOGLE_CLIENT_ID: str = os.getenv("GOOGLE_CLIENT_ID", "")
    GOOGLE_CLIENT_SECRET: str = os.getenv("GOOGLE_CLIENT_SECRET", "")
    GOOGLE_REDIRECT_URI: str = os.getenv("GOOGLE_REDIRECT_URI", "postmessage")

settings = Settings()
```

**변경 의도:** Google OAuth 자격증명을 환경변수에서 로드. `GOOGLE_REDIRECT_URI` 기본값을 `postmessage`로 설정 (팝업 방식 고정값).

---

### 5. backend/app/schemas/user.py 수정

**변경 전:**
```python
class DeleteResponse(BaseModel):
    """삭제 응답"""
    message: str
    deleted_user_id: int
```

**변경 후:**
```python
class DeleteResponse(BaseModel):
    """삭제 응답"""
    message: str
    deleted_user_id: int


class GoogleLoginRequest(BaseModel):
    """구글 로그인 요청: 프론트엔드에서 전송하는 Authorization Code"""
    code: str
```

**변경 의도:** 프론트엔드에서 전송하는 Authorization Code를 받는 요청 스키마 추가.

---

### 6. backend/app/api/v1/auth.py 수정

**변경 전:**
```python
from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.orm import Session

from app.api.deps import get_db
from app.core.security import hash_password, verify_password
from app.schemas.user import (
    UserRegisterRequest,
    UserResponse,
    LoginRequest,
    LoginResponse,
)

import sys
sys.path.insert(0, '/code')
from models import User

router = APIRouter()


@router.post("/login", response_model=LoginResponse)
def login(login_request: LoginRequest, db: Session = Depends(get_db)):
    ...


@router.post("/register", response_model=UserResponse, status_code=status.HTTP_201_CREATED)
def register_user(user: UserRegisterRequest, db: Session = Depends(get_db)):
    ...
```

**변경 후:**
```python
from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.orm import Session
from authlib.integrations.httpx_client import AsyncOAuth2Client

from app.api.deps import get_db
from app.core.config import settings
from app.core.security import hash_password, verify_password
from app.schemas.user import (
    UserRegisterRequest,
    UserResponse,
    LoginRequest,
    LoginResponse,
    GoogleLoginRequest,
)

import sys
sys.path.insert(0, '/code')
from models import User

router = APIRouter()


@router.post("/login", response_model=LoginResponse)
def login(login_request: LoginRequest, db: Session = Depends(get_db)):
    ...


@router.post("/auth/google", response_model=LoginResponse)
async def google_login(data: GoogleLoginRequest, db: Session = Depends(get_db)):
    """구글 로그인: Authorization Code → Token 교환 → userinfo 조회 → DB 사용자 확인"""

    # 1단계: Authorization Code → Access Token 교환
    async with AsyncOAuth2Client(
        client_id=settings.GOOGLE_CLIENT_ID,
        client_secret=settings.GOOGLE_CLIENT_SECRET,
    ) as client:
        token = await client.fetch_token(
            "https://oauth2.googleapis.com/token",
            code=data.code,
            redirect_uri=settings.GOOGLE_REDIRECT_URI,
            grant_type="authorization_code",
        )

    # 2단계: Access Token으로 구글 userinfo 조회
    async with AsyncOAuth2Client(token=token) as client:
        resp = await client.get("https://www.googleapis.com/oauth2/v3/userinfo")

    if resp.status_code != 200:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Failed to get Google user info"
        )

    google_user = resp.json()

    # 3단계: DB에서 이메일로 사용자 조회
    user = db.query(User).filter(User.email == google_user["email"]).first()
    if not user:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="No account found for this Google email. Please register first."
        )

    return LoginResponse(
        message="Login successful",
        user_id=user.id,
        email=user.email,
        nickname=user.nickname,
        role=user.role
    )


@router.post("/register", response_model=UserResponse, status_code=status.HTTP_201_CREATED)
def register_user(user: UserRegisterRequest, db: Session = Depends(get_db)):
    ...
```

**변경 의도:** Authorization Code Flow 전체 처리. Authlib `AsyncOAuth2Client`가 구글 token 엔드포인트에 POST 전송, Access Token 발급 후 userinfo 조회. DB에 없는 이메일은 401 반환 (자동 가입 없음).

---

### 7. backend/.env 수정

**변경 내용:** Google OAuth 관련 항목 추가 및 `GOOGLE_REDIRECT_URI` 값 설정

```
# 추가된 항목
GOOGLE_CLIENT_ID=***
GOOGLE_CLIENT_SECRET=***
GOOGLE_REDIRECT_URI=postmessage
```

**변경 의도:** 팝업 방식 로그인에서 구글이 요구하는 `redirect_uri` 고정값은 `postmessage`.

---

### 8. backend/.env.example 수정

**변경 전:**
```
ADMIN_EMAIL=admin@example.com
ADMIN_NICKNAME=admin
ADMIN_PASSWORD=your_admin_password
ADMIN_BIRTH_DATE=2000-01-01
```

**변경 후:**
```
ADMIN_EMAIL=admin@example.com
ADMIN_NICKNAME=admin
ADMIN_PASSWORD=your_admin_password
ADMIN_BIRTH_DATE=2000-01-01

# Google OAuth 2.0
GOOGLE_CLIENT_ID=<your-google-client-id>
GOOGLE_CLIENT_SECRET=<your-google-client-secret>
GOOGLE_REDIRECT_URI=postmessage
```

**변경 의도:** 신규 개발자가 환경변수를 설정할 수 있도록 플레이스홀더 추가.

---

## 요청 흐름

```
[사용자]
   │ "구글로 로그인" 버튼 클릭
   ▼
[Frontend: @react-oauth/google]
   │ 구글 계정 선택 팝업
   ▼
[Google 서버]
   │ Authorization Code 발급
   ▼
[Frontend]
   │ POST /api/v1/auth/google { code }
   ▼
[Backend: FastAPI /auth/google]
   │ POST https://oauth2.googleapis.com/token
   ▼
[Google 서버]
   │ Access Token 반환
   ▼
[Backend: FastAPI]
   │ GET https://www.googleapis.com/oauth2/v3/userinfo
   ▼
[Google 서버]
   │ { sub, email, name }
   ▼
[Backend: FastAPI]
   │ DB에서 email 조회 → 없으면 401
   ▼
[PostgreSQL]
   │ LoginResponse 반환
   ▼
[Frontend]
   │ localStorage 저장 → 홈(/) 이동
   ▼
[사용자]
```

> Access Token은 백엔드(5~8번 구간)에서만 존재. 브라우저에 노출되지 않음.

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| 구글 로그인 | 신규 구현 | 정상 (동작 확인) |
| 기존 이메일/비밀번호 로그인 | 변경 없음 | 정상 (pytest 15개 통과) |
| 회원가입 | 변경 없음 | 정상 |

## 결과
- 상태: 완료
- pytest: 15 passed, 1 failed (기존 Gemini mock 테스트 — 이번 작업과 무관)

## 배운 내용

## 다음 작업
- 구글 계정으로 신규 가입 기능 추가 (현재는 기존 가입된 이메일만 로그인 가능)

---

# 업무일지 (2차)

## 기본 정보
- 날짜: 2026-03-22 (일)
- 작성자: hexsera

## 진행 목표
JWT 액세스 토큰 발급 도입. 일반 로그인 및 구글 로그인 응답에 `access_token`을 추가 발급하고, 구글 최초 로그인 시 자동 회원가입 처리. 향후 API 헤더 인증 전환을 위한 준비 단계.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| python-jose 패키지 추가 | `backend/requirements.txt` | HS256 서명용 |
| JWT 환경변수 추가 | `backend/.env`, `backend/.env.example` | SECRET_KEY, ALGORITHM, EXPIRE_MINUTES |
| JWT 설정 필드 추가 | `backend/app/core/config.py` | Settings 클래스에 3개 필드 추가 |
| models.py DB 동기화 | `backend/models.py` | password nullable=True, auth_provider·google_id 컬럼 추가 |
| create_access_token 함수 추가 | `backend/app/core/security.py` | HS256 서명 JWT 생성 |
| LoginResponse 스키마 확장 | `backend/app/schemas/user.py` | access_token, token_type 필드 추가 |
| 일반 로그인 JWT 발급 | `backend/app/api/v1/auth.py` | login 엔드포인트 반환값 수정 |
| 구글 로그인 자동 가입 + JWT 발급 | `backend/app/api/v1/auth.py` | google_id 기준 조회 → 없으면 자동 생성 |
| Alembic 마이그레이션 | `backend/alembic/versions/4500d95a9077_*.py` | google_id 컬럼 추가 (DB 적용 완료) |
| access_token localStorage 저장 | `frontend/src/pages/Login/Login.jsx` | 일반·구글 로그인 양쪽 적용 |
| 로그아웃 시 access_token 제거 | `frontend/src/contexts/AuthContext.jsx` | 보안 누수 방지 |
| Docker 재빌드 및 기동 확인 | - | 에러 없이 정상 기동 확인 |

**총 약 1시간 30분**

## 주요 변경 사항

### 1. backend/requirements.txt 수정

**변경 전:**
```
Authlib==1.6.9
```

**변경 후:**
```
Authlib==1.6.9
python-jose[cryptography]==3.3.0
```

**변경 의도:** HS256 서명 JWT 생성을 위해 python-jose 추가. `[cryptography]` 옵션은 암호화 백엔드 포함.

---

### 2. backend/.env 수정

**변경 내용:** JWT 관련 항목 추가

```
# 추가된 항목
JWT_SECRET_KEY=***
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60
```

**변경 의도:** 토큰 서명 키와 알고리즘, 만료 시간을 환경변수로 관리.

---

### 3. backend/.env.example 수정

**변경 전:**
```
GOOGLE_REDIRECT_URI=postmessage
```

**변경 후:**
```
GOOGLE_REDIRECT_URI=postmessage

# JWT Authentication
JWT_SECRET_KEY=<your-random-secret-key>
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60
```

**변경 의도:** 신규 개발자가 JWT 환경변수를 빠짐없이 설정할 수 있도록 플레이스홀더 추가.

---

### 4. backend/app/core/config.py 수정

**변경 전:**
```python
class Settings:
    """애플리케이션 설정"""
    DATABASE_URL: str = os.getenv("DATABASE_URL")
    API_KEY: str = os.getenv("API_KEY")
    ADMIN_EMAIL: str = os.getenv("ADMIN_EMAIL")
    ADMIN_NICKNAME: str = os.getenv("ADMIN_NICKNAME")
    ADMIN_PASSWORD: str = os.getenv("ADMIN_PASSWORD")
    ADMIN_BIRTH_DATE: str = os.getenv("ADMIN_BIRTH_DATE")
    GEMINI_API_KEY: str = os.getenv("GEMINI_API_KEY")

    # Google OAuth
    GOOGLE_CLIENT_ID: str = os.getenv("GOOGLE_CLIENT_ID", "")
    GOOGLE_CLIENT_SECRET: str = os.getenv("GOOGLE_CLIENT_SECRET", "")
    GOOGLE_REDIRECT_URI: str = os.getenv("GOOGLE_REDIRECT_URI", "postmessage")

settings = Settings()
```

**변경 후:**
```python
class Settings:
    """애플리케이션 설정"""
    DATABASE_URL: str = os.getenv("DATABASE_URL")
    API_KEY: str = os.getenv("API_KEY")
    ADMIN_EMAIL: str = os.getenv("ADMIN_EMAIL")
    ADMIN_NICKNAME: str = os.getenv("ADMIN_NICKNAME")
    ADMIN_PASSWORD: str = os.getenv("ADMIN_PASSWORD")
    ADMIN_BIRTH_DATE: str = os.getenv("ADMIN_BIRTH_DATE")
    GEMINI_API_KEY: str = os.getenv("GEMINI_API_KEY")

    # Google OAuth
    GOOGLE_CLIENT_ID: str = os.getenv("GOOGLE_CLIENT_ID", "")
    GOOGLE_CLIENT_SECRET: str = os.getenv("GOOGLE_CLIENT_SECRET", "")
    GOOGLE_REDIRECT_URI: str = os.getenv("GOOGLE_REDIRECT_URI", "postmessage")

    # JWT
    JWT_SECRET_KEY: str = os.getenv("JWT_SECRET_KEY", "")
    JWT_ALGORITHM: str = os.getenv("JWT_ALGORITHM", "HS256")
    JWT_EXPIRE_MINUTES: int = int(os.getenv("JWT_EXPIRE_MINUTES", "60"))

settings = Settings()
```

**변경 의도:** JWT 생성에 필요한 3개 설정값을 Settings에 추가.

---

### 5. backend/models.py 수정

**변경 전:**
```python
class User(Base):
    __tablename__ = "users"

    id = Column(Integer, primary_key=True, index=True)
    user_id = Column(UUID(as_uuid=True), unique=True, nullable=True,
                    default=uuid.uuid4, index=True)
    email = Column(String(255), unique=True, index=True, nullable=False)
    nickname = Column(String(100), nullable=False)
    password = Column(String(255), nullable=False)
    birth_date = Column(Date, nullable=False)
    role = Column(String(20), nullable=False, default='user')
```

**변경 후:**
```python
class User(Base):
    __tablename__ = "users"

    id = Column(Integer, primary_key=True, index=True)
    user_id = Column(UUID(as_uuid=True), unique=True, nullable=True,
                    default=uuid.uuid4, index=True)
    email = Column(String(255), unique=True, index=True, nullable=False)
    nickname = Column(String(100), nullable=False)
    password = Column(String(255), nullable=True)   # 구글 가입 시 password 없음
    birth_date = Column(Date, nullable=False)
    role = Column(String(20), nullable=False, default='user')
    auth_provider = Column(String(20), nullable=False, default='local')  # DB에 이미 존재
    google_id = Column(String(255), nullable=True, unique=True)           # 신규 추가
```

**변경 의도:** 실제 DB 상태와 models.py를 동기화. `password`는 구글 가입 시 NULL 허용, `auth_provider`는 이미 DB에 존재하는 컬럼(Alembic 추적 밖), `google_id`는 신규 추가.

---

### 6. backend/app/core/security.py 수정

**변경 전:**
```python
# fastapi/app/core/security.py
from pwdlib import PasswordHash
from pwdlib.hashers.bcrypt import BcryptHasher
from fastapi import HTTPException, Depends
from fastapi.security import APIKeyHeader
from app.core.config import settings

API_KEY_HEADER = APIKeyHeader(name="X-API-Key", auto_error=False)
pwd_context = PasswordHash([BcryptHasher()])


def hash_password(password: str) -> str:
    return pwd_context.hash(password)


def verify_password(plain_password: str, hashed_password: str) -> bool:
    return pwd_context.verify(plain_password, hashed_password)


def verify_api_key(api_key: str = Depends(API_KEY_HEADER)) -> str:
    """API Key 검증 의존성"""
    if api_key is None:
        raise HTTPException(status_code=401, detail="API Key is missing")
    if api_key != settings.API_KEY:
        raise HTTPException(status_code=403, detail="Invalid API Key")
    return api_key
```

**변경 후:**
```python
# fastapi/app/core/security.py
from datetime import datetime, timedelta, timezone
from pwdlib import PasswordHash
from pwdlib.hashers.bcrypt import BcryptHasher
from fastapi import HTTPException, Depends
from fastapi.security import APIKeyHeader
from jose import jwt
from app.core.config import settings

API_KEY_HEADER = APIKeyHeader(name="X-API-Key", auto_error=False)
pwd_context = PasswordHash([BcryptHasher()])


def hash_password(password: str) -> str:
    return pwd_context.hash(password)


def verify_password(plain_password: str, hashed_password: str) -> bool:
    return pwd_context.verify(plain_password, hashed_password)


def create_access_token(data: dict) -> str:
    to_encode = data.copy()
    expire = datetime.now(timezone.utc) + timedelta(minutes=settings.JWT_EXPIRE_MINUTES)
    to_encode.update({"exp": expire})
    return jwt.encode(to_encode, settings.JWT_SECRET_KEY, algorithm=settings.JWT_ALGORITHM)


def verify_api_key(api_key: str = Depends(API_KEY_HEADER)) -> str:
    """API Key 검증 의존성"""
    if api_key is None:
        raise HTTPException(status_code=401, detail="API Key is missing")
    if api_key != settings.API_KEY:
        raise HTTPException(status_code=403, detail="Invalid API Key")
    return api_key
```

**변경 의도:** `create_access_token()` 함수 추가. `sub`, `email`, `role`을 클레임으로 포함하고 UTC 기준 만료시간을 설정하여 HS256으로 서명.

---

### 7. backend/app/schemas/user.py 수정

**변경 전:**
```python
class LoginResponse(BaseModel):
    """로그인 응답"""
    message: str
    user_id: int
    email: str
    nickname: str
    role: str
```

**변경 후:**
```python
class LoginResponse(BaseModel):
    """로그인 응답"""
    message: str
    user_id: int
    email: str
    nickname: str
    role: str
    access_token: str
    token_type: str
```

**변경 의도:** 기존 필드 제거 없이 `access_token`, `token_type` 추가. 프론트엔드 기존 파싱 코드 하위 호환 유지.

---

### 8. backend/app/api/v1/auth.py 수정

**변경 전:**
```python
from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.orm import Session
from authlib.integrations.httpx_client import AsyncOAuth2Client

from app.api.deps import get_db
from app.core.config import settings
from app.core.security import hash_password, verify_password
from app.schemas.user import (
    UserRegisterRequest, UserResponse, LoginRequest, LoginResponse, GoogleLoginRequest,
)

import sys
sys.path.insert(0, '/code')
from models import User

router = APIRouter()


@router.post("/login", response_model=LoginResponse)
def login(login_request: LoginRequest, db: Session = Depends(get_db)):
    """로그인 엔드포인트 (API Key 불필요)"""
    user = db.query(User).filter(User.email == login_request.email).first()
    if user is None:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid email or password")

    if not verify_password(login_request.password, user.password):
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid email or password")

    return LoginResponse(
        message="Login successful",
        user_id=user.id,
        email=user.email,
        nickname=user.nickname,
        role=user.role
    )


@router.post("/auth/google", response_model=LoginResponse)
async def google_login(data: GoogleLoginRequest, db: Session = Depends(get_db)):
    # ... (1·2단계 동일) ...

    google_user = resp.json()

    # 3단계: DB에서 이메일로 사용자 조회
    user = db.query(User).filter(User.email == google_user["email"]).first()
    if not user:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="No account found for this Google email. Please register first."
        )

    return LoginResponse(
        message="Login successful",
        user_id=user.id,
        email=user.email,
        nickname=user.nickname,
        role=user.role
    )
```

**변경 후:**
```python
import uuid
from datetime import date
from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.orm import Session
from authlib.integrations.httpx_client import AsyncOAuth2Client

from app.api.deps import get_db
from app.core.config import settings
from app.core.security import hash_password, verify_password, create_access_token
from app.schemas.user import (
    UserRegisterRequest, UserResponse, LoginRequest, LoginResponse, GoogleLoginRequest,
)

import sys
sys.path.insert(0, '/code')
from models import User

router = APIRouter()


@router.post("/login", response_model=LoginResponse)
def login(login_request: LoginRequest, db: Session = Depends(get_db)):
    """로그인 엔드포인트 (API Key 불필요)"""
    user = db.query(User).filter(User.email == login_request.email).first()
    if user is None:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid email or password")

    if not verify_password(login_request.password, user.password):
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid email or password")

    token = create_access_token({"sub": str(user.id), "email": user.email, "role": user.role})
    return LoginResponse(
        message="Login successful",
        user_id=user.id,
        email=user.email,
        nickname=user.nickname,
        role=user.role,
        access_token=token,
        token_type="bearer"
    )


@router.post("/auth/google", response_model=LoginResponse)
async def google_login(data: GoogleLoginRequest, db: Session = Depends(get_db)):
    # ... (1·2단계 동일) ...

    google_user = resp.json()

    # 3단계: google_id(sub) 기준으로 사용자 조회 → 없으면 자동 가입
    user = db.query(User).filter(User.google_id == google_user["sub"]).first()

    if not user:
        existing = db.query(User).filter(User.email == google_user["email"]).first()
        if existing:
            raise HTTPException(
                status_code=status.HTTP_400_BAD_REQUEST,
                detail="Email already registered with a different provider"
            )
        user = User(
            email=google_user["email"],
            nickname=google_user.get("name", google_user["email"].split("@")[0]),
            password=None,
            birth_date=date.today(),
            role="user",
            google_id=google_user["sub"],
            auth_provider="google"
        )
        db.add(user)
        db.commit()
        db.refresh(user)

    token = create_access_token({"sub": str(user.id), "email": user.email, "role": user.role})
    return LoginResponse(
        message="Login successful",
        user_id=user.id,
        email=user.email,
        nickname=user.nickname,
        role=user.role,
        access_token=token,
        token_type="bearer"
    )
```

**변경 의도:**
- 일반 로그인: 인증 성공 후 `create_access_token()` 호출하여 토큰 발급 후 응답에 포함
- 구글 로그인: 이메일 대신 `google_id(sub)` 기준으로 조회 변경. 첫 로그인 시 자동으로 users 테이블에 행 생성 (password=NULL, auth_provider='google')

---

### 9. backend/alembic/versions/4500d95a9077_add_google_id_to_users.py 생성

**추가:**
```python
"""add google_id to users

Revision ID: 4500d95a9077
Revises: 20e256d6d379
Create Date: 2026-03-22 08:51:37.199651
"""
from alembic import op
import sqlalchemy as sa


revision = '4500d95a9077'
down_revision = '20e256d6d379'
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.add_column('users', sa.Column('google_id', sa.String(length=255), nullable=True))
    op.create_unique_constraint(None, 'users', ['google_id'])


def downgrade() -> None:
    op.drop_constraint(None, 'users', type_='unique')
    op.drop_column('users', 'google_id')
```

**변경 의도:** `google_id` 컬럼만 추가. `auth_provider`는 DB에 이미 존재하므로 마이그레이션 제외.

> **부가 처리:** DB의 `alembic_version` 테이블이 존재하지 않는 revision `a1b2c3d4e5f6`을 참조하고 있어, 실제 head인 `20e256d6d379`로 직접 수정 후 마이그레이션 진행.

---

### 10. frontend/src/pages/Login/Login.jsx 수정

**변경 전 (구글 로그인 콜백):**
```javascript
if (res.status === 200) {
  login({
    id: res.data.user_id,
    name: res.data.nickname,
    role: res.data.role,
    email: res.data.email,
  });
  navigate(res.data.role === 'admin' ? '/admin' : '/');
}
```

**변경 후 (구글 로그인 콜백):**
```javascript
if (res.status === 200) {
  login({
    id: res.data.user_id,
    name: res.data.nickname,
    role: res.data.role,
    email: res.data.email,
  });
  localStorage.setItem('access_token', res.data.access_token);
  navigate(res.data.role === 'admin' ? '/admin' : '/');
}
```

**변경 전 (일반 로그인 콜백):**
```javascript
login({ id: response.data.user_id, name: response.data.nickname, role: response.data.role, email: response.data.email });
setLoginError(false);
```

**변경 후 (일반 로그인 콜백):**
```javascript
login({ id: response.data.user_id, name: response.data.nickname, role: response.data.role, email: response.data.email });
localStorage.setItem('access_token', response.data.access_token);
setLoginError(false);
```

**변경 의도:** 기존 `login()` 호출은 유지하고, `access_token`만 별도로 localStorage에 저장. 향후 API 요청 시 Authorization 헤더에 첨부하기 위한 준비.

---

### 11. frontend/src/contexts/AuthContext.jsx 수정

**변경 전:**
```javascript
const logout = () => {
  setUser(null);
  setIsLoggedIn(false);
  localStorage.removeItem('user');
};
```

**변경 후:**
```javascript
const logout = () => {
  setUser(null);
  setIsLoggedIn(false);
  localStorage.removeItem('user');
  localStorage.removeItem('access_token');
};
```

**변경 의도:** 로그아웃 시 `user`와 함께 `access_token`도 제거하여 토큰 잔류로 인한 보안 누수 방지.

---

## 요청 흐름 (변경 후)

```
[사용자]
   │ 로그인 (일반 또는 구글)
   ▼
[Backend: FastAPI]
   │ 인증 성공 시 create_access_token() 호출
   │ payload: { sub: user.id, email, role, exp }
   ▼
[LoginResponse]
   │ { user_id, email, nickname, role, access_token, token_type }
   ▼
[Frontend]
   │ login(userData) → localStorage['user'] 저장 (기존)
   │ localStorage.setItem('access_token', ...) → 신규
   ▼
[사용자]

로그아웃 시:
   localStorage.removeItem('user')
   localStorage.removeItem('access_token')  ← 신규
```

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| 일반 로그인 | 응답에 access_token 추가, localStorage 저장 | 미확인 (서버 기동 정상) |
| 구글 로그인 | 자동 가입 로직 추가 + access_token 발급 | 미확인 (서버 기동 정상) |
| 로그아웃 | access_token 추가 제거 | 미확인 |
| 회원가입 | 변경 없음 | 정상 (기존과 동일) |
| 점수 저장, 프로필 표시 | 변경 없음 | 정상 (기존과 동일) |

## 결과
- 상태: 완료
- Docker 재빌드 후 `Application startup complete.` 확인

## 배운 내용

## 다음 작업
- 실제 로그인 후 `jwt.io`에서 토큰 클레임(`sub`, `email`, `role`, `exp`) 확인
- API 요청 시 `Authorization: Bearer <token>` 헤더 인증 전환 (다음 단계)
