# 업무일지

## 기본 정보
- 날짜: 2026-05-05 (화)
- 작성자: hexsera

## 진행 목표

JWT 1단계 구현 — 기존 X-API-Key 기반 인증을 JWT Bearer 토큰 인증으로 교체한다.  
Access Token만 사용(수명 7일), Zustand 메모리 저장 + localStorage 임시 백업, axios 인터셉터로 자동 부착, 401 시 로그아웃.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| Zustand 설치 및 토큰 스토어 생성 | `frontend/src/store/authStore.js` | 신규 생성 |
| axios 공용 인스턴스 및 인터셉터 생성 | `frontend/src/lib/api.js` | 신규 생성 |
| AuthContext login/logout에 authStore 연동 | `frontend/src/contexts/AuthContext.jsx` | login 시그니처 변경 |
| 로그인/회원가입 토큰 전달 수정 | `frontend/src/pages/Login/Login.jsx`, `Register/Register.jsx` | localStorage 직접 저장 제거 |
| X-API-Key 하드코딩 제거 및 api 인스턴스 교체 | `frontend/src/pages/admin/AdminUserMain.jsx`, `UserInfo/UserInfo.jsx` | axios → api 인스턴스 |
| 백엔드 JWT 검증 의존성 추가 | `backend/app/api/deps.py` | `get_current_user` 함수 추가 |
| 보호 엔드포인트에 JWT 인증 적용 | `users.py`, `notices.py`, `game_sessions.py` | PUT/DELETE에 적용 |
| 테스트용 auth_client 픽스처 추가 | `backend/tests/conftest.py` | 신규 픽스처 |
| game_sessions 테스트 auth_client로 교체 | `backend/tests/test_game_sessions.py` | 전체 테스트 auth_client 사용 |
| JWT 수명 7일로 설정 | `backend/.env` | JWT_EXPIRE_MINUTES 변경 |

**총 약 2시간**

## 주요 변경 사항

### 1. `frontend/src/store/authStore.js` 생성

**추가:**
```js
import { create } from 'zustand';

export const useAuthStore = create((set) => ({
  accessToken: localStorage.getItem('access_token') || null,
  setAccessToken: (token) => {
    localStorage.setItem('access_token', token);
    set({ accessToken: token });
  },
  clearAccessToken: () => {
    localStorage.removeItem('access_token');
    set({ accessToken: null });
  },
}));
```

**변경 의도:** Access Token을 메모리(Zustand)에 저장하되, 새로고침 시 복원을 위해 localStorage에도 임시 백업(1단계 한정 타협). `getState()`로 React 바깥(axios 인터셉터)에서도 접근 가능.

---

### 2. `frontend/src/lib/api.js` 생성

**추가:**
```js
import axios from 'axios';
import { useAuthStore } from '../store/authStore';

const api = axios.create({ baseURL: '/api/v1' });

api.interceptors.request.use((config) => {
  const token = useAuthStore.getState().accessToken;
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

api.interceptors.response.use(
  (res) => res,
  (error) => {
    if (error.response?.status === 401) {
      useAuthStore.getState().clearAccessToken();
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);

export default api;
```

**변경 의도:** 모든 API 요청에 Bearer 토큰을 자동 부착하고, 401 수신 시 토큰 제거 후 로그인 페이지로 이동하는 통로를 단일화.

---

### 3. `frontend/src/contexts/AuthContext.jsx` 수정

**변경 전:**
```jsx
import { createContext, useContext, useState, useEffect } from 'react';

// ...

  const login = (userData) => {
    setUser(userData);
    setIsLoggedIn(true);
    localStorage.setItem('user', JSON.stringify(userData));
  };

  const logout = () => {
    setUser(null);
    setIsLoggedIn(false);
    localStorage.removeItem('user');
    localStorage.removeItem('access_token');
  };
```

**변경 후:**
```jsx
import { createContext, useContext, useState, useEffect } from 'react';
import { useAuthStore } from '../store/authStore';

// ...

  const login = (userData, accessToken) => {
    setUser(userData);
    setIsLoggedIn(true);
    localStorage.setItem('user', JSON.stringify(userData));
    useAuthStore.getState().setAccessToken(accessToken);
  };

  const logout = () => {
    setUser(null);
    setIsLoggedIn(false);
    localStorage.removeItem('user');
    useAuthStore.getState().clearAccessToken();
  };
```

**변경 의도:** 토큰 저장 책임을 authStore로 일원화. `login()` 호출 시 accessToken을 함께 전달받아 저장.

---

### 4. `frontend/src/pages/Login/Login.jsx` 수정

**변경 전 (일반 로그인 handleLogin):**
```jsx
login({ id: response.data.user_id, name: response.data.nickname, role: response.data.role, email: response.data.email });
localStorage.setItem('access_token', response.data.access_token);
```

**변경 후:**
```jsx
login(
  { id: response.data.user_id, name: response.data.nickname, role: response.data.role, email: response.data.email },
  response.data.access_token
);
```

**변경 전 (구글 로그인 onSuccess):**
```jsx
login({
  id: res.data.user_id,
  name: res.data.nickname,
  role: res.data.role,
  email: res.data.email,
});
localStorage.setItem('access_token', res.data.access_token);
```

**변경 후:**
```jsx
login(
  { id: res.data.user_id, name: res.data.nickname, role: res.data.role, email: res.data.email },
  res.data.access_token
);
```

**변경 의도:** 토큰 저장을 authStore에 위임. `localStorage.setItem` 직접 호출 제거.

---

### 5. `frontend/src/pages/Register/Register.jsx` 수정

**변경 전:**
```jsx
login({ id: data.user_id, name: data.nickname, role: data.role, email: data.email });
localStorage.setItem('access_token', data.access_token);
```

**변경 후:**
```jsx
login(
  { id: data.user_id, name: data.nickname, role: data.role, email: data.email },
  data.access_token
);
```

**변경 의도:** Login.jsx와 동일한 패턴 통일.

---

### 6. `frontend/src/pages/admin/AdminUserMain.jsx` 수정

**변경 전:**
```jsx
import axios from 'axios';

// ...
const response = await axios.get('/api/v1/users');
// ...
const response = await axios.get(`/api/v1/users/${user.id}`);
// ...
await axios.put(`/api/v1/users/${selectedUser.id}`, body, {
  headers: { 'X-API-Key': 'hexsera-secret-api-key-2026' }
});
// ...
await axios.delete(`/api/v1/users/${selectedUser.id}`, {
  headers: { 'X-API-Key': 'hexsera-secret-api-key-2026' }
});
```

**변경 후:**
```jsx
import api from '../../lib/api';

// ...
const response = await api.get('/users');
// ...
const response = await api.get(`/users/${user.id}`);
// ...
await api.put(`/users/${selectedUser.id}`, body);
// ...
await api.delete(`/users/${selectedUser.id}`);
```

**변경 의도:** X-API-Key 하드코딩 제거, api 인스턴스로 JWT 자동 부착.

---

### 7. `frontend/src/pages/UserInfo/UserInfo.jsx` 수정

**변경 전:**
```jsx
import axios from 'axios';

// ...
const response = await axios.get(`/api/v1/users/${user.id}`, {
  headers: { 'X-API-Key': 'hexsera-secret-api-key-2026' }
});
// ...
const response = await axios.put(
  `/api/v1/users/${user.id}`,
  updateData,
  { headers: { 'X-API-Key': 'hexsera-secret-api-key-2026' } }
);
// ...
await axios.delete(`/api/v1/users/${user.id}`, {
  headers: { 'X-API-Key': 'hexsera-secret-api-key-2026' }
});
```

**변경 후:**
```jsx
import api from '../../lib/api';

// ...
const response = await api.get(`/users/${user.id}`);
// ...
const response = await api.put(`/users/${user.id}`, updateData);
// ...
await api.delete(`/users/${user.id}`);
```

**변경 의도:** AdminUserMain.jsx와 동일, X-API-Key 하드코딩 제거.

---

### 8. `backend/app/api/deps.py` 수정

**변경 전:**
```python
from sqlalchemy.orm import Session
from app.db.session import SessionLocal


def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
```

**변경 후:**
```python
from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from jose import jwt, JWTError
from sqlalchemy.orm import Session
from app.db.session import SessionLocal
from app.core.config import settings

import sys
sys.path.insert(0, '/code')
from models import User

bearer_scheme = HTTPBearer()


def get_db():
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()


def get_current_user(
    credentials: HTTPAuthorizationCredentials = Depends(bearer_scheme),
    db: Session = Depends(get_db),
) -> User:
    """JWT Bearer 토큰 검증 의존성"""
    token = credentials.credentials
    try:
        payload = jwt.decode(token, settings.JWT_SECRET_KEY, algorithms=[settings.JWT_ALGORITHM])
        user_id = int(payload.get("sub"))
    except (JWTError, TypeError, ValueError):
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid token")
    user = db.query(User).filter(User.id == user_id).first()
    if not user:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="User not found")
    return user
```

**변경 의도:** JWT Bearer 토큰을 검증해 User 객체를 반환하는 FastAPI 의존성 함수. 보호 엔드포인트에 `Depends(get_current_user)`로 주입.

---

### 9. `backend/app/api/v1/users.py` 수정

**변경 전:**
```python
from app.api.deps import get_db

@router.put("/{user_id}", response_model=UserResponse)
def update_user(user_id: int, user_update: UserUpdateRequest, db: Session = Depends(get_db)):
    ...

@router.delete("/{user_id}", response_model=DeleteResponse)
def delete_user(user_id: int, db: Session = Depends(get_db)):
    ...
```

**변경 후:**
```python
from app.api.deps import get_db, get_current_user

@router.put("/{user_id}", response_model=UserResponse)
def update_user(
    user_id: int,
    user_update: UserUpdateRequest,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    ...

@router.delete("/{user_id}", response_model=DeleteResponse)
def delete_user(
    user_id: int,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    ...
```

**변경 의도:** PUT(수정), DELETE(삭제)에만 인증 요구. GET은 친구 검색 등 공개 기능에서 사용하므로 미적용.

---

### 10. `backend/app/api/v1/notices.py` 수정

**변경 전:**
```python
from app.api.deps import get_db

@router.post("", response_model=NoticeResponse)
def create_notice(body: NoticeCreateRequest, db: Session = Depends(get_db)):
    notice = Notice(title=body.title, content=body.content, author_id=1)
    ...

@router.delete("/{notice_id}")
def delete_notice(notice_id: int, db: Session = Depends(get_db)):
    ...
```

**변경 후:**
```python
from app.api.deps import get_db, get_current_user
from models import Notice, User

@router.post("", response_model=NoticeResponse)
def create_notice(
    body: NoticeCreateRequest,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    notice = Notice(title=body.title, content=body.content, author_id=current_user.id)
    ...

@router.delete("/{notice_id}")
def delete_notice(
    notice_id: int,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    ...
```

**변경 의도:** 공지사항 작성/삭제에 인증 요구. author_id를 하드코딩 1에서 실제 로그인 유저 ID로 변경.

---

### 11. `backend/app/api/v1/game_sessions.py` 수정

**변경 전:**
```python
from fastapi import APIRouter, HTTPException

@router.put("/{user_id}", response_model=GameSessionResponse)
def save_game_session(user_id: int, data: GameSessionSaveRequest):
    ...

@router.delete("/{user_id}", response_model=GameSessionDeleteResponse)
def delete_game_session(user_id: int):
    ...
```

**변경 후:**
```python
import sys
sys.path.insert(0, '/code')
from models import User
from fastapi import APIRouter, Depends, HTTPException
from app.api.deps import get_current_user

@router.put("/{user_id}", response_model=GameSessionResponse)
def save_game_session(
    user_id: int,
    data: GameSessionSaveRequest,
    current_user: User = Depends(get_current_user),
):
    ...

@router.delete("/{user_id}", response_model=GameSessionDeleteResponse)
def delete_game_session(
    user_id: int,
    current_user: User = Depends(get_current_user),
):
    ...
```

**변경 의도:** 게임 세션 저장/삭제에 인증 요구. GET은 게임 중 세션 복원에 사용하므로 미적용.

---

### 12. `backend/tests/conftest.py` 수정

**추가 (기존 client 픽스처 아래에 추가):**
```python
@pytest.fixture(scope="function")
def auth_client(db_session):
    """JWT 토큰이 자동 부착되는 테스트용 클라이언트"""
    from models import User
    from datetime import date
    from app.core.security import hash_password, create_access_token
    from app.api.deps import get_current_user

    def override_get_db():
        try:
            yield db_session
        finally:
            pass

    test_user = User(
        email="auth_test@test.com",
        nickname="AuthTestUser",
        password=hash_password("testpass"),
        birth_date=date(2000, 1, 1),
        role="user",
    )
    db_session.add(test_user)
    db_session.commit()
    db_session.refresh(test_user)

    token = create_access_token({"sub": str(test_user.id), "email": test_user.email, "role": test_user.role})

    app.dependency_overrides[get_db] = override_get_db
    app.dependency_overrides[get_current_user] = lambda: test_user

    with TestClient(app, headers={"Authorization": f"Bearer {token}"}) as c:
        yield c
    app.dependency_overrides.clear()
```

**변경 의도:** `get_current_user` 의존성을 테스트용 유저로 고정하는 픽스처. 인증이 필요한 엔드포인트를 테스트할 때 사용.

---

### 13. `backend/tests/test_game_sessions.py` 수정

**변경 전:** 모든 테스트 함수가 `client` 픽스처 사용  
**변경 후:** 모든 테스트 함수가 `auth_client` 픽스처 사용

**변경 의도:** PUT/DELETE 엔드포인트에 인증이 추가됨에 따라 기존 `client`(토큰 없음)에서 `auth_client`(토큰 있음)으로 교체.

---

### 14. `backend/.env` 수정

**변경 전:**
```
JWT_EXPIRE_MINUTES=60
```

**변경 후:**
```
JWT_EXPIRE_MINUTES=10080
```

**변경 의도:** Refresh Token 없이 운영하는 1단계에서 Access Token 수명을 7일로 늘려 사용자가 자주 로그아웃되지 않도록 함.

---

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| 일반 로그인 | login() 시그니처 변경, 토큰 authStore 저장 | 미확인 |
| 구글 로그인 | 동일 | 미확인 |
| 회원가입 | 동일 | 미확인 |
| 로그아웃 | authStore.clearAccessToken() 호출로 변경 | 미확인 |
| 새로고침 후 로그인 유지 | authStore 초기화 시 localStorage에서 토큰 복원 | 미확인 |
| API 요청 (전체) | Authorization 헤더 자동 부착 | 미확인 |
| 401 수신 시 자동 로그아웃 | 인터셉터에서 처리 | 미확인 |
| 관리자 회원 목록 조회 (GET) | api 인스턴스로 교체, 인증 미적용 | 미확인 |
| 관리자 회원 수정/삭제 | JWT 인증 적용, X-API-Key 제거 | 미확인 |
| 내 정보 조회/수정/탈퇴 | JWT 인증 적용, X-API-Key 제거 | 미확인 |
| 공지사항 작성/삭제 | JWT 인증 적용, author_id 하드코딩 제거 | 미확인 |
| 게임 세션 저장/삭제 | JWT 인증 적용 | 정상 (pytest 7/7 통과) |
| 게임 세션 조회 | 인증 미적용 유지 | 정상 (pytest 통과) |

## 결과
- 상태: 완료

## 배운내용


## 다음 작업
- 2단계: Refresh Token 도입 (HttpOnly 쿠키 + `/api/v1/auth/refresh` 엔드포인트)
- 5단계: localStorage 의존 제거 (앱 시작 시 `/auth/refresh` 호출로 Access Token 복원)
