

궁금한거
1. db 연결 없이 api 에서 db 연결 테스트 어떻게 하지? 가짜 db 를 만드는 방법이 있다는데? 이거 mocking 방법이 너무 궁금하다.
2. ip 로만 게스트를 판단하기에는 ip 에 문제가 많다고 한다. 게스트용 uuid 를 브라우저에 저장시켜서 사용한다는데...
3. CRUD 에 매몰되었는데, 지금 당장 필요한 기능은 나중에 필요할때 만들어도 되지 않을까?
4. api 는 어디서 ip를 가져올까?
5. SQLAlemy func 는 뭐야?
6. 테이블에서 데이터 뽑아서 테이블 만드는 기법이 있었는데 이름이 기억 안난다...

안거
1. fastapi/docs 에 api 가 너무 난잡하게 보이는데, tag 로 폴터처럼 정리할 수 있다 한다.
2. ai 이자식 받기 스키마랑 응답스키마 마음대로 만든다. 이거 기강 잡아야겠네.
3. ai 가 지시에도 없는 api 키를 막 추가하려고 한다.