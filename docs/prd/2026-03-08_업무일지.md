# 업무일지

## 기본 정보
- 날짜: 2026-03-08 (일) 12:30~1:30
- 작성자: hexsera

## 진행 목표

공지사항 게시판 프론트엔드 실행계획의 섹션 1, 2, 4를 TDD로 구현한다.
- 섹션 1: Tiptap 패키지 설치
- 섹션 2: `noticeService.js` 서비스 레이어 생성
- 섹션 4: `NoticeWritePage.jsx` 작성 페이지 구현

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| Tiptap 패키지 설치 | `frontend/package.json` | @tiptap/react, @tiptap/starter-kit, @tiptap/extension-image 각 3.20.1 |
| TDD RED 계획 문서 작성 | `docs/prd/TDD-RED-공지사항-프론트엔드-20260308.md` | 23개 테스트 케이스 목록 |
| noticeService 테스트 작성 (RED) | `frontend/src/test/noticeService.test.js` | 10개 테스트, 서비스 미존재로 실패 확인 |
| NoticeWritePage 테스트 작성 (RED) | `frontend/src/test/NoticeWritePage.test.jsx` | 13개 테스트, 서비스/페이지 미존재로 실패 확인 |
| noticeService.js 구현 (GREEN) | `frontend/src/services/noticeService.js` | 신규 생성 |
| AuthContext named export 추가 | `frontend/src/contexts/AuthContext.jsx` | `useContext(AuthContext)` 사용 위해 필요 |
| NoticeWritePage.jsx 구현 (GREEN) | `frontend/src/pages/Notice/NoticeWritePage.jsx` | 신규 생성 |
| TDD GREEN 보고서 작성 | `docs/prd/TDD-GREEN-공지사항-프론트엔드-20260308.md` | 23/23 통과 기록 |
| REFACTOR — 테스트 파일 가독성 개선 | `frontend/src/test/NoticeWritePage.test.jsx` | mock 함수 추출, 상수화 |
| 공지사항 라우트 임시 등록 | `frontend/src/App.jsx` | vite 서버 테스트용, stub 페이지 연결 |
| stub 페이지 생성 | `frontend/src/pages/Notice/NoticeListPage.jsx` | 정식 구현 전 placeholder |
| stub 페이지 생성 | `frontend/src/pages/Notice/NoticeDetailPage.jsx` | 정식 구현 전 placeholder |
| 실행계획에 임시 처리 항목 기록 | `docs/prd/공지사항-게시판-프론트엔드-실행계획.md` | 추후 재작업 필요 항목 명시 |
| AuthContext loading 상태 추가 | `frontend/src/contexts/AuthContext.jsx` | 초기 렌더 시 리다이렉트 버그 수정 |
| NoticeWritePage loading 가드 추가 | `frontend/src/pages/Notice/NoticeWritePage.jsx` | loading 완료 후 권한 판단하도록 수정 |

## 주요 변경 사항

### 1. `frontend/src/services/noticeService.js` 신규 생성

**추가:**
```js
import axios from 'axios';

export const getNotices = () =>
  axios.get('/api/v1/notices').then(res => res.data);

export const getNotice = (id) =>
  axios.get(`/api/v1/notices/${id}`).then(res => res.data);

export const createNotice = (title, content) =>
  axios.post('/api/v1/notices', { title, content }).then(res => res.data);

export const deleteNotice = (id) =>
  axios.delete(`/api/v1/notices/${id}`).then(res => res.data);

export const uploadNoticeImage = (file) => {
  const formData = new FormData();
  formData.append('file', file);
  return axios.post('/api/v1/notices/upload-image', formData).then(res => res.data);
};
```

**변경 의도:** 공지사항 관련 API 호출을 페이지 컴포넌트에서 분리하여 서비스 레이어로 관리

---

### 2. `frontend/src/contexts/AuthContext.jsx` 수정 (2회)

**변경 전 (원본):**
```jsx
import { createContext, useContext, useState, useEffect } from 'react';

const AuthContext = createContext();

export function AuthProvider({ children }) {
  const [isLoggedIn, setIsLoggedIn] = useState(false);
  const [user, setUser] = useState(null);

  useEffect(() => {
    const savedUser = localStorage.getItem('user');
    if (savedUser) {
      setUser(JSON.parse(savedUser));
      setIsLoggedIn(true);
    }
  }, []);

  // ...

  return (
    <AuthContext.Provider value={{ isLoggedIn, user, login, logout }}>
      {children}
    </AuthContext.Provider>
  );
}
```

**변경 후 (현재):**
```jsx
import { createContext, useContext, useState, useEffect } from 'react';

export const AuthContext = createContext();

export function AuthProvider({ children }) {
  const [isLoggedIn, setIsLoggedIn] = useState(false);
  const [user, setUser] = useState(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    const savedUser = localStorage.getItem('user');
    if (savedUser) {
      setUser(JSON.parse(savedUser));
      setIsLoggedIn(true);
    }
    setLoading(false);
  }, []);

  // ...

  return (
    <AuthContext.Provider value={{ isLoggedIn, user, loading, login, logout }}>
      {children}
    </AuthContext.Provider>
  );
}
```

**변경 의도:**
1. `AuthContext` named export 추가 — `useContext(AuthContext)` 직접 사용을 위해 필요
2. `loading` 상태 추가 — localStorage 초기화 전 `user=null` 상태에서 성급한 리다이렉트 발생하는 버그 수정. `useEffect`는 첫 렌더 이후 실행되므로 초기값 `null`인 채로 권한 체크가 동작하는 문제가 있었음.

---

### 3. `frontend/src/pages/Notice/NoticeWritePage.jsx` 신규 생성 + 수정

**추가 (최종 현재 상태):**
```jsx
import { useState, useContext, useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
import { useEditor, EditorContent } from '@tiptap/react';
import StarterKit from '@tiptap/starter-kit';
import Image from '@tiptap/extension-image';
import { Box, AppBar, Toolbar, Typography, Button, TextField, Container, Paper } from '@mui/material';
import { AuthContext } from '../../contexts/AuthContext';
import { createNotice, uploadNoticeImage } from '../../services/noticeService';

const COLORS = { bg: '#0F172A', card: '#1E293B', border: '#334155',
                 text: '#F1F5F9', subText: '#94A3B8', primary: '#4F46E5' };

function NoticeWritePage() {
  const navigate = useNavigate();
  const { user, loading } = useContext(AuthContext);
  const [title, setTitle] = useState('');

  useEffect(() => {
    if (loading) return;
    if (!user || user.role !== 'admin') navigate('/notice');
  }, [user, loading]);

  const editor = useEditor({
    extensions: [StarterKit, Image],
    content: '',
  });

  const handleImageUpload = async (e) => {
    const file = e.target.files[0];
    if (!file) return;
    const data = await uploadNoticeImage(file);
    editor.chain().focus().setImage({ src: data.url }).run();
  };

  const handleSubmit = async () => {
    const content = editor.getHTML();
    await createNotice(title, content);
    navigate('/notice');
  };

  return (
    <Box sx={{ minHeight: '100vh', backgroundColor: COLORS.bg }}>
      <AppBar position="static" sx={{ backgroundColor: COLORS.card,
                                      borderBottom: `1px solid ${COLORS.border}` }}>
        <Toolbar>
          <Typography variant="h6" sx={{ color: COLORS.text, cursor: 'pointer' }}
                      onClick={() => navigate('/')}>HEXSERA PINBALL</Typography>
        </Toolbar>
      </AppBar>
      <Container maxWidth="md" sx={{ py: 4 }}>
        <Typography variant="h5" sx={{ color: COLORS.text, mb: 2 }}>공지사항 작성</Typography>
        <TextField
          fullWidth
          label="제목"
          value={title}
          onChange={e => setTitle(e.target.value)}
          sx={{ mb: 2, input: { color: COLORS.text },
                label: { color: COLORS.subText },
                '& .MuiOutlinedInput-root': { '& fieldset': { borderColor: COLORS.border } } }}
        />
        <Paper sx={{ backgroundColor: '#fff', p: 2, mb: 2, minHeight: 300 }}>
          <Box sx={{ mb: 1, display: 'flex', gap: 1, flexWrap: 'wrap' }}>
            <Button size="small" variant="outlined"
                    onClick={() => editor.chain().focus().toggleBold().run()}>B</Button>
            <Button size="small" variant="outlined"
                    onClick={() => editor.chain().focus().toggleItalic().run()}>I</Button>
            <Button size="small" variant="outlined"
                    onClick={() => editor.chain().focus().toggleHeading({ level: 2 }).run()}>H2</Button>
            <Button size="small" variant="outlined" component="label">
              이미지 <input type="file" hidden accept="image/*" onChange={handleImageUpload} />
            </Button>
          </Box>
          <EditorContent editor={editor} />
        </Paper>
        <Box sx={{ display: 'flex', gap: 1, justifyContent: 'flex-end' }}>
          <Button variant="outlined" onClick={() => navigate('/notice')}
                  sx={{ color: COLORS.subText, borderColor: COLORS.border }}>취소</Button>
          <Button variant="contained" onClick={handleSubmit}
                  sx={{ backgroundColor: COLORS.primary }}>저장</Button>
        </Box>
      </Container>
    </Box>
  );
}

export default NoticeWritePage;
```

**변경 의도:** Tiptap 에디터 기반 공지사항 작성 페이지. `loading` 가드로 초기 렌더 시 리다이렉트 버그 수정.

---

### 4. `frontend/src/pages/Notice/NoticeListPage.jsx` 신규 생성 (stub)

**추가:**
```jsx
// TODO: 실행계획 섹션 3 — 정식 구현 예정 (현재 테스트용 stub)
function NoticeListPage() {
  return <div>공지사항 목록 (구현 예정)</div>;
}

export default NoticeListPage;
```

**변경 의도:** vite 서버에서 `/notice/write` 라우트 테스트를 위해 App.jsx에 라우트 등록 시 필요한 stub 페이지

---

### 5. `frontend/src/pages/Notice/NoticeDetailPage.jsx` 신규 생성 (stub)

**추가:**
```jsx
// TODO: 실행계획 섹션 5 — 정식 구현 예정 (현재 테스트용 stub)
function NoticeDetailPage() {
  return <div>공지사항 상세 (구현 예정)</div>;
}

export default NoticeDetailPage;
```

**변경 의도:** App.jsx 라우트 등록 시 필요한 stub 페이지

---

### 6. `frontend/src/App.jsx` 수정

**변경 전:**
```jsx
import UserInfo from './pages/UserInfo';

// ... (공지사항 관련 import 없음)

// <Routes> 내부
<Route path="/user/account" element={<UserInfo />} />
```

**변경 후:**
```jsx
import UserInfo from './pages/UserInfo';
// TODO: 실행계획 섹션 6 — 공지사항 라우트 (테스트용 임시 등록, 추후 정식 구현 단계에서 재검토)
import NoticeListPage from './pages/Notice/NoticeListPage';
import NoticeWritePage from './pages/Notice/NoticeWritePage';
import NoticeDetailPage from './pages/Notice/NoticeDetailPage';

// <Routes> 내부
<Route path="/user/account" element={<UserInfo />} />
{/* TODO: 실행계획 섹션 6 — 공지사항 라우트 (테스트용 임시 등록) */}
<Route path="/notice" element={<NoticeListPage />} />
<Route path="/notice/write" element={<NoticeWritePage />} />
<Route path="/notice/:id" element={<NoticeDetailPage />} />
```

**변경 의도:** vite 서버에서 `/notice/write` 접근 가능하도록 임시 라우트 등록. NoticeListPage, NoticeDetailPage는 현재 stub.

---

### 7. 테스트 파일 신규 생성

**추가: `frontend/src/test/noticeService.test.js`** — 10개 테스트
**추가: `frontend/src/test/NoticeWritePage.test.jsx`** — 13개 테스트

(파일 내용은 TDD-GREEN 보고서 참조)

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| `/notice/write` 페이지 접근 | 신규 구현 + 라우트 등록 | 정상 (vite 서버 확인) |
| `/notice` 페이지 접근 | stub 페이지 연결 | 정상 (stub 텍스트 표시) |
| `/notice/:id` 페이지 접근 | stub 페이지 연결 | 정상 (stub 텍스트 표시) |
| noticeService API 호출 5종 | 신규 구현 | 정상 (Vitest 10/10 통과) |
| NoticeWritePage admin 접근 제어 | `loading` 가드 추가 | 정상 (Vitest 13/13 통과) |
| NoticeWritePage 저장/취소 동작 | 신규 구현 | 정상 (Vitest 통과) |
| AuthContext `loading` 상태 | 신규 추가 | 정상 (기존 테스트 영향 없음) |
| AuthContext `AuthContext` named export | 신규 추가 | 정상 |
| 기존 로그인/로그아웃 기능 | AuthContext 수정 영향 | 미확인 (기존 테스트 별도 실행 안 함) |

## 결과
- 상태: 완료 (섹션 1, 2, 4)
- Vitest 23/23 통과

## 배운 내용

## 다음 작업
- 섹션 3: `NoticeListPage.jsx` 정식 구현 (TDD)
- 섹션 5: `NoticeDetailPage.jsx` 정식 구현 (TDD)
- 섹션 6: App.jsx 라우트 — stub에서 정식 구현 파일로 교체 확인
- 섹션 7: `HomePage.jsx` AppBar에 공지사항 버튼 추가

---

# 업무일지

## 기본 정보
- 날짜: 2026-03-08 (일) 14:20~15:00
- 작성자: hexsera

## 진행 목표

공지사항 게시판 프론트엔드 실행계획의 섹션 3을 TDD로 구현한다.
- 섹션 3: `NoticeListPage.jsx` 공지사항 목록 페이지 정식 구현 (stub → 완전 구현)

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| TDD RED 계획서 작성 | `docs/prd/TDD-RED-NoticeListPage-20260308.md` | 7개 테스트 케이스 (헤딩·날짜 테스트 제외) |
| NoticeListPage 테스트 작성 (RED) | `frontend/src/test/NoticeListPage.test.jsx` | 7개 테스트, stub으로 5개 실패 확인 |
| NoticeListPage.jsx 정식 구현 (GREEN) | `frontend/src/pages/Notice/NoticeListPage.jsx` | stub → 정식 구현 교체 |
| TDD GREEN 보고서 작성 | `docs/prd/TDD-GREEN-NoticeListPage-20260308.md` | 7/7 통과 기록 |
| REFACTOR — ListItem button prop 제거 | `frontend/src/pages/Notice/NoticeListPage.jsx` | MUI v7 대응, `ListItemButton`으로 교체 |

**총 약 30분**

## 주요 변경 사항

### 1. `frontend/src/pages/Notice/NoticeListPage.jsx` 수정 (stub → 정식 구현)

**변경 전 (stub):**
```jsx
// TODO: 실행계획 섹션 3 — 정식 구현 예정 (현재 테스트용 stub)
function NoticeListPage() {
  return <div>공지사항 목록 (구현 예정)</div>;
}

export default NoticeListPage;
```

**변경 후 (현재):**
```jsx
import { useState, useEffect, useContext } from 'react';
import { useNavigate } from 'react-router-dom';
import { Box, AppBar, Toolbar, Typography, Button, Container, List,
         ListItemButton, ListItemText, Divider, Paper } from '@mui/material';
import { AuthContext } from '../../contexts/AuthContext';
import HeaderUserInfo from '../../components/HeaderUserInfo';
import { getNotices } from '../../services/noticeService';

const COLORS = { bg: '#0F172A', card: '#1E293B', border: '#334155',
                 text: '#F1F5F9', subText: '#94A3B8', primary: '#4F46E5' };

function NoticeListPage() {
  const navigate = useNavigate();
  const { user } = useContext(AuthContext);
  const [notices, setNotices] = useState([]);

  useEffect(() => {
    getNotices().then(setNotices);
  }, []);

  return (
    <Box sx={{ minHeight: '100vh', backgroundColor: COLORS.bg }}>
      <AppBar position="static" sx={{ backgroundColor: COLORS.card,
                                      borderBottom: `1px solid ${COLORS.border}` }}>
        <Toolbar sx={{ justifyContent: 'space-between' }}>
          <Typography variant="h6" sx={{ color: COLORS.text, cursor: 'pointer' }}
                      onClick={() => navigate('/')}>HEXSERA PINBALL</Typography>
          <HeaderUserInfo buttonColor={COLORS.primary} buttonTextColor={COLORS.text}
                          outlinedBorderColor={COLORS.text} />
        </Toolbar>
      </AppBar>
      <Container maxWidth="md" sx={{ py: 4 }}>
        <Box sx={{ display: 'flex', justifyContent: 'space-between', mb: 2 }}>
          <Typography variant="h5" sx={{ color: COLORS.text }}>공지사항</Typography>
          {user?.role === 'admin' && (
            <Button variant="contained" onClick={() => navigate('/notice/write')}
                    sx={{ backgroundColor: COLORS.primary }}>글쓰기</Button>
          )}
        </Box>
        <Paper sx={{ backgroundColor: COLORS.card, border: `1px solid ${COLORS.border}` }}>
          <List>
            {notices.map((notice, idx) => (
              <Box key={notice.id}>
                <ListItemButton onClick={() => navigate(`/notice/${notice.id}`)}>
                  <ListItemText
                    primary={<Typography sx={{ color: COLORS.text }}>{notice.title}</Typography>}
                    secondary={<Typography sx={{ color: COLORS.subText, fontSize: '0.8rem' }}>
                      {new Date(notice.created_at).toLocaleDateString('ko-KR')}
                    </Typography>}
                  />
                </ListItemButton>
                {idx < notices.length - 1 && <Divider sx={{ borderColor: COLORS.border }} />}
              </Box>
            ))}
          </List>
        </Paper>
      </Container>
    </Box>
  );
}

export default NoticeListPage;
```

**변경 의도:**
- stub → 정식 구현. `getNotices()` 호출로 공지 목록 로드, admin에게만 글쓰기 버튼 노출, 항목 클릭 시 상세 페이지로 이동.
- REFACTOR: `ListItem button={true}` → `ListItemButton` 교체. MUI v7에서 `ListItem`의 `button` prop이 제거되어 DOM 경고 발생 → `ListItemButton` 컴포넌트로 대체.

---

### 2. `frontend/src/test/NoticeListPage.test.jsx` 신규 생성

**추가:**
```jsx
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { MemoryRouter } from 'react-router-dom';
import { AuthProvider } from '../contexts/AuthContext';

vi.mock('../services/noticeService', () => ({
  getNotices: vi.fn().mockResolvedValue([
    { id: 1, title: '첫 번째 공지', created_at: '2026-01-15T00:00:00' },
    { id: 2, title: '두 번째 공지', created_at: '2026-02-20T00:00:00' },
  ]),
}));

import { getNotices } from '../services/noticeService';

const mockNavigate = vi.fn();
vi.mock('react-router-dom', async (importOriginal) => {
  const actual = await importOriginal();
  return { ...actual, useNavigate: () => mockNavigate };
});

const ADMIN_USER = { user_id: 1, nickname: '관리자', role: 'admin', email: 'admin@test.com' };
const NORMAL_USER = { user_id: 2, nickname: '일반유저', role: 'user', email: 'user@test.com' };

function renderWithAuth(userValue) {
  if (userValue) {
    localStorage.setItem('user', JSON.stringify(userValue));
  }
  return render(
    <MemoryRouter>
      <AuthProvider>
        <NoticeListPage />
      </AuthProvider>
    </MemoryRouter>
  );
}

import NoticeListPage from '../pages/Notice/NoticeListPage';

beforeEach(() => {
  vi.clearAllMocks();
  localStorage.clear();
  getNotices.mockResolvedValue([
    { id: 1, title: '첫 번째 공지', created_at: '2026-01-15T00:00:00' },
    { id: 2, title: '두 번째 공지', created_at: '2026-02-20T00:00:00' },
  ]);
});

describe('NoticeListPage 데이터 로딩', () => {
  it('페이지 접속 시 getNotices가 호출된다', async () => { ... });
  it('API 응답의 공지 제목들이 목록에 표시된다', async () => { ... });
});

describe('NoticeListPage 글쓰기 버튼 접근 제어', () => {
  it('비로그인 상태에서 글쓰기 버튼이 보이지 않는다', async () => { ... });
  it('일반 사용자(role=user)에게 글쓰기 버튼이 보이지 않는다', async () => { ... });
  it('admin에게 글쓰기 버튼이 보인다', async () => { ... });
});

describe('NoticeListPage 네비게이션', () => {
  it('글쓰기 버튼 클릭 시 /notice/write로 이동한다', async () => { ... });
  it('공지 항목 클릭 시 /notice/{id}로 이동한다', async () => { ... });
});
```

**변경 의도:** NoticeListPage 정식 구현에 대한 TDD 테스트 커버리지 확보

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| `/notice` 공지사항 목록 페이지 | stub → 정식 구현 | 정상 (Vitest 7/7 통과) |
| admin 글쓰기 버튼 노출 | 신규 구현 | 정상 (Vitest 통과) |
| 비로그인/일반 사용자 글쓰기 버튼 미노출 | 신규 구현 | 정상 (Vitest 통과) |
| 공지 항목 클릭 → 상세 이동 | 신규 구현 | 정상 (Vitest 통과) |
| 글쓰기 버튼 클릭 → 작성 페이지 이동 | 신규 구현 | 정상 (Vitest 통과) |

## 결과
- 상태: 완료 (섹션 3)
- Vitest 7/7 통과, DOM 경고 없음

## 배운 내용

## 다음 작업
- 섹션 5: `NoticeDetailPage.jsx` 정식 구현 (TDD)
- 섹션 6: App.jsx 라우트 — stub에서 정식 구현 파일로 교체 확인
- 섹션 7: `HomePage.jsx` AppBar에 공지사항 버튼 추가

---

# 업무일지

## 기본 정보
- 날짜: 2026-03-08 (일) 15:00~15:20
- 작성자: hexsera

## 진행 목표

공지사항 게시판 프론트엔드 실행계획의 섹션 5, 6을 TDD로 구현한다.
- 섹션 5: `NoticeDetailPage.jsx` 공지사항 상세 페이지 정식 구현 (stub → 완전 구현)
- 섹션 6: `App.jsx` 공지사항 라우트 TODO 주석 제거 (정식 완료 처리)

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| TDD RED 계획서 작성 | `docs/prd/TDD-RED-NoticeDetailPage-App-HomePage-20260308.md` | 9개 테스트 케이스 |
| NoticeDetailPage 테스트 작성 (RED) | `frontend/src/test/NoticeDetailPage.test.jsx` | 9개 테스트, stub으로 전부 실패 확인 |
| NoticeDetailPage.jsx 정식 구현 (GREEN) | `frontend/src/pages/Notice/NoticeDetailPage.jsx` | stub → 정식 구현 교체 |
| App.jsx TODO 주석 제거 (GREEN) | `frontend/src/App.jsx` | 섹션 6 정식 완료 처리 |
| TDD GREEN 보고서 작성 | `docs/prd/TDD-GREEN-NoticeDetailPage-20260308.md` | 26/26 통과 기록 |

**총 약 30분**

## 주요 변경 사항

### 1. `frontend/src/pages/Notice/NoticeDetailPage.jsx` 수정 (stub → 정식 구현)

**변경 전 (stub):**
```jsx
// TODO: 실행계획 섹션 5 — 정식 구현 예정 (현재 테스트용 stub)
function NoticeDetailPage() {
  return <div>공지사항 상세 (구현 예정)</div>;
}

export default NoticeDetailPage;
```

**변경 후 (현재):**
```jsx
import { useState, useEffect, useContext } from 'react';
import { useParams, useNavigate } from 'react-router-dom';
import { Box, AppBar, Toolbar, Typography, Button, Container,
         Paper, Divider } from '@mui/material';
import { AuthContext } from '../../contexts/AuthContext';
import HeaderUserInfo from '../../components/HeaderUserInfo';
import { getNotice, deleteNotice } from '../../services/noticeService';

const COLORS = { bg: '#0F172A', card: '#1E293B', border: '#334155',
                 text: '#F1F5F9', subText: '#94A3B8', primary: '#4F46E5' };

function NoticeDetailPage() {
  const { id } = useParams();
  const navigate = useNavigate();
  const { user } = useContext(AuthContext);
  const [notice, setNotice] = useState(null);

  useEffect(() => {
    getNotice(id).then(setNotice);
  }, [id]);

  const handleDelete = async () => {
    await deleteNotice(id);
    navigate('/notice');
  };

  if (!notice) return null;

  return (
    <Box sx={{ minHeight: '100vh', backgroundColor: COLORS.bg }}>
      <AppBar position="static" sx={{ backgroundColor: COLORS.card,
                                      borderBottom: `1px solid ${COLORS.border}` }}>
        <Toolbar sx={{ justifyContent: 'space-between' }}>
          <Typography variant="h6" sx={{ color: COLORS.text, cursor: 'pointer' }}
                      onClick={() => navigate('/')}>HEXSERA PINBALL</Typography>
          <HeaderUserInfo buttonColor={COLORS.primary} buttonTextColor={COLORS.text}
                          outlinedBorderColor={COLORS.text} />
        </Toolbar>
      </AppBar>
      <Container maxWidth="md" sx={{ py: 4 }}>
        <Button onClick={() => navigate('/notice')}
                sx={{ color: COLORS.subText, mb: 2 }}>← 목록으로</Button>
        <Paper sx={{ backgroundColor: COLORS.card, border: `1px solid ${COLORS.border}`, p: 3 }}>
          <Typography variant="h5" sx={{ color: COLORS.text, mb: 1 }}>{notice.title}</Typography>
          <Typography sx={{ color: COLORS.subText, fontSize: '0.85rem', mb: 2 }}>
            {new Date(notice.created_at).toLocaleDateString('ko-KR')}
          </Typography>
          <Divider sx={{ borderColor: COLORS.border, mb: 2 }} />
          <Box sx={{ color: COLORS.text, '& img': { maxWidth: '100%' } }}
               dangerouslySetInnerHTML={{ __html: notice.content }} />
          {user?.role === 'admin' && (
            <Box sx={{ display: 'flex', gap: 1, justifyContent: 'flex-end', mt: 3 }}>
              <Button variant="contained" color="error" onClick={handleDelete}>삭제</Button>
            </Box>
          )}
        </Paper>
      </Container>
    </Box>
  );
}

export default NoticeDetailPage;
```

**변경 의도:** URL param `id`로 `getNotice(id)` 호출, 제목/날짜/HTML 콘텐츠 렌더링, admin에게만 삭제 버튼 노출, 삭제 후 목록으로 이동.

---

### 2. `frontend/src/App.jsx` 수정 (TODO 주석 제거)

**변경 전:**
```jsx
// TODO: 실행계획 섹션 6 — 공지사항 라우트 (테스트용 임시 등록, 추후 정식 구현 단계에서 재검토)
import NoticeListPage from './pages/Notice/NoticeListPage';
import NoticeWritePage from './pages/Notice/NoticeWritePage';
import NoticeDetailPage from './pages/Notice/NoticeDetailPage';

// <Routes> 내부
{/* TODO: 실행계획 섹션 6 — 공지사항 라우트 (테스트용 임시 등록) */}
<Route path="/notice" element={<NoticeListPage />} />
<Route path="/notice/write" element={<NoticeWritePage />} />
<Route path="/notice/:id" element={<NoticeDetailPage />} />
```

**변경 후 (현재):**
```jsx
import NoticeListPage from './pages/Notice/NoticeListPage';
import NoticeWritePage from './pages/Notice/NoticeWritePage';
import NoticeDetailPage from './pages/Notice/NoticeDetailPage';

// <Routes> 내부
<Route path="/notice" element={<NoticeListPage />} />
<Route path="/notice/write" element={<NoticeWritePage />} />
<Route path="/notice/:id" element={<NoticeDetailPage />} />
```

**변경 의도:** NoticeDetailPage 정식 구현 완료로 섹션 6 TODO 주석 제거. 라우트 자체는 이전부터 정상 등록된 상태.

---

### 3. `frontend/src/test/NoticeDetailPage.test.jsx` 신규 생성

**추가:**
```jsx
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { MemoryRouter, Route, Routes } from 'react-router-dom';
import { AuthProvider } from '../contexts/AuthContext';

vi.mock('../services/noticeService', () => ({
  getNotice: vi.fn().mockResolvedValue({
    id: 1,
    title: '테스트 공지 제목',
    content: '<p>공지 <strong>내용</strong></p><img src="/img/test.png" />',
    created_at: '2026-03-01T09:00:00',
  }),
  deleteNotice: vi.fn().mockResolvedValue({}),
}));

// useNavigate mock, AuthProvider 래핑, localStorage로 로그인 상태 제어
// describe 3개: 데이터 로딩(4개), 접근 제어(3개), 네비게이션(2개)
```

**변경 의도:** NoticeDetailPage 정식 구현에 대한 TDD 테스트 커버리지 확보 (9개)

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| `/notice/:id` 공지사항 상세 페이지 | stub → 정식 구현 | 정상 (Vitest 9/9 통과) |
| 공지 제목/날짜/HTML 본문 렌더링 | 신규 구현 | 정상 (Vitest 통과) |
| admin 삭제 버튼 노출 | 신규 구현 | 정상 (Vitest 통과) |
| 비로그인/일반 사용자 삭제 버튼 미노출 | 신규 구현 | 정상 (Vitest 통과) |
| 삭제 후 목록으로 이동 | 신규 구현 | 정상 (Vitest 통과) |
| 목록으로 버튼 클릭 → /notice 이동 | 신규 구현 | 정상 (Vitest 통과) |
| App.jsx 공지사항 라우트 3종 | TODO 주석만 제거, 동작 변경 없음 | 정상 (기존 동일) |

## 결과
- 상태: 완료 (섹션 5, 6)
- Vitest 26/26 통과 (noticeService 10 + NoticeListPage 7 + NoticeDetailPage 9)

## 배운 내용

## 다음 작업
- 섹션 7: `HomePage.jsx` AppBar에 공지사항 버튼 추가 (별도 세션에서 진행 예정)

---

# 업무일지

## 기본 정보
- 날짜: 2026-03-08 (일) 15:40~16:50
- 작성자: hexsera

## 진행 목표

HomeHeader에 바로가기 섹션을 추가하고, 공지사항 페이지들(NoticeListPage, NoticeDetailPage)의 헤더를 HomeHeader로 교체한다.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| 실행계획 작성 | `docs/prd/헤더-바로가기-섹션-실행계획.md` | 헤더 디자인 PRD 기반 |
| HomeHeader에 바로가기 섹션 추가 | `frontend/src/components/HomeHeader.jsx` | SHORTCUTS 배열, activeItem state, hover/toggle 버튼 |
| NoticeListPage 헤더 교체 | `frontend/src/pages/Notice/NoticeListPage.jsx` | 인라인 AppBar → HomeHeader |
| NoticeDetailPage 헤더 교체 | `frontend/src/pages/Notice/NoticeDetailPage.jsx` | 인라인 AppBar → HomeHeader |

**총 약 20분**

## 주요 변경 사항

### 1. `frontend/src/components/HomeHeader.jsx` 수정

**변경 전:**
```jsx
import { useNavigate } from 'react-router-dom';
import { AppBar, Toolbar, Typography, Button, Box } from '@mui/material';
import HeaderUserInfo from './HeaderUserInfo';

const COLORS = {
  bg: '#0F172A',
  card: '#1E293B',
  border: '#334155',
  text: '#F1F5F9',
  primary: '#4F46E5',
};

function HomeHeader() {
  const navigate = useNavigate();

  return (
    <AppBar position="static" sx={{ backgroundColor: COLORS.card, borderBottom: `1px solid ${COLORS.border}` }}>
      <Toolbar sx={{ justifyContent: 'space-between' }}>
        <Box sx={{ display: 'flex', alignItems: 'center', gap: 1 }}>
          <Typography
            variant="h6"
            sx={{ color: COLORS.text, fontWeight: 'bold', fontSize: { xs: '0.9rem', sm: '1.25rem' }, display: { xs: 'none', sm: 'block' } }}
          >
            HEXSERA PINBALL
          </Typography>
          <Button variant="contained" onClick={() => navigate('/pinball')} sx={{ backgroundColor: COLORS.primary, color: COLORS.text }}>
            게임하기
          </Button>
        </Box>
        <HeaderUserInfo buttonColor={COLORS.primary} buttonTextColor={COLORS.text} outlinedBorderColor={COLORS.text} />
      </Toolbar>
    </AppBar>
  );
}

export default HomeHeader;
```

**변경 후:**
```jsx
import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { AppBar, Toolbar, Typography, Button, Box } from '@mui/material';
import HeaderUserInfo from './HeaderUserInfo';

const COLORS = {
  bg: '#0F172A',
  card: '#1E293B',
  border: '#334155',
  text: '#F1F5F9',
  primary: '#4F46E5',
};

const SHORTCUTS = [
  { label: '공지사항', path: '/notice' },
];

function HomeHeader() {
  const navigate = useNavigate();
  const [activeItem, setActiveItem] = useState(null);

  const handleShortcut = (item) => {
    setActiveItem(prev => prev === item.label ? null : item.label);
    navigate(item.path);
  };

  return (
    <AppBar position="static" sx={{ backgroundColor: COLORS.card, borderBottom: `1px solid ${COLORS.border}` }}>
      <Toolbar sx={{ justifyContent: 'space-between' }}>
        <Box sx={{ display: 'flex', alignItems: 'center', gap: 1 }}>
          <Typography
            variant="h6"
            sx={{ color: COLORS.text, fontWeight: 'bold', fontSize: { xs: '0.9rem', sm: '1.25rem' }, display: { xs: 'none', sm: 'block' } }}
          >
            HEXSERA PINBALL
          </Typography>
          <Button variant="contained" onClick={() => navigate('/pinball')} sx={{ backgroundColor: COLORS.primary, color: COLORS.text }}>
            게임하기
          </Button>
          <Box sx={{ display: 'flex', alignItems: 'center', gap: 0.5, ml: 1 }}>
            {SHORTCUTS.map((item) => (
              <Button
                key={item.label}
                onClick={() => handleShortcut(item)}
                sx={{
                  color: activeItem === item.label ? COLORS.primary : COLORS.text,
                  backgroundColor: activeItem === item.label ? 'rgba(79,70,229,0.15)' : 'transparent',
                  borderBottom: activeItem === item.label ? `2px solid ${COLORS.primary}` : '2px solid transparent',
                  borderRadius: 0,
                  px: 1.5,
                  '&:hover': {
                    backgroundColor: 'rgba(79,70,229,0.1)',
                    color: COLORS.primary,
                  },
                  transition: 'all 0.15s ease',
                }}
              >
                {item.label}
              </Button>
            ))}
          </Box>
        </Box>
        <HeaderUserInfo buttonColor={COLORS.primary} buttonTextColor={COLORS.text} outlinedBorderColor={COLORS.text} />
      </Toolbar>
    </AppBar>
  );
}

export default HomeHeader;
```

**변경 의도:** 헤더에 바로가기 섹션을 추가. `SHORTCUTS` 배열로 추후 아이템 확장 가능. `activeItem` state로 클릭된 항목의 toggle 상태 관리. hover/active 시 배경색·텍스트색·하단 border 변경.

---

### 2. `frontend/src/pages/Notice/NoticeListPage.jsx` 수정

**변경 전:**
```jsx
import { useState, useEffect, useContext } from 'react';
import { useNavigate } from 'react-router-dom';
import { Box, AppBar, Toolbar, Typography, Button, Container, List,
         ListItemButton, ListItemText, Divider, Paper } from '@mui/material';
import { AuthContext } from '../../contexts/AuthContext';
import HeaderUserInfo from '../../components/HeaderUserInfo';
import { getNotices } from '../../services/noticeService';

// ...

  return (
    <Box sx={{ minHeight: '100vh', backgroundColor: COLORS.bg }}>
      <AppBar position="static" sx={{ backgroundColor: COLORS.card,
                                      borderBottom: `1px solid ${COLORS.border}` }}>
        <Toolbar sx={{ justifyContent: 'space-between' }}>
          <Typography variant="h6" sx={{ color: COLORS.text, cursor: 'pointer' }}
                      onClick={() => navigate('/')}>HEXSERA PINBALL</Typography>
          <HeaderUserInfo buttonColor={COLORS.primary} buttonTextColor={COLORS.text}
                          outlinedBorderColor={COLORS.text} />
        </Toolbar>
      </AppBar>
```

**변경 후:**
```jsx
import { useState, useEffect, useContext } from 'react';
import { useNavigate } from 'react-router-dom';
import { Box, Button, Container, List,
         ListItemButton, ListItemText, Divider, Paper, Typography } from '@mui/material';
import { AuthContext } from '../../contexts/AuthContext';
import HomeHeader from '../../components/HomeHeader';
import { getNotices } from '../../services/noticeService';

// ...

  return (
    <Box sx={{ minHeight: '100vh', backgroundColor: COLORS.bg }}>
      <HomeHeader />
```

**변경 의도:** 인라인 AppBar 제거, HomeHeader 컴포넌트로 교체. 공지사항 페이지에서도 홈 헤더의 바로가기 섹션이 표시되도록.

---

### 3. `frontend/src/pages/Notice/NoticeDetailPage.jsx` 수정

**변경 전:**
```jsx
import { useState, useEffect, useContext } from 'react';
import { useParams, useNavigate } from 'react-router-dom';
import { Box, AppBar, Toolbar, Typography, Button, Container,
         Paper, Divider } from '@mui/material';
import { AuthContext } from '../../contexts/AuthContext';
import HeaderUserInfo from '../../components/HeaderUserInfo';
import { getNotice, deleteNotice } from '../../services/noticeService';

// ...

  return (
    <Box sx={{ minHeight: '100vh', backgroundColor: COLORS.bg }}>
      <AppBar position="static" sx={{ backgroundColor: COLORS.card,
                                      borderBottom: `1px solid ${COLORS.border}` }}>
        <Toolbar sx={{ justifyContent: 'space-between' }}>
          <Typography variant="h6" sx={{ color: COLORS.text, cursor: 'pointer' }}
                      onClick={() => navigate('/')}>HEXSERA PINBALL</Typography>
          <HeaderUserInfo buttonColor={COLORS.primary} buttonTextColor={COLORS.text}
                          outlinedBorderColor={COLORS.text} />
        </Toolbar>
      </AppBar>
```

**변경 후:**
```jsx
import { useState, useEffect, useContext } from 'react';
import { useParams, useNavigate } from 'react-router-dom';
import { Box, Typography, Button, Container, Paper, Divider } from '@mui/material';
import { AuthContext } from '../../contexts/AuthContext';
import HomeHeader from '../../components/HomeHeader';
import { getNotice, deleteNotice } from '../../services/noticeService';

// ...

  return (
    <Box sx={{ minHeight: '100vh', backgroundColor: COLORS.bg }}>
      <HomeHeader />
```

**변경 의도:** NoticeListPage와 동일하게 인라인 AppBar 제거, HomeHeader로 교체.

---

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| 홈페이지 헤더 바로가기 섹션 | 신규 추가 (공지사항 버튼) | 미확인 |
| 헤더 hover 효과 | 신규 추가 | 미확인 |
| 헤더 toggle 효과 | 신규 추가 | 미확인 |
| NoticeListPage 헤더 | 인라인 AppBar → HomeHeader 교체 | 미확인 |
| NoticeDetailPage 헤더 | 인라인 AppBar → HomeHeader 교체 | 미확인 |
| 홈페이지 기존 헤더 (로고, 게임하기, HeaderUserInfo) | 변경 없음 | 미확인 |

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- 브라우저에서 hover/toggle 효과 시각 확인
- 필요 시 NoticeWritePage 헤더도 HomeHeader로 교체 검토

---

# 업무일지

## 기본 정보
- 날짜: 2026-03-08 (일) 16:50~17:50
- 작성자: hexsera

## 진행 목표

공지사항 API/DB 없이 프론트엔드 단독으로 테스트할 수 있도록 IndexedDB를 Mock 폴백으로 도입한다.
- API 호출 실패 시 IndexedDB에서 Mock 공지 데이터를 반환
- 페이지 컴포넌트는 수정하지 않고 서비스 레이어에서만 처리

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| 실행계획 작성 | `docs/prd/IndexDB-공지사항-폴백-실행계획.md` | PRD 분석 후 계획 수립 |
| IndexedDB 유틸 모듈 생성 | `frontend/src/utils/noticeIndexDB.js` | DB 열기, Mock 초기화, 목록/단건 조회 |
| noticeService에 폴백 로직 추가 | `frontend/src/services/noticeService.js` | getNotices, getNotice에 try/catch 폴백 |

**총 약 20분**

## 주요 변경 사항

### 1. `frontend/src/utils/noticeIndexDB.js` 신규 생성

**추가:**
```js
const DB_NAME = 'pinball_notice_db';
const DB_VERSION = 1;
const STORE_NAME = 'notices';

function openDB() {
  return new Promise((resolve, reject) => {
    const req = indexedDB.open(DB_NAME, DB_VERSION);
    req.onupgradeneeded = (e) => {
      const db = e.target.result;
      if (!db.objectStoreNames.contains(STORE_NAME)) {
        db.createObjectStore(STORE_NAME, { keyPath: 'id' });
      }
    };
    req.onsuccess = (e) => resolve(e.target.result);
    req.onerror = (e) => reject(e.target.error);
  });
}

const MOCK_NOTICES = [
  { id: 1, title: '[공지] 서비스 오픈 안내', content: '<p>핀볼 웹 서비스가 오픈되었습니다.</p>', created_at: '2026-03-01T00:00:00' },
  { id: 2, title: '[공지] 점검 안내', content: '<p>3월 10일 오전 2시~4시 서버 점검이 있습니다.</p>', created_at: '2026-03-05T00:00:00' },
];

export async function seedNoticesIfEmpty() {
  const db = await openDB();
  const tx = db.transaction(STORE_NAME, 'readonly');
  const count = await new Promise((res) => {
    const req = tx.objectStore(STORE_NAME).count();
    req.onsuccess = () => res(req.result);
  });
  if (count === 0) {
    const writeTx = db.transaction(STORE_NAME, 'readwrite');
    MOCK_NOTICES.forEach((n) => writeTx.objectStore(STORE_NAME).put(n));
  }
}

export async function getAllNoticesFromDB() {
  const db = await openDB();
  return new Promise((resolve, reject) => {
    const tx = db.transaction(STORE_NAME, 'readonly');
    const req = tx.objectStore(STORE_NAME).getAll();
    req.onsuccess = () => resolve(req.result);
    req.onerror = (e) => reject(e.target.error);
  });
}

export async function getNoticeFromDB(id) {
  const db = await openDB();
  return new Promise((resolve, reject) => {
    const tx = db.transaction(STORE_NAME, 'readonly');
    const req = tx.objectStore(STORE_NAME).get(Number(id));
    req.onsuccess = () => resolve(req.result ?? null);
    req.onerror = (e) => reject(e.target.error);
  });
}
```

**변경 의도:** API 없이 공지사항 페이지를 테스트할 수 있도록 IndexedDB를 Mock 데이터 저장소로 사용. 나중에 API 완성 시 이 파일을 삭제하여 정리.

---

### 2. `frontend/src/services/noticeService.js` 수정

**변경 전:**
```js
import axios from 'axios';

export const getNotices = () =>
  axios.get('/api/v1/notices').then(res => res.data);

export const getNotice = (id) =>
  axios.get(`/api/v1/notices/${id}`).then(res => res.data);

export const createNotice = (title, content) =>
  axios.post('/api/v1/notices', { title, content }).then(res => res.data);

export const deleteNotice = (id) =>
  axios.delete(`/api/v1/notices/${id}`).then(res => res.data);

export const uploadNoticeImage = (file) => {
  const formData = new FormData();
  formData.append('file', file);
  return axios.post('/api/v1/notices/upload-image', formData).then(res => res.data);
};
```

**변경 후:**
```js
import axios from 'axios';
import { seedNoticesIfEmpty, getAllNoticesFromDB, getNoticeFromDB } from '../utils/noticeIndexDB';

export const getNotices = async () => {
  try {
    const res = await axios.get('/api/v1/notices');
    return res.data;
  } catch {
    await seedNoticesIfEmpty();
    return getAllNoticesFromDB();
  }
};

export const getNotice = async (id) => {
  try {
    const res = await axios.get(`/api/v1/notices/${id}`);
    return res.data;
  } catch {
    await seedNoticesIfEmpty();
    return getNoticeFromDB(id);
  }
};

export const createNotice = (title, content) =>
  axios.post('/api/v1/notices', { title, content }).then(res => res.data);

export const deleteNotice = (id) =>
  axios.delete(`/api/v1/notices/${id}`).then(res => res.data);

export const uploadNoticeImage = (file) => {
  const formData = new FormData();
  formData.append('file', file);
  return axios.post('/api/v1/notices/upload-image', formData).then(res => res.data);
};
```

**변경 의도:** `getNotices`, `getNotice`에 try/catch 폴백 추가. API 성공 시 API 데이터 반환, 실패 시 IndexedDB Mock 데이터 반환. `createNotice`, `deleteNotice`, `uploadNoticeImage`는 폴백 없이 유지 (쓰기 작업은 API 필수).

---

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| `/notice` 공지사항 목록 조회 | API 실패 시 IndexedDB 폴백 추가 | 미확인 |
| `/notice/:id` 공지사항 단건 조회 | API 실패 시 IndexedDB 폴백 추가 | 미확인 |
| API 정상 시 공지사항 목록/상세 조회 | 변경 없음 (API 우선) | 미확인 |
| 공지사항 작성/삭제/이미지 업로드 | 변경 없음 | 미확인 |

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- 브라우저에서 API 없이 `/notice` 접근 시 Mock 공지 2건 표시되는지 시각 확인
- API 완성 후 `noticeIndexDB.js` 삭제 + `noticeService.js` try/catch 제거

---

# 업무일지

## 기본 정보
- 날짜: 2026-03-08 (일) 18:30~19:20
- 작성자: hexsera

## 진행 목표

공지사항 이미지 업로드 방식을 서버 API 방식에서 Base64 인라인 방식으로 전환하고, 업로드 이미지의 크기를 에디터 너비에 맞게 자동 리사이징한다.

1. `handleImageUpload`를 `FileReader` + `canvas` 기반 Base64 방식으로 교체 (서버 API 제거)
2. 이미지 너비 800px 초과 시 canvas로 리사이징 후 JPEG 80% 품질로 변환

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| handleImageUpload Base64 방식으로 교체 | `frontend/src/pages/Notice/NoticeWritePage.jsx` | FileReader + canvas 리사이징 |
| uploadNoticeImage 함수 제거 | `frontend/src/services/noticeService.js` | Dead code 제거 |
| uploadNoticeImage 테스트 블록 제거 | `frontend/src/test/noticeService.test.js` | 삭제된 함수 테스트 정리 |
| NoticeWritePage 테스트 — mock 정리 및 리사이징 테스트 교체 | `frontend/src/test/NoticeWritePage.test.jsx` | canvas/Image mock 기반 검증 |

**총 약 45분**

## 주요 변경 사항

### 1. `frontend/src/pages/Notice/NoticeWritePage.jsx` 수정

**변경 전 (`handleImageUpload` 함수 + import):**
```jsx
import { createNotice, uploadNoticeImage } from '../../services/noticeService';

const handleImageUpload = async (e) => {
  const file = e.target.files[0];
  if (!file) return;
  const data = await uploadNoticeImage(file);
  editor.chain().focus().setImage({ src: data.url }).run();
};
```

**변경 후:**
```jsx
import { createNotice } from '../../services/noticeService';

const handleImageUpload = (e) => {
  const file = e.target.files[0];
  if (!file) return;
  const reader = new FileReader();
  reader.onload = () => {
    const img = new window.Image();
    img.onload = () => {
      const MAX_WIDTH = 800;
      const scale = img.width > MAX_WIDTH ? MAX_WIDTH / img.width : 1;
      const canvas = document.createElement('canvas');
      canvas.width = img.width * scale;
      canvas.height = img.height * scale;
      canvas.getContext('2d').drawImage(img, 0, 0, canvas.width, canvas.height);
      const src = canvas.toDataURL('image/jpeg', 0.8);
      editor.chain().focus().setImage({ src }).run();
    };
    img.src = reader.result;
  };
  reader.readAsDataURL(file);
};
```

**변경 의도:**
- 서버 API(`POST /api/v1/notices/upload-image`) 없이 브라우저만으로 이미지 처리
- 에디터 너비(820px)보다 약간 작은 800px을 MAX_WIDTH로 설정해 이미지 삽입 시 레이아웃 이탈 방지
- JPEG 80% 품질로 압축해 Base64 문자열 크기 최소화
- `new window.Image()` 사용: Vite SSR 환경에서 `new Image()`가 ESM import로 처리되는 문제 우회

---

### 2. `frontend/src/services/noticeService.js` 수정

**변경 전 (파일 전체):**
```js
import axios from 'axios';
import { seedNoticesIfEmpty, getAllNoticesFromDB, getNoticeFromDB } from '../utils/noticeIndexDB';

export const getNotices = async () => {
  try {
    const res = await axios.get('/api/v1/notices');
    return res.data;
  } catch {
    await seedNoticesIfEmpty();
    return getAllNoticesFromDB();
  }
};

export const getNotice = async (id) => {
  try {
    const res = await axios.get(`/api/v1/notices/${id}`);
    return res.data;
  } catch {
    await seedNoticesIfEmpty();
    return getNoticeFromDB(id);
  }
};

export const createNotice = (title, content) =>
  axios.post('/api/v1/notices', { title, content }).then(res => res.data);

export const deleteNotice = (id) =>
  axios.delete(`/api/v1/notices/${id}`).then(res => res.data);

export const uploadNoticeImage = (file) => {
  const formData = new FormData();
  formData.append('file', file);
  return axios.post('/api/v1/notices/upload-image', formData).then(res => res.data);
};
```

**변경 후 (파일 전체):**
```js
import axios from 'axios';
import { seedNoticesIfEmpty, getAllNoticesFromDB, getNoticeFromDB } from '../utils/noticeIndexDB';

export const getNotices = async () => {
  try {
    const res = await axios.get('/api/v1/notices');
    return res.data;
  } catch {
    await seedNoticesIfEmpty();
    return getAllNoticesFromDB();
  }
};

export const getNotice = async (id) => {
  try {
    const res = await axios.get(`/api/v1/notices/${id}`);
    return res.data;
  } catch {
    await seedNoticesIfEmpty();
    return getNoticeFromDB(id);
  }
};

export const createNotice = (title, content) =>
  axios.post('/api/v1/notices', { title, content }).then(res => res.data);

export const deleteNotice = (id) =>
  axios.delete(`/api/v1/notices/${id}`).then(res => res.data);
```

**변경 의도:** Base64 방식 전환으로 `uploadNoticeImage`가 완전히 불필요해짐. 존재하지 않는 API를 참조하는 Dead Code 제거.

---

### 3. `frontend/src/test/noticeService.test.js` 수정

**변경 전 (상단 import + 제거된 블록):**
```js
import { getNotices, getNotice, createNotice, deleteNotice, uploadNoticeImage } from '../services/noticeService';

// ...

describe('uploadNoticeImage', () => {
  it('POST /api/v1/notices/upload-image를 FormData로 호출한다', async () => {
    axios.post.mockResolvedValue({ data: { url: '/uploads/img.png' } });
    const file = new File(['content'], 'img.png', { type: 'image/png' });
    await uploadNoticeImage(file);
    expect(axios.post).toHaveBeenCalledWith(
      '/api/v1/notices/upload-image',
      expect.any(FormData)
    );
    const formData = axios.post.mock.calls[0][1];
    expect(formData.get('file')).toBe(file);
  });

  it('서버 응답 data를 반환한다', async () => {
    const mockData = { url: '/uploads/img.png' };
    axios.post.mockResolvedValue({ data: mockData });
    const file = new File(['content'], 'img.png', { type: 'image/png' });
    const result = await uploadNoticeImage(file);
    expect(result).toEqual(mockData);
  });
});
```

**변경 후 (상단 import):**
```js
import { getNotices, getNotice, createNotice, deleteNotice } from '../services/noticeService';
// uploadNoticeImage describe 블록 전체 삭제
```

**변경 의도:** 삭제된 `uploadNoticeImage` 함수에 대한 테스트 제거. 존재하지 않는 export를 import하면 테스트 실행 자체가 실패함.

---

### 4. `frontend/src/test/NoticeWritePage.test.jsx` 수정

**변경 전 (mock 설정 + 이미지 테스트):**
```jsx
// mock 설정
const mockEditorChain = () => ({
  focus: () => ({
    toggleBold: () => ({ run: vi.fn() }),
    toggleItalic: () => ({ run: vi.fn() }),
    toggleHeading: () => ({ run: vi.fn() }),
    setImage: () => ({ run: vi.fn() }),  // spy 없음
  }),
});

vi.mock('../services/noticeService', () => ({
  createNotice: vi.fn().mockResolvedValue({ id: 1 }),
  uploadNoticeImage: vi.fn().mockResolvedValue({ url: '/uploads/img.png' }),
}));

// 이미지 테스트 없음
```

**변경 후 (mock 설정 + 이미지 테스트):**
```jsx
// mock 설정 — mockSetImage spy 추가
const mockSetImage = vi.fn();
const mockEditorChain = () => ({
  focus: () => ({
    toggleBold: () => ({ run: vi.fn() }),
    toggleItalic: () => ({ run: vi.fn() }),
    toggleHeading: () => ({ run: vi.fn() }),
    setImage: (args) => { mockSetImage(args); return { run: vi.fn() }; },
  }),
});

vi.mock('../services/noticeService', () => ({
  createNotice: vi.fn().mockResolvedValue({ id: 1 }),
  // uploadNoticeImage 제거
}));

// 추가된 이미지 리사이징 테스트
it('이미지 파일 선택 시 canvas로 리사이징 후 setImage가 호출된다', async () => {
  mockSetImage.mockClear();

  const mockDataURL = 'data:image/jpeg;base64,resized';
  const mockCanvas = {
    width: 0, height: 0,
    getContext: () => ({ drawImage: vi.fn() }),
    toDataURL: () => mockDataURL,
  };
  const origCreateElement = document.createElement.bind(document);
  vi.spyOn(document, 'createElement').mockImplementation((tag) =>
    tag === 'canvas' ? mockCanvas : origCreateElement(tag)
  );

  const OrigImage = window.Image;
  window.Image = class {
    constructor() { this.width = 1200; this.height = 800; }
    set src(_) { this.onload(); }
  };

  global.FileReader = class {
    readAsDataURL() { this.result = 'data:image/png;base64,abc'; this.onload(); }
  };

  const input = document.querySelector('input[type="file"]');
  fireEvent.change(input, { target: { files: [new File(['img'], 'test.png', { type: 'image/png' })] } });

  await Promise.resolve();

  expect(mockSetImage).toHaveBeenCalledWith({ src: mockDataURL });

  window.Image = OrigImage;
  vi.restoreAllMocks();
});
```

**변경 의도:** `uploadNoticeImage` mock 제거, `setImage` 호출을 추적하는 `mockSetImage` spy를 모듈 최상단에 선언하여 canvas 리사이징 후 `setImage({ src: ... })`가 올바르게 호출되는지 검증.

---

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| 공지사항 이미지 업로드 | 서버 API 방식 → FileReader + canvas Base64 방식 | 정상 (Vitest 14/14 통과) |
| 이미지 리사이징 | 신규 추가 (MAX_WIDTH 800px, JPEG 80%) | 정상 (Vitest 통과) |
| 공지사항 저장 (이미지 포함) | Base64 img 태그가 HTML content에 포함되어 createNotice 전달 | 미확인 (브라우저 미확인) |
| 공지사항 상세 이미지 렌더링 | dangerouslySetInnerHTML 방식 그대로 — 변경 없음 | 미확인 |

## 결과
- 상태: 완료
- Vitest 14/14 통과 (NoticeWritePage), 8/8 통과 (noticeService)

## 배운 내용

## 다음 작업
- 브라우저에서 이미지 업로드 → 에디터 삽입 → 저장 → 상세 페이지 렌더링 흐름 시각 확인
- 네트워크 탭에서 upload 요청이 발생하지 않는지 확인
