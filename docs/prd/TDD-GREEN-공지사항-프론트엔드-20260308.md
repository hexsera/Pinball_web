# TDD GREEN 완료: 공지사항 프론트엔드 (섹션 1, 2, 4)

## 구현 완료 항목

- Tiptap 패키지 설치 (@tiptap/react, @tiptap/starter-kit, @tiptap/extension-image 각 3.20.1)
- `noticeService.js` — 공지사항 API 호출 서비스 레이어 5개 함수
- `NoticeWritePage.jsx` — Tiptap 에디터 기반 공지사항 작성 페이지
- `AuthContext.jsx` — `AuthContext` named export 추가 (기존 default export 없었음)

## 변경된 파일

| 파일 | 변경 내용 |
|------|----------|
| `frontend/src/services/noticeService.js` | 신규 생성 — getNotices, getNotice, createNotice, deleteNotice, uploadNoticeImage |
| `frontend/src/pages/Notice/NoticeWritePage.jsx` | 신규 생성 — Tiptap 에디터, 접근 제어, 툴바, 저장/취소 |
| `frontend/src/contexts/AuthContext.jsx` | `AuthContext` named export 추가 |
| `frontend/package.json` | Tiptap 패키지 3종 의존성 추가 |

## 통과한 테스트 목록 (23개)

### noticeService.test.js (10개)
1. getNotices — GET /api/v1/notices를 호출한다
2. getNotices — 서버 응답 data 배열을 반환한다
3. getNotice — GET /api/v1/notices/{id}를 호출한다
4. getNotice — 서버 응답 data 객체를 반환한다
5. createNotice — POST /api/v1/notices를 title, content와 함께 호출한다
6. createNotice — 서버 응답 data를 반환한다
7. deleteNotice — DELETE /api/v1/notices/{id}를 호출한다
8. deleteNotice — 서버 응답 data를 반환한다
9. uploadNoticeImage — POST /api/v1/notices/upload-image를 FormData로 호출한다
10. uploadNoticeImage — 서버 응답 data를 반환한다

### NoticeWritePage.test.jsx (13개)
11. 비로그인 상태에서 /notice로 리다이렉트된다
12. 일반 사용자(role=user) 접근 시 /notice로 리다이렉트된다
13. admin 사용자는 페이지가 렌더링된다
14. 제목 입력 필드가 존재한다
15. Bold(B) 툴바 버튼이 존재한다
16. Italic(I) 툴바 버튼이 존재한다
17. H2 툴바 버튼이 존재한다
18. 이미지 업로드 버튼이 존재한다
19. 저장 버튼이 존재한다
20. 취소 버튼이 존재한다
21. 취소 버튼 클릭 시 /notice로 이동한다
22. 저장 버튼 클릭 시 createNotice가 호출된다
23. 저장 후 /notice로 이동한다

## 다음 단계

실행계획 섹션 3(NoticeListPage), 5(NoticeDetailPage), 6(App.jsx 라우트), 7(HomePage 버튼) 구현
