# 목표
api 에 월간 랭킹 테이블을 조작할 CRUD 를 생성한다.

## 세부사항
model 은 MonthlyScore 이다.
api 주소는 ai가 추천한다. 단 CRUD 형태에 맞도록 한다.
db와 연결을 하지 말고 임시 메모리로 테스트 할 수 있도록 한다.

Create 는 데이터를 저장하되 같은 아이디가 존재한다면 해당 레코드의 점수를 수정하도록 하여야한다.