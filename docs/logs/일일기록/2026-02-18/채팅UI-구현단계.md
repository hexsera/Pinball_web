# 채팅 UI 구현 단계

## 전제 조건

- Phase 1 (WebSocket 인프라) 완료 후 진행
- `PinballPage.jsx` 레이아웃 수정이 핵심 작업
- MUI 컴포넌트를 활용하여 기존 디자인 시스템과 통일

---

## Step 1: 레이아웃 구조 변경 (`PinballPage.jsx`)

**목표**: 현재 게임만 있는 화면을 `게임 영역 + 채팅 패널` 2열 레이아웃으로 전환

**현재 구조**:
```
┌─────────────────────────┐
│         Header          │
├─────────────────────────┤
│                         │
│       게임 영역          │
│     (중앙 정렬)          │
│                         │
└─────────────────────────┘
```

**변경 후 구조 (데스크탑)**:
```
┌─────────────────────────────────────┐
│              Header                 │
├──────────────────────┬──────────────┤
│                      │              │
│      게임 영역        │   채팅 패널   │
│                      │   (320px)    │
│                      │              │
└──────────────────────┴──────────────┘
```

**변경 후 구조 (모바일)**:
```
┌─────────────────────┐
│       Header        │
├─────────────────────┤
│      게임 영역       │
├─────────────────────┤
│      채팅 패널       │
│    (높이 고정)       │
└─────────────────────┘
```

**작업 내용**:
- 게임 영역 `<Box>`의 `flexGrow: 1` 유지, `flex-direction: row` 추가
- 채팅 패널 영역 `<Box>` 추가: 데스크탑 `width: 320px`, 모바일 `height: 240px`
- `gameScale` 계산 시 채팅 패널 너비(320px)를 `padding`에 반영하여 게임 영역 압박 방지
- `isMobile` 분기로 `flexDirection: isMobile ? 'column' : 'row'` 처리

---

## Step 2: ChatPanel 컴포넌트 생성

**파일**: `frontend/src/components/Chat/ChatPanel.jsx`

**구성 요소**:

```
┌──────────────────┐
│  [일반채팅] [AI]  │  ← Tabs (MUI)
├──────────────────┤
│                  │
│   메시지 목록     │  ← 스크롤 가능 영역
│                  │
│   (닉네임)       │
│   메시지 내용     │
│   시간           │
│                  │
├──────────────────┤
│ [입력창] [전송]   │  ← TextField + Button
└──────────────────┘
```

**Props**:
- `roomId`: 현재 방 ID
- `currentUser`: `{ user_id, nickname }` 객체
- `mode`: `'chat'` | `'ai'` (탭 상태)

**내부 상태**:
- `messages`: 메시지 배열 `[{ id, nickname, content, created_at, isAI }]`
- `inputValue`: 입력창 텍스트
- `activeTab`: 활성 탭 (`0`: 일반채팅, `1`: AI)
- `isConnected`: WebSocket 연결 상태

---

## Step 3: ChatMessage 컴포넌트 생성

**파일**: `frontend/src/components/Chat/ChatMessage.jsx`

**Props**:
- `message`: `{ nickname, content, created_at, isAI, isMine }`

### 컴포넌트 트리 구조

```
<Box>                          ← 행 전체 (정렬 방향 결정)
  <Box>                        ← 말풍선 래퍼 (최대 너비 제한)
    <Paper>                    ← 말풍선 본체 (배경색, 모서리)
      <Box>                    ← 상단 행 (닉네임 + 시간)
        <Typography>           ← 닉네임
        <Typography>           ← 시간 (HH:MM)
      <Typography>             ← 메시지 본문
```

### 각 컴포넌트 역할과 MUI props

#### 1. 최외곽 `<Box>` — 행 정렬

```jsx
<Box sx={{
  display: 'flex',
  justifyContent: isMine ? 'flex-end' : 'flex-start',
  mb: 1,
  px: 1,
}} />
```

- `isMine`이면 `flex-end`(오른쪽), 아니면 `flex-start`(왼쪽)
- `mb: 1`로 메시지 간 간격 확보

---

#### 2. 말풍선 래퍼 `<Box>` — 최대 너비 제한

```jsx
<Box sx={{ maxWidth: '80%' }} />
```

- `maxWidth: '80%'`로 긴 메시지가 패널 전체를 차지하지 않도록 제한

---

#### 3. `<Paper>` — 말풍선 본체

```jsx
<Paper
  elevation={0}
  sx={{
    px: 1.5,
    py: 1,
    borderRadius: isMine ? '12px 12px 2px 12px' : '12px 12px 12px 2px',
    backgroundColor: isMine ? '#1E40AF' : isAI ? '#1E3A2F' : '#1E293B',
  }}
/>
```

| 조건 | `backgroundColor` | `borderRadius` |
|------|-------------------|----------------|
| `isMine` | `#1E40AF` (파란색) | 오른쪽 아래 뾰족 |
| `isAI` | `#1E3A2F` (초록색) | 왼쪽 아래 뾰족 |
| 상대방 | `#1E293B` (어두운 회색) | 왼쪽 아래 뾰족 |

- `elevation={0}`: 그림자 제거 (배경색으로만 구분)
- `borderRadius`: 카카오톡 스타일 말풍선 꼬리 방향 표현

---

#### 4. 상단 행 `<Box>` — 닉네임 + 시간

```jsx
<Box sx={{
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
  gap: 1,
  mb: 0.5,
}} />
```

- `justifyContent: 'space-between'`으로 닉네임은 왼쪽, 시간은 오른쪽 끝 배치

---

#### 5. 닉네임 `<Typography>`

```jsx
<Typography
  variant="caption"
  sx={{
    color: isMine ? '#93C5FD' : isAI ? '#6EE7B7' : '#94A3B8',
    fontWeight: 600,
    lineHeight: 1,
  }}
>
  {isAI ? '🤖 AI' : message.nickname}
</Typography>
```

- `variant="caption"`: MUI 최소 폰트 크기 (12px), 닉네임에 적합
- AI 메시지는 닉네임 대신 `🤖 AI` 표시

---

#### 6. 시간 `<Typography>`

```jsx
<Typography
  variant="caption"
  sx={{ color: '#64748B', lineHeight: 1 }}
>
  {format(message.created_at)}  {/* → "14:32" */}
</Typography>
```

- `variant="caption"` 동일 사용, 색상을 더 흐리게 처리해 닉네임과 구분
- `created_at` ISO 문자열을 `HH:MM` 형식으로 변환하는 유틸 함수 필요

---

#### 7. 메시지 본문 `<Typography>`

```jsx
<Typography
  variant="body2"
  sx={{
    color: '#F1F5F9',
    wordBreak: 'break-word',
    whiteSpace: 'pre-wrap',
    lineHeight: 1.5,
  }}
>
  {message.content}
</Typography>
```

- `variant="body2"`: 14px, 채팅 본문에 적합
- `wordBreak: 'break-word'`: URL이나 긴 단어 자동 줄바꿈
- `whiteSpace: 'pre-wrap'`: 사용자가 입력한 줄바꿈(`\n`) 보존

---

### 렌더링 예시

**내 메시지 (isMine)**:
```
                    ┌─────────────────┐
                    │ 나       14:32  │
                    │ 안녕하세요!      │
                    └──────────────┐  │
                                   └──┘
```

**상대방 메시지**:
```
┌─────────────────┐
│  │ 홍길동  14:33 │
│  └──┐ 안녕!     │
└─────┘
```

**AI 메시지 (isAI)**:
```
┌──────────────────────────┐
│  │ 🤖 AI       14:34    │
│  └──┐ 핀볼 최고 점수는... │
└─────┘
```

---

## Step 4: WebSocket 클라이언트 훅 생성

**파일**: `frontend/src/hooks/useChatWebSocket.js`

**목적**: WebSocket 연결 로직을 ChatPanel에서 분리하여 재사용 가능하게 만들기

**제공 값**:
```js
const {
  messages,      // 수신된 메시지 배열
  sendMessage,   // 메시지 전송 함수 (content: string) => void
  isConnected,   // 연결 상태 boolean
} = useChatWebSocket(roomId, currentUser);
```

**연결 로직**:
1. `roomId`, `currentUser` 변경 시 WebSocket 재연결
2. `ws://` 또는 `wss://` 자동 선택 (현재 호스트 기준)
3. 연결 끊김 시 3초 후 자동 재연결 (최대 5회 시도)
4. 컴포넌트 언마운트 시 `ws.close()` 호출

**수신 메시지 처리** (`ws.onmessage`):

서버가 보낸 JSON 문자열을 파싱해 `messages` 상태에 추가하는 것이 핵심이다.

```js
ws.onmessage = (event) => {
  const data = JSON.parse(event.data);
  // data 형식: { type, nickname, content, created_at }

  if (data.type === 'chat') {
    setMessages((prev) => [
      ...prev,
      {
        id: Date.now(),               // 임시 key용 ID
        nickname: data.nickname,
        content: data.content,
        created_at: data.created_at,
        isAI: false,
        isMine: data.nickname === currentUser.nickname,
      },
    ]);
  }
  // type === 'system' 이면 입장/퇴장 안내 등 별도 처리
};
```

**흐름 요약**:
```
서버 JSON 문자열 수신 (event.data)
  → JSON.parse()로 객체 변환
  → type === 'chat' 인지 확인
  → setMessages(prev => [...prev, 새메시지])
  → ChatPanel이 리렌더링되며 ChatMessage 목록에 추가 표시
```

**서버 → 클라이언트 메시지 형식**:
```js
{
  type: "chat",       // "chat" | "system"
  nickname: "홍길동",
  content: "안녕하세요",
  created_at: "2026-02-18T12:00:00"
}
```

---

## Step 5: AI 채팅 API 연동

**파일**: `frontend/src/api/aiChat.js`

**함수**:
```js
export async function sendAIMessage(content) {
  // POST /api/v1/ai-chat
  // Body: { message: content }
  // Return: { reply: "AI 응답 텍스트" }
}
```

**AI 탭 동작 흐름**:
1. 사용자 입력 → 즉시 `messages`에 추가 (isMine: true)
2. 로딩 인디케이터 표시 (isLoading: true)
3. `sendAIMessage()` 호출
4. 응답 수신 → `messages`에 AI 메시지 추가 (isAI: true)
5. 로딩 인디케이터 제거

---

## Step 6: ChatPanel을 PinballPage에 연결

**수정 파일**: `frontend/src/pages/PinballPage/PinballPage.jsx`

**추가 사항**:
- `ChatPanel` import
- `roomId` 상태 관리 (URL 쿼리 파라미터 또는 Context에서 읽기)
- `currentUser` AuthContext에서 읽기
- Step 1에서 변경한 레이아웃에 `<ChatPanel>` 삽입

---

## Step 7: 스타일 및 UX 마무리

**자동 스크롤**: 새 메시지 수신 시 메시지 목록 하단으로 자동 스크롤
```js
useEffect(() => {
  messagesEndRef.current?.scrollIntoView({ behavior: 'smooth' });
}, [messages]);
```

**Enter 키 전송**: `TextField`의 `onKeyDown`에서 `Enter` 감지 시 전송

**연결 상태 표시**:
- 연결됨: 탭 하단에 초록 점 표시
- 연결 끊김: 노란 점 + "재연결 중..." 텍스트

**빈 방일 때**: "채팅을 시작하세요" 안내 문구 표시

**모바일 최적화**: 채팅 패널 높이를 `240px`로 고정, 키보드 올라올 때 채팅창 축소 방지

---

## 구현 순서 요약

```
Step 1: PinballPage.jsx 레이아웃 변경 (flex row)
  ↓
Step 2: ChatPanel.jsx 생성 (껍데기, 하드코딩 메시지로 UI 확인)
  ↓
Step 3: ChatMessage.jsx 생성 (스타일 완성)
  ↓
Step 4: useChatWebSocket.js 훅 생성 (WebSocket 연결)
  ↓
Step 5: aiChat.js API 함수 생성 (AI 탭 연동)
  ↓
Step 6: ChatPanel에 훅/API 연결 (실제 동작)
  ↓
Step 7: 스크롤, Enter키, 상태 표시 등 UX 마무리
```

---

## 파일 구조

```
frontend/src/
├── components/
│   └── Chat/
│       ├── ChatPanel.jsx       ← Step 2
│       └── ChatMessage.jsx     ← Step 3
├── hooks/
│   └── useChatWebSocket.js     ← Step 4
├── api/
│   └── aiChat.js               ← Step 5
└── pages/
    └── PinballPage/
        └── PinballPage.jsx     ← Step 1, 6 수정
```

---

## 체크리스트

- [ ] Step 1: 데스크탑/모바일 레이아웃 전환 정상 동작 확인
- [ ] Step 2~3: 하드코딩 메시지로 ChatPanel 렌더링 확인
- [ ] Step 4: WebSocket 연결/해제 시 콘솔 오류 없음
- [ ] Step 4: 브라우저 2개에서 메시지 실시간 전달 확인
- [ ] Step 5: AI 탭에서 입력 후 응답 수신 확인
- [ ] Step 7: 새 메시지 수신 시 자동 스크롤 동작
- [ ] Step 7: Enter 키로 메시지 전송 동작
- [ ] 모바일에서 게임과 채팅 패널이 세로로 쌓여 정상 표시
