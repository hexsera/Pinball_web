# 공지사항 백엔드 구현 실행계획

## 요구사항 요약

**요구사항**: Notice DB 모델, Alembic 마이그레이션, CRUD API (GET 목록/상세, POST, DELETE) — PUT(수정) API + UI는 이번 범위 외

**목적**: 현재 IndexedDB mock 데이터에 의존하는 프론트엔드를 실제 PostgreSQL 서버 데이터로 전환

---

## 현재상태 분석

- 프론트엔드: `NoticeListPage`, `NoticeDetailPage`, `NoticeWritePage` 구현 완료
- `noticeService.js`: API 호출 실패 시 IndexedDB fallback으로 동작 중
- 백엔드: `notices.py` 라우터 없음, `Notice` 모델 없음, 스키마 없음

---

## 구현 방법

- 백엔드: 기존 `users.py`, `game_visits.py` 패턴 그대로 복사해 구현
- 스키마: `backend/app/schemas/notices.py` 신규 생성
- 라우터 경로: `""` 시작 (redirect_slashes=False 규칙 준수)
- 권한 검증: 없음 (인증 헤더 불필요)

---

## 구현 단계

### 1. Notice 모델 추가 (`backend/models.py`)

```python
from sqlalchemy import Text  # 기존 import 줄에 추가

class Notice(Base):
    __tablename__ = "notices"

    id         = Column(Integer, primary_key=True, index=True)
    title      = Column(String(255), nullable=False)
    content    = Column(Text, nullable=False)
    author_id  = Column(Integer, ForeignKey('users.id'), nullable=False)
    created_at = Column(DateTime, nullable=False, server_default=func.now())
    updated_at = Column(DateTime, nullable=False, server_default=func.now(), onupdate=func.now())
```

- `content`는 Base64 이미지 포함 HTML이므로 `Text` 타입 사용
- `author_id`는 `users.id` FK (글 작성자 식별용)

---

### 2. Pydantic 스키마 생성 (`backend/app/schemas/notices.py`)

```python
from pydantic import BaseModel
from datetime import datetime
from typing import Optional

class NoticeCreateRequest(BaseModel):
    title: str
    content: str

class NoticeResponse(BaseModel):
    id: int
    title: str
    content: str
    author_id: int
    created_at: datetime
    updated_at: datetime
    class Config:
        from_attributes = True

class NoticeListResponse(BaseModel):
    id: int
    title: str
    created_at: datetime
    class Config:
        from_attributes = True

class NoticeListResult(BaseModel):
    items: list[NoticeListResponse]
    total: int
```

- `NoticeListResult`: 프론트의 `{ items, total }` 구조와 일치
- `NoticeListResponse`: 목록에서 content 제외해 응답 크기 감소
- `NoticeUpdateRequest`: 이번 범위 외 (PUT 미구현)

---

### 3. API 라우터 생성 (`backend/app/api/v1/notices.py`)

```python
import sys
sys.path.insert(0, '/code')
from models import Notice
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from app.api.deps import get_db
from app.schemas.notices import (
    NoticeCreateRequest, NoticeResponse, NoticeListResult
)

router = APIRouter()

@router.get("", response_model=NoticeListResult)
def get_notices(skip: int = 0, limit: int = 10, db: Session = Depends(get_db)):
    total = db.query(Notice).count()
    items = db.query(Notice).order_by(Notice.created_at.desc()).offset(skip).limit(limit).all()
    return {"items": items, "total": total}

@router.get("/{notice_id}", response_model=NoticeResponse)
def get_notice(notice_id: int, db: Session = Depends(get_db)):
    notice = db.query(Notice).filter(Notice.id == notice_id).first()
    if not notice:
        raise HTTPException(status_code=404, detail="Notice not found")
    return notice

@router.post("", response_model=NoticeResponse)
def create_notice(body: NoticeCreateRequest, db: Session = Depends(get_db)):
    notice = Notice(title=body.title, content=body.content, author_id=1)
    db.add(notice); db.commit(); db.refresh(notice)
    return notice

@router.delete("/{notice_id}")
def delete_notice(notice_id: int, db: Session = Depends(get_db)):
    notice = db.query(Notice).filter(Notice.id == notice_id).first()
    if not notice:
        raise HTTPException(status_code=404, detail="Notice not found")
    db.delete(notice); db.commit()
    return {"message": "deleted", "id": notice_id}
```

**각 엔드포인트 동작 흐름:**

- `GET ""` (목록): `skip`, `limit` 쿼리 파라미터 수신 → 전체 개수 COUNT 조회 → `created_at` 내림차순으로 offset/limit 적용해 행 조회 → `{ items, total }` 반환
- `GET "/{notice_id}"` (상세): `notice_id` 수신 → DB에서 해당 id 행 조회 → 없으면 404 반환 → 있으면 전체 필드 반환
- `POST ""` (작성): `{ title, content }` 수신 → `author_id=1` 고정으로 Notice 행 생성 → DB에 저장 → 저장된 행 전체 필드 반환
- `DELETE "/{notice_id}"` (삭제): `notice_id` 수신 → DB에서 해당 id 행 조회 → 없으면 404 반환 → 있으면 행 삭제 → `{ message, id }` 반환

- `author_id=1`: 권한 검증 없으므로 admin 계정의 고정 ID 사용
- 모든 경로 `""` 시작 (307 방지)

---

### 4. main.py에 라우터 등록 (`backend/main.py`)

```python
# import 줄 수정
from app.api.v1 import users, auth, monthly_scores, game_visits, friends, chat, pinball_ai, game_sessions, notices

# 라우터 등록 줄 추가
app.include_router(notices.router, prefix="/api/v1/notices", tags=["Notices"])
```

- 기존 라우터 등록 패턴과 동일한 방식

---

### 5. Alembic 마이그레이션 실행

```bash
docker compose exec fastapi alembic revision --autogenerate -m "add notices table"
docker compose exec fastapi alembic upgrade head
```

- `autogenerate`가 `models.py`의 `Notice` 클래스를 감지해 DDL 생성
- `upgrade head`로 PostgreSQL에 `notices` 테이블 실제 생성

---

## 수정/생성할 파일 목록

| 파일 경로 | 작업 유형 | 변경 내용 |
|-----------|-----------|-----------|
| `backend/models.py` | 수정 | `Notice` 클래스 추가, `Text` import 추가 |
| `backend/app/schemas/notices.py` | 생성 | Pydantic 스키마 3개 정의 |
| `backend/app/api/v1/notices.py` | 생성 | 라우터 4개 정의 (GET 목록/상세, POST, DELETE) — PUT 미구현 |
| `backend/main.py` | 수정 | notices import 및 라우터 등록 |

---

## 완료 체크리스트

- [ ] `GET /api/v1/notices?skip=0&limit=10` → `{ items, total }` 응답 확인
- [ ] `GET /api/v1/notices/{id}` → 공지사항 상세 내용 응답 확인
- [ ] `POST /api/v1/notices` → 공지사항 작성 후 목록에 표시 확인
- [ ] `DELETE /api/v1/notices/{id}` → 삭제 후 목록에서 사라짐 확인
- [ ] PostgreSQL `notices` 테이블 생성 확인 (`docker compose exec fastapi alembic current`)
- [ ] `PUT /api/v1/notices/{id}` + 수정 UI: 미구현 (다음 계획)
- [ ] 브라우저 새로고침 후 IndexedDB fallback 없이 서버 데이터 표시 확인
