# Redis 부하 테스트 논의

- **날짜**: 2026-04-04
- **주제**: Redis 기반 랭킹 시스템 부하 테스트 설계 및 전략 논의

---

## 논의 배경

Redis Sorted Set 기반 랭킹 캐시(Phase 2)와 AOF 영속성(Phase 2 논의) 구현 이후,
실제 트래픽 상황에서 Redis 및 FastAPI 백엔드가 어떻게 동작하는지 검증이 필요하다.

---

## 목표

`GET /api/v1/monthly-scores` 엔드포인트를 기준으로, Redis 캐시 유무에 따른 성능 차이를 수치로 검증한다.

---

## 현재 구조 파악

### `GET /api/v1/monthly-scores` 요청 흐름

```
요청 수신
  └─ Redis Sorted Set 조회 (zrevrangebyscore)
       ├─ 캐시 히트 → 즉시 응답
       └─ 캐시 미스 → DB 조회(warm_up_sorted_set) → Redis 재구성 → 응답
```

- Redis 장애 시 자동으로 DB 직접 조회로 폴백 (`except Exception: pass`)
- `POST /api/v1/monthly-scores` → DB 쓰기 후 Redis zadd 동기화

---

## 비교 시나리오

| 시나리오 | 설명 |
|---|---|
| **A: Redis 없음** | Redis 비활성화 → 항상 DB 직접 조회 (폴백 경로) |
| **B: Redis 있음** | 현재 운영 방식 → Sorted Set 캐시 히트 |

---

## 측정 지표

| 지표 | 설명 |
|---|---|
| 응답 시간 (latency) | 평균, p50, p95, p99 |
| 처리량 (throughput) | RPS (Requests Per Second) |
| DB 쿼리 횟수 | 동시 N 요청 시 DB hit 수 |
| 에러율 | 부하 증가 시 오류 발생 지점 |

### 응답 시간 (latency) — p50, p95, p99

100개의 요청을 빠른 것부터 느린 것 순으로 줄을 세웠을 때, 앞에서 몇 번째에 해당하는지를 나타낸다.

```
1번(가장빠름) ... 50번 ... 95번 ... 99번 ... 100번(가장느림)
```

- **p50**: 50번째. 절반은 이것보다 빠르고 절반은 느리다. 일반적인 사용자 경험에 가깝다.
- **p95**: 95번째. 상위 5% 안에 드는 느린 요청들의 대표값. 평균이 50ms인데 p95가 800ms라면, 대부분은 빠르지만 느린 요청은 꽤 심하게 느리다는 것을 알 수 있다. 평균만 보면 이 사실이 숨겨진다.
- **p99**: 99번째. 가장 극단적인 1개(100번째)를 제외한 사실상 최악의 케이스. 100번째(최댓값)를 쓰지 않는 이유는 네트워크 순간 끊김이나 서버 GC 같은 일회성 이상치에 너무 민감하게 반응하기 때문. p99는 이런 우연적 이상치를 제거하면서도 "실제로 경험할 수 있는 최악"에 가장 가까운 값이다.

---

### 처리량 (throughput) — RPS

서버가 1초에 처리할 수 있는 요청 수. RPS = Requests Per Second.
Redis가 있을 때 RPS가 더 높다면, 같은 시간에 더 많은 사용자를 처리할 수 있다는 의미.

### DB 쿼리 횟수 — DB hit

요청이 들어왔을 때 데이터베이스까지 실제로 조회가 발생한 횟수.

- Redis 있음: 캐시에서 바로 응답 → DB hit 거의 0
- Redis 없음: 요청 100개 → DB 조회 100번 발생

DB는 Redis보다 훨씬 느리고 자원을 많이 사용하므로, 동시 요청이 많아질수록 DB hit 수가 병목이 된다.

### 에러율 — 부하 증가 시 오류 발생 지점

부하를 점점 높였을 때 서버가 요청을 정상 처리하지 못하고 오류(타임아웃, 연결 실패 등)를 반환하기 시작하는 지점.

예: 동시 요청 50개까지는 에러 0%인데 200개부터 에러율 5%가 된다면 → 한계는 50~200 사이.
Redis 유무에 따라 이 한계치가 달라지는지를 비교하는 것이 핵심.

---

## 결정 사항

- [x] **테스트 환경**: 실제 서버 — 이미 운영 중이므로 사전 준비 불필요. 포트폴리오용 사이트라 실제 사용자 없음.
- [x] **Redis 비활성화 방식**: Redis 컨테이너 다운 — 사용자가 없으므로 운영 영향 없음.

- [x] **부하 테스트 도구**: k6
  - 2026년 기준 GitHub 스타 수 1위 (~29,900개)
  - 2025 GigaOm Radar Report에서 Leader + Outperformer 선정
  - CI/CD 연동, Kubernetes 네이티브 테스트 표준으로 자리잡는 중
  - JavaScript/TypeScript로 시나리오 작성, 개발자 친화적

---

## mock 데이터 개수와 테스트 유의미성

### 결론

이 테스트에서 mock 데이터 개수는 측정 결과에 거의 영향을 주지 않는다. **현재 50개로 충분하다.**

### 이유

이번 테스트의 핵심 변수는 데이터 크기가 아니라 **요청 경로(Redis vs DB)**다.

- **Redis**: 메모리에서 Sorted Set 읽기 → 데이터 50개든 5000개든 응답 시간 차이 거의 없음
- **DB**: 네트워크 + 쿼리 실행 + 결과 직렬화 → 데이터 크기보다 **동시 요청 수**가 병목을 만드는 주요 원인

동시 100~200개 요청이 모두 DB를 직접 치면 커넥션 풀 경쟁이 발생하고 병목이 드러난다. 이것이 이번 테스트의 설계 의도다.

### 최소 조건

`monthly_scores` 레코드가 **0개**이면 Redis warm-up도 안 되고 빈 배열을 반환하게 되어 캐시 효과 측정 자체가 불가능하다. 최소 1개 이상 존재해야 한다.

---

## k6 실시간 모니터링 (웹 대시보드)

### 옵션 비교

| 방식 | 설명 | 설치 |
|---|---|---|
| **k6 내장 웹 대시보드** | 테스트 실행 중 브라우저에서 실시간 그래프 확인 | 불필요 (v0.47+) |
| **Grafana + InfluxDB** | 테스트 전에 대시보드 미리 띄워두고 데이터 유입 확인 가능 | docker-compose 추가 설정 필요 |
| **k6 Cloud** | 공식 클라우드 대시보드, 유료/무료 플랜 | 불필요 (`--out cloud`) |

### 내장 웹 대시보드 사용법

```bash
K6_WEB_DASHBOARD=true k6 run backend/tests/load/monthly_scores_load_test.js
```

- 실행 시 터미널에 `Web dashboard: http://localhost:5665` 출력
- 브라우저에서 `http://localhost:5665` 접속하면 RPS/응답시간/에러율 실시간 그래프 확인 가능
- **테스트 실행 중에만 접속 가능** — 독립 서버가 아닌 k6 프로세스에 내장된 방식이므로 테스트 전에는 켤 수 없음

### 결정

내장 웹 대시보드로 진행. 별도 설치 없이 k6 v1.7.1에서 바로 사용 가능.

