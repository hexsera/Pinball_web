# 게임 세션 유지 기능 구현

**날짜**: 2026-03-31 ~ 2026-04-01  
**상태**: 완료  
**연관 문서**: [2026-03-31-02-Redis-설치.md](2026-03-31-02-Redis-설치.md)

---

## 작업 개요

핀볼 게임 도중 페이지를 나갔다가 돌아와도 이전 게임 상태(점수·목숨·스테이지)를 이어서 진행할 수 있도록 세션 유지 기능을 추가한다.

**적용 대상**: 로그인 사용자만  
**저장 대상**: 점수, 목숨, 스테이지 (공 위치 제외 — 복귀 시 스테이지 시작 위치에서 재개)  
**저장 위치**: Redis

---

## 주요 결정 사항

| 결정 | 선택 | 이유 |
|------|------|------|
| 적용 대상 | 로그인 사용자만 | 비로그인 사용자는 서버 저장 불가 |
| 저장 위치 | Redis | 임시 데이터에 적합, TTL로 자동 만료 |
| 복구 정밀도 | 점수·목숨·스테이지만 | 공 위치는 물리 재현이 복잡하고 체감 차이 작음 |
| 세션 수 | 사용자당 1개 (Redis key로 구분) | 진행 중 세션은 항상 최신 1개만 유지 |
| 저장 타이밍 | 30초마다 + 목숨 감소 시 즉시 | 목숨 손실은 가장 중요한 상태 변화 |

---

## 완료된 작업

### Redis 설치 (완료)

- `docker-compose.yml`에 `redis:8.6.2` 서비스 추가
- `backend/requirements.txt`에 `redis==7.1.1` 추가
- `backend/app/redis_client.py` 신규 생성

```python
# backend/app/redis_client.py
import redis, os

redis_client = redis.Redis(
    host=os.getenv("REDIS_HOST", "redis-server"),
    port=int(os.getenv("REDIS_PORT", 6379)),
    decode_responses=True
)
```

- `backend/.env.example`에 `REDIS_HOST`, `REDIS_PORT` 항목 추가
- 테스트 조건 5가지 모두 통과 확인

---

## 완료된 프론트엔드 작업 (2026-04-01)

### gameSession.js 헬퍼 생성 (TDD 완료)

- `frontend/src/utils/gameSession.js` 신규 생성
- `frontend/src/test/gameSession.test.js` 신규 생성 (9개 케이스 전부 통과)
- 함수: `saveGameSession`, `loadGameSession`, `deleteGameSession`

### Pinball.jsx 수정 완료

- import 추가: `saveGameSession`, `loadGameSession`, `deleteGameSession`
- 마운트 시 세션 자동 복원 useEffect 추가
- 30초 주기 세션 저장 useEffect 추가
- deathZone 충돌 시 즉시 저장 추가
- `submitScore()` 내 게임오버 시 세션 삭제 추가
- `handleRestart()` 내 재시작 시 세션 삭제 추가

### 미완료 테스트 (나중에 수행)

백엔드 API 구현 완료. 아래 항목은 브라우저에서 직접 확인 필요 (미수행):
- [x] 로그인 후 페이지 이탈 → 재진입 시 점수·목숨·스테이지 복원 확인
- [x] 복원 후 게임 시작 화면(Space/클릭 대기) 정상 표시 확인
- [x] 비로그인 진입 시 초기값(0, 3, 1) 확인
- [x] 목숨 감소 시 Network 탭에서 PUT 요청 확인
- [x] 30초마다 Network 탭에서 PUT 요청 확인
- [x] 게임오버/재시작 후 Network 탭에서 DELETE 요청 확인

---

## 완료된 백엔드 작업 (2026-04-01)

### game_sessions 라우터 구현 (TDD 완료)

- `backend/app/schemas/game_session.py` 신규 생성 (스키마 3개)
- `backend/app/schemas/__init__.py`에 GameSession 스키마 export 추가
- `backend/app/api/v1/game_sessions.py` 신규 생성 (GET/PUT/DELETE)
- `backend/main.py`에 `/api/v1/game-sessions` 라우터 등록
- `backend/tests/test_game_sessions.py` 신규 생성 (TDD, 테스트 7/7 통과)
- Redis key 구조: `game_session:{user_id}`, TTL 7200초

---

## ~~미완료 작업~~

---

## 저장 흐름 요약

```
[페이지 진입]
  로그인 유저 → GET /api/v1/game-sessions/{user_id}
    세션 있음 → score/lives/stage 자동 복원, 공은 시작 위치
              → 게임 시작 화면(Space/클릭 대기)은 그대로 유지
    세션 없음 → 초기값(0, 3, 1)으로 새 게임
  비로그인 → 초기값(0, 3, 1)으로 새 게임

[게임 진행 중]
  30초마다 + 목숨 감소 시 → PUT /api/v1/game-sessions/{user_id}

[게임 오버 / 재시작]
  DELETE /api/v1/game-sessions/{user_id}
  점수 제출 (기존 submitScore 유지)
```

---

## 현재 프로젝트 구조 참고

### 백엔드 라우터 디렉토리
```
backend/app/api/v1/
  auth.py
  chat.py
  game_visits.py       ← 패턴 참고
  monthly_scores.py    ← 패턴 참고
  pinball_ai.py
  users.py
  game_sessions.py     ← 신규 생성 예정
```

### 백엔드 스키마 디렉토리
```
backend/app/schemas/
  __init__.py          ← export 추가 필요
  game_visit.py
  monthly_score.py     ← 패턴 참고
  user.py
  game_session.py      ← 신규 생성 예정
```

### 프론트엔드 관련 파일
```
frontend/src/pages/Pinball/
  Pinball.jsx          ← 수정 대상 (메인 게임 컴포넌트)
  pinballRestart.js    ← getRestartState() 정의 (항상 초기값 반환)
  stageConfigs.js      ← STAGE_CONFIGS 정의

frontend/src/utils/
  noticeIndexDB.js     ← 패턴 참고
  gameSession.js       ← 신규 생성 예정
```

### 인증 컨텍스트
```
frontend/src/contexts/AuthContext.jsx
  - useAuth() 훅으로 user 객체 접근
  - user.id: 로그인 사용자 식별자
  - !user || !user.id: 비로그인 판별 조건
```

---

## 변경 파일 목록

| 파일 | 상태 | 작업 |
|------|------|------|
| `docker-compose.yml` | 완료 | redis 서비스 추가 |
| `backend/requirements.txt` | 완료 | `redis==7.1.1` 추가 |
| `backend/app/redis_client.py` | 완료 | 신규 생성 |
| `backend/.env.example` | 완료 | REDIS_HOST, REDIS_PORT 추가 |
| `backend/app/schemas/game_session.py` | 완료 | 신규 생성 |
| `backend/app/schemas/__init__.py` | 완료 | GameSession 스키마 export 추가 |
| `backend/app/api/v1/game_sessions.py` | 완료 | 신규 생성 (GET/PUT/DELETE) |
| `backend/main.py` | 완료 | 라우터 등록 |
| `backend/tests/test_game_sessions.py` | 완료 | 신규 생성 (TDD, 테스트 7/7 통과) |
| `frontend/src/utils/gameSession.js` | 완료 | 신규 생성 (TDD, 테스트 9/9 통과) |
| `frontend/src/test/gameSession.test.js` | 완료 | 신규 생성 (TDD RED 테스트) |
| `frontend/src/pages/Pinball/Pinball.jsx` | 완료 | 세션 복원·저장·삭제 로직 추가 |
