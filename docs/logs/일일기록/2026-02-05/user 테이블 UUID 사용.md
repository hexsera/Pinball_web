# 개요
user 테이블에 UUID 용 컬럼을 추가하도록 한다. 

## 상세사항
1. user 테이블에 user_id 컬럼을 추가한다. 이 컬럼은 나중에 PK 로 지정될 것이다. 
2. user_id 컬럼 레코드에 UUID 를 채운다.
3. 현재 api 와 연동때문에 id 컬럼을 삭제할 수 없다. 현재 plan 에서 하지말고 나중에 api 를 변경하도록 한다.