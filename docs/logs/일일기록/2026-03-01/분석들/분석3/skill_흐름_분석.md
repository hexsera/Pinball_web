# OpenClaw Skill 시스템 전체 흐름 분석

> 20년차 프로그래머의 시선으로 코드를 분석하여, 초보자도 이해할 수 있도록 작성했습니다.

---

## Skill이 뭔가요? (쉬운 설명)

**Skill**은 OpenClaw AI에게 "이런 능력이 있어, 이렇게 쓰면 돼"라고 알려주는 **설명서**입니다.

예를 들어, GitHub Skill은 AI에게 이렇게 알려줍니다:
> "나는 `gh` 라는 GitHub CLI 도구를 사용할 수 있어. GitHub PR 목록, 이슈 조회, 저장소 관리 등을 할 수 있어."

Skill은 실제 코드가 아니라 **마크다운 문서(SKILL.md)** 형태로 작성됩니다.

---

## 전체 흐름 한눈에 보기

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         OpenClaw Skill 동작 흐름                             │
└─────────────────────────────────────────────────────────────────────────────┘

  사용자                OpenClaw                         LLM (AI)
  ──────              ──────────────────────────         ──────────────────
    │                        │                                  │
    │  "PR 목록 보여줘"       │                                  │
    │ ──────────────────>    │                                  │
    │                        │                                  │
    │            ┌───────────┤                                  │
    │            │ 1. Skill  │                                  │
    │            │    로딩   │                                  │
    │            │  (SKILL.md│                                  │
    │            │   파일    │                                  │
    │            │   읽기)   │                                  │
    │            └───────────┤                                  │
    │                        │                                  │
    │            ┌───────────┤                                  │
    │            │ 2. 시스템 │                                  │
    │            │   프롬프트│                                  │
    │            │   생성    │                                  │
    │            │  (Skill   │                                  │
    │            │  내용 포함│                                  │
    │            └───────────┤                                  │
    │                        │                                  │
    │                        │  [시스템 프롬프트 + 사용자 메시지]  │
    │                        │ ──────────────────────────────> │
    │                        │                                  │
    │                        │              ┌───────────────────┤
    │                        │              │ 3. LLM가 Skill    │
    │                        │              │    읽고 분석       │
    │                        │              │    (어떤 Skill     │
    │                        │              │     쓸지 판단)     │
    │                        │              └───────────────────┤
    │                        │                                  │
    │                        │  [Tool 호출 요청 (exec: gh pr list)]
    │                        │ <────────────────────────────── │
    │                        │                                  │
    │            ┌───────────┤                                  │
    │            │ 4. Tool   │                                  │
    │            │   실행    │                                  │
    │            │ (실제로   │                                  │
    │            │  gh 명령어│                                  │
    │            │  실행)    │                                  │
    │            └───────────┤                                  │
    │                        │                                  │
    │                        │  [Tool 실행 결과 반환]             │
    │                        │ ──────────────────────────────> │
    │                        │                                  │
    │                        │  [최종 답변 생성 후 반환]          │
    │                        │ <────────────────────────────── │
    │                        │                                  │
    │  "PR 목록입니다: ..."   │                                  │
    │ <──────────────────── │                                  │
    │                        │                                  │
```

---

## Step 1: Skill 정의 (SKILL.md 파일)

### Skill 파일 구조

모든 Skill은 `skills/{이름}/SKILL.md` 형태로 저장됩니다.

```
openclaw/
└── skills/
    ├── github/
    │   └── SKILL.md    ← GitHub Skill 설명서
    ├── spotify-player/
    │   └── SKILL.md    ← Spotify Skill 설명서
    └── weather/
        └── SKILL.md    ← 날씨 Skill 설명서
```

### SKILL.md 파일 예시 (GitHub Skill)

```markdown
---
name: github
description: "GitHub operations via `gh` CLI"
metadata:
  {
    "openclaw": {
      "emoji": "🐙",
      "requires": { "bins": ["gh"] },
      "install": [
        {
          "kind": "brew",
          "formula": "gh",
          "label": "Install GitHub CLI (brew)"
        }
      ]
    }
  }
---

# GitHub Skill

Use the `gh` CLI to interact with GitHub...
(여기에 AI가 이해할 상세 지침 작성)
```

**핵심 구성요소:**
- `name`: Skill 고유 이름
- `description`: 짧은 설명 (AI가 어떤 Skill 쓸지 판단 시 사용)
- `metadata.openclaw.requires.bins`: 이 Skill 사용에 필요한 외부 프로그램 목록
- `metadata.openclaw.install`: 자동 설치 방법

**관련 타입 정의 파일:** [src/agents/skills/types.ts](openclaw_git/src/agents/skills/types.ts)

```
SkillEntry {
  skill       ← 실제 Skill 데이터 (이름, 파일경로, 내용)
  frontmatter ← SKILL.md 헤더 메타데이터
  metadata    ← openclaw 전용 설정 (requires, install 등)
  invocation  ← 호출 정책 (user-invocable, disable-model-invocation)
}
```

---

## Step 2: Skill 로딩 과정

### 어디서 Skill을 찾나요?

[src/agents/skills/workspace.ts](openclaw_git/src/agents/skills/workspace.ts) (221~406번 라인)에서 여러 위치를 순서대로 탐색합니다:

```
┌─────────────────────────────────────────────────────────┐
│              Skill 탐색 경로 (우선순위 낮음 → 높음)          │
├─────────────────────────────────────────────────────────┤
│  1. bundled     openclaw 기본 내장 skills/               │
│  2. extra       config에서 지정한 추가 디렉토리            │
│  3. managed     ~/.openclaw/skills (사용자 관리)          │
│  4. personal    ~/.agents/skills (개인 agent skill)      │
│  5. project     {작업폴더}/.agents/skills                │
│  6. workspace   {작업폴더}/skills  ← 최우선              │
├─────────────────────────────────────────────────────────┤
│  같은 이름의 Skill이 있으면, 위 순서에서 더 아래(우선순위    │
│  높은)것이 덮어씁니다.                                     │
└─────────────────────────────────────────────────────────┘
```

### Skill 파싱 흐름

```
SKILL.md 파일 읽기
       │
       ▼
parseFrontmatter()                     ← frontmatter.ts:21
  (--- 사이의 YAML 헤더 파싱)
       │
       ▼
resolveOpenClawMetadata()              ← frontmatter.ts:81
  (openclaw 전용 메타데이터 추출)
       │
       ▼
resolveSkillInvocationPolicy()         ← frontmatter.ts:103
  (호출 정책 결정)
       │
       ▼
SkillEntry 객체 생성 완료
```

### Skill 필터링

[src/agents/skills/workspace.ts](openclaw_git/src/agents/skills/workspace.ts) (67~88번 라인) `filterSkillEntries()` 함수에서:

```
┌────────────────────────────────────────────┐
│           Skill 포함 여부 판단 기준           │
├────────────────────────────────────────────┤
│  ✓ OS 호환성 (linux/macos/windows)          │
│  ✓ 필요 바이너리 설치 여부 (requires.bins)   │
│  ✓ 필요 환경변수 존재 여부 (requires.env)    │
│  ✓ config에서 명시적으로 비활성화 여부       │
│  ✓ skillFilter (특정 Skill만 쓰도록 제한)    │
└────────────────────────────────────────────┘
```

---

## Step 3: 시스템 프롬프트에 Skill 포함

### LLM에게 Skill을 어떻게 알려주나요?

Skill은 **직접 실행**되지 않습니다. 대신 AI(LLM)의 **시스템 프롬프트** (= AI에게 주는 사전 지침)에 포함되어, AI가 무슨 능력을 갖고 있는지 알게 됩니다.

### SKILL.md에서 실제로 꺼내는 것

**SKILL.md 전체 내용을 프롬프트에 넣는 게 아닙니다.** 딱 3가지만 추출합니다.

```
SKILL.md
─────────────────────────────────────────────────────
---
name: github              ← ① 이름만 가져옴
description: "GitHub..."  ← ② 짧은 설명만 가져옴
metadata: { ... }            (가져오지 않음)
---

# GitHub Skill
Use the gh CLI to...         (가져오지 않음 — 본문은 나중에 AI가 직접 읽음)
```

꺼내는 3가지:
1. **`name`** — frontmatter의 name 필드
2. **`description`** — frontmatter의 description 필드
3. **`filePath`** — 해당 SKILL.md의 절대 경로 (내용이 아니라 경로 자체)

코드 출처: `@mariozechner/pi-coding-agent` 패키지의 `loadSkillFromFile()` 함수:

```javascript
return {
  skill: {
    name,                          // frontmatter.name
    description: frontmatter.description,  // frontmatter.description
    filePath,                      // 파일 경로 (내용 아님!)
    baseDir: skillDir,
  }
};
```

### 시스템 프롬프트에 들어가는 실제 모습

`formatSkillsForPrompt()` 함수 (`@mariozechner/pi-coding-agent` dist/core/skills.js)가 위 3가지를 XML로 조립합니다:

```xml
The following skills provide specialized instructions for specific tasks.
Use the read tool to load a skill's file when the task matches its description.

<available_skills>
  <skill>
    <name>github</name>
    <description>GitHub operations via gh CLI</description>
    <location>/home/user/.openclaw/skills/github/SKILL.md</location>
  </skill>
  <skill>
    <name>weather</name>
    <description>Get weather information</description>
    <location>/home/user/.openclaw/skills/weather/SKILL.md</location>
  </skill>
</available_skills>
```

[src/agents/system-prompt.ts](openclaw_git/src/agents/system-prompt.ts) (20~42번 라인) `buildSkillsSection()` 함수가 이 XML 앞에 추가 지침을 붙입니다:

```
## Skills (mandatory)

Before replying: scan <available_skills> entries.
- 딱 하나의 Skill이 맞으면: SKILL.md를 read tool로 읽고 따르세요.
- 여러 Skill이 맞으면: 가장 구체적인 것 하나 선택.
- 없으면: Skill 읽지 마세요.

(위 XML 내용)
```

**중요한 설계 원칙:** AI는 먼저 **목록(이름 + 짧은 설명 + 경로)만** 봅니다. 그리고 필요한 Skill이 있다고 판단되면, 그때서야 `read` tool로 해당 SKILL.md 전체를 읽어서 상세 지침을 얻습니다.

```
만약 SKILL.md 본문 전체를 다 넣는다면...
  → Skill 54개 × 각 수백~수천 글자 = 수만 글자 → 비용 폭발

실제 설계:
  → 목록(이름 + 짧은 설명 + 경로)만 프롬프트에 포함 → 수백 글자
  → AI가 필요하다고 판단할 때만 read tool로 SKILL.md 본문을 읽음
```

### 프롬프트 제한

```
최대 150개  Skill까지 목록에 포함 가능
최대 30,000 글자까지 Skill 목록 내용 제한
홈 경로 (~) 로 축약하여 token 절약
```

---

## Step 4: LLM의 Skill 선택 및 Tool 호출

### LLM이 어떻게 Skill을 사용하나요?

```
┌─────────────────────────────────────────────────────────────┐
│                 LLM 내부 처리 흐름                             │
└─────────────────────────────────────────────────────────────┘

사용자: "GitHub 저장소의 PR 목록 보여줘"
         │
         ▼
[LLM 1차 분석]
 - available_skills에서 "github" 발견
 - description: "GitHub operations via gh CLI" → 딱 맞네!
         │
         ▼
[read tool 호출] → SKILL.md 내용 읽기
 {
   "tool": "read",
   "path": "~/.openclaw/skills/github/SKILL.md"
 }
         │
         ▼
[SKILL.md 내용 수신]
 "gh pr list --repo {owner}/{repo} 명령어로 PR 조회..."
         │
         ▼
[exec tool 호출] → 실제 명령어 실행
 {
   "tool": "exec",
   "command": "gh pr list --repo owner/repo --json number,title,state"
 }
         │
         ▼
[exec 결과 수신]
 "1  Add feature  OPEN\n2  Fix bug  MERGED"
         │
         ▼
[최종 답변 생성]
 "GitHub 저장소의 최신 PR 목록입니다:
  1. Add feature (진행중)
  2. Fix bug (완료)"
```

### Tool의 종류 (Skill이 활용하는 도구들)

[src/agents/pi-tools.ts](openclaw_git/src/agents/pi-tools.ts) (171~226번 라인) `createOpenClawCodingTools()` 함수가 생성하는 도구들:

```
┌─────────────────────────────────────────────────────────┐
│                    사용 가능한 Tools                      │
├──────────────┬──────────────────────────────────────────┤
│ read         │ 파일 읽기                                 │
│ write        │ 파일 쓰기                                 │
│ edit         │ 파일 수정                                 │
│ exec         │ 쉘 명령어 실행  ← Skill이 주로 활용       │
│ ls / glob    │ 파일 목록 조회                            │
│ grep         │ 파일 내용 검색                            │
│ message      │ 채널에 메시지 전송                        │
│ cron         │ 반복 작업 등록                            │
│ [채널 전용]  │ Slack/Discord 등 채널별 특수 기능         │
│ [플러그인]   │ Extension이 추가한 커스텀 tool             │
└──────────────┴──────────────────────────────────────────┘
```

**핵심 포인트:** Skill 자체는 Tool이 아닙니다! Skill은 AI에게 "어떤 도구를 어떻게 쓰면 돼"를 알려주는 **설명서**이고, 실제로는 기존 Tool(주로 `exec`)을 사용합니다.

---

## Step 5: 실행 결과 반환

### 결과가 사용자에게 도달하는 경로

[src/agents/pi-embedded-subscribe.ts](openclaw_git/src/agents/pi-embedded-subscribe.ts) (34번~ 라인) `subscribeEmbeddedPiSession()` 함수:

```
Tool 실행 완료
      │
      ▼
subscribeEmbeddedPiSession
├── onToolResult()
│   └── Tool 결과 메타데이터 저장
│       (성공/실패, 소요시간, 에러 메시지)
│
├── onAssistantText()
│   └── LLM 답변 텍스트를 스트리밍
│       (글자 단위로 실시간 전송)
│
└── onBlockReply()
    └── 블록 단위 완성된 응답
          │
          ▼
    Channel Dock (channels/dock.ts)
          │
          ▼
    ┌─────────────────────────────┐
    │  메시징 채널로 최종 전송      │
    │  Slack / Discord / Telegram  │
    │  WhatsApp / LINE / iMessage  │
    └─────────────────────────────┘
```

---

## 전체 코드 흐름도 (완전 상세 버전)

```
┌────────────────────────────────────────────────────────────────────────┐
│                    OpenClaw Skill 전체 코드 흐름                         │
└────────────────────────────────────────────────────────────────────────┘

[사용자가 메시지 보냄]
         │
         ▼
[채널 수신] (src/telegram/, src/discord/, src/slack/ 등)
         │
         ▼
[메시지 라우팅] (src/routing/)
         │
         ▼
[Agent 실행 시작] src/agents/pi-embedded-runner/run.ts
         │
         ├──► [1단계: Skill 로딩]
         │         │
         │         ▼
         │    loadWorkspaceSkillEntries()
         │    ┌─────────────────────────────────────────┐
         │    │  skills/ 폴더들 순서대로 스캔             │
         │    │   └─ 각 SKILL.md 파일 읽기              │
         │    │        └─ parseFrontmatter() 파싱        │
         │    │             └─ SkillEntry 객체 생성      │
         │    └─────────────────────────────────────────┘
         │         │
         │    filterSkillEntries()
         │    ┌─────────────────────────────────────────┐
         │    │  OS, 바이너리, 환경변수 조건 확인         │
         │    │  조건 맞는 Skill만 선택                  │
         │    └─────────────────────────────────────────┘
         │
         ├──► [2단계: 시스템 프롬프트 생성]
         │         │
         │         ▼
         │    buildAgentSystemPrompt()       (system-prompt.ts:196)
         │    ┌─────────────────────────────────────────┐
         │    │  기본 지침 + 워크스페이스 정보            │
         │    │  + buildSkillsSection()                  │
         │    │    └─ Skill 목록을 XML 형태로 포함       │
         │    │    └─ "해당 Skill 있으면 읽어서 따르라"  │
         │    └─────────────────────────────────────────┘
         │
         ├──► [3단계: Tool 목록 생성]
         │         │
         │         ▼
         │    createOpenClawCodingTools()    (pi-tools.ts:171)
         │    ┌─────────────────────────────────────────┐
         │    │  read, write, edit, exec 등 기본 도구   │
         │    │  + 채널 전용 도구                        │
         │    │  + 플러그인 도구                         │
         │    └─────────────────────────────────────────┘
         │
         ▼
[LLM API 호출]
┌─────────────────────────────────────────────────────┐
│  Input:  시스템 프롬프트 (Skill 목록 포함)            │
│          + 대화 히스토리                              │
│          + Tool 정의 목록                            │
│                                                      │
│  LLM 처리:                                           │
│  1. Skill 목록에서 관련 Skill 탐색                    │
│  2. read tool로 SKILL.md 읽기                        │
│  3. 지침에 따라 exec tool로 실제 동작 실행            │
│  4. 결과를 바탕으로 최종 답변 생성                     │
└─────────────────────────────────────────────────────┘
         │
         ▼
[결과 처리] subscribeEmbeddedPiSession()  (pi-embedded-subscribe.ts:34)
         │
         ├── Tool 실행 결과 수신 → onToolResult()
         ├── LLM 텍스트 스트리밍 → onAssistantText()
         └── 블록 단위 응답 → onBlockReply()
                   │
                   ▼
         [채널 전송] (src/channels/dock.ts)
                   │
                   ▼
         [사용자에게 최종 답변 도달]
```

---

## 데이터 흐름 상세 (사용자/OpenClaw/LLM 3자 관점)

```
┌──────────────┬──────────────────────────────┬────────────────────────────┐
│   사용자      │         OpenClaw              │          LLM               │
├──────────────┼──────────────────────────────┼────────────────────────────┤
│              │                              │                            │
│ "PR 목록      │                              │                            │
│  보여줘" ──> │                              │                            │
│              │ 채널에서 수신                 │                            │
│              │ 메시지 파싱                   │                            │
│              │ Skill 로딩/필터링             │                            │
│              │ 시스템 프롬프트 생성           │                            │
│              │ Tool 목록 준비                │                            │
│              │                              │                            │
│              │ ──[API 요청]────────────────> │                            │
│              │  {                           │                            │
│              │   system: "Skills: github...",│ 시스템 프롬프트 분석         │
│              │   messages: ["PR 목록..."],  │ "github skill 있네"         │
│              │   tools: [read, exec, ...]   │ → read tool 사용 결정      │
│              │  }                           │                            │
│              │                              │                            │
│              │ <─[tool_use 응답]──────────── │                            │
│              │  {                           │                            │
│              │   tool: "read",              │                            │
│              │   path: "github/SKILL.md"    │                            │
│              │  }                           │                            │
│              │                              │                            │
│              │ read tool 실행               │                            │
│              │ SKILL.md 파일 읽기           │                            │
│              │                              │                            │
│              │ ──[tool_result]─────────────> │                            │
│              │  { content: "# GitHub Skill  │ SKILL.md 내용 수신          │
│              │    Use gh pr list..." }      │ 지침 이해                   │
│              │                              │ → exec tool 사용 결정      │
│              │                              │                            │
│              │ <─[tool_use 응답]──────────── │                            │
│              │  {                           │                            │
│              │   tool: "exec",              │                            │
│              │   command: "gh pr list ..."  │                            │
│              │  }                           │                            │
│              │                              │                            │
│              │ exec tool 실행               │                            │
│              │ gh 명령어 실제 실행           │                            │
│              │                              │                            │
│              │ ──[tool_result]─────────────> │                            │
│              │  { content: "1 Add feature   │ 명령어 결과 수신             │
│              │    OPEN\n2 Fix bug MERGED" } │ 최종 답변 생성              │
│              │                              │                            │
│              │ <─[assistant 응답]────────── │                            │
│              │  "PR 목록입니다:             │                            │
│              │   1. Add feature (진행중)"   │                            │
│              │                              │                            │
│              │ 채널로 메시지 전송            │                            │
│              │                              │                            │
│ <─ "PR 목록   │                              │                            │
│   입니다..." │                              │                            │
└──────────────┴──────────────────────────────┴────────────────────────────┘
```

---

## 핵심 파일 위치 요약

| 역할 | 파일 경로 | 주요 함수 |
|------|-----------|-----------|
| Skill 타입 정의 | [src/agents/skills/types.ts](openclaw_git/src/agents/skills/types.ts) | `SkillEntry`, `OpenClawSkillMetadata` |
| Skill 파싱 | [src/agents/skills/frontmatter.ts](openclaw_git/src/agents/skills/frontmatter.ts) | `parseFrontmatter()` (21번 라인) |
| Skill 메타데이터 추출 | [src/agents/skills/frontmatter.ts](openclaw_git/src/agents/skills/frontmatter.ts) | `resolveOpenClawMetadata()` (81번 라인) |
| Skill 로딩/필터 | [src/agents/skills/workspace.ts](openclaw_git/src/agents/skills/workspace.ts) | `loadSkillEntries()` (221번 라인) |
| Skill 프롬프트 생성 | [src/agents/skills/workspace.ts](openclaw_git/src/agents/skills/workspace.ts) | `buildWorkspaceSkillsPrompt()` (465번 라인) |
| 시스템 프롬프트 생성 | [src/agents/system-prompt.ts](openclaw_git/src/agents/system-prompt.ts) | `buildSkillsSection()` (20번 라인) |
| Tool 생성 | [src/agents/pi-tools.ts](openclaw_git/src/agents/pi-tools.ts) | `createOpenClawCodingTools()` (171번 라인) |
| 결과 처리/스트리밍 | [src/agents/pi-embedded-subscribe.ts](openclaw_git/src/agents/pi-embedded-subscribe.ts) | `subscribeEmbeddedPiSession()` (34번 라인) |
| Hook 로딩 | [src/hooks/loader.ts](openclaw_git/src/hooks/loader.ts) | `loadInternalHooks()` (41번 라인) |

---

## 쉽게 이해하는 비유

Skill 시스템을 요리에 비유하면:

```
┌─────────────────────────────────────────────────────────────────┐
│                     요리 비유로 이해하기                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  SKILL.md    = 요리 레시피 책                                     │
│                ("파스타는 이렇게 만들어요...")                     │
│                                                                  │
│  LLM(AI)     = 요리사                                            │
│                (레시피를 읽고 요리하는 주체)                       │
│                                                                  │
│  Tool(exec)  = 주방 도구                                         │
│                (칼, 냄비, 오븐 등 실제 요리 도구)                  │
│                                                                  │
│  시스템 프롬프트 = 주방 메뉴판                                    │
│                   ("오늘 만들 수 있는 요리 목록")                   │
│                                                                  │
│  흐름:                                                           │
│  손님(사용자) "파스타 주세요"                                      │
│       ↓                                                          │
│  웨이터(OpenClaw)가 요리사(LLM)에게 메뉴판 전달                   │
│       ↓                                                          │
│  요리사가 메뉴판에서 "파스타" 발견                                 │
│       ↓                                                          │
│  파스타 레시피(SKILL.md)를 읽음                                   │
│       ↓                                                          │
│  레시피대로 주방 도구(exec) 사용해서 요리                          │
│       ↓                                                          │
│  완성된 요리를 손님에게 전달                                       │
└─────────────────────────────────────────────────────────────────┘
```

---

## 정리: Skill의 핵심 특징

1. **Skill = 설명서, Tool = 도구**
   - Skill 자체는 실행 코드가 아니라 AI에게 주는 마크다운 설명서
   - 실제 실행은 `exec`, `read` 등 기존 Tool로 이루어짐

2. **지연 로딩 (Lazy Loading)**
   - AI가 처음에는 Skill 목록만 봄
   - 필요할 때만 해당 SKILL.md를 읽음
   - → 불필요한 token 낭비 없음

3. **우선순위 기반 덮어쓰기**
   - 사용자가 직접 만든 Skill이 기본 Skill을 덮어씀
   - 프로젝트별로 커스텀 Skill 가능

4. **플러그 앤 플레이**
   - Skill 폴더에 SKILL.md 하나 추가하면 바로 작동
   - 코드 수정 불필요

---

*분석 대상: openclaw_git/ 소스코드 기준*
*작성일: 2026-02-28*
