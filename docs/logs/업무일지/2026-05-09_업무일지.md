# 업무일지

## 기본 정보
- 날짜: 2026-05-09 (토)
- 작성자: hexsera

## 진행 목표

어드민 페이지(`/admin`, `/admin/users`, `/admin/statistics`)에 프론트엔드 라우트 가드를 도입하여, `role=admin`인 사용자만 진입 가능하도록 한다. 비로그인 및 일반 user는 `/`로 리다이렉트한다.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| jwt-decode 패키지 설치 | `frontend/package.json` | base64url 디코딩 처리용 경량 라이브러리 |
| AdminRoute 가드 컴포넌트 생성 | `frontend/src/components/AdminRoute.jsx` | 신규 생성 |
| 어드민 라우트 Outlet 패턴으로 변경 | `frontend/src/App.jsx` | 가드로 3개 라우트 묶기 |
| AdminRoute 단위 테스트 작성 | `frontend/src/test/AdminRoute.test.jsx` | 신규 생성, 4케이스 |
| 프론트엔드 빌드 및 nginx 배포 | `frontend/dist/` | nginx-server 컨테이너에 복사 |

**총 1시간 0분**

## 주요 변경 사항

### 1. frontend/src/components/AdminRoute.jsx 생성

**추가:**
```jsx
import { Navigate, Outlet } from 'react-router-dom';
import { jwtDecode } from 'jwt-decode';
import { useAuthStore } from '../store/authStore';

export default function AdminRoute() {
  const accessToken = useAuthStore((s) => s.accessToken);
  if (!accessToken) return <Navigate to="/" replace />;

  try {
    const { role } = jwtDecode(accessToken);
    if (role !== 'admin') return <Navigate to="/" replace />;
  } catch {
    return <Navigate to="/" replace />;
  }

  return <Outlet />;
}
```

**변경 의도:** 어드민 라우트 진입 시 JWT payload를 디코딩해 `role === 'admin'`인 경우에만 자식 라우트를 렌더링한다. 비로그인·일반 user·디코딩 실패 모두 `/`로 통일 리다이렉트. 서명 검증은 하지 않으며(백엔드 책임), payload 디코딩만 수행한다.

---

### 2. frontend/src/App.jsx 수정

**변경 전:**
```jsx
import { AdminUserPage, AdminStatisticsPage } from './pages/admin';
// ... (AdminRoute import 없음)

<Route path="/admin" element={<AdminUserPage/>} />
<Route path="/admin/users" element={<AdminUserPage/>} />
<Route path="/admin/statistics" element={<AdminStatisticsPage/>} />
```

**변경 후:**
```jsx
import { AdminUserPage, AdminStatisticsPage } from './pages/admin';
import AdminRoute from './components/AdminRoute';

<Route element={<AdminRoute />}>
  <Route path="/admin" element={<AdminUserPage/>} />
  <Route path="/admin/users" element={<AdminUserPage/>} />
  <Route path="/admin/statistics" element={<AdminStatisticsPage/>} />
</Route>
```

**변경 의도:** 어드민 라우트 3개를 Outlet 패턴으로 묶어 `AdminRoute` 가드 한 번으로 일괄 보호. `/admin`과 `/admin/users` 중복 매핑은 기존 의도를 유지하여 그대로 둔다(후속 검토 예정).

---

### 3. frontend/src/test/AdminRoute.test.jsx 생성

**추가:**
```jsx
import { render, screen } from '@testing-library/react';
import { MemoryRouter, Routes, Route } from 'react-router-dom';
import { describe, it, expect, vi, beforeEach } from 'vitest';
import AdminRoute from '../components/AdminRoute';
import { useAuthStore } from '../store/authStore';

vi.mock('jwt-decode', () => ({
  jwtDecode: vi.fn(),
}));

import { jwtDecode } from 'jwt-decode';

function setup(token, role) {
  useAuthStore.setState({ accessToken: token });

  if (token && role !== undefined) {
    jwtDecode.mockReturnValue({ role });
  } else if (token && role === undefined) {
    jwtDecode.mockImplementation(() => { throw new Error('invalid token'); });
  }

  render(
    <MemoryRouter initialEntries={['/admin']}>
      <Routes>
        <Route element={<AdminRoute />}>
          <Route path="/admin" element={<div>Admin Page</div>} />
        </Route>
        <Route path="/" element={<div>Home</div>} />
      </Routes>
    </MemoryRouter>
  );
}

describe('AdminRoute', () => {
  beforeEach(() => {
    vi.clearAllMocks();
    useAuthStore.setState({ accessToken: null });
  });

  it('토큰 없으면 Home으로 리다이렉트', () => {
    setup(null, null);
    expect(screen.getByText('Home')).toBeInTheDocument();
  });

  it('role=admin이면 어드민 페이지 렌더', () => {
    setup('valid.token.here', 'admin');
    expect(screen.getByText('Admin Page')).toBeInTheDocument();
  });

  it('role=user면 Home으로 리다이렉트', () => {
    setup('valid.token.here', 'user');
    expect(screen.getByText('Home')).toBeInTheDocument();
  });

  it('디코딩 실패하면 Home으로 리다이렉트', () => {
    setup('broken.token', undefined);
    expect(screen.getByText('Home')).toBeInTheDocument();
  });
});
```

**변경 의도:** `AdminRoute` 4가지 분기(토큰 없음, admin, user, 디코딩 실패)를 단위 테스트로 검증. `jwtDecode`와 `useAuthStore`를 모킹해 실제 JWT 없이 테스트 가능하도록 구성.

## 라우트 가드 동작 흐름

```
브라우저 → /admin 접속
              │
              ▼
        AdminRoute 실행
              │
     accessToken 있음?
       │            │
      No            Yes
       │            │
       ▼            ▼
  → / 리다이렉트  jwtDecode(token)
                    │
              role === 'admin'?
               │           │
              No            Yes
               │            │
               ▼            ▼
          → / 리다이렉트  <Outlet /> 렌더
                            │
                            ▼
                     자식 라우트 표시
                  (AdminUserPage 등)
```

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| 어드민 유저 관리 페이지 (`/admin`, `/admin/users`) | 가드 추가 — admin만 진입 가능 | 정상 |
| 어드민 통계 페이지 (`/admin/statistics`) | 가드 추가 — admin만 진입 가능 | 정상 |
| 비로그인 사용자의 `/admin` 접근 | 가드로 차단 → `/` 리다이렉트 | 정상 |
| 일반 user의 `/admin` 접근 | 가드로 차단 → `/` 리다이렉트 | 정상 |
| admin 새로고침 후 `/admin` 재접속 | 토큰 복원 전 가드가 먼저 동작해 일시적으로 `/`로 튕김 | 알려진 한계 (후속 작업) |

## 결과
- 상태: 완료 (새로고침 타이밍 문제는 후속 세션으로 분리)
- 테스트: 4/4 통과
- 빌드: 오류 없음, nginx 컨테이너 배포 완료

## 배운 내용

## 다음 작업
- 새로고침 시 Access Token 복원 완료 전 가드가 먼저 동작하는 문제 처리 (로딩 상태 도입)
- 백엔드 어드민 API 엔드포인트에 `role=admin` 검증 의존성 추가
- `/admin`과 `/admin/users` 중복 매핑 의도 확인 및 정리

---

# 업무일지 (2차)

## 기본 정보
- 날짜: 2026-05-09 (토)
- 작성자: hexsera

## 진행 목표

백엔드 users API 5개 엔드포인트에 JWT 기반 접근 권한 검증을 도입한다. 비로그인 접근 차단, 타인 정보 수정·삭제 차단, 회원가입 시 role 임의 지정 차단이 목표다.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| 권한 의존성 함수 2개 신설 | `backend/app/api/deps.py` | `require_admin`, `require_self_or_admin` 추가 |
| 회원가입 스키마에서 role 필드 제거 | `backend/app/schemas/user.py` | `UserCreateRequest.role` 삭제 |
| users 라우터 5개 의존성 변경 + role 고정 | `backend/app/api/v1/users.py` | import 추가, 라우터 시그니처 수정 |
| 친구 검색 bare axios → api 인터셉터로 교체 | `frontend/src/pages/FriendPage/FriendPage.jsx` | 401 오류 수정 |
| 헤더 유저 정보 bare axios → api 인터셉터로 교체 | `frontend/src/components/HeaderUserInfo.jsx` | 전 페이지 401 오류 수정 (로그인 가드 도입 후 발견된 파생 버그) |
| 프론트엔드 빌드 및 nginx 배포 | `frontend/dist/` | 2회 빌드 |
| curl 시나리오 검증 (D-1~D-4) | — | 전 항목 통과 |

**총 2시간 30분**

## 주요 변경 사항

### 1. backend/app/api/deps.py 수정

**변경 전:**
```python
def get_current_user(
    credentials: HTTPAuthorizationCredentials | None = Depends(bearer_scheme),
) -> dict:
    """JWT 서명 검증 의존성 — DB 조회 없음(Stateless)"""
    if credentials is None:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Not authenticated")
    try:
        payload = jwt.decode(
            credentials.credentials,
            settings.JWT_SECRET_KEY,
            algorithms=[settings.JWT_ALGORITHM],
        )
        return payload
    except (JWTError, TypeError, ValueError):
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid token")
# (이후 함수 없음)
```

**변경 후:**
```python
def get_current_user(
    credentials: HTTPAuthorizationCredentials | None = Depends(bearer_scheme),
) -> dict:
    """JWT 서명 검증 의존성 — DB 조회 없음(Stateless)"""
    if credentials is None:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Not authenticated")
    try:
        payload = jwt.decode(
            credentials.credentials,
            settings.JWT_SECRET_KEY,
            algorithms=[settings.JWT_ALGORITHM],
        )
        return payload
    except (JWTError, TypeError, ValueError):
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid token")


def require_admin(current_user: dict = Depends(get_current_user)) -> dict:
    if current_user.get("role") != "admin":
        raise HTTPException(status_code=status.HTTP_403_FORBIDDEN, detail="Admin required")
    return current_user


def require_self_or_admin(
    user_id: int,
    current_user: dict = Depends(get_current_user),
) -> dict:
    is_self = int(current_user.get("sub", 0)) == user_id
    is_admin = current_user.get("role") == "admin"
    if not (is_self or is_admin):
        raise HTTPException(status_code=status.HTTP_403_FORBIDDEN, detail="Forbidden")
    return current_user
```

**변경 의도:** 권한 로직을 라우터 본문 밖으로 분리. `require_admin`은 admin role만 통과, `require_self_or_admin`은 경로의 `user_id`와 토큰 `sub`가 일치하거나 admin인 경우만 통과. 미충족 시 403 반환.

---

### 2. backend/app/schemas/user.py 수정

**변경 전:**
```python
class UserCreateRequest(BaseModel):
    """범용 사용자 생성 요청 (role 포함)"""
    email: str
    nickname: str
    password: str
    birth_date: date
    role: str
```

**변경 후:**
```python
class UserCreateRequest(BaseModel):
    """회원가입 요청"""
    email: str
    nickname: str
    password: str
    birth_date: date
```

**변경 의도:** 회원가입 요청에서 `role` 필드를 제거해 클라이언트가 `"role": "admin"`을 보내도 Pydantic이 무시하도록 처리. 서버가 `role="user"`로 강제 대입.

---

### 3. backend/app/api/v1/users.py 수정

**변경 전:**
```python
from app.api.deps import get_db, get_current_user

@router.post("", response_model=UserResponse)
def create_user(user: UserCreateRequest, db: Session = Depends(get_db)):
    """범용 사용자 생성 엔드포인트 (role 지정 가능)"""
    ...
    db_user = User(
        email=user.email,
        nickname=user.nickname,
        password=hash_password(user.password),
        birth_date=user.birth_date,
        role=user.role       # 클라이언트 값 그대로 사용
    )

@router.get("", response_model=List[UserResponse])
def get_all_users(
    nickname: Optional[str] = Query(None),
    db: Session = Depends(get_db)   # 인증 없음
):

@router.get("/{user_id}", response_model=UserResponse)
def get_user(
    user_id: int,
    db: Session = Depends(get_db)   # 인증 없음
):

@router.put("/{user_id}", response_model=UserResponse)
def update_user(
    user_id: int,
    user_update: UserUpdateRequest,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user),  # 인증만, 권한 체크 없음
):

@router.delete("/{user_id}", response_model=DeleteResponse)
def delete_user(
    user_id: int,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user),  # 인증만, 권한 체크 없음
):
```

**변경 후:**
```python
from app.api.deps import get_db, get_current_user, require_self_or_admin

@router.post("", response_model=UserResponse)
def create_user(user: UserCreateRequest, db: Session = Depends(get_db)):
    """회원가입"""
    ...
    db_user = User(
        email=user.email,
        nickname=user.nickname,
        password=hash_password(user.password),
        birth_date=user.birth_date,
        role="user",        # 서버 강제 대입
    )

@router.get("", response_model=List[UserResponse])
def get_all_users(
    nickname: Optional[str] = Query(None),
    db: Session = Depends(get_db),
    _auth: dict = Depends(get_current_user),   # 로그인 가드 추가
):

@router.get("/{user_id}", response_model=UserResponse)
def get_user(
    user_id: int,
    db: Session = Depends(get_db),
    _auth: dict = Depends(get_current_user),   # 로그인 가드 추가
):

@router.put("/{user_id}", response_model=UserResponse)
def update_user(
    user_id: int,
    user_update: UserUpdateRequest,
    db: Session = Depends(get_db),
    _auth: dict = Depends(require_self_or_admin),  # 본인/admin 체크로 교체
):

@router.delete("/{user_id}", response_model=DeleteResponse)
def delete_user(
    user_id: int,
    db: Session = Depends(get_db),
    _auth: dict = Depends(require_self_or_admin),  # 본인/admin 체크로 교체
):
```

**변경 의도:** 각 엔드포인트에 적합한 권한 의존성을 부착. `_auth` 변수명은 라우터 본문에서 사용하지 않음을 표시(lint 경고 회피). 본문 비즈니스 로직은 변경하지 않음.

---

### 4. frontend/src/pages/FriendPage/FriendPage.jsx 수정

**변경 전:**
```jsx
import axios from 'axios';
// api import 없음

const handleSearch = async () => {
  const response = await axios.get('/api/v1/users', {
    params: { nickname: searchNickname }
  });
```

**변경 후:**
```jsx
import axios from 'axios';
import api from '../../lib/api';

const handleSearch = async () => {
  const response = await api.get('/users', {
    params: { nickname: searchNickname }
  });
```

**변경 의도:** `GET /users`에 로그인 가드가 생기면서 Authorization 헤더가 필요해짐. bare `axios` 대신 인터셉터가 붙은 `api` 인스턴스로 교체. baseURL이 `/api/v1`이므로 경로도 `/api/v1/users` → `/users`로 수정.

---

### 5. frontend/src/components/HeaderUserInfo.jsx 수정

**변경 전:**
```jsx
import axios from 'axios';

useEffect(() => {
  if (isLoggedIn && user?.id) {
    axios.get(`/api/v1/users/${user.id}`)
      .then(res => setRole(res.data.role))
      .catch(() => setRole(null));
  }
}, [isLoggedIn, user?.id]);
```

**변경 후:**
```jsx
import api from '../lib/api';

useEffect(() => {
  if (isLoggedIn && user?.id) {
    api.get(`/users/${user.id}`)
      .then(res => setRole(res.data.role))
      .catch(() => setRole(null));
  }
}, [isLoggedIn, user?.id]);
```

**변경 의도:** 헤더 컴포넌트는 모든 페이지에 붙어있어, bare `axios`로 `GET /users/{id}`를 호출하면 전 페이지에서 401이 발생. `api` 인터셉터로 교체해 토큰이 자동으로 붙도록 수정.

## 권한 처리 흐름

```
[요청 도착]
     │
     ▼
get_current_user        ← JWT 없음/위조 → 401
     │
     ▼
require_self_or_admin   ← 타인 id + 일반 user → 403
     │
     ▼
라우터 본문 (비즈니스 로직)
```

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| 회원가입 (`POST /users`) | role 필드 제거 + 서버 강제 'user' 대입 | 정상 |
| 사용자 목록 조회 (`GET /users`) | 로그인 가드 추가 | 정상 |
| 사용자 단건 조회 (`GET /users/{id}`) | 로그인 가드 추가 | 정상 |
| 사용자 정보 수정 (`PUT /users/{id}`) | 본인/admin 체크 추가 | 정상 |
| 사용자 삭제 (`DELETE /users/{id}`) | 본인/admin 체크 추가 | 정상 |
| 친구 검색 (프론트) | `api` 인터셉터로 교체 | 정상 |
| 헤더 유저 정보 표시 (프론트, 전 페이지) | `api` 인터셉터로 교체 | 정상 |
| role=admin 임의 가입 | 스키마에서 role 제거로 차단 | 정상 (차단됨) |

## 결과
- 상태: 완료
- curl 시나리오 D-1~D-4 전 항목 통과
- 빌드: 오류 없음, nginx 배포 완료

## 배운 내용

## 다음 작업
- 응답 스키마 권한별 분기 — `GET /users`, `GET /users/{id}` 이메일 등 풀필드 노출 차단
- `UserUpdateRequest.role` 처리 — 일반 user가 본인 role을 'admin'으로 바꿀 수 있는 잔여 위험
- `/users/me` 엔드포인트 분리
- 회원가입 경로 분리 (`POST /auth/register`)
- 다른 어드민 라우터(`notices.py` 등) 권한 점검

---

# 업무일지 (3차)

## 기본 정보
- 날짜: 2026-05-09 (토)
- 작성자: hexsera

## 진행 목표

AI가 임의로 추가한 `get_current_user` 의존성을 `game_sessions`, `notices` 라우터에서 제거한다. 이 두 라우터는 원래 인증 없이 동작하도록 설계된 것이며, 의도치 않게 붙은 인증 가드를 되돌린다.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| game_sessions PUT/DELETE에서 `get_current_user` 제거 | `backend/app/api/v1/game_sessions.py` | 불필요한 `User` import도 제거 |
| notices POST/DELETE에서 `get_current_user` 제거 | `backend/app/api/v1/notices.py` | `author_id`를 DB 실제값 기준 1로 하드코딩 |

**총 20분**

## 주요 변경 사항

### 1. backend/app/api/v1/game_sessions.py 수정

**변경 전:**
```python
import json
import sys
sys.path.insert(0, '/code')
from models import User
from datetime import datetime, timezone
from fastapi import APIRouter, Depends, HTTPException
from app.api.deps import get_current_user
from app.redis_client import redis_client
from app.schemas.game_session import (
    GameSessionSaveRequest,
    GameSessionResponse,
    GameSessionDeleteResponse,
)

REDIS_TTL = 7200
router = APIRouter()


@router.put("/{user_id}", response_model=GameSessionResponse)
def save_game_session(
    user_id: int,
    data: GameSessionSaveRequest,
    current_user: User = Depends(get_current_user),
):
    ...


@router.delete("/{user_id}", response_model=GameSessionDeleteResponse)
def delete_game_session(
    user_id: int,
    current_user: User = Depends(get_current_user),
):
    ...
```

**변경 후:**
```python
import json
from datetime import datetime, timezone
from fastapi import APIRouter, HTTPException
from app.redis_client import redis_client
from app.schemas.game_session import (
    GameSessionSaveRequest,
    GameSessionResponse,
    GameSessionDeleteResponse,
)

REDIS_TTL = 7200
router = APIRouter()


@router.put("/{user_id}", response_model=GameSessionResponse)
def save_game_session(
    user_id: int,
    data: GameSessionSaveRequest,
):
    ...


@router.delete("/{user_id}", response_model=GameSessionDeleteResponse)
def delete_game_session(
    user_id: int,
):
    ...
```

**변경 의도:** AI가 임의로 추가한 인증 가드 제거. game_sessions은 인증 없이 동작하도록 설계된 라우터이며, 불필요한 `User` import와 `Depends` import도 함께 정리.

---

### 2. backend/app/api/v1/notices.py 수정

**변경 전:**
```python
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from app.api.deps import get_db, get_current_user

@router.post("", response_model=NoticeResponse)
def create_notice(
    body: NoticeCreateRequest,
    db: Session = Depends(get_db),
    current_user: dict = Depends(get_current_user),
):
    notice = Notice(title=body.title, content=body.content, author_id=int(current_user["sub"]))
    ...


@router.delete("/{notice_id}")
def delete_notice(
    notice_id: int,
    db: Session = Depends(get_db),
    current_user: dict = Depends(get_current_user),
):
    ...
```

**변경 후:**
```python
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from app.api.deps import get_db

@router.post("", response_model=NoticeResponse)
def create_notice(
    body: NoticeCreateRequest,
    db: Session = Depends(get_db),
):
    notice = Notice(title=body.title, content=body.content, author_id=1)
    ...


@router.delete("/{notice_id}")
def delete_notice(
    notice_id: int,
    db: Session = Depends(get_db),
):
    ...
```

**변경 의도:** AI가 임의로 추가한 인증 가드 제거. `author_id`는 DB 전체 레코드가 1로 고정되어 있어 하드코딩으로 처리. `get_current_user` import도 함께 제거.

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| 게임 세션 저장 (`PUT /game-sessions/{user_id}`) | 인증 가드 제거 | 미확인 |
| 게임 세션 삭제 (`DELETE /game-sessions/{user_id}`) | 인증 가드 제거 | 미확인 |
| 공지사항 생성 (`POST /notices`) | 인증 가드 제거, author_id=1 고정 | 미확인 |
| 공지사항 삭제 (`DELETE /notices/{id}`) | 인증 가드 제거 | 미확인 |

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- `UserUpdateRequest.role` 자가승격 취약점 수정
- notices/game_sessions 권한 설계 재검토 (어떤 엔드포인트에 인증이 필요한지 명시적으로 정의)
