# 업무일지
**금일 업무일지에 진행된 작업은 모두 롤백되었음**
**금일업무로인한 변경사황은 없음**

## 기본 정보
- 날짜: 2026-03-20 (금)
- 작성자: hexsera

## 진행 목표

Google OAuth 2.0 소셜 로그인 및 JWT 인증 도입 PRD(PHASE 1)의 프론트엔드 구현.
- 로그인 페이지에 구글 로그인 버튼 추가
- OAuth 콜백 페이지 신규 생성
- 자체 로그인/회원가입 흐름에 Authorization Code → JWT 교환 추가
- 로그아웃 시 서버 측 JWT 쿠키 삭제 API 호출 추가
- TDD(Red-Green-Refactor) 방법론으로 진행

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| TDD RED 계획 작성 | `docs/prd/TDD-RED-Phase1-OAuth-UI-2026-03-20.md` | 18개 테스트 케이스 목록 |
| Login 테스트 추가 | `frontend/src/test/Login.test.jsx` | 구글 로그인 버튼·JWT 교환 관련 6개 RED 테스트 추가 |
| OAuthCallback 테스트 생성 | `frontend/src/test/OAuthCallback.test.jsx` | 신규 — 7개 RED 테스트 |
| Register 테스트 생성 | `frontend/src/test/Register.test.jsx` | 신규 — 3개 RED 테스트 |
| AuthContext 테스트 생성 | `frontend/src/test/AuthContext.test.jsx` | 신규 — 2개 RED 테스트 |
| TDD GREEN 계획 작성 | `docs/prd/TDD-GREEN-PLAN-Phase1-OAuth-UI-2026-03-20.md` | 파일별 상세 구현 계획 |
| AuthContext 수정 | `frontend/src/contexts/AuthContext.jsx` | logout에 API 호출 추가 |
| OAuthCallback 페이지 생성 | `frontend/src/pages/OAuthCallback/OAuthCallback.jsx` | 신규 — OAuth 콜백 처리 페이지 |
| Login 페이지 수정 | `frontend/src/pages/Login/Login.jsx` | 구글 로그인 버튼·JWT 교환 흐름 추가 |
| Register 페이지 수정 | `frontend/src/pages/Register/Register.jsx` | JWT 교환 및 홈 이동으로 변경 |
| App 라우터 수정 | `frontend/src/App.jsx` | /oauth/callback 라우트 추가 |
| TDD GREEN 완료 보고서 | `docs/prd/TDD-GREEN-Phase1-OAuth-UI-2026-03-20.md` | 변경 이력 기록 |

**총 약 2시간**

## 주요 변경 사항

### 인증 흐름 변경 개요

```
[기존 자체 로그인 흐름]
POST /api/v1/login
  → 성공(200) → login({ id, name, role, email }) → 페이지 이동

[변경 후 자체 로그인 흐름]
POST /api/v1/login
  → 성공(200) → authorization_code 수신
    → POST /api/v1/auth/token({ authorization_code })
      → 성공 → login({ id, name, role, email }) → 페이지 이동
      → 실패 → loginError 표시

[신규 구글 로그인 흐름]
"구글로 로그인" 버튼 클릭
  → GET /api/v1/auth/google/login
    → authorization_url 수신
      → window.location.href = authorization_url (구글 인증 페이지로 이동)
        → 구글 인증 완료
          → /oauth/callback?code=xxx 로 리다이렉트
            → POST /api/v1/auth/token({ authorization_code: code })
              → 성공 → login() → 페이지 이동
              → 실패 → /login 이동

[신규 로그아웃 흐름]
logout() 호출
  → POST /api/v1/auth/logout (서버 JWT 쿠키 삭제)
  → localStorage.removeItem('user')
  → 상태 초기화
```

---

### 1. `frontend/src/contexts/AuthContext.jsx` 수정

**변경 전:**
```jsx
import { createContext, useContext, useState, useEffect } from 'react';

export const AuthContext = createContext();

export function AuthProvider({ children }) {
  const [isLoggedIn, setIsLoggedIn] = useState(false);
  const [user, setUser] = useState(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    const savedUser = localStorage.getItem('user');
    if (savedUser) {
      setUser(JSON.parse(savedUser));
      setIsLoggedIn(true);
    }
    setLoading(false);
  }, []);

  const login = (userData) => {
    setUser(userData);
    setIsLoggedIn(true);
    localStorage.setItem('user', JSON.stringify(userData));
  };

  // 로그아웃 함수
  const logout = () => {
    setUser(null);
    setIsLoggedIn(false);
    localStorage.removeItem('user');
  };

  return (
    <AuthContext.Provider value={{ isLoggedIn, user, loading, login, logout }}>
      {children}
    </AuthContext.Provider>
  );
}

export function useAuth() {
  return useContext(AuthContext);
}
```

**변경 후:**
```jsx
import { createContext, useContext, useState, useEffect } from 'react';
import axios from 'axios';

export const AuthContext = createContext();

export function AuthProvider({ children }) {
  const [isLoggedIn, setIsLoggedIn] = useState(false);
  const [user, setUser] = useState(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    const savedUser = localStorage.getItem('user');
    if (savedUser) {
      setUser(JSON.parse(savedUser));
      setIsLoggedIn(true);
    }
    setLoading(false);
  }, []);

  const login = (userData) => {
    setUser(userData);
    setIsLoggedIn(true);
    localStorage.setItem('user', JSON.stringify(userData));
  };

  // 로그아웃 함수
  const logout = async () => {
    await axios.post('/api/v1/auth/logout');
    setUser(null);
    setIsLoggedIn(false);
    localStorage.removeItem('user');
  };

  return (
    <AuthContext.Provider value={{ isLoggedIn, user, loading, login, logout }}>
      {children}
    </AuthContext.Provider>
  );
}

export function useAuth() {
  return useContext(AuthContext);
}
```

**변경 의도:** 로그아웃 시 서버에서 httpOnly JWT 쿠키를 삭제해야 하므로, 프론트엔드가 직접 쿠키를 조작하는 대신 서버 API를 호출하여 처리.

---

### 2. `frontend/src/pages/OAuthCallback/OAuthCallback.jsx` 신규 생성

**추가 (전체 코드):**
```jsx
import { useEffect } from 'react';
import { useNavigate, useSearchParams } from 'react-router-dom';
import { Box, CircularProgress } from '@mui/material';
import { useAuth } from '../../contexts/AuthContext';
import axios from 'axios';

function OAuthCallback() {
  const navigate = useNavigate();
  const [searchParams] = useSearchParams();
  const { login } = useAuth();

  useEffect(() => {
    const code = searchParams.get('code');

    if (!code) {
      navigate('/login');
      return;
    }

    axios.post('/api/v1/auth/token', { authorization_code: code })
      .then((response) => {
        const data = response.data;
        login({ id: data.user_id, name: data.nickname, role: data.role, email: data.email });
        if (data.role === 'admin') {
          navigate('/admin');
        } else {
          navigate('/');
        }
      })
      .catch(() => {
        navigate('/login');
      });
  }, []);

  return (
    <Box display="flex" justifyContent="center" alignItems="center" minHeight="100vh">
      <CircularProgress />
    </Box>
  );
}

export default OAuthCallback;
```

**변경 의도:** 구글 OAuth 인증 완료 후 `/oauth/callback?code=xxx`로 리다이렉트되는 페이지. 진입 즉시 로딩 스피너를 표시하고, URL의 Authorization Code를 추출해 JWT로 교환한 뒤 role에 따라 적절한 페이지로 이동.

---

### 3. `frontend/src/pages/Login/Login.jsx` 수정

**변경 전 (`handleLogin` 함수 + JSX 버튼 영역):**
```jsx
// handleLogin 함수
const handleLogin = async () => {
  try {
    const response = await axios.post('/api/v1/login', {
      email: username,
      password: password
    });

    if (response.status === 200) {
      console.log('로그인 성공:', response.data);
      login({ id: response.data.user_id, name: response.data.nickname, role: response.data.role, email: response.data.email });
      setLoginError(false);

      if (response.data.role === 'admin') {
        navigate('/admin');
      } else {
        navigate('/');
      }
    }
  } catch (error) {
    setLoginError(true);
    if (error.response) {
      console.log('로그인 실패:', error.response.data.detail);
    }
  }
};

// JSX — 버튼 영역 (로그인 버튼, 회원가입 버튼만 존재)
<Box sx={{ display: 'flex', justifyContent: 'center', mt: 3 }}>
  <Button
    variant="contained"
    sx={{ width: '300px', backgroundColor: COLORS.primary, color: COLORS.text }}
    onClick={handleLogin}
  >
    로그인
  </Button>
</Box>
<Box sx={{ display: 'flex', justifyContent: 'center', mt: 3 }}>
  <Button
    variant="outlined"
    sx={{ width: '300px', borderColor: COLORS.text, color: COLORS.text }}
    onClick={()=>navigate('/Register')}
  >
    회원가입
  </Button>
</Box>
```

**변경 후 (`handleGoogleLogin` 추가 + `handleLogin` 수정 + JSX 버튼 영역):**
```jsx
// 구글 로그인 버튼 클릭 시
const handleGoogleLogin = async () => {
  const response = await axios.get('/api/v1/auth/google/login');
  window.location.href = response.data.authorization_url;
};

// handleLogin 함수
const handleLogin = async () => {
  try {
    const response = await axios.post('/api/v1/login', {
      email: username,
      password: password
    });

    if (response.status === 200) {
      const tokenResponse = await axios.post('/api/v1/auth/token', {
        authorization_code: response.data.authorization_code,
      });
      login({
        id: tokenResponse.data.user_id,
        name: tokenResponse.data.nickname,
        role: tokenResponse.data.role,
        email: tokenResponse.data.email,
      });
      setLoginError(false);

      if (tokenResponse.data.role === 'admin') {
        navigate('/admin');
      } else {
        navigate('/');
      }
    }
  } catch (error) {
    setLoginError(true);
    if (error.response) {
      console.log('로그인 실패:', error.response.data.detail);
    }
  }
};

// JSX — 버튼 영역 (로그인, 회원가입, 구글 로그인 버튼)
<Box sx={{ display: 'flex', justifyContent: 'center', mt: 3 }}>
  <Button
    variant="contained"
    sx={{ width: '300px', backgroundColor: COLORS.primary, color: COLORS.text }}
    onClick={handleLogin}
  >
    로그인
  </Button>
</Box>
<Box sx={{ display: 'flex', justifyContent: 'center', mt: 3 }}>
  <Button
    variant="outlined"
    sx={{ width: '300px', borderColor: COLORS.text, color: COLORS.text }}
    onClick={()=>navigate('/Register')}
  >
    회원가입
  </Button>
</Box>
<Box sx={{ display: 'flex', justifyContent: 'center', mt: 2 }}>
  <Button
    variant="outlined"
    sx={{ width: '300px', borderColor: COLORS.border, color: COLORS.text }}
    onClick={handleGoogleLogin}
  >
    구글로 로그인
  </Button>
</Box>
```

**변경 의도:**
- `handleGoogleLogin`: 구글 OAuth URL을 서버에서 받아 브라우저를 구글 인증 페이지로 이동시킴
- `handleLogin` 수정: 기존 `/api/v1/login` 성공 후 바로 localStorage를 저장하던 방식 → Authorization Code를 받아 `/api/v1/auth/token`으로 JWT를 교환하는 2단계 흐름으로 변경

---

### 4. `frontend/src/pages/Register/Register.jsx` 수정

**변경 전 (`handleRegister` step 2 성공 처리 + import):**
```jsx
import { useState } from 'react';
import { Box, TextField, Button, Container, Typography, Paper, Alert, CardMedia } from '@mui/material';
import { useNavigate } from 'react-router-dom';
import axios from 'axios';
import Aurora from '../../components/Aurora/Aurora';

// ...

function Register() {
  // ...
  const navigate = useNavigate();

  const handleRegister = async () => {
    // ...
    else if (registerStep == 2) {
      // ...
      try {
        const response = await axios.post('/api/v1/register', data);

        // 성공: 201 상태코드와 User 객체 반환
        if (response.status === 201) {
          console.log('회원가입 성공:', response.data);
          navigate('/login');
        }
      } catch (error) {
        // ...
      }
    }
  };
```

**변경 후 (`handleRegister` step 2 성공 처리 + import):**
```jsx
import { useState } from 'react';
import { Box, TextField, Button, Container, Typography, Paper, Alert, CardMedia } from '@mui/material';
import { useNavigate } from 'react-router-dom';
import { useAuth } from '../../contexts/AuthContext';
import axios from 'axios';
import Aurora from '../../components/Aurora/Aurora';

// ...

function Register() {
  // ...
  const navigate = useNavigate();
  const { login } = useAuth();

  const handleRegister = async () => {
    // ...
    else if (registerStep == 2) {
      // ...
      try {
        const response = await axios.post('/api/v1/register', data);

        // 성공: 201 상태코드와 User 객체 반환
        if (response.status === 201) {
          const tokenResponse = await axios.post('/api/v1/auth/token', {
            authorization_code: response.data.authorization_code,
          });
          login({
            id: tokenResponse.data.user_id,
            name: tokenResponse.data.nickname,
            role: tokenResponse.data.role,
            email: tokenResponse.data.email,
          });
          navigate('/');
        }
      } catch (error) {
        // ...
      }
    }
  };
```

**변경 의도:** 회원가입 완료 후 로그인 페이지(`/login`)로 이동하던 것을, JWT 교환 후 바로 홈(`/`)으로 이동하도록 변경 → 사용자가 회원가입 직후 다시 로그인할 필요 없이 자동으로 로그인 상태가 됨.

---

### 5. `frontend/src/App.jsx` 수정

**변경 전:**
```jsx
import NoticeDetailPage from './pages/Notice/NoticeDetailPage';

// Routes 내부
<Route path="/notice/:id" element={<NoticeDetailPage />} />
```

**변경 후:**
```jsx
import NoticeDetailPage from './pages/Notice/NoticeDetailPage';
import OAuthCallback from './pages/OAuthCallback/OAuthCallback';

// Routes 내부
<Route path="/notice/:id" element={<NoticeDetailPage />} />
<Route path="/oauth/callback" element={<OAuthCallback />} />
```

**변경 의도:** 구글 OAuth 콜백 URL(`/oauth/callback`)을 라우터에 등록하여 OAuthCallback 페이지가 렌더링되도록 설정.

---

### 6. 테스트 파일 추가/수정

**추가된 테스트 파일:**

| 파일 | 테스트 수 | 내용 |
|------|---------|------|
| `frontend/src/test/OAuthCallback.test.jsx` | 7개 | 콜백 페이지 로딩 스피너, JWT 교환, role 기반 이동, 에러 처리 |
| `frontend/src/test/Register.test.jsx` | 3개 | 회원가입 후 JWT 교환, 홈 이동, localStorage 저장 |
| `frontend/src/test/AuthContext.test.jsx` | 2개 | logout 시 API 호출, localStorage 제거 |

**수정된 테스트 파일:**

| 파일 | 변경 내용 |
|------|---------|
| `frontend/src/test/Login.test.jsx` | 기존 1개 테스트의 mock 수정(post 2회), 신규 6개 테스트 추가 |

---

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| 자체 로그인 | 로그인 성공 후 JWT 교환 단계 추가 | 정상 (테스트 7개 통과) |
| 회원가입 | 가입 성공 후 JWT 교환 → 홈 이동으로 변경 | 정상 (테스트 3개 통과) |
| 구글 로그인 버튼 | 신규 추가 — 버튼 클릭 시 구글 인증 페이지로 이동 | 정상 (테스트 통과, 백엔드 미구현으로 실제 동작은 미확인) |
| OAuth 콜백 페이지 | 신규 — /oauth/callback 진입 시 JWT 교환 처리 | 정상 (테스트 7개 통과, 백엔드 미구현으로 실제 동작은 미확인) |
| 로그아웃 | 로그아웃 시 서버 쿠키 삭제 API 호출 추가 | 정상 (테스트 2개 통과) |

## 결과
- 상태: 완료
- 테스트: **19/19 통과** (신규 17개 + 기존 2개)

## 배운 내용

## 다음 작업
- PHASE 2: 백엔드 — Authorization Code + JWT 발급 구현
  - PyJWT 패키지 설치 및 JWT 설정 추가
  - 메모리 기반 Authorization Code 저장소 구현
  - POST /api/v1/login, POST /api/v1/register 응답에 authorization_code 추가
  - POST /api/v1/auth/token 엔드포인트 신규 구현
  - POST /api/v1/auth/logout 엔드포인트 신규 구현
  - GET /api/v1/auth/google/login, GET /api/v1/auth/google/callback 구현
  - User 모델에 provider 컬럼 추가, password nullable 변경

---

# 업무일지 (2차)

## 기본 정보
- 날짜: 2026-03-20 (금)
- 작성자: hexsera

## 진행 목표

구글 로그인 버튼 디자인을 구글 공식 브랜드 가이드라인에 맞게 변경하고, 버튼 순서를 조정한다.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| TDD RED/GREEN 테스트 검증 | `frontend/src/test/Login.test.jsx` 외 3개 | 18개 테스트 전체 통과 확인 |
| 구글 로그인 버튼 디자인 변경 | `frontend/src/pages/Login/Login.jsx` | GoogleIcon SVG 컴포넌트 추가, 구글 브랜드 스타일 적용 |
| 구글 로그인 버튼 순서 변경 | `frontend/src/pages/Login/Login.jsx` | 구글 로그인 ↔ 회원가입 버튼 순서 교체 |

**총 약 20분**

## 주요 변경 사항

### 1. `frontend/src/pages/Login/Login.jsx` 수정

**변경 전 (버튼 영역 전체):**
```jsx
<Box sx={{ display: 'flex', justifyContent: 'center', mt: 3 }}>
  <Button
    variant="contained"
    sx={{ width: '300px', backgroundColor: COLORS.primary, color: COLORS.text }}
    onClick={handleLogin}
  >
    로그인
  </Button>
</Box>
<Box sx={{ display: 'flex', justifyContent: 'center', mt: 3 }}>
  <Button
    variant="outlined"
    sx={{ width: '300px', borderColor: COLORS.text, color: COLORS.text }}
    onClick={()=>navigate('/Register')}
  >
    회원가입
  </Button>
</Box>
<Box sx={{ display: 'flex', justifyContent: 'center', mt: 2 }}>
  <Button
    variant="outlined"
    sx={{ width: '300px', borderColor: COLORS.border, color: COLORS.text }}
    onClick={handleGoogleLogin}
  >
    구글로 로그인
  </Button>
</Box>
```

**변경 후 (버튼 영역 전체 + GoogleIcon 컴포넌트 추가):**
```jsx
// 파일 상단에 추가된 GoogleIcon 컴포넌트
const GoogleIcon = () => (
  <svg width="18" height="18" viewBox="0 0 18 18" xmlns="http://www.w3.org/2000/svg">
    <path fill="#4285F4" d="M17.64 9.2c0-.637-.057-1.251-.164-1.84H9v3.481h4.844c-.209 1.125-.843 2.078-1.796 2.717v2.258h2.908c1.702-1.567 2.684-3.874 2.684-6.615z"/>
    <path fill="#34A853" d="M9 18c2.43 0 4.467-.806 5.956-2.184l-2.908-2.258c-.806.54-1.837.86-3.048.86-2.344 0-4.328-1.584-5.036-3.711H.957v2.332A8.997 8.997 0 0 0 9 18z"/>
    <path fill="#FBBC05" d="M3.964 10.707A5.41 5.41 0 0 1 3.682 9c0-.593.102-1.17.282-1.707V4.961H.957A8.996 8.996 0 0 0 0 9c0 1.452.348 2.827.957 4.039l3.007-2.332z"/>
    <path fill="#EA4335" d="M9 3.58c1.321 0 2.508.454 3.44 1.345l2.582-2.58C13.463.891 11.426 0 9 0A8.997 8.997 0 0 0 .957 4.961L3.964 6.293C4.672 4.166 6.656 3.58 9 3.58z"/>
  </svg>
);

// 버튼 영역 (순서: 로그인 → 구글로 로그인 → 회원가입)
<Box sx={{ display: 'flex', justifyContent: 'center', mt: 3 }}>
  <Button
    variant="contained"
    sx={{ width: '300px', backgroundColor: COLORS.primary, color: COLORS.text }}
    onClick={handleLogin}
  >
    로그인
  </Button>
</Box>
<Box sx={{ display: 'flex', justifyContent: 'center', mt: 3 }}>
  <Button
    variant="outlined"
    startIcon={<GoogleIcon />}
    sx={{
      width: '300px',
      backgroundColor: '#FFFFFF',
      color: '#3c4043',
      borderColor: '#dadce0',
      textTransform: 'none',
      fontFamily: 'Google Sans, Roboto, sans-serif',
      fontWeight: 500,
      fontSize: '14px',
      '&:hover': {
        backgroundColor: '#f8f9fa',
        borderColor: '#dadce0',
        boxShadow: '0 1px 3px rgba(60,64,67,.3)',
      },
    }}
    onClick={handleGoogleLogin}
  >
    구글로 로그인
  </Button>
</Box>
<Box sx={{ display: 'flex', justifyContent: 'center', mt: 2 }}>
  <Button
    variant="outlined"
    sx={{ width: '300px', borderColor: COLORS.text, color: COLORS.text }}
    onClick={()=>navigate('/Register')}
  >
    회원가입
  </Button>
</Box>
```

**변경 의도:**
- 구글 공식 브랜드 가이드라인 적용: 흰색 배경, 어두운 회색 텍스트, 연한 회색 테두리, 구글 4색 SVG 로고
- 외부 이미지 의존 없이 인라인 SVG로 구글 로고를 직접 렌더링
- 버튼 순서를 로그인 → 구글로 로그인 → 회원가입으로 변경 (소셜 로그인 강조)

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| 구글 로그인 버튼 UI | 텍스트만 있던 버튼에 구글 4색 로고 추가, 브랜드 색상 적용 | 정상 (테스트 7개 통과) |
| 로그인 페이지 버튼 순서 | 로그인 → 회원가입 → 구글로 로그인 → 로그인 → 구글로 로그인 → 회원가입 | 정상 |

## 결과
- 상태: 완료
- 테스트: **7/7 통과** (Login.test.jsx)

## 배운 내용

## 다음 작업
- PHASE 2: 백엔드 — Authorization Code + JWT 발급 구현
