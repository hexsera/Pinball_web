# 업무일지

## 기본 정보
- 날짜: 2026-04-03 (금)
- 작성자: hexsera

## 진행 목표

공지사항 목록 페이지에서 전체 공지를 한 번에 표시하던 방식을 페이지네이션으로 전환한다.
10개씩 나눠 보여주고, 페이지 번호 버튼(최대 5개 슬라이딩)으로 이동할 수 있도록 개선한다.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| getNotices 테스트 수정 | `frontend/src/test/noticeService.test.js` | skip/limit 파라미터 및 { items, total } 응답 구조로 변경 |
| getNotices 함수 수정 | `frontend/src/services/noticeService.js` | 파라미터 추가, 응답 구조 변경, IndexedDB fallback 동기화 |
| NoticeListPage 테스트 수정 및 추가 | `frontend/src/test/NoticeListPage.test.jsx` | mock 구조 변경 + 페이지네이션 테스트 3개 추가 |
| NoticeListPage UI 수정 | `frontend/src/pages/Notice/NoticeListPage.jsx` | useSearchParams 도입, 페이지 상태 관리, 페이지 버튼 UI 추가 |

**총 1시간 30분**

## 주요 변경 사항

### 1. noticeService.test.js 수정

**변경 전:**
```javascript
describe('getNotices', () => {
  it('GET /api/v1/notices를 호출한다', async () => {
    axios.get.mockResolvedValue({ data: [] });
    await getNotices();
    expect(axios.get).toHaveBeenCalledWith('/api/v1/notices');
  });

  it('서버 응답 data 배열을 반환한다', async () => {
    const mockData = [{ id: 1, title: '공지1' }];
    axios.get.mockResolvedValue({ data: mockData });
    const result = await getNotices();
    expect(result).toEqual(mockData);
  });
});
```

**변경 후:**
```javascript
describe('getNotices', () => {
  it('GET /api/v1/notices를 skip, limit 파라미터와 함께 호출한다', async () => {
    axios.get.mockResolvedValue({ data: { items: [], total: 0 } });
    await getNotices(0, 10);
    expect(axios.get).toHaveBeenCalledWith('/api/v1/notices', { params: { skip: 0, limit: 10 } });
  });

  it('서버 응답의 { items, total } 구조를 반환한다', async () => {
    const mockData = { items: [{ id: 1, title: '공지1' }], total: 1 };
    axios.get.mockResolvedValue({ data: mockData });
    const result = await getNotices(0, 10);
    expect(result).toEqual(mockData);
  });
});
```

**변경 의도:** `getNotices`가 `skip`, `limit` 파라미터를 받고 `{ items, total }` 구조를 반환하는 것을 검증하도록 수정.

---

### 2. noticeService.js 수정

**변경 전:**
```javascript
export const getNotices = async () => {
  try {
    const res = await axios.get('/api/v1/notices');
    return res.data;
  } catch {
    await seedNoticesIfEmpty();
    return getAllNoticesFromDB();
  }
};
```

**변경 후:**
```javascript
export const getNotices = async (skip = 0, limit = 10) => {
  try {
    const res = await axios.get('/api/v1/notices', { params: { skip, limit } });
    return res.data; // { items, total }
  } catch {
    await seedNoticesIfEmpty();
    const all = await getAllNoticesFromDB();
    return {
      items: all.slice(skip, skip + limit),
      total: all.length,
    };
  }
};
```

**변경 의도:** 페이지네이션 파라미터(`skip`, `limit`)를 API에 전달하고, IndexedDB fallback도 동일한 `{ items, total }` 구조로 반환해 API 성공/실패 시 응답 형태를 통일.

---

### 3. NoticeListPage.test.jsx 수정 및 추가

**변경 전 (mock 부분):**
```javascript
vi.mock('../services/noticeService', () => ({
  getNotices: vi.fn().mockResolvedValue([
    { id: 1, title: '첫 번째 공지', created_at: '2026-01-15T00:00:00' },
    { id: 2, title: '두 번째 공지', created_at: '2026-02-20T00:00:00' },
  ]),
}));

// beforeEach
getNotices.mockResolvedValue([
  { id: 1, title: '첫 번째 공지', created_at: '2026-01-15T00:00:00' },
  { id: 2, title: '두 번째 공지', created_at: '2026-02-20T00:00:00' },
]);

function renderWithAuth(userValue) {
  if (userValue) {
    localStorage.setItem('user', JSON.stringify(userValue));
  }
  return render(
    <MemoryRouter>
      <AuthProvider>
        <NoticeListPage />
      </AuthProvider>
    </MemoryRouter>
  );
}
```

**변경 후 (mock 부분 + 신규 테스트):**
```javascript
vi.mock('../services/noticeService', () => ({
  getNotices: vi.fn().mockResolvedValue({
    items: [
      { id: 1, title: '첫 번째 공지', created_at: '2026-01-15T00:00:00' },
      { id: 2, title: '두 번째 공지', created_at: '2026-02-20T00:00:00' },
    ],
    total: 2,
  }),
}));

// beforeEach
getNotices.mockResolvedValue({
  items: [
    { id: 1, title: '첫 번째 공지', created_at: '2026-01-15T00:00:00' },
    { id: 2, title: '두 번째 공지', created_at: '2026-02-20T00:00:00' },
  ],
  total: 2,
});

function renderWithAuth(userValue, initialEntry = '/') {
  if (userValue) {
    localStorage.setItem('user', JSON.stringify(userValue));
  }
  return render(
    <MemoryRouter initialEntries={[initialEntry]}>
      <AuthProvider>
        <NoticeListPage />
      </AuthProvider>
    </MemoryRouter>
  );
}

// 신규 추가: 페이지네이션 테스트
describe('NoticeListPage 페이지네이션', () => {
  it('total 기반으로 페이지 버튼이 렌더링된다', async () => {
    getNotices.mockResolvedValue({
      items: Array.from({ length: 10 }, (_, i) => ({
        id: i + 1, title: `공지 ${i + 1}`, created_at: '2026-01-01T00:00:00',
      })),
      total: 25,
    });
    renderWithAuth(null);
    await waitFor(() => {
      expect(screen.getByRole('button', { name: '1' })).toBeInTheDocument();
      expect(screen.getByRole('button', { name: '2' })).toBeInTheDocument();
      expect(screen.getByRole('button', { name: '3' })).toBeInTheDocument();
    });
  });

  it('페이지 버튼 클릭 시 해당 페이지 데이터를 불러온다', async () => {
    getNotices.mockResolvedValue({
      items: Array.from({ length: 10 }, (_, i) => ({
        id: i + 1, title: `공지 ${i + 1}`, created_at: '2026-01-01T00:00:00',
      })),
      total: 25,
    });
    renderWithAuth(null);
    await waitFor(() => {
      expect(screen.getByRole('button', { name: '2' })).toBeInTheDocument();
    });
    fireEvent.click(screen.getByRole('button', { name: '2' }));
    await waitFor(() => {
      expect(getNotices).toHaveBeenCalledWith(10, 10);
    });
  });

  it('현재 페이지 버튼이 contained variant로 활성화 표시된다', async () => {
    getNotices.mockResolvedValue({
      items: Array.from({ length: 10 }, (_, i) => ({
        id: i + 1, title: `공지 ${i + 1}`, created_at: '2026-01-01T00:00:00',
      })),
      total: 25,
    });
    renderWithAuth(null, '/?page=2');
    await waitFor(() => {
      const btn2 = screen.getByRole('button', { name: '2' });
      expect(btn2).toHaveClass('MuiButton-contained');
    });
  });
});
```

**변경 의도:** mock 응답 구조를 `{ items, total }`로 변경하고, URL 쿼리스트링(`?page=N`) 테스트를 위해 `initialEntry` 파라미터 추가. 페이지 버튼 렌더링/클릭/활성화 3개 케이스 신규 추가.

---

### 4. NoticeListPage.jsx 수정

**변경 전:**
```javascript
import { useState, useEffect, useContext } from 'react';
import { useNavigate } from 'react-router-dom';
import { Box, Button, Container, List,
         ListItemButton, ListItemText, Divider, Paper, Typography } from '@mui/material';
import { AuthContext } from '../../contexts/AuthContext';
import HomeHeader from '../../components/HomeHeader';
import { getNotices } from '../../services/noticeService';

const COLORS = { bg: '#0F172A', card: '#1E293B', border: '#334155',
                 text: '#F1F5F9', subText: '#94A3B8', primary: '#4F46E5' };

function NoticeListPage() {
  const navigate = useNavigate();
  const { user } = useContext(AuthContext);
  const [notices, setNotices] = useState([]);

  useEffect(() => {
    getNotices().then(setNotices);
  }, []);

  return (
    <Box sx={{ minHeight: '100vh', backgroundColor: COLORS.bg }}>
      <HomeHeader />
      <Container maxWidth="md" sx={{ py: 4 }}>
        <Box sx={{ display: 'flex', justifyContent: 'space-between', mb: 2 }}>
          <Typography variant="h5" sx={{ color: COLORS.text }}>공지사항</Typography>
          {user?.role === 'admin' && (
            <Button variant="contained" onClick={() => navigate('/notice/write')}
                    sx={{ backgroundColor: COLORS.primary }}>글쓰기</Button>
          )}
        </Box>
        <Paper sx={{ backgroundColor: COLORS.card, border: `1px solid ${COLORS.border}` }}>
          <List>
            {notices.map((notice, idx) => (
              <Box key={notice.id}>
                <ListItemButton onClick={() => navigate(`/notice/${notice.id}`)}>
                  <ListItemText
                    primary={<Typography sx={{ color: COLORS.text }}>{notice.title}</Typography>}
                    secondary={<Typography sx={{ color: COLORS.subText, fontSize: '0.8rem' }}>
                      {new Date(notice.created_at).toLocaleDateString('ko-KR')}
                    </Typography>}
                  />
                </ListItemButton>
                {idx < notices.length - 1 && <Divider sx={{ borderColor: COLORS.border }} />}
              </Box>
            ))}
          </List>
        </Paper>
      </Container>
    </Box>
  );
}

export default NoticeListPage;
```

**변경 후:**
```javascript
import { useState, useEffect, useContext } from 'react';
import { useNavigate, useSearchParams } from 'react-router-dom';
import { Box, Button, Container, List,
         ListItemButton, ListItemText, Divider, Paper, Typography } from '@mui/material';
import { AuthContext } from '../../contexts/AuthContext';
import HomeHeader from '../../components/HomeHeader';
import { getNotices } from '../../services/noticeService';

const COLORS = { bg: '#0F172A', card: '#1E293B', border: '#334155',
                 text: '#F1F5F9', subText: '#94A3B8', primary: '#4F46E5' };

const LIMIT = 10;

function NoticeListPage() {
  const navigate = useNavigate();
  const { user } = useContext(AuthContext);
  const [searchParams, setSearchParams] = useSearchParams();
  const page = parseInt(searchParams.get('page') || '1', 10);

  const [notices, setNotices] = useState([]);
  const [total, setTotal] = useState(0);

  useEffect(() => {
    const skip = (page - 1) * LIMIT;
    getNotices(skip, LIMIT).then(({ items, total }) => {
      setNotices(items);
      setTotal(total);
    });
  }, [page]);

  const totalPages = Math.ceil(total / LIMIT);
  const pageGroupStart = Math.floor((page - 1) / 5) * 5 + 1;
  const pageGroupEnd = Math.min(pageGroupStart + 4, totalPages);

  return (
    <Box sx={{ minHeight: '100vh', backgroundColor: COLORS.bg }}>
      <HomeHeader />
      <Container maxWidth="md" sx={{ py: 4 }}>
        <Box sx={{ display: 'flex', justifyContent: 'space-between', mb: 2 }}>
          <Typography variant="h5" sx={{ color: COLORS.text }}>공지사항</Typography>
          {user?.role === 'admin' && (
            <Button variant="contained" onClick={() => navigate('/notice/write')}
                    sx={{ backgroundColor: COLORS.primary }}>글쓰기</Button>
          )}
        </Box>
        <Paper sx={{ backgroundColor: COLORS.card, border: `1px solid ${COLORS.border}` }}>
          <List>
            {notices.map((notice, idx) => (
              <Box key={notice.id}>
                <ListItemButton onClick={() => navigate(`/notice/${notice.id}`)}>
                  <ListItemText
                    primary={<Typography sx={{ color: COLORS.text }}>{notice.title}</Typography>}
                    secondary={<Typography sx={{ color: COLORS.subText, fontSize: '0.8rem' }}>
                      {new Date(notice.created_at).toLocaleDateString('ko-KR')}
                    </Typography>}
                  />
                </ListItemButton>
                {idx < notices.length - 1 && <Divider sx={{ borderColor: COLORS.border }} />}
              </Box>
            ))}
          </List>
        </Paper>
        {totalPages > 1 && (
          <Box sx={{ display: 'flex', justifyContent: 'center', gap: 1, mt: 2 }}>
            <Button
              disabled={page === 1}
              onClick={() => setSearchParams({ page: page - 1 })}
              sx={{ color: COLORS.text, borderColor: COLORS.border }}
              variant="outlined"
            >&lt;</Button>
            {Array.from({ length: pageGroupEnd - pageGroupStart + 1 }, (_, i) => {
              const p = pageGroupStart + i;
              return (
                <Button
                  key={p}
                  variant={p === page ? 'contained' : 'outlined'}
                  onClick={() => setSearchParams({ page: p })}
                  sx={p === page
                    ? { backgroundColor: COLORS.primary }
                    : { color: COLORS.text, borderColor: COLORS.border }
                  }
                >{p}</Button>
              );
            })}
            <Button
              disabled={page === totalPages}
              onClick={() => setSearchParams({ page: page + 1 })}
              sx={{ color: COLORS.text, borderColor: COLORS.border }}
              variant="outlined"
            >&gt;</Button>
          </Box>
        )}
      </Container>
    </Box>
  );
}

export default NoticeListPage;
```

**변경 의도:** `useSearchParams`로 URL `?page=N`을 읽어 현재 페이지를 관리. `page` 변경 시 해당 페이지 데이터를 다시 불러옴. `totalPages > 1`일 때만 페이지 버튼 영역을 표시해 공지가 10개 이하일 때 버튼이 보이지 않도록 처리.

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| 공지사항 목록 표시 | 전체 목록 → 10개씩 페이지네이션으로 변경 | 정상 |
| 공지사항 페이지 이동 | 페이지 번호 버튼 신규 추가 | 정상 |
| 공지사항 목록 URL 유지 | `?page=N` 쿼리스트링으로 페이지 상태 관리 | 정상 |
| IndexedDB fallback | 전체 반환 → skip/limit으로 잘라 { items, total } 반환 | 정상 |

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- 공지사항 백엔드 구현 (Notice 모델, 마이그레이션, CRUD API)
- 공지사항 수정 기능 추가 (NoticeEditPage + PUT API)
