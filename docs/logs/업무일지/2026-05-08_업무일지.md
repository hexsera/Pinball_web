# 업무일지

## 기본 정보
- 날짜: 2026-05-08 (목)
- 작성자: hexsera

## 진행 목표

Access Token 검증 방식을 Stateless로 전환. JWT 본래 설계(서버 무상태)에 맞게 `get_current_user`의 DB 조회를 제거하고, payload에 `nickname`을 추가.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| payload에 nickname 추가 | `backend/app/api/v1/auth.py` | 로그인·refresh 2곳 모두 적용 |
| get_current_user DB 조회 제거 | `backend/app/api/deps.py` | 서명 검증 후 payload dict 반환으로 변경 |
| current_user 속성 접근 수정 | `backend/app/api/v1/notices.py` | `.id` → `int(current_user["sub"])` |
| 불필요 import 제거 | `backend/app/api/deps.py`, `backend/app/api/v1/notices.py` | User 모델 import 삭제 |
| 백엔드 컨테이너 재시작 | — | FastAPI 변경사항 반영 확인 |

**총 약 30분**

## 주요 변경 사항

### 1. backend/app/api/v1/auth.py 수정

**변경 전:**
```python
def _create_login_response(user, response: Response) -> LoginResponse:
    access_token = create_access_token({"sub": str(user.id), "email": user.email, "role": user.role})
    ...

# refresh 엔드포인트
    new_access = create_access_token({"sub": str(user.id), "email": user.email, "role": user.role})
```

**변경 후:**
```python
def _create_login_response(user, response: Response) -> LoginResponse:
    access_token = create_access_token({"sub": str(user.id), "email": user.email, "role": user.role, "nickname": user.nickname})
    ...

# refresh 엔드포인트
    new_access = create_access_token({"sub": str(user.id), "email": user.email, "role": user.role, "nickname": user.nickname})
```

**변경 의도:** `nickname`을 payload에 포함해 이후 라우터에서 DB 조회 없이 닉네임을 꺼낼 수 있도록 한다. 로그인과 refresh 두 곳 모두 동일하게 적용해 토큰 재발급 시에도 payload 구성이 일관되게 유지된다.

---

### 2. backend/app/api/deps.py 수정

**변경 전:**
```python
from sqlalchemy.orm import Session
from app.db.session import SessionLocal
from app.core.config import settings

import sys
sys.path.insert(0, '/code')
from models import User

bearer_scheme = HTTPBearer(auto_error=False)


def get_db():
    """DB 세션 의존성 - 모든 라우터에서 재사용"""
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()


def get_current_user(
    credentials: HTTPAuthorizationCredentials | None = Depends(bearer_scheme),
    db: Session = Depends(get_db),
) -> User:
    """JWT Bearer 토큰 검증 의존성"""
    if credentials is None:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Not authenticated")
    token = credentials.credentials
    try:
        payload = jwt.decode(token, settings.JWT_SECRET_KEY, algorithms=[settings.JWT_ALGORITHM])
        user_id = int(payload.get("sub"))
    except (JWTError, TypeError, ValueError):
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid token")
    user = db.query(User).filter(User.id == user_id).first()
    if not user:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="User not found")
    return user
```

**변경 후:**
```python
from sqlalchemy.orm import Session
from app.db.session import SessionLocal
from app.core.config import settings

bearer_scheme = HTTPBearer(auto_error=False)


def get_db():
    """DB 세션 의존성 - 모든 라우터에서 재사용"""
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()


def get_current_user(
    credentials: HTTPAuthorizationCredentials | None = Depends(bearer_scheme),
) -> dict:
    """JWT 서명 검증 의존성 — DB 조회 없음(Stateless)"""
    if credentials is None:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Not authenticated")
    try:
        payload = jwt.decode(
            credentials.credentials,
            settings.JWT_SECRET_KEY,
            algorithms=[settings.JWT_ALGORITHM],
        )
        return payload  # {"sub": "1", "email": "...", "role": "...", "nickname": "...", "exp": ...}
    except (JWTError, TypeError, ValueError):
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid token")

    # [비활성화] DB 조회 방식 — JWT 설계 의도(Stateless)와 맞지 않아 제거
    # user_id = int(payload.get("sub"))
    # user = db.query(User).filter(User.id == user_id).first()
    # if not user:
    #     raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="User not found")
    # return user
```

**변경 의도:** JWT 본래 설계(Stateless)에 맞게 DB 조회를 제거한다. 서명 검증만으로 사용자 정보를 확인하며, payload를 dict로 직접 반환한다. 기존 DB 조회 코드는 롤백 참고용으로 주석 보존. `User` import도 불필요해져 함께 제거.

---

### 3. backend/app/api/v1/notices.py 수정

**변경 전:**
```python
import sys
sys.path.insert(0, '/code')
from models import Notice, User
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from app.api.deps import get_db, get_current_user

...

@router.post("", response_model=NoticeResponse)
def create_notice(
    body: NoticeCreateRequest,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    notice = Notice(title=body.title, content=body.content, author_id=current_user.id)
    ...

@router.delete("/{notice_id}")
def delete_notice(
    notice_id: int,
    db: Session = Depends(get_db),
    current_user: User = Depends(get_current_user),
):
    ...
```

**변경 후:**
```python
import sys
sys.path.insert(0, '/code')
from models import Notice
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from app.api.deps import get_db, get_current_user

...

@router.post("", response_model=NoticeResponse)
def create_notice(
    body: NoticeCreateRequest,
    db: Session = Depends(get_db),
    current_user: dict = Depends(get_current_user),
):
    notice = Notice(title=body.title, content=body.content, author_id=int(current_user["sub"]))
    ...

@router.delete("/{notice_id}")
def delete_notice(
    notice_id: int,
    db: Session = Depends(get_db),
    current_user: dict = Depends(get_current_user),
):
    ...
```

**변경 의도:** `get_current_user`의 반환 타입이 `User` → `dict`로 바뀌었으므로 타입 힌트와 속성 접근 방식을 맞춰 수정. `current_user.id` → `int(current_user["sub"])`로 변경 (`sub`은 문자열이므로 `int()` 변환 필수). 불필요해진 `User` import 제거.

## 인증 흐름 변경

```
변경 전:
요청 → Bearer 토큰 추출 → JWT 서명 검증 → DB 조회(users) → User 객체 반환

변경 후:
요청 → Bearer 토큰 추출 → JWT 서명 검증 → payload dict 반환
```

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| 로그인 토큰 발급 | payload에 nickname 추가 | 미확인 |
| Refresh Token 갱신 | payload에 nickname 추가 | 미확인 |
| 공지 작성 (POST /api/v1/notices) | author_id 추출 방식 변경 | 미확인 |
| 공지 삭제 (DELETE /api/v1/notices/{id}) | 인증 확인 방식 변경 | 미확인 |
| 인증 필요 라우터 전반 | DB 조회 제거, 서명 검증만 수행 | 미확인 |

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- 완료 체크리스트 직접 확인 (jwt.io 디코딩, 공지 CRUD 테스트, 401 반환 확인)
- 4단계: Refresh Token Rotation (백엔드)
- 이슈 해결: 보호 라우트 가드 구현
- 이슈 해결: FriendPage localStorage 직접 참조 제거
