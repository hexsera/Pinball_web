# 업무일지

## 기본 정보
- 날짜: 2026-02-15 (일) 11:30~12:20
- 작성자: hexsera

## 진행 목표

`HeaderUserInfo.jsx`의 아바타 메뉴 dialog를 PRD 요구사항에 맞게 수정한다:
1. dialog 상단에 사용자 이메일 표시
2. dialog 오른쪽 상단에 닫기 버튼 추가
3. "프로필" → "계정설정"으로 변경, 클릭 시 `/dashboard` 계정 섹션으로 이동
4. "친구" MenuItem 추가, 클릭 시 `/dashboard` 친구 섹션으로 이동

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| PRD 요구사항 분석 및 계획 작성 | `PRD/유저 dialog 수정.md`, `PRD/HeaderUserInfo_dialog_수정_계획.md` | TDD 사이클 설계 포함 |
| [RED] 이메일 표시 테스트 작성 | `src/test/HeaderUserInfo.test.jsx` | 실패 확인 완료 |
| [RED] 닫기 버튼 테스트 작성 | `src/test/HeaderUserInfo.test.jsx` | 실패 확인 완료 |
| [RED] 계정설정 MenuItem 테스트 작성 | `src/test/HeaderUserInfo.test.jsx` | 실패 확인 완료 |
| [RED] 친구 MenuItem 테스트 작성 | `src/test/HeaderUserInfo.test.jsx` | 실패 확인 완료 |
| [GREEN] HeaderUserInfo 컴포넌트 수정 | `src/components/HeaderUserInfo.jsx` | 4개 기능 한 번에 구현 |
| [RED] Login email 저장 테스트 작성 | `src/test/Login.test.jsx` | 신규 파일, 실패 확인 완료 |
| [GREEN] Login 컴포넌트 email 저장 추가 | `src/pages/Login/Login.jsx` | 로그인 시 email 함께 저장 |
| Dashboard location.state 연동 | `src/pages/Dashboard/Dashboard.jsx` | navigate state로 섹션 초기화 |
| 전체 테스트 회귀 확인 | — | 11/11 통과 |

## 주요 변경 사항

### 1. HeaderUserInfo.jsx 수정

**변경 전:**
```jsx
import { Box, IconButton, Badge, Avatar, Typography, Button, Menu, MenuItem } from '@mui/material';
import { Notifications, Mail } from '@mui/icons-material';

// Menu 내부
<MenuItem>프로필</MenuItem>
<MenuItem onClick={handlePageNavigation}>페이지 이동</MenuItem>
<MenuItem onClick={() => AvatarLogoutButton()}>로그아웃</MenuItem>
```

**변경 후:**
```jsx
import { Box, IconButton, Badge, Avatar, Typography, Button, Menu, MenuItem, Divider } from '@mui/material';
import { Notifications, Mail, Close } from '@mui/icons-material';

// Menu 내부
<Box sx={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', px: 2, py: 1 }}>
  <Typography variant="body2" color="text.secondary">{user.email}</Typography>
  <IconButton aria-label="닫기" size="small" onClick={handleAvatarMenuClose}>
    <Close fontSize="small" />
  </IconButton>
</Box>
<Divider />
<MenuItem onClick={() => { navigate('/dashboard', { state: { section: '계정' } }); handleAvatarMenuClose(); }}>계정설정</MenuItem>
<MenuItem onClick={() => { navigate('/dashboard', { state: { section: '친구' } }); handleAvatarMenuClose(); }}>친구</MenuItem>
<MenuItem onClick={handlePageNavigation}>페이지 이동</MenuItem>
<MenuItem onClick={() => AvatarLogoutButton()}>로그아웃</MenuItem>
```

**특징:**
- 메뉴 상단에 로그인된 사용자의 이메일 표시 (비활성 영역)
- Close 아이콘 버튼으로 메뉴 닫기 가능
- "프로필" 대신 "계정설정"으로 변경, `navigate state`로 Dashboard 계정 섹션 직접 이동
- "친구" MenuItem 신규 추가, `navigate state`로 Dashboard 친구 섹션 직접 이동

---

### 2. Login.jsx 수정

**변경 전:**
```jsx
login({ id: response.data.user_id, name: response.data.nickname, role: response.data.role });
```

**변경 후:**
```jsx
login({ id: response.data.user_id, name: response.data.nickname, role: response.data.role, email: response.data.email });
```

**특징:**
- 백엔드 로그인 API는 이미 `email`을 반환하고 있었으나 프론트에서 저장하지 않았음
- `localStorage`의 `user` 객체에 `email` 추가 저장하여 HeaderUserInfo에서 이메일 표시 가능

---

### 3. Dashboard.jsx 수정

**변경 전:**
```jsx
import { useNavigate } from 'react-router-dom';

function Maindashboard() {
  const navigate = useNavigate();
  // ...
  const handleDrawerToggle = () => { ... };
```

**변경 후:**
```jsx
import { useNavigate, useLocation } from 'react-router-dom';

function Maindashboard() {
  const navigate = useNavigate();
  const location = useLocation();
  // ...
  const handleDrawerToggle = () => { ... };

  useEffect(() => {
    if (location.state?.section === '계정') {
      setShowUserInfo(true);
      setShowFriend(false);
    } else if (location.state?.section === '친구') {
      setShowFriend(true);
      setShowUserInfo(false);
    }
  }, [location.state]);
```

**특징:**
- `navigate('/dashboard', { state: { section: '계정' } })` 로 이동 시 자동으로 계정 섹션 활성화
- `navigate('/dashboard', { state: { section: '친구' } })` 로 이동 시 자동으로 친구 섹션 활성화
- URL에 노출되지 않는 `navigate state` 방식 사용

---

### 4. HeaderUserInfo.test.jsx 수정 (테스트 추가)

**추가된 테스트 (7개 신규):**
```
이메일 표시
  ✓ 아바타 메뉴를 열면 사용자 이메일이 표시된다

닫기 버튼
  ✓ 아바타 메뉴에 닫기 버튼이 있다
  ✓ 닫기 버튼 클릭 시 메뉴가 닫힌다

계정설정 이동
  ✓ 메뉴에 "계정설정" MenuItem이 존재한다
  ✓ "계정설정" 클릭 시 /dashboard로 이동하고 section이 "계정"이다

친구 이동
  ✓ 메뉴에 "친구" MenuItem이 존재한다
  ✓ "친구" 클릭 시 /dashboard로 이동하고 section이 "친구"이다
```

---

### 5. Login.test.jsx 신규 생성

**추가:**
```jsx
// src/test/Login.test.jsx (신규)
it('로그인 성공 시 localStorage에 email이 저장된다', async () => {
  // axios mock으로 로그인 응답에 email 포함
  // localStorage의 user.email이 저장되는지 검증
});
```

---

### 데이터 흐름

```
HeaderUserInfo (아바타 메뉴 클릭)
       │
       ├── "계정설정" 클릭
       │       │
       │       ▼
       │   navigate('/dashboard', { state: { section: '계정' } })
       │       │
       │       ▼
       │   Dashboard.jsx (useEffect → location.state 감지)
       │       │
       │       ▼
       │   setShowUserInfo(true) → UserInfo 컴포넌트 표시
       │
       └── "친구" 클릭
               │
               ▼
           navigate('/dashboard', { state: { section: '친구' } })
               │
               ▼
           Dashboard.jsx (useEffect → location.state 감지)
               │
               ▼
           setShowFriend(true) → FriendPage 컴포넌트 표시
```

## 결과
- 상태: 완료


## 배운 내용

## 다음 작업
- `FriendPage.test.jsx` — "친구" 메뉴 클릭 시 FriendPage 표시 테스트 기존 실패 (`setShowPinball` 미정의 버그) 수정

---

# 업무일지 (2차)

## 기본 정보
- 날짜: 2026-02-15 (일) 1:00~1:10
- 작성자: hexsera

## 진행 목표

`AdminStatisticsMain.jsx`의 API 호출 문제를 해결한다:
1. API 호출 주소 trailing slash 제거
2. Mock 데이터 관련 코드 전체 제거

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| API 경로 trailing slash 제거 | `src/pages/admin/AdminStatisticsMain.jsx` | 307 리다이렉트 방지 |
| `generateMockDailyData` 함수 제거 | `src/pages/admin/AdminStatisticsMain.jsx` | 개발용 임시 함수 삭제 |
| catch 블록 Mock 데이터 로직 제거 | `src/pages/admin/AdminStatisticsMain.jsx` | 에러 로그만 유지 |

## 주요 변경 사항

### 1. AdminStatisticsMain.jsx 수정

**변경 전:**
```jsx
// 임시 일별 데이터 생성 함수 (3개월 분량)
const generateMockDailyData = (startDate, endDate) => {
  const dailyData = [];
  // ... 랜덤 더미 데이터 생성 로직
  return dailyData;
};

const response = await axios.get('/api/v1/game_visits/', { ... });

} catch (error) {
  console.error('통계 데이터 로딩 실패:', error);
  const mockDailyData = generateMockDailyData(mockStartDate, mockEndDate);
  setDailyData(mockDailyData);
}
```

**변경 후:**
```jsx
const response = await axios.get('/api/v1/game_visits', { ... });

} catch (error) {
  console.error('통계 데이터 로딩 실패:', error);
}
```

**특징:**
- `CLAUDE.md`의 `redirect_slashes=False` 설정에 따라 trailing slash 제거로 307 리다이렉트 방지
- `generateMockDailyData` 함수(19줄) 전체 삭제
- `catch` 블록에서 Mock 데이터 생성 및 `setDailyData(mockDailyData)` 호출 제거
- API 정상 응답 처리(`setDailyData(dailyDataFromAPI.stats)`)는 기존과 동일하게 유지

### 데이터 흐름

```
AdminStatisticsMain (마운트)
       │
       ▼
GET /api/v1/game_visits?start_date=...&end_date=...
       │
       ▼
FastAPI: get_daily_visit_stats()
       │  날짜별 접속자 수 집계 (game_visits 테이블)
       ▼
{ stats: [{date, user_count}, ...], total_days, start_date, end_date }
       │
       ▼
setDailyData(response.data.stats)
       │
       ▼
LineChart (x축: date, y축: user_count)
```

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업

---

# 업무일지 (3차)

## 기본 정보
- 날짜: 2026-02-15 (일) 13:10~13:25
- 작성자: hexsera

## 진행 목표

어드민 페이지 사이드바에서 대시보드 항목을 제거하고, `/admin` 접속 시 회원관리 페이지를 바로 표시한다.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| PRD 작성 | `PRD/어드민 페이지 대시보드 숨기기prd.md` | 요구사항 문서 |
| 실행계획 작성 | `PRD/어드민-대시보드-숨기기-실행계획.md` | 구현 계획 |
| 사이드바 대시보드 항목 제거 | `src/pages/admin/AdminSidebar.jsx` | DashboardIcon import 포함 제거 |
| `/admin` 라우트 변경 | `src/App.jsx` | AdminPage → AdminUserPage |

## 주요 변경 사항

### 1. AdminSidebar.jsx 수정

**변경 전:**
```jsx
import { Dashboard as DashboardIcon, People, BarChart } from '@mui/icons-material';

const menuItems = [
  { text: '대시보드', icon: <DashboardIcon />, path: '/admin' },
  { text: '회원관리', icon: <People />, path: '/admin/users' },
  { text: '통계', icon: <BarChart />, path: '/admin/statistics' },
];
```

**변경 후:**
```jsx
import { People, BarChart } from '@mui/icons-material';

const menuItems = [
  { text: '회원관리', icon: <People />, path: '/admin' },
  { text: '통계', icon: <BarChart />, path: '/admin/statistics' },
];
```

**특징:**
- `DashboardIcon` import 제거로 불필요한 의존성 삭제
- 대시보드 menuItem 제거, 사이드바에 회원관리·통계 2개 항목만 표시
- 회원관리의 path를 `/admin/users` → `/admin`으로 변경하여 진입점과 일치

---

### 2. App.jsx 수정

**변경 전:**
```jsx
import { AdminPage, AdminUserPage, AdminStatisticsPage } from './pages/admin';

<Route path="/admin" element={<AdminPage/>} />
```

**변경 후:**
```jsx
import { AdminUserPage, AdminStatisticsPage } from './pages/admin';

<Route path="/admin" element={<AdminUserPage/>} />
```

**특징:**
- `AdminPage` import 제거
- `/admin` 라우트가 `AdminUserPage`를 렌더링하여 접속 즉시 회원관리 화면 표시

---

### 라우트 변경 구조

```
변경 전
/admin            → AdminPage (대시보드)
/admin/users      → AdminUserPage (회원관리)
/admin/statistics → AdminStatisticsPage (통계)

변경 후
/admin            → AdminUserPage (회원관리)
/admin/statistics → AdminStatisticsPage (통계)
```

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업

---

# 업무일지 (4차)

## 기본 정보
- 날짜: 2026-02-15 (일) 13:40~14:00
- 작성자: hexsera

## 진행 목표

`HeaderUserInfo.jsx`의 "페이지 이동" 메뉴 항목을 PRD 요구사항에 맞게 수정한다:
1. role이 'admin'인 사용자에게만 페이지 이동 항목 표시 (API 호출로 role 획득)
2. 어드민 페이지(`/admin`)에 있으면 "메인 페이지 이동", 아니면 "관리 페이지 이동"으로 문구 변경

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| PRD 분석 및 RED 계획 작성 | `PRD/TDD-RED-어드민페이지이동수정-2026-02-15.md` | 테스트 케이스 8개 설계 |
| [RED] 새 요구사항 테스트 8개 작성 | `src/test/HeaderUserInfo.test.jsx` | 6개 실패 확인 완료 |
| [GREEN] HeaderUserInfo 컴포넌트 수정 | `src/components/HeaderUserInfo.jsx` | API 호출 및 조건부 렌더링 구현 |
| [REFACTOR] 테스트 비동기 처리 개선 | `src/test/HeaderUserInfo.test.jsx` | act() 경고 제거 |
| GREEN 완료 보고서 작성 | `PRD/TDD-GREEN-어드민페이지이동수정-2026-02-15.md` | 15개 테스트 전체 통과 |

## 주요 변경 사항

### 1. HeaderUserInfo.jsx 수정

**변경 전:**
```jsx
import { useState } from 'react';
// axios import 없음

function HeaderUserInfo() {
  // role 관련 state 없음

  // 모든 로그인 사용자에게 "페이지 이동" 표시
  <MenuItem onClick={handlePageNavigation}>페이지 이동</MenuItem>
}
```

**변경 후:**
```jsx
import { useState, useEffect } from 'react';
import axios from 'axios';

function HeaderUserInfo() {
  const [role, setRole] = useState(null);

  useEffect(() => {
    if (isLoggedIn && user?.id) {
      axios.get(`/api/v1/users/${user.id}`)
        .then(res => setRole(res.data.role))
        .catch(() => setRole(null));
    }
  }, [isLoggedIn, user?.id]);

  const isAdmin = role === 'admin';
  const pageNavLabel = location.pathname.startsWith('/admin')
    ? '메인 페이지 이동'
    : '관리 페이지 이동';

  // admin에게만 조건부 렌더링
  {isAdmin && (
    <MenuItem onClick={handlePageNavigation}>{pageNavLabel}</MenuItem>
  )}
}
```

**특징:**
- `useEffect`로 컴포넌트 마운트 시 `GET /api/v1/users/{id}` 호출하여 최신 role 획득
- `isAdmin` 변수로 admin 여부 판단, 조건부 렌더링으로 일반 유저에게 항목 숨김
- 현재 경로에 따라 "메인 페이지 이동" / "관리 페이지 이동" 문구 동적 변경

---

### 2. HeaderUserInfo.test.jsx 수정

**추가된 테스트 (8개 신규):**
```
페이지 이동 항목 표시 여부 (role 기반)
  ✓ role이 admin인 사용자는 메뉴에 "관리 페이지 이동"이 표시된다
  ✓ role이 user인 사용자는 메뉴에 페이지 이동 항목이 표시되지 않는다
  ✓ role이 없는 사용자는 메뉴에 페이지 이동 항목이 표시되지 않는다

페이지 이동 문구 변경 (경로 기반)
  ✓ admin 유저가 /admin 경로에 있으면 "메인 페이지 이동"이 표시된다
  ✓ admin 유저가 /admin 경로가 아니면 "관리 페이지 이동"이 표시된다

API로 role 가져오기
  ✓ 컴포넌트 마운트 시 GET /api/v1/users/{id}를 호출한다

페이지 이동 내비게이션
  ✓ "관리 페이지 이동" 클릭 시 /admin으로 이동한다
  ✓ "메인 페이지 이동" 클릭 시 /로 이동한다
```

**REFACTOR - 비동기 처리 개선:**
- 기존 동기 테스트들에 `await waitFor(() => expect(axios.get).toHaveBeenCalled())` 추가
- 컴포넌트 마운트 후 axios 비동기 호출 완료를 기다린 뒤 UI 검증하도록 수정
- act() 경고 완전 제거

---

### 데이터 흐름

```
HeaderUserInfo 마운트
       │
       ▼
GET /api/v1/users/{user.id}
       │
       ├── role: 'admin' → isAdmin = true
       │       │
       │       ├── /admin 경로 → "메인 페이지 이동" 표시
       │       └── 그 외 경로  → "관리 페이지 이동" 표시
       │
       └── role: 'user' 또는 없음 → isAdmin = false
               │
               └── 페이지 이동 MenuItem 숨김
```

## 결과
- 상태: 완료
- 테스트: 15개 전체 통과, act() 경고 없음

## 배운 내용

## 다음 작업

# 업무일지

## 기본 정보
- 날짜: 2026-02-15 (일) 14:20~15:00
- 작성자: hexsera

## 진행 목표

HomePage 메인화면 UI 개선 — 헤더 인증 버튼 통합 및 반응형 레이아웃 적용

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| HeaderUserInfo에 색상 props 추가 | `frontend/src/components/HeaderUserInfo.jsx` | 기존 사용처 영향 없음 |
| HomePage 헤더 로그인/회원가입 버튼 → HeaderUserInfo 교체 | `frontend/src/pages/HomePage/HomePage.jsx` | |
| 헤더 타이틀 모바일 숨김, 게임하기 버튼 좌측 이동 및 contained 스타일 | `frontend/src/pages/HomePage/HomePage.jsx` | |
| Hero 섹션 반응형 처리 (여백, 문구 분기, CTA 버튼 숨김) | `frontend/src/pages/HomePage/HomePage.jsx` | |
| 모든 반응형 브레이크포인트 `md` → `sm` 변경 | `frontend/src/pages/HomePage/HomePage.jsx` | 600px 기준으로 전환 |

## 주요 변경 사항

### 1. HeaderUserInfo.jsx 수정

**변경 전:**
```jsx
function HeaderUserInfo() {
  // ...
  ) : (
    <>
      <Button variant="contained" onClick={() => navigate('/login')}>
        로그인
      </Button>
      <Button variant="outlined" onClick={() => navigate('/Register')}>
        회원가입
      </Button>
    </>
  )}
```

**변경 후:**
```jsx
function HeaderUserInfo({ buttonColor, buttonTextColor, outlinedBorderColor }) {
  // ...
  ) : (
    <>
      <Button
        variant="contained"
        onClick={() => navigate('/login')}
        sx={buttonColor ? { backgroundColor: buttonColor, color: buttonTextColor } : {}}
      >
        로그인
      </Button>
      <Button
        variant="outlined"
        onClick={() => navigate('/Register')}
        sx={buttonColor ? { color: buttonTextColor, borderColor: outlinedBorderColor } : {}}
      >
        회원가입
      </Button>
    </>
  )}
```

**특징:**
- `buttonColor`, `buttonTextColor`, `outlinedBorderColor` props를 추가하여 부모에서 색상 주입 가능
- props가 없으면 MUI 기본 색상을 유지하므로 기존 사용처(Dashboard 등) 영향 없음

---

### 2. HomePage.jsx — 헤더(Toolbar) 구조 변경

**변경 전:**
```jsx
<Toolbar sx={{ justifyContent: 'space-between' }}>
  <Typography variant="h6" sx={{ color: COLORS.text, fontWeight: 'bold' }}>
    🎯 HEXSERA PINBALL
  </Typography>
  <Box>
    <Button onClick={() => navigate('/pinball')} sx={{ color: COLORS.text }}>
      게임하기
    </Button>
    <Button onClick={() => navigate('/login')} variant="outlined"
      sx={{ color: COLORS.text, borderColor: COLORS.text, ml: 1 }}>
      로그인
    </Button>
    <Button onClick={() => navigate('/Register')} variant="contained"
      sx={{ backgroundColor: COLORS.primary, ml: 1 }}>
      회원가입
    </Button>
  </Box>
</Toolbar>
```

**변경 후:**
```jsx
<Toolbar sx={{ justifyContent: 'space-between' }}>
  {/* 좌측: 타이틀 + 게임하기 버튼 */}
  <Box sx={{ display: 'flex', alignItems: 'center', gap: 1 }}>
    <Typography
      variant="h6"
      sx={{
        color: COLORS.text,
        fontWeight: 'bold',
        fontSize: { xs: '0.9rem', sm: '1.25rem' },
        display: { xs: 'none', sm: 'block' },
      }}
    >
      HEXSERA PINBALL
    </Typography>
    <Button
      variant="contained"
      onClick={() => navigate('/pinball')}
      sx={{ backgroundColor: COLORS.primary, color: COLORS.text }}
    >
      게임하기
    </Button>
  </Box>
  {/* 우측: HeaderUserInfo */}
  <HeaderUserInfo
    buttonColor={COLORS.primary}
    buttonTextColor={COLORS.text}
    outlinedBorderColor={COLORS.text}
  />
</Toolbar>
```

**특징:**
- 로그인/회원가입 버튼 3개를 `HeaderUserInfo` 컴포넌트 1개로 대체
- "게임하기" 버튼을 우측에서 좌측(타이틀 오른쪽)으로 이동, `variant="contained"` 적용
- 타이틀은 `sm`(600px) 미만에서 숨김 처리

---

### 3. HomePage.jsx — Hero 섹션 반응형 처리

**변경 전:**
```jsx
<Grid container spacing={4} sx={{ mt: 16 }}>
  <Grid size={{ xs: 12, md: 8 }}>
    <Box sx={{ display: 'flex', flexDirection: 'column', gap: 3, pt: { md: 6 } }}>
      <Typography variant="h3" sx={{ color: COLORS.text, fontWeight: 'bold' }}>
        세상에서 가장 짜릿한<br />핀볼 게임을 경험하세요.
      </Typography>
      <Button variant="contained" size="large" onClick={() => navigate('/pinball')}
        sx={{ backgroundColor: COLORS.primary, width: 'fit-content', px: 4, py: 1.5 }}>
        ▶ 지금 바로 플레이하기
      </Button>
```

**변경 후:**
```jsx
<Grid container spacing={4} sx={{ mt: { xs: 8, sm: 16 } }}>
  <Grid size={{ xs: 12, md: 8 }}>
    <Box sx={{ display: 'flex', flexDirection: 'column', gap: 3, pt: { md: 6 } }}>
      <Typography variant="h3" sx={{ color: COLORS.text, fontWeight: 'bold' }}>
        <Box component="span" sx={{ display: { xs: 'none', sm: 'inline' } }}>
          세상에서 가장 짜릿한<br />핀볼 게임을 경험하세요.
        </Box>
        <Box component="span" sx={{ display: { xs: 'inline', sm: 'none' } }}>
          세상에서<br />가장 짜릿한<br />핀볼게임을<br />체험하세요
        </Box>
      </Typography>
      <Button variant="contained" size="large" onClick={() => navigate('/pinball')}
        sx={{ backgroundColor: COLORS.primary, width: 'fit-content', px: 4, py: 1.5 }}>
        ▶ 지금 바로 플레이하기
      </Button>
```

**특징:**
- Hero 상단 여백 `mt: { xs: 8, sm: 16 }` — 모바일에서 여백 축소
- 소개 문구를 두 `<Box component="span">`으로 분리하여 화면 크기에 따라 전환
- 모든 반응형 기준을 `md`(900px)에서 `sm`(600px)으로 통일

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- 없음

---

# 업무일지 (5차)

## 기본 정보
- 날짜: 2026-02-15 (일) 15:00~15:40
- 작성자: hexsera

## 진행 목표

`Login.jsx`와 `Register.jsx`의 색상 테마를 `HomePage.jsx`와 통일하고 Aurora 애니메이션 배경을 적용한다.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| PRD 작성 | `PRD/로그인 배경 변경 PRD.md` | 요구사항 문서 |
| 실행계획 작성 | `PRD/로그인 배경 변경-실행계획.md` | 구현 계획 |
| Login.jsx Aurora 배경 및 dark 테마 적용 | `src/pages/Login/Login.jsx` | COLORS 정의, Aurora import 포함 |
| Login.jsx 로고 xs 숨김 처리 | `src/pages/Login/Login.jsx` | 모바일 대응 |
| Register.jsx Aurora 배경 및 dark 테마 적용 | `src/pages/Register/Register.jsx` | Login과 동일 테마, textFieldSx 공통 객체 추가 |

## 주요 변경 사항

### 1. Login.jsx 수정

**변경 전:**
```jsx
// import 없음 (Aurora)

return (
  <Box sx={{ minHeight: '100vh', backgroundColor: '#87CEEB', ... }}>
    <Container maxWidth="sm">
      <Paper sx={{ padding: 4, backgroundColor: 'white', borderRadius: 2 }}>
        <CardMedia sx={{ marginBottom: 2, objectFit: 'contain' }} />
        <Typography component="h1" variant="h5">로그인</Typography>
        <TextField fullWidth sx={{ mt: 1 }} ... />
        <Button variant="contained" sx={{ width: '300px' }}>로그인</Button>
        <Button variant="outlined" sx={{ width: '300px' }}>회원가입</Button>
      </Paper>
    </Container>
  </Box>
);
```

**변경 후:**
```jsx
import Aurora from '../../components/Aurora/Aurora';

const COLORS = {
  bg: '#0F172A',
  card: 'rgba(30, 41, 59, 0.85)',
  border: '#334155',
  text: '#F1F5F9',
  subText: '#94A3B8',
  primary: '#4F46E5',
};

return (
  <Box sx={{ position: 'relative', minHeight: '100vh', backgroundColor: COLORS.bg, ... }}>
    {/* Aurora 배경 (z-index: 0) */}
    <Box sx={{ position: 'absolute', inset: 0, zIndex: 0, overflow: 'hidden' }}>
      <Aurora colorStops={['#467ee5', '#7C3AED', '#908aff']} amplitude={1.2} speed={1.2} blend={0.35} />
    </Box>
    {/* 콘텐츠 (z-index: 1) */}
    <Box sx={{ position: 'relative', zIndex: 1, ... }}>
      <Paper sx={{ backgroundColor: COLORS.card, border: `1px solid ${COLORS.border}` }}>
        <CardMedia sx={{ display: { xs: 'none', sm: 'block' }, ... }} />
        <Typography sx={{ color: COLORS.text }}>로그인</Typography>
        <TextField sx={{ '& .MuiOutlinedInput-root': { color: COLORS.text, ... } }} />
        <Button sx={{ backgroundColor: COLORS.primary, color: COLORS.text }}>로그인</Button>
        <Button variant="outlined" sx={{ borderColor: COLORS.text, color: COLORS.text }}>회원가입</Button>
      </Paper>
    </Box>
  </Box>
);
```

**특징:**
- 배경색 `#87CEEB`(하늘색) → `#0F172A`(dark navy)로 변경
- Aurora WebGL 애니메이션 배경 레이어 추가 (z-index 0), 콘텐츠는 z-index 1로 분리
- Paper 카드를 반투명(`rgba(30, 41, 59, 0.85)`) dark 스타일로 변경, border 추가
- TextField 테두리·텍스트·placeholder 색상 dark 테마 적용
- 로고 이미지 xs 화면에서 숨김 처리 (`display: { xs: 'none', sm: 'block' }`)

---

### 2. Register.jsx 수정

**변경 전:**
```jsx
// Aurora import 없음

return (
  <Box sx={{ minHeight: '100vh', backgroundColor: '#87CEEB', ... }}>
    <Paper sx={{ backgroundColor: 'white' }}>
      <Typography component="h1" variant="h5">회원가입</Typography>
      <TextField sx={{ mt: 1 }} label="이메일" />
      <Button variant="contained" sx={{ width: '300px' }}>다음</Button>
    </Paper>
  </Box>
);
```

**변경 후:**
```jsx
import Aurora from '../../components/Aurora/Aurora';

const COLORS = { /* Login과 동일 */ };

const textFieldSx = {
  mt: 1,
  '& .MuiOutlinedInput-root': { color: COLORS.text, ... },
  '& .MuiInputLabel-root': { color: COLORS.subText },
  '& .MuiInputLabel-root.Mui-focused': { color: COLORS.primary },
};

return (
  <Box sx={{ position: 'relative', minHeight: '100vh', backgroundColor: COLORS.bg, ... }}>
    <Box sx={{ position: 'absolute', inset: 0, zIndex: 0, overflow: 'hidden' }}>
      <Aurora colorStops={['#467ee5', '#7C3AED', '#908aff']} amplitude={1.2} speed={1.2} blend={0.35} />
    </Box>
    <Box sx={{ position: 'relative', zIndex: 1, ... }}>
      <Paper sx={{ backgroundColor: COLORS.card, border: `1px solid ${COLORS.border}` }}>
        <CardMedia sx={{ display: { xs: 'none', sm: 'block' } }} />
        {step[registerStep]}
      </Paper>
    </Box>
  </Box>
);
```

**특징:**
- Login.jsx와 동일한 COLORS, Aurora 구조 적용
- Register에는 MUI `label` prop이 있어 `textFieldSx` 공통 객체에 `MuiInputLabel` 색상도 포함
- `textFieldSx` 객체를 컴포넌트 외부에 정의하여 3개 단계(이메일/비밀번호/생년월일)에서 재사용
- 로고 이미지 xs 숨김 처리 및 주석 처리된 로고 코드 정식 추가

---

### 페이지 구조 변경

```
변경 전
Login / Register
└── Box (backgroundColor: '#87CEEB')
    └── Paper (backgroundColor: 'white')
        └── 폼 콘텐츠

변경 후
Login / Register
└── Box (position: relative, backgroundColor: '#0F172A')
    ├── Box (position: absolute, z-index: 0)
    │   └── Aurora (WebGL 애니메이션 배경)
    └── Box (position: relative, z-index: 1)
        └── Paper (backgroundColor: rgba(30,41,59,0.85), border)
            └── 폼 콘텐츠 (dark 테마)
```

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- 없음

---

# 업무일지 (6차)

## 기본 정보
- 날짜: 2026-02-15 (일) 5:00 ~ 5:30
- 작성자: hexsera

## 진행 목표

`HeaderUserInfo.jsx` UI 개선:
1. 메시지·종 아이콘 버튼 제거
2. Avatar와 닉네임 사이 간격 추가
3. Avatar 내부에 유저 이름 첫 글자 표시
4. xs 화면에서 닉네임 텍스트 숨김

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| 종·메시지 IconButton 제거 및 import 정리 | `src/components/HeaderUserInfo.jsx` | Notifications, Mail, Badge 삭제 |
| Avatar 첫 글자 적용 및 간격 추가 | `src/components/HeaderUserInfo.jsx` | user.name 기반 |
| xs 화면 닉네임 숨김 처리 | `src/components/HeaderUserInfo.jsx` | display breakpoint 적용 |
| user.nickname → user.name 오류 수정 | `src/components/HeaderUserInfo.jsx` | 잘못된 필드명 수정 |

## 주요 변경 사항

### 1. HeaderUserInfo.jsx 수정

**변경 전:**
```jsx
import {
  Box, IconButton, Badge, Avatar, Typography,
  Button, Menu, MenuItem, Divider
} from '@mui/material';
import { Notifications, Mail, Close } from '@mui/icons-material';

<IconButton onClick={() => navigate('/admin')}>
  <Badge><Notifications /></Badge>
</IconButton>
<IconButton>
  <Badge><Mail /></Badge>
</IconButton>
<Button id="basic-button" onClick={AvatarButtonClick}>
  <Avatar sx={{ width: 40, height: 40, ml: 1 }}>U</Avatar>
  <Typography>{user.name}</Typography>
</Button>
```

**변경 후:**
```jsx
import {
  Box, IconButton, Avatar, Typography,
  Button, Menu, MenuItem, Divider
} from '@mui/material';
import { Close } from '@mui/icons-material';

<Button id="basic-button" onClick={AvatarButtonClick}>
  <Avatar sx={{ width: 40, height: 40 }}>
    {user.name ? user.name[0].toUpperCase() : 'U'}
  </Avatar>
  <Typography sx={{ ml: 1, display: { xs: 'none', sm: 'block' } }}>{user.name}</Typography>
</Button>
```

**특징:**
- `Notifications`, `Mail`, `Badge` import 및 `IconButton` 2개 제거
- Avatar `ml: 1` 제거 → Typography `ml: 1`로 이동하여 Avatar-닉네임 간 간격 확보
- Avatar 내부 `'U'` → `user.name[0].toUpperCase()` (name 없을 시 `'U'` fallback)
- Typography `display: { xs: 'none', sm: 'block' }` 적용 — xs(600px 미만)에서 닉네임 숨김
- 초기 `user.nickname` 사용 → 실제 user 객체 필드인 `user.name`으로 수정

---

### 컴포넌트 구조 변경

```
변경 전 (로그인 상태)
Box
├── IconButton (Notifications)
├── IconButton (Mail)
└── Button#basic-button
    ├── Avatar → 'U'
    └── Typography → user.name

변경 후 (로그인 상태)
Box
└── Button#basic-button
    ├── Avatar → user.name[0] (항상 표시)
    └── Typography (xs: 숨김 / sm~: 표시)
```

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- 없음


# 업무일지

## 기본 정보
- 날짜: 2026-02-15 (일) 18:00~18:30, 19:00~19:30
- 작성자: hexsera

## 진행 목표
친구 페이지(FriendPage) 기능을 TDD(테스트 주도 개발)로 완성한다.
- 검색 및 친구추가 API 연결
- 친구 요청란 표시 개선 (receiver_id 필터링, 닉네임 표시)
- 현재 친구란 상대방 닉네임 올바르게 표시
- 현재 친구 월간 최고점수 표시
- 리팩토링 수행

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| TDD RED: 검색/친구추가 API 연결 테스트 작성 | FriendSearch.test.jsx | 10개 테스트 (실패 확인) |
| TDD GREEN: 검색/친구추가 API 연결 구현 | FriendPage.jsx | MOCK 제거 후 실제 API 연동 |
| FriendPage.test.jsx 회귀 테스트 수정 | FriendPage.test.jsx | Dashboard 통합 테스트 제거, axios mock 추가 |
| TDD RED: 친구 요청/목록 표시 수정 테스트 작성 | FriendRequest.test.jsx | 4개 테스트 추가 |
| TDD GREEN: 친구 요청/목록 표시 수정 구현 | FriendPage.jsx | receiver_id 필터링, 닉네임 방향 처리 |
| TDD RED: 월간 최고점수 표시 테스트 작성 | FriendList.test.jsx | 3개 테스트 추가 |
| TDD GREEN: 월간 최고점수 표시 구현 | FriendPage.jsx | Promise.all로 병렬 API 호출 |
| TDD REFACTOR: 코드 정리 | FriendPage.jsx | 함수 위치 이동, 변수 추출 |
| UI 수정: 점수 표시 위치 변경 | FriendPage.jsx, FriendList.test.jsx | 오른쪽 끝 "최고점수:N" 형식 |

## 주요 변경 사항

### 1. FriendSearch.test.jsx 전면 교체

**변경 전:**
```jsx
// MOCK 데이터 기반 단순 렌더링 테스트
```

**변경 후:**
```jsx
// axios mock 기반 API 연동 테스트 10개
// - GET /api/v1/users?nickname= 호출 검증
// - 검색 결과 표시 및 에러 처리
// - POST /api/friend-requests 호출 검증
// - 이미 친구/pending 요청 시 버튼 "요청됨" + disabled 검증
```

**특징:**
- 기존 MOCK 기반 테스트를 실제 API 호출 검증 테스트로 교체
- 버튼 상태 제어 4가지 케이스 검증 (이미 친구, 요청 중, disabled 확인, 정상 활성화)

---

### 2. FriendPage.jsx 검색/친구추가 API 연결

**변경 전:**
```jsx
const MOCK_USERS = [ ... ]; // MOCK 데이터

const handleSearch = async () => {
  // MOCK 데이터 반환
  setSearchResults(MOCK_USERS);
  setHasSearched(true);
};

const handleAddFriend = (targetUser) => {
  console.log('친구 추가:', targetUser);
};
```

**변경 후:**
```jsx
const handleSearch = async () => {
  setSearchError(null);
  setHasSearched(false);
  try {
    const response = await axios.get('/api/v1/users', {
      params: { nickname: searchNickname }
    });
    setSearchResults(response.data);
    setHasSearched(true);
  } catch (error) {
    console.error('검색 실패:', error);
    setSearchError('검색에 실패했습니다');
  }
};

const handleAddFriend = async (targetUser) => {
  const currentUser = getCurrentUser();
  if (!currentUser) return;
  setActionError(null);
  try {
    await axios.post('/api/friend-requests', {
      requester_id: currentUser.id,
      receiver_id: targetUser.id
    });
    await fetchPendingRequests();
  } catch (error) {
    console.error('친구 추가 실패:', error);
    setActionError(error.response?.data?.detail || '친구 추가에 실패했습니다');
  }
};

const getFriendButtonState = (targetUser) => {
  const currentUser = getCurrentUser();
  if (!currentUser) return { label: '친구추가', disabled: false };
  const isAlreadyFriend = friendList.some(
    (f) => f.requester_id === targetUser.id || f.receiver_id === targetUser.id
  );
  if (isAlreadyFriend) return { label: '요청됨', disabled: true };
  const hasSentRequest = pendingRequests.some(
    (f) => f.requester_id === currentUser.id && f.receiver_id === targetUser.id
  );
  if (hasSentRequest) return { label: '요청됨', disabled: true };
  return { label: '친구추가', disabled: false };
};
```

**특징:**
- MOCK_USERS 상수 완전 제거
- 실제 API 연동으로 전환
- 친구추가 후 pendingRequests 갱신으로 버튼 상태 자동 업데이트

---

### 3. FriendRequest.test.jsx 수정 — receiver_id 필터링 및 닉네임 표시

**변경 전:**
```jsx
const MOCK_PENDING_REQUESTS = {
  requests: [
    { id: 1, requester_id: 3, receiver_id: 1, status: 'pending' },
    // nickname 필드 없음
  ]
};
```

**변경 후:**
```jsx
const MOCK_PENDING_REQUESTS = {
  requests: [
    { id: 1, requester_id: 3, requester_nickname: '홍길동', receiver_id: 1, receiver_nickname: '나', status: 'pending' },
    { id: 2, requester_id: 5, requester_nickname: '박영희', receiver_id: 1, receiver_nickname: '나', status: 'pending' },
  ]
};

// 추가 테스트
it('receiver_id가 내 id인 요청만 친구 요청란에 표시된다', ...)
it('친구 요청란에 requester_nickname이 표시된다', ...)
it('receiver_id가 내 id인 경우 requester_nickname을 친구 닉네임으로 표시한다', ...)
it('requester_id가 내 id인 경우 receiver_nickname을 친구 닉네임으로 표시한다', ...)
```

**특징:**
- mock 데이터에 nickname 필드 추가
- receiver_id 필터링 동작 검증
- 양방향(내가 requester / 내가 receiver) 닉네임 표시 검증

---

### 4. FriendPage.jsx 친구 요청/목록 표시 수정

**변경 전:**
```jsx
// 친구 요청: 모든 pending 요청 표시
// 친구 닉네임: 항상 receiver_nickname 표시
const receivedRequests = pendingRequests; // 필터 없음
```

**변경 후:**
```jsx
// 현재 사용자에게 온 요청만 필터링
const receivedRequests = pendingRequests.filter(r => r.receiver_id === currentUser?.id);

// 친구 목록에서 상대방 닉네임 선택
const isReceiver = friendship.receiver_id === currentUser?.id;
const friendNickname = isReceiver
  ? friendship.requester_nickname
  : friendship.receiver_nickname;
const friendId = isReceiver
  ? friendship.requester_id
  : friendship.receiver_id;
```

**특징:**
- API가 양방향 친구 관계를 반환해도 UI에서 올바르게 필터링
- 내가 receiver인 경우 → 상대방(requester) 닉네임 표시
- 내가 requester인 경우 → 상대방(receiver) 닉네임 표시

---

### 5. FriendList.test.jsx 수정 — 월간 최고점수 테스트 추가

**변경 전:**
```jsx
const MOCK_ACCEPTED_FRIENDS = {
  requests: [
    { id: 3, requester_id: 1, receiver_id: 2, status: 'accepted' },
    // nickname 필드 없음
  ]
};
```

**변경 후:**
```jsx
const MOCK_ACCEPTED_FRIENDS = {
  requests: [
    { id: 3, requester_id: 1, requester_nickname: '나', receiver_id: 2, receiver_nickname: '테스트유저2', status: 'accepted' },
    { id: 4, requester_id: 4, requester_nickname: '김철수', receiver_id: 1, receiver_nickname: '나', status: 'accepted' },
  ]
};

// 월간 점수 테스트 3개 추가
it('친구 목록 로드 시 각 친구의 GET /api/v1/monthly-scores/{user_id}를 호출한다', ...)
it('친구의 월간 점수가 있으면 점수가 표시된다', ...) // '최고점수:9999' 검증
it('친구의 월간 점수가 없으면 "-"가 표시된다', ...)  // 404 응답 시 '-' 검증
```

---

### 6. FriendPage.jsx 월간 최고점수 표시 구현

**추가된 코드:**
```jsx
// state 추가
const [friendScores, setFriendScores] = useState({});

// fetchFriendList 내 점수 조회 추가
const scores = {};
await Promise.all(friends.map(async (friendship) => {
  const friendId = friendship.receiver_id === user.id
    ? friendship.requester_id
    : friendship.receiver_id;
  try {
    const scoreRes = await axios.get(`/api/v1/monthly-scores/${friendId}`);
    scores[friendId] = scoreRes.data.score;
  } catch {
    scores[friendId] = null;
  }
}));
setFriendScores(scores);

// JSX에서 점수 표시 (오른쪽 끝)
const score = friendScores[friendId];
<ListItem
  secondaryAction={
    <Typography variant="body2" color="text.secondary">
      최고점수:{score != null ? score : '-'}
    </Typography>
  }
>
  <ListItemText primary={friendNickname} />
</ListItem>
```

**특징:**
- `Promise.all`로 모든 친구의 점수를 병렬 조회 (성능 최적화)
- 404 등 에러 발생 시 null로 저장 후 '-' 표시
- `secondaryAction`으로 오른쪽 끝에 "최고점수:N" 형식 표시

---

### 7. FriendPage.jsx 리팩토링

**주요 변경:**
```
변경 전: getCurrentUser 함수가 컴포넌트 내부에 정의
변경 후: 모듈 최상위로 이동 (컴포넌트 밖)

변경 전: receivedRequests 계산이 JSX 렌더링 도중 수행
변경 후: 렌더링 직전에 변수로 추출

변경 전: fetchFriendList에서 user를 다시 getCurrentUser()로 조회
변경 후: 함수 파라미터로 받은 user 재사용 (클로저 활용)

변경 전: isReceiver 로직이 JSX 안에 인라인
변경 후: const isReceiver 변수로 추출하여 가독성 향상
```

**특징:**
- 동작 변경 없이 코드 구조만 개선
- 모든 43개 테스트 통과 유지

---

## 결과

- 상태: 완료

## 배운 내용

## 다음 작업

---

# 업무일지 (7차)

## 기본 정보
- 날짜: 2026-02-15 (일) 19:30~20:25
- 작성자: hexsera

## 진행 목표

`Dashboard.jsx`의 헤더·사이드바를 독립 컴포넌트로 분리하고, `FriendPage.jsx`와 `UserInfo.jsx`가 각자 헤더·사이드바를 직접 조합하는 구조(AdminPage 패턴)로 전환한다. 라우팅도 `/user/friend`, `/user/account`로 독립 경로를 부여한다.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| DashboardHeader.jsx 생성 | `src/pages/Dashboard/DashboardHeader.jsx` | AppBar 헤더 독립 컴포넌트 |
| DashboardSidebar.jsx 생성 | `src/pages/Dashboard/DashboardSidebar.jsx` | Drawer 사이드바 독립 컴포넌트, useNavigate 라우팅 |
| FriendPage.jsx 레이아웃 재구성 | `src/pages/FriendPage/FriendPage.jsx` | 헤더·사이드바 직접 조합 |
| UserInfo.jsx 레이아웃 재구성 | `src/pages/UserInfo/UserInfo.jsx` | 헤더·사이드바 직접 조합 |
| Dashboard.jsx 인라인 코드 제거 | `src/pages/Dashboard/Dashboard.jsx` | AppBar/Drawer/showFriend/showUserInfo 제거 |
| App.jsx 라우트 추가 | `src/App.jsx` | `/user/friend`, `/user/account` 직접 라우팅 |
| HeaderUserInfo.jsx 경로 수정 | `src/components/HeaderUserInfo.jsx` | `/dashboard` state → `/user/account`, `/user/friend` 직접 이동 |
| HeaderUserInfo.jsx 로그아웃 후 이동 추가 | `src/components/HeaderUserInfo.jsx` | 로그아웃 시 `navigate('/')` 추가 |

## 주요 변경 사항

### 1. DashboardHeader.jsx 생성

**추가:**
```jsx
// frontend/src/pages/Dashboard/DashboardHeader.jsx
function DashboardHeader({ onMobileToggle }) {
  return (
    <AppBar position="fixed" sx={{ width: { sm: `calc(100% - 260px)` }, ml: { sm: '260px' }, ... }}>
      <Toolbar sx={{ justifyContent: 'space-between' }}>
        <IconButton onClick={onMobileToggle} sx={{ display: { sm: 'none' } }}>
          <MenuIcon />
        </IconButton>
        <HeaderUserInfo />
      </Toolbar>
    </AppBar>
  );
}
```

**특징:**
- Dashboard.jsx에 인라인으로 있던 AppBar 코드를 독립 컴포넌트로 분리
- 모바일 토글 콜백 `onMobileToggle`을 props로 수신

---

### 2. DashboardSidebar.jsx 생성

**추가:**
```jsx
// frontend/src/pages/Dashboard/DashboardSidebar.jsx
const menuItems = [
  { text: '게임하기', path: '/pinball' },
  { text: '친구',    path: '/user/friend' },
  { text: '계정',    path: '/user/account' },
];

function DashboardSidebar({ currentMenu, mobileOpen, onMobileClose }) {
  const navigate = useNavigate();
  // currentMenu와 item.text가 일치하면 선택 스타일 적용
  // 메뉴 클릭 시 navigate(path) + onMobileClose() 호출
}
```

**특징:**
- 메뉴 클릭 시 `useNavigate`로 해당 경로로 직접 이동
- `currentMenu` prop으로 현재 선택된 메뉴 항목 하이라이트 표시
- 모바일·데스크탑 Drawer를 각각 렌더링

---

### 3. FriendPage.jsx / UserInfo.jsx 레이아웃 재구성

**변경 전:**
```jsx
// FriendPage — 콘텐츠만 반환
return <Box data-testid="friend-page" sx={{ display: 'flex', ... }}> ... </Box>;

// UserInfo — 콘텐츠만 반환
return <Box sx={{ display: 'flex', flexDirection: 'column', gap: 3 }}> ... </Box>;
```

**변경 후:**
```jsx
// FriendPage (UserInfo도 동일 구조)
return (
  <Box sx={{ display: 'flex' }}>
    <DashboardHeader onMobileToggle={() => setMobileOpen(true)} />
    <DashboardSidebar currentMenu="친구" mobileOpen={mobileOpen} onMobileClose={...} />
    <Box component="main" sx={{ flexGrow: 1, p: 3, backgroundColor: '#F9FAFB', minHeight: '100vh' }}>
      <Toolbar />
      <Container maxWidth="xl">
        {/* 기존 콘텐츠 */}
      </Container>
    </Box>
  </Box>
);
```

**특징:**
- AdminPage.jsx 패턴 적용 — 각 페이지가 헤더·사이드바·콘텐츠를 직접 조합
- FriendPage는 `currentMenu="친구"`, UserInfo는 `currentMenu="계정"` 고정값 전달

---

### 4. Dashboard.jsx 인라인 코드 제거

**변경 전:**
```jsx
const [showUserInfo, setShowUserInfo] = useState(false);
const [showFriend, setShowFriend] = useState(false);
// AppBar, Drawer 인라인 JSX (~220줄)
{showUserInfo ? <UserInfo /> : showFriend ? <FriendPage /> : null}
```

**변경 후:**
```jsx
const [mobileOpen, setMobileOpen] = useState(false);
return (
  <Box sx={{ display: 'flex' }}>
    <DashboardHeader onMobileToggle={() => setMobileOpen(true)} />
    <DashboardSidebar currentMenu="" mobileOpen={mobileOpen} onMobileClose={...} />
    <Box component="main" ...>
      {isLoggedIn ? <Typography>좌측 메뉴에서 선택하세요.</Typography> : <Button>로그인하기</Button>}
    </Box>
  </Box>
);
```

**특징:**
- showFriend/showUserInfo 상태 및 location.state 처리 로직 완전 제거
- 410줄 → 56줄로 대폭 축소

---

### 5. App.jsx / HeaderUserInfo.jsx 수정

**변경 전:**
```jsx
// App.jsx — /user/* 경로 없음
navigate('/dashboard', { state: { section: '계정' } })  // HeaderUserInfo
navigate('/dashboard', { state: { section: '친구' } })   // HeaderUserInfo
function AvatarLogoutButton() { logout(); }
```

**변경 후:**
```jsx
// App.jsx
<Route path="/user/friend" element={<FriendPage />} />
<Route path="/user/account" element={<UserInfo />} />

// HeaderUserInfo.jsx
navigate('/user/account')
navigate('/user/friend')
function AvatarLogoutButton() { logout(); navigate('/'); }
```

**특징:**
- Dashboard state 방식 제거, 독립 라우트로 직접 이동
- 로그아웃 후 홈(`/`)으로 이동

---

### 라우트 구조 변경

```
변경 전
/dashboard → Dashboard
             ├── AppBar (인라인)
             ├── Drawer (인라인)
             └── Main
                 ├── showUserInfo → <UserInfo />
                 └── showFriend  → <FriendPage />

변경 후
/dashboard     → Dashboard
                 ├── DashboardHeader
                 ├── DashboardSidebar
                 └── Main (안내 메시지)

/user/friend   → FriendPage
                 ├── DashboardHeader
                 ├── DashboardSidebar (currentMenu="친구")
                 └── Main (친구 콘텐츠)

/user/account  → UserInfo
                 ├── DashboardHeader
                 ├── DashboardSidebar (currentMenu="계정")
                 └── Main (계정 콘텐츠)
```

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- 없음
