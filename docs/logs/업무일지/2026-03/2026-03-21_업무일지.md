# 업무일지
**금일 변경사항은 모두 롤백되었음**

## 기본 정보
- 날짜: 2026-03-21 (토)
- 작성자: hexsera

## 진행 목표
Google OAuth 2.0 Authorization Code Flow를 사용한 구글 로그인 기능 프론트엔드 구현 (Phase 1). TDD(Red→Green→Refactor) 방식으로 진행.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| @react-oauth/google 패키지 설치 | `frontend/package.json` | npm install |
| GoogleOAuthProvider 앱 래핑 | `frontend/src/main.jsx` | 최상위 Provider 추가 |
| useGoogleLogin hook + 구글 로그인 버튼 추가 | `frontend/src/pages/Login/Login.jsx` | flow: 'auth-code', redirect_uri 포함 |
| VITE_GOOGLE_CLIENT_ID 환경변수 설정 | `frontend/.env` | 실제 Client ID 입력 (신규 생성) |
| Login 컴포넌트 테스트 추가 | `frontend/src/test/Login.test.jsx` | 구글 로그인 관련 6개 테스트 추가 |

**총 약 1시간 30분**

## 주요 변경 사항

### 1. frontend/src/main.jsx 수정

**변경 전:**
```jsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.jsx';

const rootElement = document.getElementById('root');
const root = ReactDOM.createRoot(rootElement);

root.render(<App />);
```

**변경 후:**
```jsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App.jsx';
import { GoogleOAuthProvider } from '@react-oauth/google';

const rootElement = document.getElementById('root');
const root = ReactDOM.createRoot(rootElement);

root.render(
  <GoogleOAuthProvider clientId={import.meta.env.VITE_GOOGLE_CLIENT_ID}>
    <App />
  </GoogleOAuthProvider>
);
```

**변경 의도:** 앱 전체에서 `useGoogleLogin` hook을 사용할 수 있도록 최상위 컴포넌트를 `GoogleOAuthProvider`로 래핑. `clientId`는 환경변수에서 읽어 하드코딩 방지.

---

### 2. frontend/src/pages/Login/Login.jsx 수정

**변경 전 (import 및 함수 상단):**
```jsx
import { useState } from 'react';
import { Box, TextField, Button, Container, Typography, Paper, Alert, CardMedia} from '@mui/material';
import { useNavigate } from 'react-router-dom';
import { useAuth } from '../../contexts/AuthContext';
import axios from 'axios';
import Aurora from '../../components/Aurora/Aurora';

// ...

function Login() {
  const [username, setUsername] = useState('');
  const [password, setPassword] = useState('');
  const [loginError, setLoginError] = useState(false);
  const navigate = useNavigate();
  const { login } = useAuth();

  // 확인 버튼을 눌렀을 때 실행되는 함수
  const handleLogin = async () => { ... };
```

**변경 전 (JSX 버튼 영역):**
```jsx
                <Box sx={{ display: 'flex', justifyContent: 'center', mt: 3 }}>
                  <Button
                    variant="outlined"
                    sx={{ width: '300px', borderColor: COLORS.text, color: COLORS.text }}
                    onClick={()=>navigate('/Register')}
                  >
                    회원가입
                  </Button>
                </Box>
              </Box>
```

**변경 후 (import 및 함수 상단):**
```jsx
import { useState } from 'react';
import { Box, TextField, Button, Container, Typography, Paper, Alert, CardMedia} from '@mui/material';
import { useNavigate } from 'react-router-dom';
import { useAuth } from '../../contexts/AuthContext';
import axios from 'axios';
import Aurora from '../../components/Aurora/Aurora';
import { useGoogleLogin } from '@react-oauth/google';

// ...

function Login() {
  const [username, setUsername] = useState('');
  const [password, setPassword] = useState('');
  const [loginError, setLoginError] = useState(false);
  const navigate = useNavigate();
  const { login } = useAuth();

  const googleLogin = useGoogleLogin({
    flow: 'auth-code',
    redirect_uri: 'https://hexsera.com',
    onSuccess: async (codeResponse) => {
      const res = await axios.post('/api/v1/auth/google', { code: codeResponse.code });
      if (res.status === 200) {
        login({ id: res.data.user_id, name: res.data.nickname, role: res.data.role, email: res.data.email });
        navigate(res.data.role === 'admin' ? '/admin' : '/');
      }
    },
    onError: () => alert('구글 로그인에 실패했습니다.'),
  });

  // 확인 버튼을 눌렀을 때 실행되는 함수
  const handleLogin = async () => { ... };
```

**변경 후 (JSX 버튼 영역):**
```jsx
                <Box sx={{ display: 'flex', justifyContent: 'center', mt: 3 }}>
                  <Button
                    variant="outlined"
                    sx={{ width: '300px', borderColor: COLORS.text, color: COLORS.text }}
                    onClick={()=>navigate('/Register')}
                  >
                    회원가입
                  </Button>
                </Box>
                <Box sx={{ display: 'flex', justifyContent: 'center', mt: 2 }}>
                  <button onClick={() => googleLogin()}>구글로 로그인</button>
                </Box>
              </Box>
```

**변경 의도:** `flow: 'auth-code'`로 Authorization Code Flow 사용 — Access Token이 브라우저에 노출되지 않음. `redirect_uri: 'https://hexsera.com'`은 팝업 방식에서 도메인 명시. 성공 시 기존 `login()` 함수를 재사용하여 localStorage 저장 및 역할 기반 페이지 이동.

---

### 3. frontend/.env 생성 (신규)

**추가:**
```env
VITE_GOOGLE_CLIENT_ID=<GOOGLE_CLIENT_ID>
```

**변경 의도:** Google Cloud Console에서 발급받은 OAuth 2.0 클라이언트 ID를 환경변수로 관리. `VITE_` 접두사로 Vite가 클라이언트에 노출. CLIENT_SECRET은 백엔드 전용으로 이 파일에 포함하지 않음.

---

### 4. frontend/src/test/Login.test.jsx 수정

**변경 전:**
```jsx
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
import { MemoryRouter } from 'react-router-dom';
import Login from '../pages/Login/Login';
import { AuthProvider } from '../contexts/AuthContext';
import axios from 'axios';

vi.mock('axios');

describe('Login 컴포넌트', () => {
  afterEach(() => {
    localStorage.clear();
    vi.clearAllMocks();
  });

  it('로그인 성공 시 localStorage에 email이 저장된다', async () => {
    axios.post.mockResolvedValue({
      status: 200,
      data: {
        user_id: 1,
        nickname: '테스트유저',
        role: 'user',
        email: 'test@example.com',
      },
    });

    render(
      <MemoryRouter>
        <AuthProvider>
          <Login />
        </AuthProvider>
      </MemoryRouter>
    );

    fireEvent.change(screen.getByPlaceholderText('이메일을 입력해주세요.'), {
      target: { value: 'test@example.com' },
    });
    fireEvent.change(screen.getByPlaceholderText('비밀번호를 입력해주세요.'), {
      target: { value: 'password123' },
    });
    fireEvent.click(screen.getByRole('button', { name: '로그인' }));

    await waitFor(() => {
      const saved = JSON.parse(localStorage.getItem('user'));
      expect(saved.email).toBe('test@example.com');
    });
  });
});
```

**변경 후:**
```jsx
import { render, screen, fireEvent, waitFor, act } from '@testing-library/react';
import { describe, it, expect, afterEach, vi } from 'vitest';
import { MemoryRouter } from 'react-router-dom';
import Login from '../pages/Login/Login';
import { AuthProvider } from '../contexts/AuthContext';
import axios from 'axios';

vi.mock('axios');

vi.mock('../components/Aurora/Aurora', () => ({
  default: () => null,
}));

const mockGoogleLogin = vi.fn();
vi.mock('@react-oauth/google', () => ({
  GoogleOAuthProvider: ({ children }) => <>{children}</>,
  useGoogleLogin: ({ onSuccess, onError }) => {
    mockGoogleLogin._onSuccess = onSuccess;
    mockGoogleLogin._onError = onError;
    return mockGoogleLogin;
  },
}));

const renderLogin = () =>
  render(
    <MemoryRouter>
      <AuthProvider>
        <Login />
      </AuthProvider>
    </MemoryRouter>
  );

describe('Login 컴포넌트', () => {
  afterEach(() => {
    localStorage.clear();
    vi.clearAllMocks();
  });

  it('로그인 성공 시 localStorage에 email이 저장된다', async () => { ... });
  it('구글 로그인 버튼이 렌더링된다', () => { ... });
  it('구글 로그인 버튼 클릭 시 googleLogin 함수가 호출된다', () => { ... });
  it('구글 로그인 성공 시 POST /api/v1/auth/google를 호출한다', async () => { ... });
  it('구글 로그인 성공 후 role=user이면 /로 이동한다', async () => { ... });
  it('구글 로그인 성공 후 role=admin이면 /admin으로 이동한다', async () => { ... });
  it('구글 로그인 실패 시 alert를 호출한다', () => { ... });
});
```

**변경 의도:** Aurora(WebGL) 및 `@react-oauth/google` 모킹 추가로 jsdom 환경에서 테스트 가능하게 함. `act()` 래핑으로 React 상태 업데이트 경고 제거. 구글 로그인 관련 6개 테스트 추가.

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| 이메일/비밀번호 로그인 | 변경 없음 (회귀 테스트 통과) | 정상 |
| 구글 로그인 버튼 렌더링 | Login.jsx에 버튼 신규 추가 | 정상 |
| 구글 로그인 팝업 → 백엔드 전송 | useGoogleLogin onSuccess → POST /api/v1/auth/google | 미확인 (백엔드 Phase 2 미구현) |
| 구글 로그인 후 페이지 이동 | role 기반 navigate 로직 추가 | 정상 (테스트 통과) |
| 구글 로그인 실패 처리 | onError → alert 호출 | 정상 (테스트 통과) |

## 결과
- 상태: 완료 (Phase 1 한정)
- 테스트: 7/7 통과 (경고 없음)
- Phase 2 (Backend: POST /api/v1/auth/google 엔드포인트), Phase 3 (Google Cloud Console 등록) 미진행

## 배운 내용

## 다음 작업
- ~~Phase 2: 백엔드 `POST /api/v1/auth/google` 엔드포인트 구현 (Authlib, httpx)~~ ✅ 완료
- Phase 3: Google Cloud Console에서 hexsera.com 도메인 Authorized JavaScript origins 등록 확인
- `frontend/.env`를 `.gitignore`에 추가하여 Client ID git 커밋 방지 검토
- `GOOGLE_REDIRECT_URI` 값 확인 및 정리 (`postmessage` vs 도메인)

---

# 업무일지 (Phase 2: Backend)

## 기본 정보
- 날짜: 2026-03-21 (토)
- 작성자: hexsera

## 진행 목표
Google OAuth 2.0 백엔드 구현 (Phase 2). TDD(Red→Green→Refactor) 방식으로 `POST /api/v1/auth/google` 엔드포인트 구현.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| RED 테스트 11개 작성 | `backend/tests/test_google_auth.py` | 신규 생성 |
| Authlib 패키지 추가 | `backend/requirements.txt` | Authlib==1.6.9 |
| Google OAuth 설정 변수 추가 | `backend/app/core/config.py` | CLIENT_ID, SECRET, REDIRECT_URI |
| GoogleLoginRequest 스키마 추가 | `backend/app/schemas/user.py` | code: str |
| User 모델 nullable 변경 | `backend/models.py` | password, birth_date → nullable=True |
| POST /auth/google 엔드포인트 추가 | `backend/app/api/v1/auth.py` | AsyncOAuth2Client 사용 |
| Google OAuth 환경변수 설정 | `backend/.env` | CLIENT_ID, SECRET, REDIRECT_URI 실제값 입력 |
| alembic 마이그레이션 실행 | `backend/alembic/versions/` | password, birth_date nullable 프로덕션 DB 적용 |

**총 약 2시간**

## 주요 변경 사항

### 1. backend/requirements.txt 수정

**변경 전:**
```txt
httpx==0.27.0
Faker==24.0.0
```

**변경 후:**
```txt
httpx==0.27.0
Authlib==1.6.9
Faker==24.0.0
```

**변경 의도:** OAuth 2.0 Token 교환 및 userinfo 조회를 처리하는 Authlib 라이브러리 추가. httpx는 이미 설치되어 있어 추가 불필요.

---

### 2. backend/app/core/config.py 수정

**변경 전:**
```python
    GEMINI_API_KEY: str = os.getenv("GEMINI_API_KEY")

settings = Settings()
```

**변경 후:**
```python
    GEMINI_API_KEY: str = os.getenv("GEMINI_API_KEY")
    GOOGLE_CLIENT_ID: str = os.getenv("GOOGLE_CLIENT_ID", "")
    GOOGLE_CLIENT_SECRET: str = os.getenv("GOOGLE_CLIENT_SECRET", "")
    GOOGLE_REDIRECT_URI: str = os.getenv("GOOGLE_REDIRECT_URI", "postmessage")

settings = Settings()
```

**변경 의도:** Token 교환 시 구글에 전달할 클라이언트 자격증명을 환경변수로 관리. 기본값은 `postmessage`.

---

### 3. backend/app/schemas/user.py 수정

**변경 전:**
```python
class DeleteResponse(BaseModel):
    """삭제 응답"""
    message: str
    deleted_user_id: int
```

**변경 후:**
```python
class DeleteResponse(BaseModel):
    """삭제 응답"""
    message: str
    deleted_user_id: int


class GoogleLoginRequest(BaseModel):
    code: str  # Authorization Code
```

**변경 의도:** 프론트엔드가 전송하는 Authorization Code를 받는 요청 스키마 추가.

---

### 4. backend/models.py 수정

**변경 전:**
```python
    password = Column(String(255), nullable=False)
    birth_date = Column(Date, nullable=False)
```

**변경 후:**
```python
    password = Column(String(255), nullable=True)
    birth_date = Column(Date, nullable=True)
```

**변경 의도:** 구글 로그인 사용자는 비밀번호/생년월일이 없으므로 NULL 허용으로 변경. 기존 이메일/비밀번호 로그인 사용자에게는 영향 없음.

---

### 5. backend/app/api/v1/auth.py 수정

**변경 전:**
```python
# backend/app/api/v1/auth.py
from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.orm import Session

from app.api.deps import get_db
from app.core.security import hash_password, verify_password
from app.schemas.user import (
    UserRegisterRequest,
    UserResponse,
    LoginRequest,
    LoginResponse,
)

import sys
sys.path.insert(0, '/code')
from models import User

router = APIRouter()


@router.post("/login", response_model=LoginResponse)
def login(
    login_request: LoginRequest,
    db: Session = Depends(get_db)
):
    """로그인 엔드포인트 (API Key 불필요)"""
    user = db.query(User).filter(User.email == login_request.email).first()
    if user is None:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Invalid email or password"
        )

    if not verify_password(login_request.password, user.password):
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Invalid email or password"
        )

    return LoginResponse(
        message="Login successful",
        user_id=user.id,
        email=user.email,
        nickname=user.nickname,
        role=user.role
    )
```

**변경 후:**
```python
# backend/app/api/v1/auth.py
from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.orm import Session
from authlib.integrations.httpx_client import AsyncOAuth2Client

from app.api.deps import get_db
from app.core.security import hash_password, verify_password
from app.core.config import settings
from app.schemas.user import (
    UserRegisterRequest,
    UserResponse,
    LoginRequest,
    LoginResponse,
    GoogleLoginRequest,
)

import sys
sys.path.insert(0, '/code')
from models import User

router = APIRouter()


def _build_login_response(user: User) -> LoginResponse:
    return LoginResponse(
        message="Login successful",
        user_id=user.id,
        email=user.email,
        nickname=user.nickname,
        role=user.role
    )


@router.post("/login", response_model=LoginResponse)
def login(
    login_request: LoginRequest,
    db: Session = Depends(get_db)
):
    """로그인 엔드포인트 (API Key 불필요)"""
    user = db.query(User).filter(User.email == login_request.email).first()
    if user is None:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Invalid email or password"
        )

    if not verify_password(login_request.password, user.password):
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Invalid email or password"
        )

    return _build_login_response(user)


@router.post("/auth/google", response_model=LoginResponse)
async def google_login(data: GoogleLoginRequest, db: Session = Depends(get_db)):
    """Google OAuth 2.0 Authorization Code Flow 로그인"""
    async with AsyncOAuth2Client(
        client_id=settings.GOOGLE_CLIENT_ID,
        client_secret=settings.GOOGLE_CLIENT_SECRET,
    ) as client:
        token = await client.fetch_token(
            "https://oauth2.googleapis.com/token",
            code=data.code,
            redirect_uri=settings.GOOGLE_REDIRECT_URI,
            grant_type="authorization_code",
        )

    async with AsyncOAuth2Client(token=token) as client:
        resp = await client.get("https://www.googleapis.com/oauth2/v3/userinfo")

    if resp.status_code != 200:
        raise HTTPException(status_code=401, detail="Failed to get Google user info")

    google_user = resp.json()

    user = db.query(User).filter(User.email == google_user["email"]).first()
    if not user:
        user = User(
            email=google_user["email"],
            nickname=google_user.get("name", google_user["email"].split("@")[0]),
            password=None,
            birth_date=None,
            role="user"
        )
        db.add(user)
        db.commit()
        db.refresh(user)

    return _build_login_response(user)
```

**변경 의도:** `POST /auth/google` 엔드포인트 추가. `_build_login_response()` 헬퍼로 login/google_login 양쪽의 LoginResponse 생성 중복 제거.

---

### 6. backend/.env 수정

**변경 전:**
```env
# Gemini API
GEMINI_API_KEY=<GEMINI_API_KEY>
```

**변경 후:**
```env
# Gemini API
GEMINI_API_KEY=<GEMINI_API_KEY>

# Google OAuth 2.0
GOOGLE_CLIENT_ID=<GOOGLE_CLIENT_ID>
GOOGLE_CLIENT_SECRET=<GOOGLE_CLIENT_SECRET>
GOOGLE_REDIRECT_URI=https://hexsera.com
```

**변경 의도:** Google Cloud Console에서 발급받은 실제 자격증명 등록. `GOOGLE_REDIRECT_URI`는 추후 `postmessage` 또는 도메인 중 결정 필요.

---

### 7. backend/tests/test_google_auth.py 신규 생성

**추가:** (TDD RED 테스트 11개)
```python
"""
Google OAuth 2.0 백엔드 TDD 테스트
Phase 2: POST /api/v1/auth/google 엔드포인트
"""
# 스키마 테스트 2개
# 모델 테스트 2개
# 엔드포인트 테스트 7개 (AsyncOAuth2Client mock 사용)
```

파일 위치: `backend/tests/test_google_auth.py`

**변경 의도:** TDD RED→GREEN→REFACTOR 사이클로 구현. Google 외부 API는 `unittest.mock.patch`로 mock 처리.

---

### 8. alembic 마이그레이션 실행

**실행 명령:**
```bash
docker exec fastapi-server alembic revision --autogenerate -m "make password and birth_date nullable for oauth"
docker exec fastapi-server alembic upgrade head
```

**생성된 파일:** `backend/alembic/versions/27cd1d3a54de_make_password_and_birth_date_nullable_.py`

**변경 의도:** `models.py`의 nullable 변경사항을 프로덕션 PostgreSQL DB에 실제 적용. 미적용 시 `NotNullViolation` 에러 발생.

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| 구글 로그인 백엔드 처리 | POST /auth/google 신규 추가 | 정상 (테스트 11/11 통과, 실제 로그인 확인) |
| 이메일/비밀번호 로그인 | `_build_login_response()` 헬퍼로 리팩토링 | 정상 (기존 테스트 통과) |
| 신규 유저 자동 생성 | 구글 로그인 시 DB에 없으면 password=None으로 생성 | 정상 |
| 기존 유저 재로그인 | 이미 있는 이메일이면 기존 유저 반환 | 정상 (테스트 통과) |
| 회원가입 | password nullable 변경 영향 없음 | 정상 |

## 결과
- 상태: 완료 (Phase 2)
- 테스트: 11/11 통과
- 실제 구글 로그인 DB 저장 확인 (NotNullViolation 수정 후 정상)

## 배운 내용

## 다음 작업
- Phase 3: Google Cloud Console Authorized JavaScript origins에 hexsera.com 등록 확인
- `GOOGLE_REDIRECT_URI` 값 결정: `postmessage` vs `https://hexsera.com`
- `backend/.env`를 `.gitignore`에 추가하여 CLIENT_SECRET git 커밋 방지 검토

---

# 업무일지 (PHASE 2: OAuth 2.0 내부 Authorization Server)
**이작업은 취소됨**
## 기본 정보
- 날짜: 2026-03-21 (토)
- 작성자: hexsera

## 진행 목표

OAuth 2.0 Authorization Code Flow 백엔드 Authorization Server 구현 (내부 서비스용).
기존 이메일+비밀번호 직접 전송 방식에서 JWT 기반 토큰 인증으로 전환하기 위한 서버 측 엔드포인트를 TDD 방식으로 구축한다.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| TDD RED — 테스트 30개 작성 | `tests/test_auth_codes.py` | 신규 (6개) |
| TDD RED — 테스트 30개 작성 | `tests/test_jwt_utils.py` | 신규 (9개) |
| TDD RED — 테스트 30개 작성 | `tests/test_oauth_endpoints.py` | 신규 (11개) |
| TDD RED — 테스트 30개 작성 | `tests/test_bearer_auth.py` | 신규 (4개) |
| 의존성 추가 | `requirements.txt` | python-jose, python-multipart 추가 |
| OAuth 클라이언트 설정 파일 | `app/core/oauth_clients.py` | 신규 |
| Authorization Code 임시 저장소 | `app/core/auth_codes.py` | 신규 |
| JWT 토큰 유틸리티 | `app/core/jwt_utils.py` | 신규 |
| SECRET_KEY 환경변수 추가 | `app/core/config.py` | 수정 |
| Bearer Token 인증 의존성 | `app/core/dependencies.py` | 신규 |
| OAuth 엔드포인트 3개 구현 | `app/api/v1/oauth.py` | 신규 |
| 라우터 등록 | `main.py` | oauth 라우터 추가 |

**총 약 3시간**

## 주요 변경 사항

### 1. requirements.txt 수정

**변경 전:**
```text
pwdlib[bcrypt]
```

**변경 후:**
```text
pwdlib[bcrypt]
python-jose[cryptography]==3.3.0
python-multipart
```

**변경 의도:** JWT 생성/검증을 위한 `python-jose`, `POST /oauth/token`의 Form 데이터 파싱을 위한 `python-multipart` 추가.

---

### 2. app/core/config.py 수정

**변경 전:**
```python
    GOOGLE_REDIRECT_URI: str = os.getenv("GOOGLE_REDIRECT_URI", "postmessage")

settings = Settings()
```

**변경 후:**
```python
    GOOGLE_REDIRECT_URI: str = os.getenv("GOOGLE_REDIRECT_URI", "postmessage")
    SECRET_KEY: str = os.getenv("SECRET_KEY", "change-me-in-production")

settings = Settings()
```

**변경 의도:** JWT 서명 키를 환경변수로 관리. 프로덕션에서는 `.env`에 `SECRET_KEY=<openssl rand -hex 32 결과값>` 설정 필요.

---

### 3. main.py 수정

**변경 전:**
```python
from app.api.v1 import users, auth, monthly_scores, game_visits, friends, chat, pinball_ai

# ...

app.include_router(pinball_ai.router, prefix="/api/v1/pinball_ai", tags=["Pinball AI"])
```

**변경 후:**
```python
from app.api.v1 import users, auth, monthly_scores, game_visits, friends, chat, pinball_ai, oauth

# ...

app.include_router(pinball_ai.router, prefix="/api/v1/pinball_ai", tags=["Pinball AI"])
app.include_router(oauth.router, prefix="/api/v1", tags=["OAuth"])
```

**변경 의도:** 새로 구현한 OAuth 라우터를 `/api/v1` 접두사로 앱에 등록.

---

### 4. app/core/oauth_clients.py 신규 생성

**추가:**
```python
OAUTH_CLIENTS = {
    "pinball-web-client": {
        "client_id": "pinball-web-client",
        "redirect_uris": [
            "https://hexsera.com/callback",
            "http://localhost:5173/callback",
        ],
        "scope": "openid profile",
        "grant_types": ["authorization_code"],
        "response_types": ["code"],
    }
}
```

**변경 의도:** 내부 OAuth 클라이언트(프론트엔드)를 DB 없이 설정 파일로 관리. `redirect_uris` 외 주소로는 code 발급이 차단된다.

---

### 5. app/core/auth_codes.py 신규 생성

**추가:**
```python
import secrets
import time
from typing import Dict

_codes: Dict[str, dict] = {}


def create_code(user_id: int, client_id: str, redirect_uri: str) -> str:
    code = secrets.token_urlsafe(32)
    _codes[code] = {
        "user_id": user_id,
        "client_id": client_id,
        "redirect_uri": redirect_uri,
        "expires_at": time.time() + 600,  # 10분
    }
    return code


def consume_code(code: str) -> dict | None:
    data = _codes.pop(code, None)
    if data and data["expires_at"] > time.time():
        return data
    return None
```

**변경 의도:** Authorization Code를 서버 메모리에 10분간 임시 저장. `consume_code`는 `pop`으로 즉시 삭제해 동일 code 재사용을 방지한다.

---

### 6. app/core/jwt_utils.py 신규 생성

**추가:**
```python
from jose import jwt
from datetime import datetime, timedelta
from app.core.config import settings

ALGORITHM = "HS256"


def create_access_token(user_id: int, email: str, role: str) -> str:
    payload = {
        "sub": str(user_id),
        "email": email,
        "role": role,
        "exp": datetime.utcnow() + timedelta(hours=1),
        "iat": datetime.utcnow(),
        "type": "access",
    }
    return jwt.encode(payload, settings.SECRET_KEY, algorithm=ALGORITHM)


def create_refresh_token(user_id: int) -> str:
    payload = {
        "sub": str(user_id),
        "exp": datetime.utcnow() + timedelta(days=30),
        "iat": datetime.utcnow(),
        "type": "refresh",
    }
    return jwt.encode(payload, settings.SECRET_KEY, algorithm=ALGORITHM)


def decode_token(token: str) -> dict:
    return jwt.decode(token, settings.SECRET_KEY, algorithms=[ALGORITHM])
```

**변경 의도:** Access Token(1시간)과 Refresh Token(30일)을 HS256으로 서명하는 JWT 유틸리티. `sub`에 user_id를 문자열로 저장.

---

### 7. app/core/dependencies.py 신규 생성

**추가:**
```python
import sys
sys.path.insert(0, '/code')

from fastapi import Depends, HTTPException, Security
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from sqlalchemy.orm import Session

from app.api.deps import get_db
from app.core.jwt_utils import decode_token
from models import User

bearer_scheme = HTTPBearer(auto_error=False)


def get_current_user(
    credentials: HTTPAuthorizationCredentials = Security(bearer_scheme),
    db: Session = Depends(get_db),
) -> User:
    if not credentials:
        raise HTTPException(status_code=401, detail="Not authenticated")
    try:
        payload = decode_token(credentials.credentials)
    except Exception:
        raise HTTPException(status_code=401, detail="Invalid token")
    user = db.query(User).filter(User.id == int(payload["sub"])).first()
    if not user:
        raise HTTPException(status_code=401, detail="User not found")
    return user


def get_current_user_optional(
    credentials: HTTPAuthorizationCredentials = Security(bearer_scheme),
    db: Session = Depends(get_db),
) -> User | None:
    if not credentials:
        return None
    try:
        payload = decode_token(credentials.credentials)
        user = db.query(User).filter(User.id == int(payload["sub"])).first()
        return user
    except Exception:
        return None
```

**변경 의도:** `Authorization: Bearer <token>` 헤더를 검증하는 FastAPI 의존성 함수 2종.
- `get_current_user`: 인증 필수. 토큰 없거나 잘못되면 401.
- `get_current_user_optional`: 인증 선택. 토큰 없으면 None 반환 (authorize 엔드포인트용).

---

### 8. app/api/v1/oauth.py 신규 생성

**추가:**
```python
import sys
sys.path.insert(0, '/code')

from fastapi import APIRouter, Depends, HTTPException, Form
from fastapi.responses import RedirectResponse
from sqlalchemy.orm import Session

from app.api.deps import get_db
from app.core.oauth_clients import OAUTH_CLIENTS
from app.core.auth_codes import create_code, consume_code
from app.core.jwt_utils import create_access_token, create_refresh_token
from app.core.dependencies import get_current_user, get_current_user_optional
from models import User

router = APIRouter()


@router.get("/oauth/authorize")
def authorize(
    response_type: str,
    client_id: str,
    redirect_uri: str,
    state: str,
    current_user: User = Depends(get_current_user_optional),
    db: Session = Depends(get_db),
):
    client = OAUTH_CLIENTS.get(client_id)
    if not client or redirect_uri not in client["redirect_uris"]:
        raise HTTPException(status_code=400, detail="Invalid client or redirect_uri")

    if current_user is None:
        return RedirectResponse(
            f"/login?next=/oauth/authorize&client_id={client_id}&redirect_uri={redirect_uri}&state={state}"
        )

    code = create_code(current_user.id, client_id, redirect_uri)
    return RedirectResponse(f"{redirect_uri}?code={code}&state={state}")


@router.post("/oauth/token")
def token(
    grant_type: str = Form(...),
    code: str = Form(...),
    redirect_uri: str = Form(...),
    client_id: str = Form(...),
    db: Session = Depends(get_db),
):
    code_data = consume_code(code)
    if not code_data or code_data["redirect_uri"] != redirect_uri:
        raise HTTPException(status_code=400, detail="Invalid or expired code")

    user = db.query(User).filter(User.id == code_data["user_id"]).first()
    if not user:
        raise HTTPException(status_code=400, detail="User not found")

    return {
        "access_token": create_access_token(user.id, user.email, user.role),
        "refresh_token": create_refresh_token(user.id),
        "token_type": "Bearer",
        "expires_in": 3600,
        "user": {
            "id": user.id,
            "email": user.email,
            "nickname": user.nickname,
            "role": user.role,
        },
    }


@router.get("/oauth/me")
def me(current_user: User = Depends(get_current_user)):
    return {
        "id": current_user.id,
        "email": current_user.email,
        "nickname": current_user.nickname,
        "role": current_user.role,
    }
```

**변경 의도:** OAuth 2.0 Authorization Code Flow의 3개 엔드포인트 구현.
- `GET /api/v1/oauth/authorize`: code 발급 (미인증 시 /login 리다이렉트)
- `POST /api/v1/oauth/token`: code → Access Token + Refresh Token 교환
- `GET /api/v1/oauth/me`: Bearer Token으로 현재 사용자 조회 (테스트 및 프론트 연동용)

---

## 요청 흐름

```
프론트엔드
    │
    ▼
GET /api/v1/oauth/authorize
    │
    ├─ Bearer Token 없음 ──▶ 302 /login 리다이렉트
    │
    └─ Bearer Token 있음 ──▶ Authorization Code 생성 (메모리, 10분)
                                      │
                                      ▼
                           302 redirect_uri?code=...&state=...
                                      │
                                      ▼
                           POST /api/v1/oauth/token
                                      │
                                      ▼
                    access_token (1h) + refresh_token (30d) + user 정보
                                      │
                                      ▼
                    이후 API 요청: Authorization: Bearer <access_token>
                                      │
                                      ▼
                    get_current_user → JWT 검증 → DB user 조회
```

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| OAuth Authorization Code 발급 | `GET /api/v1/oauth/authorize` 신규 | 정상 (테스트 30/30 PASSED) |
| OAuth Token 교환 | `POST /api/v1/oauth/token` 신규 | 정상 (테스트 30/30 PASSED) |
| Bearer Token 인증 | `get_current_user` 의존성 신규 | 정상 (테스트 30/30 PASSED) |
| 현재 사용자 조회 | `GET /api/v1/oauth/me` 신규 | 정상 (테스트 30/30 PASSED) |
| 기존 이메일/비밀번호 로그인 | 변경 없음 | 정상 (기존 테스트 19/19 PASSED) |
| Google OAuth 로그인 | 변경 없음 | 정상 (기존 테스트 19/19 PASSED) |
| 친구 요청 | 변경 없음 | 정상 (기존 테스트 19/19 PASSED) |

## 결과
- 상태: 완료
- 신규 테스트 30개 PASSED
- 기존 테스트 19개 PASSED (회귀 없음)
- 전체 49개 테스트 PASSED

## 배운내용

## 다음 작업
- PHASE 1 프론트엔드: `react-oidc-context` 설치 및 `AuthProvider` 설정
- `backend/.env`에 `SECRET_KEY=<openssl rand -hex 32>` 프로덕션 키 추가 필요
- 프론트엔드 axios 인터셉터에서 Bearer Token 자동 주입 구현
- `GET /api/v1/oauth/authorize` 호출 시 현재는 Bearer Token으로 인증 — 프론트 연동 시 쿠키 기반 세션 인증으로 전환 검토 필요

---

# 업무일지

## 기본 정보
- 날짜: 2026-03-21 (토)
- 작성자: hexsera

## 진행 목표
Google 로그인 개선: `auth_provider` 필드로 계정 유형 구분, `birth_date` NOT NULL 적용, Google 공식 버튼 디자인 적용, localStorage에 `authProvider` 저장.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| Alembic 마이그레이션 생성 및 실행 | `backend/alembic/versions/a1b2c3d4e5f6_...py` | birth_date NOT NULL, auth_provider 컬럼 추가 |
| User 모델 수정 | `backend/models.py` | birth_date nullable=False, auth_provider 필드 추가 |
| auth.py 수정 | `backend/app/api/v1/auth.py` | Google 유저 birth_date=today(), auth_provider='google', LoginResponse에 auth_provider 포함 |
| users.py 수정 | `backend/app/api/v1/users.py` | create_user에 auth_provider='local' 명시 |
| schemas 수정 | `backend/app/schemas/user.py` | LoginResponse, UserResponse에 auth_provider 필드 추가 |
| Login.jsx 수정 | `frontend/src/pages/Login/Login.jsx` | Google 공식 SVG 버튼 디자인, authProvider localStorage 저장 |

**총 약 30분**

## 주요 변경 사항

### 1. backend/alembic/versions/a1b2c3d4e5f6_add_auth_provider_notnull_birth_date.py 신규 생성

**추가:**
```python
"""add auth_provider and notnull birth_date

Revision ID: a1b2c3d4e5f6
Revises: 27cd1d3a54de
Create Date: 2026-03-21 10:00:00.000000

"""
from alembic import op
import sqlalchemy as sa


revision = 'a1b2c3d4e5f6'
down_revision = '27cd1d3a54de'
branch_labels = None
depends_on = None


def upgrade() -> None:
    # 1) NULL인 birth_date를 오늘 날짜로 채운 후 NOT NULL로 변경
    op.execute("UPDATE users SET birth_date = CURRENT_DATE WHERE birth_date IS NULL")
    op.alter_column('users', 'birth_date', nullable=False)

    # 2) auth_provider 컬럼 추가
    op.add_column('users', sa.Column(
        'auth_provider',
        sa.String(20),
        nullable=False,
        server_default='local'
    ))


def downgrade() -> None:
    op.drop_column('users', 'auth_provider')
    op.alter_column('users', 'birth_date', nullable=True)
```

**변경 의도:** DB에 `auth_provider` 컬럼을 추가하고 기존 NULL birth_date를 오늘 날짜로 채워 NOT NULL 제약 적용. 기존 유저는 모두 `server_default='local'`으로 자동 설정.

---

### 2. backend/models.py 수정

**변경 전:**
```python
class User(Base):
    __tablename__ = "users"

    id = Column(Integer, primary_key=True, index=True)
    user_id = Column(UUID(as_uuid=True), unique=True, nullable=True,
                    default=uuid.uuid4, index=True)
    email = Column(String(255), unique=True, index=True, nullable=False)
    nickname = Column(String(100), nullable=False)
    password = Column(String(255), nullable=True)
    birth_date = Column(Date, nullable=True)
    role = Column(String(20), nullable=False, default='user')
```

**변경 후:**
```python
class User(Base):
    __tablename__ = "users"

    id = Column(Integer, primary_key=True, index=True)
    user_id = Column(UUID(as_uuid=True), unique=True, nullable=True,
                    default=uuid.uuid4, index=True)
    email = Column(String(255), unique=True, index=True, nullable=False)
    nickname = Column(String(100), nullable=False)
    password = Column(String(255), nullable=True)
    birth_date = Column(Date, nullable=False)
    auth_provider = Column(String(20), nullable=False, default='local')
    role = Column(String(20), nullable=False, default='user')
```

**변경 의도:** ORM 모델에서 `birth_date` NOT NULL 반영, `auth_provider` 필드 선언으로 google/local 계정 구분 가능하게 함.

---

### 3. backend/app/api/v1/auth.py 수정

**변경 전:**
```python
from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.orm import Session
from authlib.integrations.httpx_client import AsyncOAuth2Client

# ...

def _build_login_response(user: User) -> LoginResponse:
    return LoginResponse(
        message="Login successful",
        user_id=user.id,
        email=user.email,
        nickname=user.nickname,
        role=user.role
    )

# Google 유저 생성 코드
    if not user:
        user = User(
            email=google_user["email"],
            nickname=google_user.get("name", google_user["email"].split("@")[0]),
            password=None,
            birth_date=None,
            role="user"
        )

# 일반 회원가입 코드
    db_user = User(
        email=user.email,
        nickname=user.nickname,
        password=hash_password(user.password),
        birth_date=user.birth_date,
        role="user"
    )
```

**변경 후:**
```python
from fastapi import APIRouter, Depends, HTTPException, status
from sqlalchemy.orm import Session
from authlib.integrations.httpx_client import AsyncOAuth2Client
from datetime import date

# ...

def _build_login_response(user: User) -> LoginResponse:
    return LoginResponse(
        message="Login successful",
        user_id=user.id,
        email=user.email,
        nickname=user.nickname,
        role=user.role,
        auth_provider=user.auth_provider
    )

# Google 유저 생성 코드
    if not user:
        user = User(
            email=google_user["email"],
            nickname=google_user.get("name", google_user["email"].split("@")[0]),
            password=None,
            birth_date=date.today(),
            auth_provider='google',
            role="user"
        )

# 일반 회원가입 코드
    db_user = User(
        email=user.email,
        nickname=user.nickname,
        password=hash_password(user.password),
        birth_date=user.birth_date,
        auth_provider='local',
        role="user"
    )
```

**변경 의도:** Google 신규 유저 생성 시 `birth_date=date.today()`, `auth_provider='google'` 설정. 로그인 응답에 `auth_provider` 포함.

---

### 4. backend/app/api/v1/users.py 수정

**변경 전:**
```python
    db_user = User(
        email=user.email,
        nickname=user.nickname,
        password=hash_password(user.password),
        birth_date=user.birth_date,
        role=user.role
    )
```

**변경 후:**
```python
    db_user = User(
        email=user.email,
        nickname=user.nickname,
        password=hash_password(user.password),
        birth_date=user.birth_date,
        auth_provider='local',
        role=user.role
    )
```

**변경 의도:** 범용 유저 생성 엔드포인트에서도 `auth_provider='local'`을 명시하여 코드 일관성 확보.

---

### 5. backend/app/schemas/user.py 수정

**변경 전:**
```python
class UserResponse(BaseModel):
    """사용자 응답 (password 제외)"""
    id: int
    email: str
    nickname: str
    birth_date: date
    role: str

    class Config:
        from_attributes = True


class LoginResponse(BaseModel):
    """로그인 응답"""
    message: str
    user_id: int
    email: str
    nickname: str
    role: str
```

**변경 후:**
```python
class UserResponse(BaseModel):
    """사용자 응답 (password 제외)"""
    id: int
    email: str
    nickname: str
    birth_date: date
    auth_provider: str
    role: str

    class Config:
        from_attributes = True


class LoginResponse(BaseModel):
    """로그인 응답"""
    message: str
    user_id: int
    email: str
    nickname: str
    role: str
    auth_provider: str
```

**변경 의도:** API 응답 스키마에 `auth_provider` 포함 — 프론트엔드에서 계정 유형 식별 가능.

---

### 6. frontend/src/pages/Login/Login.jsx 수정

**변경 전:**
```jsx
  const googleLogin = useGoogleLogin({
    flow: 'auth-code',
    redirect_uri: 'https://hexsera.com',
    onSuccess: async (codeResponse) => {
      const res = await axios.post('/api/v1/auth/google', { code: codeResponse.code });
      if (res.status === 200) {
        login({ id: res.data.user_id, name: res.data.nickname, role: res.data.role, email: res.data.email });
        navigate(res.data.role === 'admin' ? '/admin' : '/');
      }
    },
    onError: () => alert('구글 로그인에 실패했습니다.'),
  });

  // handleLogin 내부
        login({ id: response.data.user_id, name: response.data.nickname, role: response.data.role, email: response.data.email });

  // Google 버튼
                <Box sx={{ display: 'flex', justifyContent: 'center', mt: 2 }}>
                  <button onClick={() => googleLogin()}>구글로 로그인</button>
                </Box>
```

**변경 후:**
```jsx
  const googleLogin = useGoogleLogin({
    flow: 'auth-code',
    redirect_uri: 'https://hexsera.com',
    onSuccess: async (codeResponse) => {
      const res = await axios.post('/api/v1/auth/google', { code: codeResponse.code });
      if (res.status === 200) {
        login({ id: res.data.user_id, name: res.data.nickname, role: res.data.role, email: res.data.email, authProvider: res.data.auth_provider });
        navigate(res.data.role === 'admin' ? '/admin' : '/');
      }
    },
    onError: () => alert('구글 로그인에 실패했습니다.'),
  });

  // handleLogin 내부
        login({ id: response.data.user_id, name: response.data.nickname, role: response.data.role, email: response.data.email, authProvider: response.data.auth_provider });

  // Google 버튼
                <Box sx={{ display: 'flex', justifyContent: 'center', mt: 2 }}>
                  <button
                    onClick={() => googleLogin()}
                    style={{
                      display: 'flex',
                      alignItems: 'center',
                      gap: '12px',
                      backgroundColor: '#ffffff',
                      border: '1px solid #dadce0',
                      borderRadius: '4px',
                      padding: '10px 24px',
                      cursor: 'pointer',
                      fontFamily: 'Roboto, sans-serif',
                      fontSize: '14px',
                      fontWeight: '500',
                      color: '#3c4043',
                      boxShadow: '0 1px 2px rgba(0,0,0,0.2)',
                      width: '300px',
                      justifyContent: 'center',
                    }}
                    onMouseEnter={e => e.currentTarget.style.backgroundColor = '#f8f9fa'}
                    onMouseLeave={e => e.currentTarget.style.backgroundColor = '#ffffff'}
                  >
                    <svg width="18" height="18" viewBox="0 0 18 18" xmlns="http://www.w3.org/2000/svg">
                      <path d="M17.64 9.2c0-.637-.057-1.251-.164-1.84H9v3.481h4.844c-.209 1.125-.843 2.078-1.796 2.717v2.258h2.908c1.702-1.567 2.684-3.875 2.684-6.615z" fill="#4285F4"/>
                      <path d="M9 18c2.43 0 4.467-.806 5.956-2.18l-2.908-2.259c-.806.54-1.837.86-3.048.86-2.344 0-4.328-1.584-5.036-3.711H.957v2.332A8.997 8.997 0 0 0 9 18z" fill="#34A853"/>
                      <path d="M3.964 10.71A5.41 5.41 0 0 1 3.682 9c0-.593.102-1.17.282-1.71V4.958H.957A8.996 8.996 0 0 0 0 9c0 1.452.348 2.827.957 4.042l3.007-2.332z" fill="#FBBC05"/>
                      <path d="M9 3.58c1.321 0 2.508.454 3.44 1.345l2.582-2.58C13.463.891 11.426 0 9 0A8.997 8.997 0 0 0 .957 4.958L3.964 6.29C4.672 4.163 6.656 3.58 9 3.58z" fill="#EA4335"/>
                    </svg>
                    Google로 로그인
                  </button>
                </Box>
```

**변경 의도:** Google 공식 브랜드 가이드라인 버튼 적용 (SVG 로고 인라인 포함, hover 효과). 로그인 성공 시 `authProvider` 필드를 localStorage에 저장.

---

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| Google 로그인 (신규 유저 생성) | birth_date=today(), auth_provider='google'으로 저장 | 미확인 |
| 일반 로그인 | LoginResponse에 auth_provider 추가 반환 | 미확인 |
| 일반 회원가입 | auth_provider='local' 명시 저장 | 미확인 |
| localStorage 유저 정보 | authProvider 필드 추가 저장 | 미확인 |
| Google 로그인 버튼 UI | 공식 Google 디자인 버튼으로 교체 | 미확인 |
| DB users 테이블 | auth_provider 컬럼 추가, birth_date NOT NULL 변환 | 정상 (마이그레이션 성공) |

## 결과
- 상태: 완료 (마이그레이션 적용 완료, 백엔드 재시작 완료)

## 배운내용

## 다음 작업
- Google 로그인 후 신규 유저의 `birth_date`가 오늘 날짜로 저장되는지 확인
- 브라우저 localStorage에 `authProvider: "google"` 저장 여부 확인
- 일반 로그인 후 `authProvider: "local"` 저장 여부 확인
- Google 로그인 버튼 UI 화면에서 확인


