# 업무일지

## 기본 정보
- 날짜: 2026-05-19 (화)
- 작성자: hexsera

## 진행 목표

홈페이지 월간 랭킹 fetch를 React Query로 교체하여 로딩 상태 표시, 캐시, 주기적 자동 갱신을 적용한다.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| React Query 패키지 설치 | `frontend/package.json` | `@tanstack/react-query` 추가 |
| QueryClientProvider 설정 | `frontend/src/main.jsx` | 앱 최상단 감싸기 |
| useQuery 적용 및 UI 분기 처리 | `frontend/src/pages/HomePage/HomePage.jsx` | useEffect/useState 제거, isPending/isError 추가 |
| 프론트엔드 빌드 및 배포 | `frontend/dist/` | nginx 컨테이너에 배포 |

**총 약 20분**

## 주요 변경 사항

### 1. `frontend/src/main.jsx` 수정

**변경 전:**
```jsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import { GoogleOAuthProvider } from '@react-oauth/google';
import App from './App.jsx';

const rootElement = document.getElementById('root');
const root = ReactDOM.createRoot(rootElement);

root.render(
  <GoogleOAuthProvider clientId={import.meta.env.VITE_GOOGLE_CLIENT_ID}>
    <App />
  </GoogleOAuthProvider>
);
```

**변경 후:**
```jsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import { GoogleOAuthProvider } from '@react-oauth/google';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import App from './App.jsx';

const queryClient = new QueryClient();

const rootElement = document.getElementById('root');
const root = ReactDOM.createRoot(rootElement);

root.render(
  <QueryClientProvider client={queryClient}>
    <GoogleOAuthProvider clientId={import.meta.env.VITE_GOOGLE_CLIENT_ID}>
      <App />
    </GoogleOAuthProvider>
  </QueryClientProvider>
);
```

**변경 의도:** 앱 전체에서 `useQuery`를 사용하기 위해 `QueryClientProvider`로 감싸는 설정 추가.

---

### 2. `frontend/src/pages/HomePage/HomePage.jsx` 수정

**변경 전:**
```jsx
import { useState, useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
import {
  Box, Grid, Container,
  Table, TableBody, TableCell, TableContainer,
  TableHead, TableRow, Paper, Typography, Button
} from '@mui/material';
import axios from 'axios';

// ...

function HomePage() {
  const navigate = useNavigate();

  const [ranking, setRanking] = useState([]);
  const [rankingError, setRankingError] = useState(false);

  useEffect(() => {
    axios.get('/api/v1/monthly-scores')
      .then((res) => {
        setRanking(res.data.scores.slice(0, 10));
      })
      .catch(() => {
        setRankingError(true);
      });
  }, []);

  // ...

  {rankingError ? (
    <Typography sx={{ color: COLORS.subText }}>랭킹을 불러올 수 없습니다.</Typography>
  ) : (
    <TableContainer>...</TableContainer>
  )}
}
```

**변경 후:**
```jsx
import { useNavigate } from 'react-router-dom';
import {
  Box, Grid, Container,
  Table, TableBody, TableCell, TableContainer,
  TableHead, TableRow, Paper, Typography, Button
} from '@mui/material';
import { useQuery } from '@tanstack/react-query';
import axios from 'axios';

// ...

function HomePage() {
  const navigate = useNavigate();

  const { data: ranking = [], isPending, isError } = useQuery({
    queryKey: ['monthly-scores'],
    queryFn: () => axios.get('/api/v1/monthly-scores').then(res => res.data.scores.slice(0, 10)),
    staleTime: 1000 * 60 * 2,
    refetchInterval: 1000 * 30,
  });

  // ...

  {isPending ? (
    <Typography sx={{ color: COLORS.subText }}>랭킹을 불러오는 중...</Typography>
  ) : isError ? (
    <Typography sx={{ color: COLORS.subText }}>랭킹을 불러올 수 없습니다.</Typography>
  ) : (
    <TableContainer>...</TableContainer>
  )}
}
```

**변경 의도:** `useEffect` + `useState` 직접 fetch 방식을 React Query로 교체. `staleTime` 2분으로 재방문 시 캐시 재사용, `refetchInterval` 30초로 주기적 자동 갱신, `isPending`으로 로딩 상태 표시 추가.

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| 홈페이지 월간 랭킹 표시 | useQuery로 fetch 방식 교체 | 정상 |
| 홈페이지 랭킹 로딩 상태 | isPending으로 "불러오는 중..." 텍스트 추가 | 정상 |
| 홈페이지 랭킹 에러 처리 | isError로 분기 처리 | 정상 |
| 앱 전체 | QueryClientProvider 추가 | 정상 |

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- FriendPage React Query 적용
- UserInfo 페이지 React Query 적용
- AdminUserMain React Query 적용
- 공지사항 ETag + Cache-Control 구현
