# 업무일지

## 기본 정보
- 날짜: 2026-05-22 (금)
- 작성자: hexsera

## 진행 목표
공지사항 목록/상세 API에 HTTP 캐시(`Cache-Control`, `ETag`)를 적용해 반복 요청 시 서버 트래픽을 줄인다.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| 목록 API에 Cache-Control 헤더 추가 | `backend/app/api/v1/notices.py` | `public, max-age=600` (10분) |
| 상세 API에 Cache-Control + ETag 헤더 추가 | `backend/app/api/v1/notices.py` | ETag는 응답 바디 MD5 해시, 304 반환 로직 포함 |
| ETag 동작 curl로 검증 | — | 304 반환 및 DB 수정 후 200 전환 확인 |

**총 1시간**

## 주요 변경 사항

### 1. `backend/app/api/v1/notices.py` 수정

**변경 전:**
```python
import sys
sys.path.insert(0, '/code')
from models import Notice
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.orm import Session
from app.api.deps import get_db
from app.schemas.notices import (
    NoticeCreateRequest, NoticeResponse, NoticeListResult
)

router = APIRouter()


@router.get("", response_model=NoticeListResult)
def get_notices(skip: int = 0, limit: int = 10, db: Session = Depends(get_db)):
    total = db.query(Notice).count()
    items = db.query(Notice).order_by(Notice.created_at.desc()).offset(skip).limit(limit).all()
    return {"items": items, "total": total}


@router.get("/{notice_id}", response_model=NoticeResponse)
def get_notice(notice_id: int, db: Session = Depends(get_db)):
    notice = db.query(Notice).filter(Notice.id == notice_id).first()
    if not notice:
        raise HTTPException(status_code=404, detail="Notice not found")
    return notice
```

**변경 후:**
```python
import sys
import hashlib
import json
sys.path.insert(0, '/code')
from models import Notice
from fastapi import APIRouter, Depends, HTTPException, Request, Response
from fastapi.responses import Response as FastAPIResponse
from sqlalchemy.orm import Session
from app.api.deps import get_db
from app.schemas.notices import (
    NoticeCreateRequest, NoticeResponse, NoticeListResult
)

router = APIRouter()


@router.get("", response_model=NoticeListResult)
def get_notices(skip: int = 0, limit: int = 10, db: Session = Depends(get_db), response: Response = None):
    total = db.query(Notice).count()
    items = db.query(Notice).order_by(Notice.created_at.desc()).offset(skip).limit(limit).all()
    response.headers["Cache-Control"] = "public, max-age=600"
    return {"items": items, "total": total}


@router.get("/{notice_id}")
def get_notice(notice_id: int, request: Request, db: Session = Depends(get_db)):
    notice = db.query(Notice).filter(Notice.id == notice_id).first()
    if not notice:
        raise HTTPException(status_code=404, detail="Notice not found")

    body = json.dumps({
        "id": notice.id,
        "title": notice.title,
        "content": notice.content,
        "author_id": notice.author_id,
        "created_at": notice.created_at.isoformat(),
        "updated_at": notice.updated_at.isoformat(),
    }, ensure_ascii=False)

    etag = f'"{hashlib.md5(body.encode()).hexdigest()}"'

    if request.headers.get("If-None-Match") == etag:
        return FastAPIResponse(status_code=304, headers={"ETag": etag, "Cache-Control": "public, max-age=3600"})

    return FastAPIResponse(
        content=body,
        media_type="application/json",
        headers={
            "Cache-Control": "public, max-age=600",
            "ETag": etag,
        }
    )
```

**변경 의도:** 공지 상세는 base64 이미지 포함으로 응답이 크기 때문에, 내용이 바뀌지 않으면 서버가 304만 응답해 바디 전송을 생략한다. 목록은 ETag 없이 max-age만으로 충분하다(글 하나만 바뀌어도 ETag가 달라져 절약 효과 없음).

## 캐시 동작 흐름

```
브라우저 (첫 요청)
    │
    ▼
GET /api/v1/notices/34
    │
    ▼
FastAPI → 응답 바디 MD5 해시 → ETag 생성
    │
    ▼
200 OK + Cache-Control: public, max-age=600 + ETag: "abc123"
    │
    ▼
브라우저 캐시 저장 (600초)

── 600초 경과 후 ──

브라우저 (재요청)
    │
    ▼
GET /api/v1/notices/34 + If-None-Match: "abc123"
    │
    ├─ 내용 변경 없음 → 304 Not Modified (바디 없음, 트래픽 절감)
    │
    └─ 내용 변경됨  → 200 OK + 새 바디 + 새 ETag
```

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| 공지사항 목록 조회 (`GET /api/v1/notices`) | Cache-Control 헤더 추가 | 정상 |
| 공지사항 상세 조회 (`GET /api/v1/notices/{id}`) | Cache-Control + ETag 추가, 304 반환 로직 | 정상 |
| 공지사항 생성 (`POST /api/v1/notices`) | 변경 없음 | 정상 |
| 공지사항 삭제 (`DELETE /api/v1/notices/{id}`) | 변경 없음 | 정상 |

## 결과
- 상태: 완료
- curl로 검증: `If-None-Match` 일치 시 304, DB 수정 후 동일 ETag로 요청 시 200 확인

## 배운 내용

## 다음 작업
- 공지사항 수정 기능(`PUT /api/v1/notices/{id}`) 구현 (현재 미구현)
- 목록 캐시 무효화 전략 검토 (공지 추가/삭제 시 브라우저 캐시가 즉시 반영되지 않는 문제)
