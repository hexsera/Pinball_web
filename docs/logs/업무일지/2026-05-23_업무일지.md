# 업무일지

## 기본 정보
- 날짜: 2026-05-23 (토)
- 작성자: hexsera

## 진행 목표
공지사항 리소스에 ACL을 적용하기 위한 DB 구조 설계 및 생성. Phase 1(DB)만 완료, Phase 2(API 연동)는 다음 작업으로 분리.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| ACL 개념 학습 및 설계 논의 | `docs/history/2026-05-23-46b663-01-acl-rbac-설계논의.md` | 신규 생성 |
| AclPermission Enum 추가 | `backend/models.py` | `read`, `manage` 두 가지 권한 |
| AclEntry 모델 추가 | `backend/models.py` | 범용 ACL 테이블 |
| Alembic 마이그레이션 생성 및 적용 | `backend/alembic/versions/e389a7b38f3d_add_acl_entries_table.py` | acl_entries 테이블 DB 생성 |
| 실행계획 작성 | `PRD/공지사항-ACL-실행계획.md` | Phase 1/2 분리 |

**총 3시간**

## 주요 변경 사항

### 1. `backend/models.py` 수정

**변경 전:**
```python
from sqlalchemy import Column, Integer, String, Date, DateTime, Boolean, UniqueConstraint, CheckConstraint, ForeignKey, Text
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.sql import func
import uuid
from app.db.session import Base


class User(Base):
    __tablename__ = "users"

    id = Column(Integer, primary_key=True, index=True)
    user_id = Column(UUID(as_uuid=True), unique=True, nullable=True,
                    default=uuid.uuid4, index=True)
    email = Column(String(255), unique=True, index=True, nullable=False)
    nickname = Column(String(100), nullable=False)
    password = Column(String(255), nullable=True)
    birth_date = Column(Date, nullable=False)
    role = Column(String(20), nullable=False, default='user')
    auth_provider = Column(String(20), nullable=False, default='local')
    google_id = Column(String(255), nullable=True, unique=True)


# ... (Friendship, MonthlyScore, GameVisit 동일)


class Notice(Base):
    __tablename__ = "notices"

    id         = Column(Integer, primary_key=True, index=True)
    title      = Column(String(255), nullable=False)
    content    = Column(Text, nullable=False)
    author_id  = Column(Integer, ForeignKey('users.id'), nullable=False)
    created_at = Column(DateTime, nullable=False, server_default=func.now())
    updated_at = Column(DateTime, nullable=False, server_default=func.now(), onupdate=func.now())
```

**변경 후:**
```python
from enum import Enum
from sqlalchemy import Column, Integer, String, Date, DateTime, Boolean, UniqueConstraint, CheckConstraint, ForeignKey, Text
from sqlalchemy.dialects.postgresql import UUID
from sqlalchemy.sql import func
import uuid
from app.db.session import Base


class User(Base):
    __tablename__ = "users"

    id = Column(Integer, primary_key=True, index=True)
    user_id = Column(UUID(as_uuid=True), unique=True, nullable=True,
                    default=uuid.uuid4, index=True)
    email = Column(String(255), unique=True, index=True, nullable=False)
    nickname = Column(String(100), nullable=False)
    password = Column(String(255), nullable=True)
    birth_date = Column(Date, nullable=False)
    role = Column(String(20), nullable=False, default='user')
    auth_provider = Column(String(20), nullable=False, default='local')
    google_id = Column(String(255), nullable=True, unique=True)


# ... (Friendship, MonthlyScore, GameVisit 동일)


class Notice(Base):
    __tablename__ = "notices"

    id         = Column(Integer, primary_key=True, index=True)
    title      = Column(String(255), nullable=False)
    content    = Column(Text, nullable=False)
    author_id  = Column(Integer, ForeignKey('users.id'), nullable=False)
    created_at = Column(DateTime, nullable=False, server_default=func.now())
    updated_at = Column(DateTime, nullable=False, server_default=func.now(), onupdate=func.now())


class AclPermission(str, Enum):
    read   = "read"
    manage = "manage"


class AclEntry(Base):
    __tablename__ = "acl_entries"

    id            = Column(Integer, primary_key=True, index=True)
    actor_id      = Column(String(50), nullable=False, index=True)
    resource_type = Column(String(50), nullable=False, index=True)
    resource_id   = Column(Integer, nullable=False, index=True)
    permission    = Column(String(20), nullable=False)

    __table_args__ = (
        UniqueConstraint('actor_id', 'resource_type', 'resource_id', 'permission', name='uq_acl_entry'),
        CheckConstraint("permission IN ('read', 'manage')", name='ck_acl_permission'),
    )
```

**변경 의도:** 공지사항 ACL 적용을 위해 범용 ACL 테이블 모델 추가. `resource_type` 컬럼으로 다른 리소스에도 재사용 가능하도록 설계.

---

### 2. `backend/alembic/versions/e389a7b38f3d_add_acl_entries_table.py` 생성

**추가:**
```python
"""add acl_entries table

Revision ID: e389a7b38f3d
Revises: 77047707274c
Create Date: 2026-05-23 11:36:30.956220
"""
from alembic import op
import sqlalchemy as sa

revision = 'e389a7b38f3d'
down_revision = '77047707274c'
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.drop_index('ix_notice_acl_entries_actor_id', table_name='notice_acl_entries')
    op.drop_index('ix_notice_acl_entries_id', table_name='notice_acl_entries')
    op.drop_index('ix_notice_acl_entries_resource_id', table_name='notice_acl_entries')
    op.drop_table('notice_acl_entries')


def downgrade() -> None:
    op.create_table('notice_acl_entries',
    sa.Column('id', sa.INTEGER(), autoincrement=True, nullable=False),
    sa.Column('actor_id', sa.VARCHAR(length=50), autoincrement=False, nullable=False),
    sa.Column('resource_id', sa.INTEGER(), autoincrement=False, nullable=False),
    sa.Column('permission', sa.VARCHAR(length=20), autoincrement=False, nullable=False),
    sa.CheckConstraint("permission::text = ANY (ARRAY['read'::character varying, 'manage'::character varying]::text[])", name='ck_notice_acl_permission'),
    sa.ForeignKeyConstraint(['resource_id'], ['notices.id'], name='notice_acl_entries_resource_id_fkey', ondelete='CASCADE'),
    sa.PrimaryKeyConstraint('id', name='notice_acl_entries_pkey'),
    sa.UniqueConstraint('actor_id', 'resource_id', 'permission', name='uq_notice_acl')
    )
    op.create_index('ix_notice_acl_entries_resource_id', 'notice_acl_entries', ['resource_id'], unique=False)
    op.create_index('ix_notice_acl_entries_id', 'notice_acl_entries', ['id'], unique=False)
    op.create_index('ix_notice_acl_entries_actor_id', 'notice_acl_entries', ['actor_id'], unique=False)
```

**변경 의도:** 공지사항 전용 테이블(`notice_acl_entries`)에서 범용 테이블(`acl_entries`)로 전환. `resource_type` 컬럼 추가로 여러 리소스 타입을 하나의 테이블로 관리.

> **롤백 방법**: `alembic downgrade -1` 실행 시 `notice_acl_entries` 테이블로 복원됨. `acl_entries` 테이블은 별도로 수동 삭제 필요.

---

### 3. `PRD/공지사항-ACL-실행계획.md` 생성

**추가:** Phase 1(DB), Phase 2(API 연동)로 분리된 실행계획 문서 신규 생성.

---

### 4. `docs/history/2026-05-23-46b663-01-acl-rbac-설계논의.md` 생성

**추가:** ACL 개념 학습 내용을 담은 설계 논의 문서 신규 생성.
- ACL 핵심 개념, 데이터 모델 설계
- permission enum 관리, ACL 항목 생성 시점 및 트랜잭션
- default-allow 패턴, `*` actor, 명시적 규칙 우선순위
- 테이블 구조 선택(하나 vs 여러 개), 소유자 개념

## DB 구조

```
acl_entries
┌────┬──────────┬───────────────┬─────────────┬────────────┐
│ id │ actor_id │ resource_type │ resource_id │ permission │
└────┴──────────┴───────────────┴─────────────┴────────────┘
     VARCHAR(50)  VARCHAR(50)    INTEGER       VARCHAR(20)
     ("*" 허용)   ("notice" 등)               ("read"/"manage")
```

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| acl_entries 테이블 | 신규 생성 | 정상 |
| 공지사항 CRUD | 아직 ACL 미연동 (Phase 2 예정) | 정상 (기존 동작 유지) |

## 결과
- 상태: 진행중 (Phase 1 완료 / Phase 2 미완료)

## 배운내용

## 다음 작업
- `acl_resources` 테이블 추가 (리소스 타입 유효성 FK 보장)
- Phase 2: API 연동 (`can_notice()` 함수, create/delete/update 권한 체크)
