# 업무일지

## 기본 정보
- 날짜: 2026-05-07 (수)
- 작성자: hexsera

## 진행 목표

JWT 인증 3단계 구현: 페이지 로드 시 여러 API 요청이 동시에 401을 받을 때 `/auth/refresh`가 중복 호출되지 않도록 동시 401 큐잉 처리 추가.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| 동시 401 큐잉 로직 추가 | `frontend/src/lib/api.js` | `isRefreshing` 플래그 + `failedQueue` 도입 |
| 프론트엔드 빌드 및 배포 | `frontend/dist/` → `nginx-server` 컨테이너 | `docker cp`로 nginx에 반영 |

**총 약 30분**

## 주요 변경 사항

### 1. frontend/src/lib/api.js 수정

**변경 전:**
```javascript
import axios from 'axios';
import { useAuthStore } from '../store/authStore';

const api = axios.create({ baseURL: '/api/v1' });

api.interceptors.request.use((config) => {
  const token = useAuthStore.getState().accessToken;
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

api.interceptors.response.use(
  (res) => res,
  async (error) => {
    const original = error.config;
    if (error.response?.status === 401 && !original._retry) {
      original._retry = true;
      try {
        const { data } = await axios.post('/api/v1/auth/refresh', {}, { withCredentials: true });
        useAuthStore.getState().setAccessToken(data.access_token);
        original.headers.Authorization = `Bearer ${data.access_token}`;
        return api(original);
      } catch {
        useAuthStore.getState().clearAccessToken();
        window.location.href = '/login';
      }
    }
    return Promise.reject(error);
  }
);

export default api;
```

**변경 후:**
```javascript
import axios from 'axios';
import { useAuthStore } from '../store/authStore';

const api = axios.create({ baseURL: '/api/v1' });

let isRefreshing = false;
let failedQueue = [];

const processQueue = (error, token = null) => {
  failedQueue.forEach((prom) => {
    if (error) prom.reject(error);
    else prom.resolve(token);
  });
  failedQueue = [];
};

api.interceptors.request.use((config) => {
  const token = useAuthStore.getState().accessToken;
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});

api.interceptors.response.use(
  (res) => res,
  async (error) => {
    const original = error.config;
    if (error.response?.status !== 401 || original._retry) {
      return Promise.reject(error);
    }

    if (isRefreshing) {
      return new Promise((resolve, reject) => {
        failedQueue.push({ resolve, reject });
      })
        .then((token) => {
          original.headers.Authorization = `Bearer ${token}`;
          return api(original);
        })
        .catch((err) => Promise.reject(err));
    }

    original._retry = true;
    isRefreshing = true;

    try {
      const { data } = await axios.post('/api/v1/auth/refresh', {}, { withCredentials: true });
      useAuthStore.getState().setAccessToken(data.access_token);
      processQueue(null, data.access_token);
      original.headers.Authorization = `Bearer ${data.access_token}`;
      return api(original);
    } catch (refreshError) {
      processQueue(refreshError, null);
      useAuthStore.getState().clearAccessToken();
      window.location.href = '/login';
      return Promise.reject(refreshError);
    } finally {
      isRefreshing = false;
    }
  }
);

export default api;
```

**변경 의도:** 동시에 여러 401이 발생했을 때 `/auth/refresh`가 중복 호출되는 문제 방지. 첫 번째 401만 갱신을 수행하고 나머지는 큐에 대기시킨 후, 갱신 완료 시 새 토큰으로 일괄 재시도한다. 4단계 Refresh Token Rotation 도입의 전제 조건이기도 하다.

## 동시 401 처리 흐름

```
여러 요청이 동시에 401 수신
         │
         ▼
  첫 번째 401?
  ┌───────┴───────┐
  YES             NO (isRefreshing=true)
  │               │
  ▼               ▼
isRefreshing    failedQueue에 등록
= true          (Promise로 대기)
  │
  ▼
/auth/refresh 호출
  │
  ┌───────┴───────┐
 성공             실패
  │               │
  ▼               ▼
processQueue    processQueue
(새 토큰 전달)  (에러 전달)
  │               │
  ▼               ▼
큐 대기 요청    큐 대기 요청
일괄 재시도     전부 실패 처리
                  │
                  ▼
              /login 리다이렉트
```

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| API 요청 인증 (Access Token 자동 부착) | 변경 없음 (요청 인터셉터 그대로) | 정상 |
| 401 수신 시 자동 토큰 갱신 | 동시 401 큐잉 로직 추가 | 정상 |
| 갱신 실패 시 로그아웃 처리 | 큐에 대기 중인 요청들도 일괄 실패 처리 후 /login 이동 | 미확인 |

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- 4단계: Refresh Token Rotation (백엔드 — refresh 호출마다 새 Refresh 발급, 옛 Refresh 무효화)
- 5단계: 새로고침 대응 정리 (localStorage Access Token 의존 제거)

---

# 업무일지 (2)

## 기본 정보
- 날짜: 2026-05-07 (수)
- 작성자: hexsera

## 진행 목표

JWT 인증 5단계 구현: Access Token의 localStorage 의존을 제거하고, 앱 시작 시 Refresh Token 쿠키로 Access Token을 메모리에 복원하는 방식으로 전환. 併行해서 토큰 없음 상태에서 백엔드가 403 대신 401을 반환하도록 수정.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| HTTPBearer auto_error=False 설정 | `backend/app/api/deps.py` | 헤더 없을 때 403→401로 통일 |
| localStorage 읽기/쓰기 제거 | `frontend/src/store/authStore.js` | Access Token 메모리 전용으로 변경 |
| 앱 시작 시 refresh로 상태 복원 | `frontend/src/contexts/AuthContext.jsx` | localStorage user 의존 제거 |
| 백엔드 컨테이너 재시작 | — | FastAPI 변경사항 반영 |
| 프론트엔드 빌드 및 배포 | `frontend/dist/` → `nginx-server` 컨테이너 | `docker cp`로 nginx에 반영 |

**총 약 1시간**

## 주요 변경 사항

### 1. backend/app/api/deps.py 수정

**변경 전:**
```python
bearer_scheme = HTTPBearer()


def get_current_user(
    credentials: HTTPAuthorizationCredentials = Depends(bearer_scheme),
    db: Session = Depends(get_db),
) -> User:
    """JWT Bearer 토큰 검증 의존성"""
    token = credentials.credentials
    try:
        payload = jwt.decode(token, settings.JWT_SECRET_KEY, algorithms=[settings.JWT_ALGORITHM])
        user_id = int(payload.get("sub"))
    except (JWTError, TypeError, ValueError):
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid token")
    user = db.query(User).filter(User.id == user_id).first()
    if not user:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="User not found")
    return user
```

**변경 후:**
```python
bearer_scheme = HTTPBearer(auto_error=False)


def get_current_user(
    credentials: HTTPAuthorizationCredentials | None = Depends(bearer_scheme),
    db: Session = Depends(get_db),
) -> User:
    """JWT Bearer 토큰 검증 의존성"""
    if credentials is None:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Not authenticated")
    token = credentials.credentials
    try:
        payload = jwt.decode(token, settings.JWT_SECRET_KEY, algorithms=[settings.JWT_ALGORITHM])
        user_id = int(payload.get("sub"))
    except (JWTError, TypeError, ValueError):
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid token")
    user = db.query(User).filter(User.id == user_id).first()
    if not user:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="User not found")
    return user
```

**변경 의도:** `HTTPBearer(auto_error=True)` 기본값은 Authorization 헤더가 없을 때 FastAPI가 자동으로 403을 반환한다. 프론트 인터셉터는 401만 처리하므로 403은 refresh 흐름을 타지 못했다. `auto_error=False`로 변경해 헤더 없음도 401로 통일하여 인터셉터가 모든 인증 실패 상황에서 refresh를 시도하도록 한다.

---

### 2. frontend/src/store/authStore.js 수정

**변경 전:**
```javascript
import { create } from 'zustand';

export const useAuthStore = create((set) => ({
  accessToken: localStorage.getItem('access_token') || null,
  setAccessToken: (token) => {
    localStorage.setItem('access_token', token);
    set({ accessToken: token });
  },
  clearAccessToken: () => {
    localStorage.removeItem('access_token');
    set({ accessToken: null });
  },
}));
```

**변경 후:**
```javascript
import { create } from 'zustand';

export const useAuthStore = create((set) => ({
  accessToken: null,
  setAccessToken: (token) => set({ accessToken: token }),
  clearAccessToken: () => set({ accessToken: null }),
}));
```

**변경 의도:** Access Token을 localStorage에 저장하면 XSS 공격으로 탈취 가능하다. 메모리(JS 변수)에만 저장하도록 변경해 XSS 위협 표면을 최소화한다. 새로고침 시 Access Token 소실은 AuthContext에서 앱 시작 시 refresh 호출로 복원한다.

---

### 3. frontend/src/contexts/AuthContext.jsx 수정

**변경 전:**
```javascript
import { createContext, useContext, useState, useEffect } from 'react';
import { useAuthStore } from '../store/authStore';
import api from '../lib/api';

export const AuthContext = createContext();

export function AuthProvider({ children }) {
  const [isLoggedIn, setIsLoggedIn] = useState(false);
  const [user, setUser] = useState(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    const savedUser = localStorage.getItem('user');
    if (savedUser) {
      setUser(JSON.parse(savedUser));
      setIsLoggedIn(true);
    }
    setLoading(false);
  }, []);

  const login = (userData, accessToken) => {
    setUser(userData);
    setIsLoggedIn(true);
    localStorage.setItem('user', JSON.stringify(userData));
    useAuthStore.getState().setAccessToken(accessToken);
  };

  const logout = async () => {
    try {
      await api.post('/auth/logout', {}, { withCredentials: true });
    } finally {
      setUser(null);
      setIsLoggedIn(false);
      localStorage.removeItem('user');
      useAuthStore.getState().clearAccessToken();
    }
  };

  return (
    <AuthContext.Provider value={{ isLoggedIn, user, loading, login, logout }}>
      {children}
    </AuthContext.Provider>
  );
}

export function useAuth() {
  return useContext(AuthContext);
}
```

**변경 후:**
```javascript
import { createContext, useContext, useState, useEffect } from 'react';
import axios from 'axios';
import { useAuthStore } from '../store/authStore';
import api from '../lib/api';

export const AuthContext = createContext();

export function AuthProvider({ children }) {
  const [isLoggedIn, setIsLoggedIn] = useState(false);
  const [user, setUser] = useState(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    axios.post('/api/v1/auth/refresh', {}, { withCredentials: true })
      .then(({ data }) => {
        const token = data.access_token;
        useAuthStore.getState().setAccessToken(token);
        const payload = JSON.parse(atob(token.split('.')[1]));
        return axios.get(`/api/v1/users/${payload.sub}`, {
          headers: { Authorization: `Bearer ${token}` },
        });
      })
      .then(({ data }) => {
        setUser(data);
        setIsLoggedIn(true);
      })
      .catch(() => {
        // Refresh 쿠키 없거나 만료 → 비로그인 상태 유지
      })
      .finally(() => setLoading(false));
  }, []);

  const login = (userData, accessToken) => {
    setUser(userData);
    setIsLoggedIn(true);
    useAuthStore.getState().setAccessToken(accessToken);
  };

  const logout = async () => {
    try {
      await api.post('/auth/logout', {}, { withCredentials: true });
    } finally {
      setUser(null);
      setIsLoggedIn(false);
      useAuthStore.getState().clearAccessToken();
    }
  };

  return (
    <AuthContext.Provider value={{ isLoggedIn, user, loading, login, logout }}>
      {children}
    </AuthContext.Provider>
  );
}

export function useAuth() {
  return useContext(AuthContext);
}
```

**변경 의도:** localStorage의 `user` 데이터 의존을 제거하고, 앱 마운트 시 `/auth/refresh`를 호출해 Refresh 쿠키가 유효하면 Access Token을 메모리에 저장 후 `/users/{user_id}`로 유저 정보를 가져온다. Refresh 실패 시 비로그인 상태로 유지한다. `login()`과 `logout()`에서도 localStorage 읽기/쓰기를 제거했다.

## 새로고침 시 인증 복원 흐름

```
[페이지 새로고침]
        │
        ▼
  AuthProvider 마운트
        │
        ▼
  /api/v1/auth/refresh 호출
  (Refresh 쿠키 자동 첨부)
        │
   ┌────┴────┐
  성공       실패
   │         │
   ▼         ▼
새 Access  비로그인
Token 발급  상태 유지
   │
   ▼
/api/v1/users/{id} 호출
   │
   ▼
user, isLoggedIn 상태 설정
→ 로그인 상태로 화면 표시
```

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| 새로고침 후 로그인 상태 유지 | localStorage→refresh 쿠키 기반 복원으로 변경 | 정상 |
| 로그인 | localStorage 저장 제거, 메모리에만 저장 | 정상 |
| 로그아웃 | localStorage 삭제 제거 | 정상 |
| 토큰 없음 시 401 반환 | HTTPBearer auto_error=False로 403→401 통일 | 정상 |
| FriendPage 유저 정보 표시 | localStorage.getItem('user') 직접 참조 — 변경 영향받음 | 미확인 |
| 보호 라우트 접근 제한 | 라우트 가드 미구현으로 Refresh 만료 시 리다이렉트 안됨 | 비정상 |

## 결과
- 상태: 완료 (미해결 이슈 2건 별도 등록)

## 배운 내용

## 다음 작업
- 이슈 해결: 보호 라우트 가드 구현 (`docs/issue/2026-05-07-보호-라우트-가드-미구현.md`)
- 이슈 해결: FriendPage localStorage 직접 참조 제거 (`docs/issue/2026-05-07-FriendPage-localStorage-직접-참조.md`)
- 4단계: Refresh Token Rotation (백엔드)
