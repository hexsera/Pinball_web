# 목적
데이터베이스의 모든 테이블을 삭제한 후 다시 생성한다.
데이터베이스 내부 데이터는 모두 제거한다.
alembic 도 초기화가 추천된다면 초기화 한다.

## 상세사항
테이블 제거 전 모든 테이블의 constraints 를 체크한다. (model 이 아닌 DB 에서 확인한다.)
체크한 constraints 가 테이블이 다시 생성될때 유지되는지 확인한다.