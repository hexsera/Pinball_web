# 업무일지

## 기본 정보
- 날짜: 2026-03-07 (토) 13:00~13:50
- 작성자: hexsera

## 진행 목표

핀볼 플레이 데이터를 받아 Gemini AI로 플레이스타일(`attack`/`defence`)을 분석하는
`POST /api/v1/pinball_ai/playstyle` 엔드포인트를 TDD 방식으로 구현한다.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| RED 계획 문서 작성 | `PRD/TDD-RED-플레이스타일분석API-20260307.md` | 5개 테스트 케이스 정의 |
| RED 테스트 작성 | `backend/tests/test_pinball_ai.py` | 5개 테스트 전부 실패 확인 |
| 스키마 생성 | `backend/app/schemas/pinball_ai.py` | PlayDataPoint, PlaystyleResponse |
| 라우터 생성 | `backend/app/api/v1/pinball_ai.py` | POST /playstyle 엔드포인트 |
| 라우터 등록 | `backend/main.py` | prefix="/api/v1/pinball_ai" |
| Gemini 오류 처리 추가 | `backend/app/api/v1/pinball_ai.py` | try/except, print, success=False |
| GREEN 완료 보고서 작성 | `PRD/TDD-GREEN-플레이스타일분석API-20260307.md` | |

**총 1시간 30분**

## 주요 변경 사항

### 1. `backend/app/schemas/pinball_ai.py` 생성

**추가:**
```python
from typing import Literal
from pydantic import BaseModel


class PlayDataPoint(BaseModel):
    timestamp: float
    ball_x: float
    ball_y: float
    ball_vx: float
    ball_vy: float
    left_flipper: bool
    right_flipper: bool


PlaystyleRequest = list[PlayDataPoint]


class PlaystyleResponse(BaseModel):
    success: bool
    playstyle: Literal["attack", "defence"]
```

**특징:**
- `PlayDataPoint`: 프레임 단위 핀볼 플레이 데이터 한 점을 표현
- `PlaystyleResponse`: `success`로 Gemini 호출 성공 여부, `playstyle`은 분석 결과
- `Literal["attack", "defence"]`로 응답값 범위를 스키마 레벨에서 제한

---

### 2. `backend/app/api/v1/pinball_ai.py` 생성 및 오류 처리 추가

**최종 코드:**
```python
import random
from typing import Annotated
from google import genai
from fastapi import APIRouter, Body
from app.core.config import settings
from app.schemas.pinball_ai import PlayDataPoint, PlaystyleResponse

router = APIRouter()

PLAYSTYLE_PROMPT = (
    "다음은 핀볼 게임 플레이 데이터다. "
    "이 데이터를 분석하여 플레이어의 성향을 'attack' 또는 'defence' 중 하나로만 답해라. "
    "다른 텍스트 없이 단어 하나만 반환해라.\n\n"
)


@router.post("/playstyle", response_model=PlaystyleResponse)
def analyze_playstyle(
    play_data: Annotated[list[PlayDataPoint], Body(min_length=1)],
):
    client = genai.Client(api_key=settings.GEMINI_API_KEY)
    data_str = "\n".join(str(point.model_dump()) for point in play_data)
    try:
        response = client.models.generate_content(
            model="gemini-2.5-flash",
            contents=PLAYSTYLE_PROMPT + data_str,
        )
        playstyle = response.text.strip().lower()
        if playstyle not in ("attack", "defence"):
            playstyle = "attack"
        return PlaystyleResponse(success=True, playstyle=playstyle)
    except Exception as e:
        print(f"[pinball_ai] Gemini 오류: {e}")
        playstyle = random.choice(["attack", "defence"])
        return PlaystyleResponse(success=False, playstyle=playstyle)
```

**특징:**
- Gemini 클라이언트를 요청마다 1회용으로 생성 (PRD 명세)
- `Body(min_length=1)`로 빈 배열 요청을 422로 차단
- Gemini 오류 시 Docker 로그에 `[pinball_ai] Gemini 오류: ...` 출력
- 오류 발생 시 `success: false`, `playstyle`은 랜덤 반환

---

### 3. `backend/main.py` 수정

**변경 전:**
```python
from app.api.v1 import users, auth, monthly_scores, game_visits, friends, chat
...
app.include_router(chat.router, prefix="/api/v1", tags=["Chat"])
```

**변경 후:**
```python
from app.api.v1 import users, auth, monthly_scores, game_visits, friends, chat, pinball_ai
...
app.include_router(chat.router, prefix="/api/v1", tags=["Chat"])
app.include_router(pinball_ai.router, prefix="/api/v1/pinball_ai", tags=["Pinball AI"])
```

---

### 4. `backend/tests/test_pinball_ai.py` 생성

```
테스트 결과 (RED → GREEN)

  test_playstyle_returns_200              FAILED → PASSED
  test_playstyle_response_has_success_true  FAILED → PASSED
  test_playstyle_response_has_valid_playstyle FAILED → PASSED
  test_playstyle_empty_array_returns_422  FAILED → PASSED
  test_playstyle_missing_field_returns_422  FAILED → PASSED
```

**특징:**
- Gemini API는 `unittest.mock.patch`로 모킹하여 네트워크 없이 테스트
- 422 케이스는 mock 없이 스키마 검증 레벨에서 확인

## API 흐름

```
POST /api/v1/pinball_ai/playstyle
        │
        ▼
   스키마 검증 (PlayDataPoint 배열, 빈 배열 차단)
        │ 422 반환 (검증 실패)
        │
        ▼
   Gemini API 호출 (gemini-2.5-flash)
        │
   ┌────┴────┐
성공        실패
   │          │
   ▼          ▼
success:True  print 오류 로그
playstyle:    success:False
"attack"/     playstyle: 랜덤
"defence"
```

## 결과
- 상태: 완료
- 테스트: 5/5 통과

## 배운 내용

## 다음 작업
- ~~시스템 프롬프트 고도화 (더 정교한 플레이스타일 분석)~~ ✓ 완료
- ~~Gemini 응답 스키마(response schema) 적용으로 `attack`/`defence` 강제 반환~~ ✓ 완료
- ~~프론트엔드에서 플레이 데이터 수집 및 API 연동~~ ✓ 완료

---

# 업무일지

## 기본 정보
- 날짜: 2026-03-07 (토) 14:00~15:20
- 작성자: hexsera

## 진행 목표

Gemini AI 플레이스타일 분석 API의 신뢰성을 높이기 위해 다음 세 가지를 개선한다.
1. `response_schema`를 적용해 Gemini 응답을 JSON으로 강제 (`"attack"` / `"defence"` / `"none"`)
2. `reason` 필드 추가로 AI 판단 근거 반환
3. 프론트엔드 데이터 포인트 필드명을 백엔드 스키마(snake_case)에 맞게 수정

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| PlaystyleResponse 스키마 수정 | `backend/app/schemas/pinball_ai.py` | `"none"` 추가, `reason: str` 필드 추가 |
| response_schema 및 상세 프롬프트 적용 | `backend/app/api/v1/pinball_ai.py` | TypedDict 정의, 프롬프트 교체, JSON 파싱 변경 |
| 프론트엔드 데이터 포인트 필드명 수정 | `frontend/src/pages/AIPinball/AIPinball.jsx` | camelCase → snake_case, timestamp float 변환 |
| parsePlaystyleResponse `"none"` 처리 추가 | `frontend/src/pages/AIPinball/playstyleService.js` | `"none"` 케이스 명시적 처리 |

**총 30분**

## 주요 변경 사항

### 1. `backend/app/schemas/pinball_ai.py` 수정

**변경 전:**
```python
class PlaystyleResponse(BaseModel):
    success: bool
    playstyle: Literal["attack", "defence"]
```

**변경 후:**
```python
class PlaystyleResponse(BaseModel):
    success: bool
    playstyle: Literal["attack", "defence", "none"]
    reason: str
```

**변경 의도:** 데이터 부족 등 판단 불가 상황을 `"none"`으로 정상 처리하고, AI 판단 근거를 `reason`에 담아 반환하기 위해.

---

### 2. `backend/app/api/v1/pinball_ai.py` 수정

**변경 전:**
```python
import random
from typing import Annotated
from google import genai
from fastapi import APIRouter, Body
from app.core.config import settings
from app.schemas.pinball_ai import PlayDataPoint, PlaystyleResponse

router = APIRouter()

PLAYSTYLE_PROMPT = (
    "다음은 핀볼 게임 플레이 데이터다. "
    "이 데이터를 분석하여 플레이어의 성향을 'attack' 또는 'defence' 중 하나로만 답해라. "
    "다른 텍스트 없이 단어 하나만 반환해라.\n\n"
)


@router.post("/playstyle", response_model=PlaystyleResponse)
def analyze_playstyle(
    play_data: Annotated[list[PlayDataPoint], Body(min_length=1)],
):
    client = genai.Client(api_key=settings.GEMINI_API_KEY)
    data_str = "\n".join(str(point.model_dump()) for point in play_data)
    try:
        response = client.models.generate_content(
            model="gemini-2.5-flash",
            contents=PLAYSTYLE_PROMPT + data_str,
        )
        playstyle = response.text.strip().lower()
        if playstyle not in ("attack", "defence"):
            playstyle = "attack"
        return PlaystyleResponse(success=True, playstyle=playstyle)
    except Exception as e:
        print(f"[pinball_ai] Gemini 오류: {e}")
        playstyle = random.choice(["attack", "defence"])
        return PlaystyleResponse(success=False, playstyle=playstyle)
```

**변경 후:**
```python
import json
import random
from typing import Annotated, Literal
from typing_extensions import TypedDict
from google import genai
from fastapi import APIRouter, Body
from app.core.config import settings
from app.schemas.pinball_ai import PlayDataPoint, PlaystyleResponse

router = APIRouter()


class PlaystyleSchema(TypedDict):
    playstyle: Literal["attack", "defence", "none"]
    reason: str


PLAYSTYLE_PROMPT = """너는 핀볼 게임 플레이 데이터를 분석하는 AI다.

[작업]
아래 데이터를 분석하여 플레이어 성향을 다음 세 가지 중 하나로 분류한다.
- attack: 플리퍼를 정확한 타이밍에 제어하고, 공의 속도와 방향을 활용해 높은 점수를 노리는 공격적 플레이
- defence: 플리퍼를 자주 연타하거나 과도하게 조작해 공을 떨어뜨리지 않는 데 집중하는 수비적 플레이
- none: 데이터가 너무 적거나, 플리퍼 입력이 전혀 없어 성향을 판단할 수 없는 경우

[데이터 수집 조건]
이 데이터는 공이 플립퍼 주변 영역에 진입했을 때만 수집된다. 즉, 각 데이터 포인트는 플레이어가 플리퍼를 조작해야 하는 상황에서 기록된 것이다.

[입력 데이터 필드 설명]
- timestamp: 데이터 기록 시각 (초 단위)
- ball_x, ball_y: 공의 위치 좌표. 게임 화면 기준으로 x는 가로, y는 세로(아래로 갈수록 증가)
- ball_vx, ball_vy: 공의 속도 벡터. 양수 vx는 오른쪽, 양수 vy는 아래쪽 방향
- left_flipper: 왼쪽 플립퍼 상태. 회전축 위치 (x=225, y=995), 길이 100. true면 플립퍼가 올라간(타격) 상태
- right_flipper: 오른쪽 플립퍼 상태. 회전축 위치 (x=440, y=995), 길이 100. true면 플립퍼가 올라간(타격) 상태

[none 선택 조건]
- 데이터 포인트가 5개 미만인 경우
- 모든 left_flipper, right_flipper가 false여서 플리퍼 조작 패턴을 알 수 없는 경우
- 위 조건에 해당하면 reason에 판단 불가 이유를 한국어로 작성

[reason 작성 규칙]
- attack 또는 defence 선택 시: 판단 근거를 한국어로 1~2문장으로 작성
- none 선택 시: 판단할 수 없는 이유를 한국어로 1~2문장으로 작성

[플레이 데이터]
"""


@router.post("/playstyle", response_model=PlaystyleResponse)
def analyze_playstyle(
    play_data: Annotated[list[PlayDataPoint], Body(min_length=1)],
):
    client = genai.Client(api_key=settings.GEMINI_API_KEY)
    data_str = "\n".join(str(point.model_dump()) for point in play_data)
    try:
        response = client.models.generate_content(
            model="gemini-2.5-flash",
            contents=PLAYSTYLE_PROMPT + data_str,
            config=genai.types.GenerateContentConfig(
                response_mime_type="application/json",
                response_schema=PlaystyleSchema,
            ),
        )
        result = json.loads(response.text)
        playstyle = result["playstyle"]
        reason = result["reason"]
        return PlaystyleResponse(success=True, playstyle=playstyle, reason=reason)
    except Exception as e:
        print(f"[pinball_ai] Gemini 오류: {e}")
        playstyle = random.choice(["attack", "defence"])
        return PlaystyleResponse(
            success=False,
            playstyle=playstyle,
            reason="분석 중 오류가 발생했습니다.",
        )
```

**변경 의도:**
- `TypedDict`로 `PlaystyleSchema` 정의 → Gemini SDK에 `response_schema`로 전달해 JSON 응답 강제
- `response_mime_type="application/json"` 설정으로 Gemini가 항상 JSON으로 응답하도록 제한
- `response.text.strip()`의 텍스트 파싱 방식을 `json.loads(response.text)`로 교체 → 파싱 실패로 인한 fallback 제거
- 예외 시 `reason="분석 중 오류가 발생했습니다."` 반환

---

### 3. `frontend/src/pages/AIPinball/AIPinball.jsx` 수정 (데이터 포인트 생성 블록)

**변경 전:**
```javascript
const elapsed = ((Date.now() - gameStartTime) / 1000).toFixed(2);
const dataPoint = {
  timestamp: `${elapsed}s`,
  ballX: parseFloat(ball.position.x.toFixed(2)),
  ballY: parseFloat(ball.position.y.toFixed(2)),
  ballVX: parseFloat(ball.velocity.x.toFixed(2)),
  ballVY: parseFloat(ball.velocity.y.toFixed(2)),
  leftFlipper: isLeftKeyPressedRef.current,
  rightFlipper: isRightKeyPressedRef.current,
};
```

**변경 후:**
```javascript
const elapsed = parseFloat(((Date.now() - gameStartTime) / 1000).toFixed(2));
const dataPoint = {
  timestamp: elapsed,
  ball_x: parseFloat(ball.position.x.toFixed(2)),
  ball_y: parseFloat(ball.position.y.toFixed(2)),
  ball_vx: parseFloat(ball.velocity.x.toFixed(2)),
  ball_vy: parseFloat(ball.velocity.y.toFixed(2)),
  left_flipper: isLeftKeyPressedRef.current,
  right_flipper: isRightKeyPressedRef.current,
};
```

**변경 의도:**
- 백엔드 `PlayDataPoint` 스키마 필드명(snake_case)과 일치시킴
- `timestamp`를 문자열(`"1.23s"`) → float(`1.23`)으로 변환 (스키마가 `float` 타입 요구)

---

### 4. `frontend/src/pages/AIPinball/playstyleService.js` 수정

**변경 전:**
```javascript
export function parsePlaystyleResponse(response) {
  if (response.playstyle === 'attack') return 'small';
  if (response.playstyle === 'defence') return 'big';
  return null;
}
```

**변경 후:**
```javascript
export function parsePlaystyleResponse(response) {
  if (response.playstyle === 'attack') return 'small';
  if (response.playstyle === 'defence') return 'big';
  if (response.playstyle === 'none') return null;
  return null;
}
```

**변경 의도:** `"none"` 응답이 추가됨에 따라 명시적으로 처리. 결과는 동일하게 `null` 반환 (랜덤 스킬 fallback으로 이어짐).

---

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| `POST /api/v1/pinball_ai/playstyle` 응답 구조 | `reason` 필드 추가, `"none"` playstyle 추가 | 미확인 |
| Gemini 플레이스타일 분석 | 텍스트 파싱 → JSON schema 강제 방식으로 변경 | 미확인 |
| AIPinball 플레이 데이터 수집 | 필드명 camelCase → snake_case, timestamp float 변환 | 미확인 |
| AIPinball 스킬 결정 로직 | `"none"` 응답 시 랜덤 스킬 fallback으로 처리 | 미확인 |

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- `docker compose up -d fastapi` 로 백엔드 재시작 후 실제 API 동작 확인
- `"none"` 응답 시 프론트엔드에서 사용자에게 안내 메시지 표시 여부 검토

---

# 업무일지

## 기본 정보
- 날짜: 2026-03-07 (토) 16:00~17:30
- 작성자: hexsera

## 진행 목표

평문 비밀번호가 DB에 저장되는 보안 취약점 해결 (OAuth 토큰 인증 구현 PHASE 1).
`bcrypt` 해싱을 적용하여 신규 회원가입/로그인 및 기존 유저 비밀번호를 모두 해시값으로 전환한다.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| bcrypt 패키지 추가 | `backend/requirements.txt` | passlib 대신 bcrypt 직접 사용 (호환성 문제) |
| hash_password / verify_password 함수 추가 | `backend/app/core/security.py` | bcrypt 직접 호출 방식 |
| 로그인 평문 비교 제거, 회원가입 해싱 적용 | `backend/app/api/v1/auth.py` | verify_password / hash_password 적용 |
| create_user 엔드포인트 해싱 적용 | `backend/app/api/v1/users.py` | hash_password 적용 |
| admin 시딩 해싱 적용 | `backend/seed.py` | hash_password 적용 |
| 테스트 fixture 해싱 적용 | `backend/tests/conftest.py` | sample_users fixture |
| mock 데이터 생성 해싱 적용 | `backend/scripts/mock/seed_mock_data.py` | hash_password 적용 |
| 기존 유저 재해싱 스크립트 생성 및 실행 | `backend/scripts/rehash_passwords.py` | DB 유저 103명 재해싱 완료 |

## 주요 변경 사항

### 1. `backend/requirements.txt` 수정

**변경 전:**
```text
fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlalchemy==2.0.25
pymysql==1.1.0
psycopg2-binary==2.9.9
cryptography
python-dotenv
alembic==1.13.1
pytest==8.0.0
httpx==0.27.0
Faker==24.0.0
google-genai
```

**변경 후:**
```text
fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlalchemy==2.0.25
pymysql==1.1.0
psycopg2-binary==2.9.9
cryptography
python-dotenv
alembic==1.13.1
pytest==8.0.0
httpx==0.27.0
Faker==24.0.0
google-genai
bcrypt
```

**변경 의도:** bcrypt 해싱을 위한 패키지 추가. 최초에는 `passlib[bcrypt]==1.7.4`를 추가했으나, 컨테이너에 설치된 bcrypt 5.0.0과 호환되지 않아(`passlib`은 bcrypt 4.x 이하만 지원) `bcrypt`를 직접 사용하는 방식으로 변경.

---

### 2. `backend/app/core/security.py` 수정

**변경 전:**
```python
# fastapi/app/core/security.py
from fastapi import HTTPException, Depends
from fastapi.security import APIKeyHeader
from app.core.config import settings

API_KEY_HEADER = APIKeyHeader(name="X-API-Key", auto_error=False)

def verify_api_key(api_key: str = Depends(API_KEY_HEADER)) -> str:
    """API Key 검증 의존성"""
    if api_key is None:
        raise HTTPException(status_code=401, detail="API Key is missing")
    if api_key != settings.API_KEY:
        raise HTTPException(status_code=403, detail="Invalid API Key")
    return api_key
```

**변경 후:**
```python
# fastapi/app/core/security.py
import bcrypt
from fastapi import HTTPException, Depends
from fastapi.security import APIKeyHeader
from app.core.config import settings

API_KEY_HEADER = APIKeyHeader(name="X-API-Key", auto_error=False)


def hash_password(password: str) -> str:
    return bcrypt.hashpw(password.encode("utf-8"), bcrypt.gensalt()).decode("utf-8")


def verify_password(plain_password: str, hashed_password: str) -> bool:
    return bcrypt.checkpw(plain_password.encode("utf-8"), hashed_password.encode("utf-8"))


def verify_api_key(api_key: str = Depends(API_KEY_HEADER)) -> str:
    """API Key 검증 의존성"""
    if api_key is None:
        raise HTTPException(status_code=401, detail="API Key is missing")
    if api_key != settings.API_KEY:
        raise HTTPException(status_code=403, detail="Invalid API Key")
    return api_key
```

**변경 의도:** `hash_password`, `verify_password` 함수를 추가하여 모든 비밀번호 처리를 이 두 함수로 일원화.

---

### 3. `backend/app/api/v1/auth.py` 수정

**변경 전:**
```python
from app.api.deps import get_db
from app.schemas.user import (
    UserRegisterRequest,
    UserResponse,
    LoginRequest,
    LoginResponse,
)

# ...

    if user.password != login_request.password:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Invalid email or password"
        )

# ...

    db_user = User(
        email=user.email,
        nickname=user.nickname,
        password=user.password,
        birth_date=user.birth_date,
        role="user"
    )
```

**변경 후:**
```python
from app.api.deps import get_db
from app.core.security import hash_password, verify_password
from app.schemas.user import (
    UserRegisterRequest,
    UserResponse,
    LoginRequest,
    LoginResponse,
)

# ...

    if not verify_password(login_request.password, user.password):
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="Invalid email or password"
        )

# ...

    db_user = User(
        email=user.email,
        nickname=user.nickname,
        password=hash_password(user.password),
        birth_date=user.birth_date,
        role="user"
    )
```

**변경 의도:** 로그인 시 평문 비교 제거 후 `verify_password`로 교체. 회원가입 시 평문 저장 제거 후 `hash_password`로 교체.

---

### 4. `backend/app/api/v1/users.py` 수정

**변경 전 (create_user 관련):**
```python
from app.api.deps import get_db
from app.schemas.user import (...)

# ...

    db_user = User(
        email=user.email,
        nickname=user.nickname,
        password=user.password,
        birth_date=user.birth_date,
        role=user.role
    )
```

**변경 후 (create_user 관련):**
```python
from app.api.deps import get_db
from app.core.security import hash_password
from app.schemas.user import (...)

# ...

    db_user = User(
        email=user.email,
        nickname=user.nickname,
        password=hash_password(user.password),
        birth_date=user.birth_date,
        role=user.role
    )
```

**변경 의도:** 관리자용 유저 직접 생성 엔드포인트도 평문 저장 방지.

---

### 5. `backend/seed.py` 수정

**변경 전:**
```python
from app.core.security import hash_password  # 없음

admin_user = User(
    ...
    password=os.getenv("ADMIN_PASSWORD"),
    ...
)
```

**변경 후:**
```python
from app.core.security import hash_password

admin_user = User(
    ...
    password=hash_password(os.getenv("ADMIN_PASSWORD")),
    ...
)
```

**변경 의도:** 컨테이너 기동 시 자동 생성되는 admin 계정도 bcrypt 해싱 적용.

---

### 6. `backend/tests/conftest.py` 수정 (sample_users fixture)

**변경 전:**
```python
@pytest.fixture(scope="function")
def sample_users(db_session):
    from models import User
    from datetime import date

    user1 = User(
        email="user1@test.com",
        nickname="User1",
        password="password1",
        birth_date=date(2000, 1, 1),
        role="user"
    )
    user2 = User(
        email="user2@test.com",
        nickname="User2",
        password="password2",
        birth_date=date(2000, 1, 2),
        role="user"
    )
    db_session.add(user1)
    db_session.add(user2)
    db_session.commit()
    db_session.refresh(user1)
    db_session.refresh(user2)
    return [user1, user2]
```

**변경 후:**
```python
@pytest.fixture(scope="function")
def sample_users(db_session):
    from models import User
    from datetime import date
    from app.core.security import hash_password

    user1 = User(
        email="user1@test.com",
        nickname="User1",
        password=hash_password("password1"),
        birth_date=date(2000, 1, 1),
        role="user"
    )
    user2 = User(
        email="user2@test.com",
        nickname="User2",
        password=hash_password("password2"),
        birth_date=date(2000, 1, 2),
        role="user"
    )
    db_session.add(user1)
    db_session.add(user2)
    db_session.commit()
    db_session.refresh(user1)
    db_session.refresh(user2)
    return [user1, user2]
```

**변경 의도:** 테스트 fixture가 DB에 직접 삽입하므로 해싱 필요. 미적용 시 로그인 API 테스트에서 인증 실패 발생.

---

### 7. `backend/scripts/mock/seed_mock_data.py` 수정

**변경 전:**
```python
from app.db.session import SessionLocal
from models import User, MonthlyScore

# ...
            password="password123!",
```

**변경 후:**
```python
from app.db.session import SessionLocal
from app.core.security import hash_password
from models import User, MonthlyScore

# ...
            password=hash_password("password123!"),
```

**변경 의도:** mock 데이터도 평문 저장 시 DB 일관성이 깨지므로 해싱 적용.

---

### 8. `backend/scripts/rehash_passwords.py` 신규 생성

**추가:**
```python
import sys
sys.path.insert(0, '/code')

from app.db.session import SessionLocal
from app.core.security import hash_password
from models import User

db = SessionLocal()
try:
    users = db.query(User).all()
    count = 0
    for user in users:
        if not user.password.startswith("$2b$"):
            user.password = hash_password(user.password)
            count += 1
    db.commit()
    print(f"완료: {count}명 재해싱, {len(users) - count}명 스킵")
finally:
    db.close()
```

**변경 의도:** 코드 배포 전 이미 DB에 저장된 평문 비밀번호를 bcrypt로 일괄 재해싱하기 위한 1회성 스크립트. `$2b$`로 시작하지 않는(= 평문인) 비밀번호만 재해싱하고 이미 해시된 경우는 스킵.

**실행 결과:** 기존 DB 유저 103명 전원 재해싱 완료.

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| 로그인 (`POST /api/v1/login`) | 평문 비교 → bcrypt verify_password 로 교체 | 정상 (테스트 15/16 통과) |
| 회원가입 (`POST /api/v1/register`) | 평문 저장 → hash_password 적용 | 정상 (테스트 통과) |
| 유저 생성 (`POST /api/v1/users`) | 평문 저장 → hash_password 적용 | 정상 (테스트 통과) |
| admin 시딩 (`seed.py`) | 평문 저장 → hash_password 적용 | 미확인 (컨테이너 재시작 시 확인 가능) |
| 친구 요청 (`/api/friend-requests/`) | 직접 변경 없음, fixture 해싱으로 테스트 환경 복구 | 정상 (8/8 통과) |
| mock 데이터 생성 (`seed_mock_data.py`) | 평문 저장 → hash_password 적용 | 미확인 |
| 기존 DB 유저 비밀번호 | rehash_passwords.py 실행으로 전원 재해싱 | 정상 (103명 완료) |

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- PHASE 2: JWT Access/Refresh Token 인증 구현
  - `python-jose[cryptography]` 패키지 추가
  - `SECRET_KEY` 등 JWT 환경변수 추가
  - `create_access_token`, `create_refresh_token`, `decode_token` 함수 구현
  - `users.refresh_token` 컬럼 추가 (Alembic 마이그레이션)
  - 로그인 응답에 토큰 반환
  - `POST /api/v1/token/refresh` 엔드포인트 추가
  - `get_current_user()` 의존성 구현
  - 보호 API에 인증 의존성 적용

---

# 업무일지

## 기본 정보
- 날짜: 2026-03-07 (토) 19:00~20:00
- 작성자: hexsera

## 진행 목표

AIPinball 스킬 타이밍 개선:
1. API 응답 폴백 대기시간을 10초 → 30초로 연장
2. API 응답이 도착하면 즉시 스킬 활성화 (30초를 기다리지 않음)

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| responseTimer 딜레이 10000→30000 변경 및 즉시 적용 로직 추가 | `frontend/src/pages/AIPinball/AIPinball.jsx` | API 응답 즉시 clearTimeout + 스킬 적용 |

**총 15분**

## 주요 변경 사항

### 1. `frontend/src/pages/AIPinball/AIPinball.jsx` 수정

**변경 전:**
```javascript
sendPlaystyleData(playstyleDataRef.current)
  .then((data) => {
    const skill = parsePlaystyleResponse(data);
    if (skill) {
      pendingSkillRef.current = skill;
    }
  })
  .catch((err) => {
    console.error('플레이스타일 API 오류:', err);
  });

// 10초 대기 후 결과 반영 (응답 없으면 랜덤)
responseTimerRef.current = setTimeout(() => {
  if (pendingSkillRef.current === null) {
    console.warn('API 응답이 오지 않았습니다. 랜덤 스킬을 적용합니다.');
  }
  const resolved = pendingSkillRef.current ?? getRandomSkill();
  skillStateRef.current = resolved;
  setSkillState(resolved);
}, 10000);
```

**변경 후:**
```javascript
sendPlaystyleData(playstyleDataRef.current)
  .then((data) => {
    const skill = parsePlaystyleResponse(data);
    if (skill) {
      clearTimeout(responseTimerRef.current); // 폴백 타이머 취소
      skillStateRef.current = skill;
      setSkillState(skill);
    }
  })
  .catch((err) => {
    console.error('플레이스타일 API 오류:', err);
  });

// 30초 대기 후 랜덤 스킬 폴백 (응답이 오면 타이머 취소됨)
responseTimerRef.current = setTimeout(() => {
  console.warn('API 응답이 오지 않았습니다. 랜덤 스킬을 적용합니다.');
  const skill = getRandomSkill();
  skillStateRef.current = skill;
  setSkillState(skill);
}, 30000);
```

**변경 의도:**
- 네트워크가 느린 환경에서 10초 이내 응답 불가로 랜덤 폴백되는 경우를 줄이기 위해 폴백 타이머를 30초로 연장
- API 응답이 도착하면 `clearTimeout`으로 폴백 타이머를 즉시 취소하고 스킬 적용 → 불필요한 대기 없이 즉시 스킬 활성화

## 스킬 타이밍 흐름

```
스킬 충전 버튼 클릭
        │
        ▼
   데이터 수집 시작 (0~10초)
   충전 UI 바 10초 동안 채워짐
        │
        ▼ (10초 후)
   수집 종료 → API 호출
   responseTimerRef 시작 (30초 카운트)
        │
   ┌────┴──────────────────────┐
   │ 30초 이내 응답 도착        │ 30초 초과 / 오류
   ▼                           ▼
clearTimeout(responseTimer)  랜덤 스킬 적용
즉시 스킬 활성화
```

## 영향받은 기능

| 기능 | 변경 내용 | 작동 상태 |
|------|----------|-----------|
| AIPinball 스킬 충전 UI | 변경 없음 (10초 기준 유지) | 정상 |
| AIPinball API 응답 대기 | 폴백 타이머 10초 → 30초 | 미확인 |
| AIPinball 스킬 즉시 적용 | 응답 도착 시 clearTimeout + 즉시 활성화 | 미확인 |
| AIPinball 랜덤 스킬 폴백 | 30초 초과 또는 API 오류 시 랜덤 적용 | 미확인 |

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- 실제 네트워크 환경에서 스킬 즉시 적용 확인
- API 오류(catch) 시 30초 후 랜덤 스킬 적용 확인
