# 업무일지

## 기본 정보
- 날짜: 2026-03-06 (금) 03:00~3:50
- 작성자: hexsera

## 진행 목표
핀볼 게임 UI에 필살기 아이콘 및 로딩 표시 추가 — 필살기 AI 분석 진행 상태를 사용자에게 시각적으로 알려주고, 분석 결과에 따른 필살기 유형(BIG/SMALL)을 아이콘으로 표시한다.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| SkillIcon 컴포넌트 생성 | `frontend/src/pages/AIPinball/SkillIcon.jsx` | 신규 파일 |
| SkillIcon import 추가 | `frontend/src/pages/AIPinball/AIPinball.jsx` | 상단 import |
| skillState 상태 추가 | `frontend/src/pages/AIPinball/AIPinball.jsx` | useState 추가 |
| 게임 영역에 SkillIcon 배치 | `frontend/src/pages/AIPinball/AIPinball.jsx` | 왼쪽 아래 absolute 배치 |
| 테스트용 키 이벤트 추가 | `frontend/src/pages/AIPinball/AIPinball.jsx` | B/S/L 키로 상태 전환 |


## 주요 변경 사항

### 1. SkillIcon.jsx 생성

**추가:**
```jsx
import { Box, Typography } from '@mui/material';
import CircularProgress from '@mui/material/CircularProgress';

function SkillIcon({ skillState }) {
  return (
    <Box sx={{
      width: '80px',
      height: '80px',
      borderRadius: '12px',
      backgroundColor: '#000000cc',
      border: '2px solid #ffffff',
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      justifyContent: 'center',
      gap: '6px',
    }}>
      {skillState === 'loading' && (
        <>
          <CircularProgress size={32} sx={{ color: '#ffffffaa' }} />
          <Typography sx={{ color: '#ffffff88', fontSize: '9px', letterSpacing: '0.5px' }}>
            LOADING
          </Typography>
        </>
      )}
      {skillState === 'big' && (
        <>
          <Box sx={{
            width: '38px', height: '38px', borderRadius: '50%',
            backgroundColor: '#2196f3',
            boxShadow: '0 0 10px #2196f3',
            border: '2px solid #64b5f6',
          }} />
          <Typography sx={{ color: '#64b5f6', fontSize: '9px', fontWeight: 'bold', letterSpacing: '0.5px' }}>
            BIG
          </Typography>
        </>
      )}
      {skillState === 'small' && (
        <>
          <Box sx={{
            width: '20px', height: '20px', borderRadius: '50%',
            backgroundColor: '#e94560',
            boxShadow: '0 0 10px #e94560',
            border: '2px solid #ff6b6b',
          }} />
          <Typography sx={{ color: '#ff6b6b', fontSize: '9px', fontWeight: 'bold', letterSpacing: '0.5px' }}>
            SMALL
          </Typography>
        </>
      )}
    </Box>
  );
}

export default SkillIcon;
```

**특징:**
- 80×80px 둥근 검은 사각형 프레임 (`borderRadius: 12px`, 흰색 2px 테두리)
- `loading`: CircularProgress(32px) + "LOADING" 텍스트
- `big`: 파란 원(38px) + 파란 글로우 + "BIG" 텍스트
- `small`: 빨간 원(20px) + 빨간 글로우 + "SMALL" 텍스트 — 크기 차이로 직관적 구분

---

### 2. AIPinball.jsx 수정

**변경 전:**
```jsx
import { STAGE_CONFIGS, BUMPER_RADIUS } from './aiStageConfigs';
```

**변경 후:**
```jsx
import { STAGE_CONFIGS, BUMPER_RADIUS } from './aiStageConfigs';
import SkillIcon from './SkillIcon';
```

---

**변경 전:**
```jsx
const [isMuted, setIsMuted] = useState(false);
```

**변경 후:**
```jsx
const [isMuted, setIsMuted] = useState(false);
const [skillState, setSkillState] = useState('loading');
```

---

**변경 전 (게임 영역 캔버스 아래):**
```jsx
<div ref={sceneRef} style={{ position: 'relative', zIndex: 2 }} />

{/* 모바일 조작 버튼 (터치 디바이스에서만 표시) */}
```

**변경 후:**
```jsx
<div ref={sceneRef} style={{ position: 'relative', zIndex: 2 }} />

<Box sx={{
  position: 'absolute',
  bottom: '120px',
  left: '20px',
  zIndex: 5,
}}>
  <SkillIcon skillState={skillState} />
</Box>

{/* 모바일 조작 버튼 (터치 디바이스에서만 표시) */}
```

---

**변경 전 (handleKeyDown 내부):**
```javascript
// 'n' 키로 스테이지 전환 (테스트용)
if (event.key === 'n' || event.key === 'N') {
```

**변경 후:**
```javascript
// 필살기 상태 전환 (개발 확인용)
if (event.key === 'b' || event.key === 'B') {
  setSkillState('big');
}
if (event.key === 's' || event.key === 'S') {
  setSkillState('small');
}
if (event.key === 'l' || event.key === 'L') {
  setSkillState('loading');
}
// 'n' 키로 스테이지 전환 (테스트용)
if (event.key === 'n' || event.key === 'N') {
```

**특징:**
- 아이콘은 `position: absolute`, `bottom: 120px`, `left: 20px`으로 배치 — 모바일 플리퍼 버튼과 겹치지 않음
- `zIndex: 5` — 캔버스(z-index: 2) 위, 오버레이(z-index: 10) 아래
- B/S/L 키는 AI API 연동 전 개발 확인용이며, 이후 API 응답으로 대체 예정

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- 아이콘 위치 조정
- loading 을 스피너와 40초동안 물차는 효과.

---

# 업무일지

## 기본 정보
- 날짜: 2026-03-06 (금) 13:20~24:00
- 작성자: hexsera

## 진행 목표
필살기 큰공/작은공 실제 물리 동작 구현 — `b` 키로 큰공(파란색), `s` 키로 작은공(빨간색)으로 공을 교체하고 5초 후 원래 공으로 자동 복귀하는 메커니즘 구현. 이후 작은공 크기를 기본 공의 0.7배로 조정.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| ref 선언 추가 | `AIPinball.jsx` | bigBallRef, smallBallRef, specialActiveRef, specialTimerRef, activateSpecialRef |
| 필살기 body 사전 생성 | `AIPinball.jsx` | 게임 초기화 시 월드에 추가하지 않고 미리 생성 |
| activateSpecial 함수 구현 | `AIPinball.jsx` | 공 교체 + 5초 후 복귀 로직 |
| handleKeyDown b/s 키 연동 | `AIPinball.jsx` | 기존 setSkillState에 activateSpecial 호출 추가 |
| cleanup 타이머 해제 추가 | `AIPinball.jsx` | 컴포넌트 언마운트 시 clearTimeout |
| 작은공 크기 조정 | `AIPinball.jsx` | 반지름 4.5 → 10.5 (기본 15의 0.7배) |

## 주요 변경 사항

### 1. AIPinball.jsx — ref 선언 추가

**변경 전:**
```jsx
const runnerRef = useRef(null);
```

**변경 후:**
```jsx
const runnerRef = useRef(null);
const bigBallRef = useRef(null);
const smallBallRef = useRef(null);
const specialActiveRef = useRef(false);
const specialTimerRef = useRef(null);
const activateSpecialRef = useRef(null);
```

**특징:**
- `specialActiveRef`: 필살기 중복 발동 방지 플래그
- `activateSpecialRef`: useEffect 내부 함수를 외부(handleKeyDown)에서 참조하기 위한 ref

---

### 2. AIPinball.jsx — 필살기 body 사전 생성 및 activateSpecial 함수

**추가 (ballRef.current = ball 직후):**
```jsx
// 필살기 공 사전 생성 (월드에는 추가 안 함)
const bigBall = Bodies.circle(662, SHELF_Y - 20, 30, {
  restitution: 0.8, friction: 0, frictionAir: 0,
  density: ball.density + 0.5 / (Math.PI * 30 * 30),
  render: { fillStyle: '#3498db' }
});
const smallBall = Bodies.circle(662, SHELF_Y - 20, 10.5, {
  restitution: 0.8, friction: 0, frictionAir: 0,
  density: Math.max(ball.density - 0.5 / (Math.PI * 10.5 * 10.5), 0.0001),
  render: { fillStyle: '#e74c3c' }
});
bigBallRef.current = bigBall;
smallBallRef.current = smallBall;

// 필살기 발동/복귀 함수
const activateSpecial = (specialBall) => {
  if (specialActiveRef.current) return;
  specialActiveRef.current = true;

  const currentBall = ballRef.current;
  const pos = { ...currentBall.position };
  const vel = { ...currentBall.velocity };
  const angVel = currentBall.angularVelocity;

  World.remove(engine.world, currentBall);
  Body.setPosition(specialBall, pos);
  Body.setVelocity(specialBall, vel);
  Body.setAngularVelocity(specialBall, angVel);
  World.add(engine.world, specialBall);
  ballRef.current = specialBall;

  specialTimerRef.current = setTimeout(() => {
    const pos2 = { ...specialBall.position };
    const vel2 = { ...specialBall.velocity };
    const angVel2 = specialBall.angularVelocity;

    World.remove(engine.world, specialBall);
    Body.setPosition(currentBall, pos2);
    Body.setVelocity(currentBall, vel2);
    Body.setAngularVelocity(currentBall, angVel2);
    World.add(engine.world, currentBall);
    ballRef.current = currentBall;
    specialActiveRef.current = false;
  }, 5000);
};
activateSpecialRef.current = activateSpecial;
```

**특징:**
- 공 교체 시 `position`, `velocity`, `angularVelocity`를 복사해 물리 연속성 유지
- `ballRef.current` 교체로 충돌 감지 코드가 올바른 공을 참조하도록 동기화
- 큰공 반지름 30 (기본 15의 2배), 작은공 반지름 10.5 (기본 15의 0.7배)

---

### 3. AIPinball.jsx — handleKeyDown b/s 키 연동

**변경 전:**
```javascript
if (event.key === 'b' || event.key === 'B') {
  setSkillState('big');
}
if (event.key === 's' || event.key === 'S') {
  setSkillState('small');
}
```

**변경 후:**
```javascript
if (event.key === 'b' || event.key === 'B') {
  setSkillState('big');
  activateSpecialRef.current?.(bigBallRef.current);
}
if (event.key === 's' || event.key === 'S') {
  setSkillState('small');
  activateSpecialRef.current?.(smallBallRef.current);
}
```

---

### 4. AIPinball.jsx — cleanup 타이머 해제

**변경 전:**
```javascript
Render.stop(render);
Runner.stop(runner);
Engine.clear(engine);
```

**변경 후:**
```javascript
if (specialTimerRef.current) {
  clearTimeout(specialTimerRef.current);
}
Render.stop(render);
Runner.stop(runner);
Engine.clear(engine);
```

---

### 5. AIPinball.jsx — 작은공 크기 조정

**변경 전:**
```javascript
const smallBall = Bodies.circle(662, SHELF_Y - 20, 4.5, {
  density: Math.max(ball.density - 0.5 / (Math.PI * 4.5 * 4.5), 0.0001),
```

**변경 후:**
```javascript
const smallBall = Bodies.circle(662, SHELF_Y - 20, 10.5, {
  density: Math.max(ball.density - 0.5 / (Math.PI * 10.5 * 10.5), 0.0001),
```

**특징:**
- 기본 공 반지름 15 × 0.7 = 10.5 — 시각적으로 구별 가능하면서 너무 작지 않은 크기

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- 필살기 활성 중 공이 죽음 구역에 떨어졌을 때 목숨 차감 후 원래 공으로 복귀 처리 확인
- loading 스피너 + 40초 물 차는 효과 구현
- AI 분석 결과에 따른 필살기 유형 자동 선택 연동

---

# 업무일지

## 기본 정보
- 날짜: 2026-03-06 (금) 14:00~15:10
- 작성자: hexsera

## 진행 목표
Phase 1 실행 — 플립퍼 영역에 sensor body를 생성하고 공이 그 안에 있을 때 100ms마다 플레이스타일 데이터를 수집. 40초 후 수집 데이터를 콘솔 출력하고 랜덤으로 필살기 종류를 결정해 SkillIcon에 반영. `a` 키로 필살기 발동, `b`/`s`/`l` 테스트 키 제거.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| useRef 4개 추가 | `AIPinball.jsx` | isBallInTriggerRef, playstyleDataRef, collectIntervalRef, analysisTimerRef |
| ball에 label 추가 | `AIPinball.jsx` | 충돌 이벤트 식별용 `label: 'ball'` |
| triggerBody 생성 및 World 추가 | `AIPinball.jsx` | 플립퍼 영역 덮는 녹색 sensor body |
| 충돌 이벤트 등록 | `AIPinball.jsx` | collisionStart/End로 공 진입·이탈 감지 |
| startGame에 interval + 40초 타이머 추가 | `AIPinball.jsx` | 100ms 수집, 40초 후 랜덤 필살기 결정 |
| handleKeyDown 수정 | `AIPinball.jsx` | b/s/l 제거, a 키 필살기 발동 추가 |
| cleanup에 interval/timer 해제 추가 | `AIPinball.jsx` | 메모리 누수 방지 |
| 수집 데이터 형식 개선 | `AIPinball.jsx` | timestamp → 경과 초(0.00s), 숫자 소수점 2자리 |

## 주요 변경 사항

### 1. AIPinball.jsx — useRef 4개 추가

**변경 전:**
```jsx
const activateSpecialRef = useRef(null);
```

**변경 후:**
```jsx
const activateSpecialRef = useRef(null);
const isBallInTriggerRef = useRef(false);
const playstyleDataRef = useRef([]);
const collectIntervalRef = useRef(null);
const analysisTimerRef = useRef(null);
```

---

### 2. AIPinball.jsx — ball label 추가 및 triggerBody 생성

**변경 전:**
```javascript
const ball = Bodies.circle(60, SHELF_Y - 220, 15, {
  restitution: 0.8,
  friction: 0,
  frictionAir: 0,
  render: { fillStyle: '#e94560' }
});
```

**변경 후:**
```javascript
const ball = Bodies.circle(60, SHELF_Y - 220, 15, {
  restitution: 0.8,
  friction: 0,
  frictionAir: 0,
  label: 'ball',
  render: { fillStyle: '#e94560' }
});
```

**추가 (leftFlipper 생성 직전):**
```javascript
const triggerBody = Bodies.rectangle(332, 995, 215, 60, {
  isStatic: true,
  isSensor: true,
  label: 'flipperTrigger',
  render: {
    fillStyle: '#00ff00',
    opacity: 0.3
  }
});
```

**특징:**
- 너비 215px: 왼쪽 플립퍼 끝(x=225) ~ 오른쪽 플립퍼 끝(x=440) 포괄
- `isSensor: true`: 물리 충돌 없이 감지만 수행, 녹색 반투명으로 화면에 표시

---

### 3. AIPinball.jsx — 충돌 이벤트 등록

**추가 (기존 범퍼 collisionStart 이벤트 직전):**
```javascript
Events.on(engine, 'collisionStart', (event) => {
  event.pairs.forEach(({ bodyA, bodyB }) => {
    const isTrigger = bodyA.label === 'flipperTrigger' || bodyB.label === 'flipperTrigger';
    const isBall = bodyA.label === 'ball' || bodyB.label === 'ball';
    if (isTrigger && isBall) isBallInTriggerRef.current = true;
  });
});

Events.on(engine, 'collisionEnd', (event) => {
  event.pairs.forEach(({ bodyA, bodyB }) => {
    const isTrigger = bodyA.label === 'flipperTrigger' || bodyB.label === 'flipperTrigger';
    const isBall = bodyA.label === 'ball' || bodyB.label === 'ball';
    if (isTrigger && isBall) isBallInTriggerRef.current = false;
  });
});
```

---

### 4. AIPinball.jsx — startGame에 데이터 수집 interval + 40초 타이머

**변경 전:**
```javascript
const startGame = () => {
  if (gameStartedRef.current) return;
  gameStartedRef.current = true;
  setGameStarted(true);
  engineRef.current.timing.timeScale = 1;
  bgmRef.current?.play().catch(() => {});
};
```

**변경 후:**
```javascript
const startGame = () => {
  if (gameStartedRef.current) return;
  gameStartedRef.current = true;
  setGameStarted(true);
  engineRef.current.timing.timeScale = 1;
  bgmRef.current?.play().catch(() => {});

  playstyleDataRef.current = [];
  const gameStartTime = Date.now();
  collectIntervalRef.current = setInterval(() => {
    if (!isBallInTriggerRef.current) return;
    const ball = ballRef.current;
    if (!ball) return;
    const elapsed = ((Date.now() - gameStartTime) / 1000).toFixed(2);
    const dataPoint = {
      timestamp: `${elapsed}s`,
      ballX: parseFloat(ball.position.x.toFixed(2)),
      ballY: parseFloat(ball.position.y.toFixed(2)),
      ballVX: parseFloat(ball.velocity.x.toFixed(2)),
      ballVY: parseFloat(ball.velocity.y.toFixed(2)),
      leftFlipper: isLeftKeyPressedRef.current,
      rightFlipper: isRightKeyPressedRef.current,
    };
    playstyleDataRef.current.push(dataPoint);
    console.log('데이터 수집:', dataPoint);
  }, 100);

  analysisTimerRef.current = setTimeout(() => {
    clearInterval(collectIntervalRef.current);
    console.log('수집된 플레이스타일 데이터:', playstyleDataRef.current);
    const randomSkill = Math.random() < 0.5 ? 'big' : 'small';
    setSkillState(randomSkill);
  }, 40000);
};
```

**특징:**
- `timestamp`: 게임 시작 기준 경과 시간을 `"1.23s"` 형식으로 저장
- 숫자 값은 `parseFloat(...toFixed(2))`로 소수점 2자리까지만 저장
- 공이 트리거 영역 밖에 있으면 수집하지 않고 즉시 return

---

### 5. AIPinball.jsx — handleKeyDown 수정 (b/s/l 제거, a 키 추가)

**변경 전:**
```javascript
if (event.key === 'b' || event.key === 'B') {
  setSkillState('big');
  activateSpecialRef.current?.(bigBallRef.current);
}
if (event.key === 's' || event.key === 'S') {
  setSkillState('small');
  activateSpecialRef.current?.(smallBallRef.current);
}
if (event.key === 'l' || event.key === 'L') {
  setSkillState('loading');
}
```

**변경 후:**
```javascript
if (event.key === 'a' || event.key === 'A') {
  if (skillState === 'big') {
    activateSpecialRef.current?.(bigBallRef.current);
  } else if (skillState === 'small') {
    activateSpecialRef.current?.(smallBallRef.current);
  }
  // 'loading' 상태면 아무것도 하지 않음
}
```

---

### 6. AIPinball.jsx — cleanup에 해제 코드 추가

**변경 전:**
```javascript
if (specialTimerRef.current) {
  clearTimeout(specialTimerRef.current);
}
Render.stop(render);
```

**변경 후:**
```javascript
if (specialTimerRef.current) {
  clearTimeout(specialTimerRef.current);
}
clearInterval(collectIntervalRef.current);
clearTimeout(analysisTimerRef.current);
Render.stop(render);
```

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- 수집된 데이터를 AI API에 전송하여 플레이스타일 분류 (Phase 2)
- loading 스피너 + 40초 물 차는 효과 구현
- 필살기 활성 중 공 사망 시 원래 공으로 복귀 처리 확인

## 진행 목표
Phase 2 실행 — `playstyleService.js` 서비스 레이어를 분리하고 Vitest TDD로 구현. `AIPinball.jsx`의 40초 단일 타이머를 30초(API 호출) + 10초(응답 대기) 이중 타이머 구조로 교체.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| TDD RED 계획 작성 | `PRD/TDD-RED-playstyleService-20260306.md` | 7개 테스트 케이스 정의 |
| RED 테스트 파일 생성 | `frontend/src/test/playstyleService.test.js` | 7개 실패 테스트 작성 |
| playstyleService.js 생성 | `frontend/src/pages/AIPinball/playstyleService.js` | 3개 순수 함수 구현 |
| AIPinball.jsx import 추가 | `frontend/src/pages/AIPinball/AIPinball.jsx` | playstyleService 3개 함수 import |
| AIPinball.jsx ref 2개 추가 | `frontend/src/pages/AIPinball/AIPinball.jsx` | responseTimerRef, pendingSkillRef |
| 40초 타이머 → 30초+10초 교체 | `frontend/src/pages/AIPinball/AIPinball.jsx` | API 호출 + 응답 대기 이중 타이머 |
| cleanup 보강 | `frontend/src/pages/AIPinball/AIPinball.jsx` | responseTimerRef 해제 추가 |
| TDD GREEN 완료 보고서 작성 | `PRD/TDD-GREEN-playstyleService-20260306.md` | 7/7 테스트 통과 기록 |

**총 약 1시간**

## 주요 변경 사항

### 1. playstyleService.js 생성

**추가:**
```javascript
import axios from 'axios';

export async function sendPlaystyleData(dataArray) {
  const response = await axios.post('/api/v1/pinball_ai/playstyle', dataArray);
  return response.data;
}

export function parsePlaystyleResponse(response) {
  if (response.playstyle === 'attack') return 'small';
  if (response.playstyle === 'defence') return 'big';
  return null;
}

export function getRandomSkill() {
  return Math.random() < 0.5 ? 'big' : 'small';
}
```

**특징:**
- `sendPlaystyleData`: axios POST로 수집된 배열 전송, `response.data` 반환
- `parsePlaystyleResponse`: `'attack'` → `'small'`, `'defence'` → `'big'`, 그 외 `null`
- `getRandomSkill`: API 실패 또는 응답 없을 때 fallback으로 사용

---

### 2. AIPinball.jsx — playstyleService import 추가

**변경 전:**
```javascript
import SkillIcon from './SkillIcon';
```

**변경 후:**
```javascript
import SkillIcon from './SkillIcon';
import { sendPlaystyleData, parsePlaystyleResponse, getRandomSkill } from './playstyleService';
```

---

### 3. AIPinball.jsx — ref 2개 추가

**변경 전:**
```javascript
const analysisTimerRef = useRef(null);
```

**변경 후:**
```javascript
const analysisTimerRef = useRef(null);
const responseTimerRef = useRef(null);
const pendingSkillRef = useRef(null);
```

**특징:**
- `pendingSkillRef`: API 응답 결과를 임시 저장, 10초 타이머 종료 시 읽음
- `responseTimerRef`: 10초 응답 대기 타이머 ID 저장 (cleanup용)

---

### 4. AIPinball.jsx — 40초 단일 타이머 → 30초+10초 이중 타이머

**변경 전:**
```javascript
// 40초 후 수집 종료 및 랜덤 필살기 결정
analysisTimerRef.current = setTimeout(() => {
  clearInterval(collectIntervalRef.current);
  console.log('수집된 플레이스타일 데이터:', playstyleDataRef.current);
  const randomSkill = Math.random() < 0.5 ? 'big' : 'small';
  setSkillState(randomSkill);
}, 40000);
```

**변경 후:**
```javascript
// 30초 후 수집 종료 및 API 호출
analysisTimerRef.current = setTimeout(() => {
  clearInterval(collectIntervalRef.current);
  console.log('수집된 플레이스타일 데이터:', playstyleDataRef.current);
  pendingSkillRef.current = null;

  sendPlaystyleData(playstyleDataRef.current)
    .then((data) => {
      const skill = parsePlaystyleResponse(data);
      if (skill) {
        pendingSkillRef.current = skill;
      }
    })
    .catch((err) => {
      console.error('플레이스타일 API 오류:', err);
    });

  // 10초 대기 후 결과 반영 (응답 없으면 랜덤)
  responseTimerRef.current = setTimeout(() => {
    setSkillState(pendingSkillRef.current ?? getRandomSkill());
  }, 10000);
}, 30000);
```

**특징:**
- 30초 후 API 호출과 동시에 10초 대기 타이머 시작 (병렬)
- 응답이 10초 내 도착하면 `pendingSkillRef`에 저장
- 10초 후 `pendingSkillRef` 값 사용, 없으면 `getRandomSkill()` fallback

---

### 5. AIPinball.jsx — cleanup 보강

**변경 전:**
```javascript
clearInterval(collectIntervalRef.current);
clearTimeout(analysisTimerRef.current);
```

**변경 후:**
```javascript
clearInterval(collectIntervalRef.current);
clearTimeout(analysisTimerRef.current);
clearTimeout(responseTimerRef.current);
```

---

### 6. playstyleService.test.js — TDD RED 테스트 (7개)

**추가:**
```javascript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { sendPlaystyleData, parsePlaystyleResponse, getRandomSkill } from '../pages/AIPinball/playstyleService';

vi.mock('axios');
import axios from 'axios';

describe('sendPlaystyleData', () => {
  it('axios를 올바른 URL로 POST 호출한다', ...)
  it('서버 응답 data를 반환한다', ...)
  it('axios 실패 시 에러를 throw한다', ...)
});

describe('parsePlaystyleResponse', () => {
  it("'attack' 응답을 'small'로 변환한다", ...)
  it("'defence' 응답을 'big'으로 변환한다", ...)
  it('알 수 없는 값은 null을 반환한다', ...)
});

describe('getRandomSkill', () => {
  it("반환값이 항상 'big' 또는 'small' 중 하나다", ...)
});
```

**테스트 결과:** 7/7 통과

---

### 데이터 흐름 (Phase 2 완료 후)

```
게임 시작
    │
    ├── setInterval 100ms 데이터 수집 시작
    │       (공이 flipperTrigger 안에 있을 때만)
    │
    ▼ (30초 후)
수집 종료 + API 호출
    │
    ├── sendPlaystyleData(배열) → POST /api/v1/pinball_ai/playstyle
    │       응답 도착 시 → parsePlaystyleResponse → pendingSkillRef 저장
    │
    └── 10초 대기 타이머 시작
            │
            ▼ (10초 후)
        pendingSkillRef ?? getRandomSkill()
            │
            ▼
        setSkillState('big' 또는 'small')
            │
            ▼
        SkillIcon 업데이트 → 사용자에게 필살기 표시
            │
            ▼ (a 키 입력 시)
        activateSpecial → 공 교체 → 5초 후 복귀
```

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- 백엔드 `/api/v1/pinball_ai/playstyle` 엔드포인트 구현
- loading 스피너 + 30초 물 차는 효과 구현 (40초 → 30초로 변경됨)
- 필살기 활성 중 공 사망 시 원래 공으로 복귀 처리 확인
- 실패한 테스트 확인

---

# 업무일지

## 기본 정보
- 날짜: 2026-03-06 (금) 18:40~19:00 21:00~21:10
- 작성자: hexsera

## 진행 목표
AIPinball 버그 수정 — `big_ball`/`small_ball` 필살기 활성 시 충돌 이벤트(범퍼 반발, 사운드, 점수, 죽음 판정)가 동작하지 않는 문제 수정 및 공 초기/부활/재시작 위치를 게임 영역 중앙(x=280, y=700)으로 통일.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| 충돌 이벤트 핸들러 ball → ballRef.current 교체 | `AIPinball.jsx` | 클로저 고정 참조 버그 수정 |
| AI beforeUpdate ball 위치 참조 수정 | `AIPinball.jsx` | AI 판단 로직 필살기 공 추적 |
| 공 초기 생성 위치 변경 | `AIPinball.jsx` | (60, SHELF_Y-220) → (280, 700) |
| 죽음 후 부활 위치 변경 | `AIPinball.jsx` | (662, 990) → (280, 700), activeBall 사용 |
| handleRestart 부활 위치 변경 | `AIPinball.jsx` | (662, 990) → (280, 700) |

## 주요 변경 사항

### 1. AIPinball.jsx — 충돌 이벤트 핸들러 ball → activeBall 교체

**변경 전:**
```javascript
if (bodyA === ball || bodyB === ball) { ... }

if ((bodyA.label === 'bumper' && bodyB === ball) || ...) {
  const direction = Vector.sub(ball.position, bumperBody.position);
  const currentVelocity = ball.velocity;
  Body.setVelocity(ball, newVelocity);
}

if ((bodyA.label === 'deathZone' && bodyB === ball) || ...) {
  Body.setPosition(ball, { x: 662, y: 990 });
  Body.setVelocity(ball, { x: 0, y: 0 });
  Body.setAngularVelocity(ball, 0);
  World.remove(engine.world, ball);
}
```

**변경 후:**
```javascript
const activeBall = ballRef.current;

if (bodyA === activeBall || bodyB === activeBall) { ... }

if ((bodyA.label === 'bumper' && bodyB === activeBall) || ...) {
  const direction = Vector.sub(activeBall.position, bumperBody.position);
  const currentVelocity = activeBall.velocity;
  Body.setVelocity(activeBall, newVelocity);
}

if ((bodyA.label === 'deathZone' && bodyB === activeBall) || ...) {
  Body.setPosition(activeBall, { x: 280, y: 700 });
  Body.setVelocity(activeBall, { x: 0, y: 0 });
  Body.setAngularVelocity(activeBall, 0);
  World.remove(engine.world, activeBall);
}
```

**특징:**
- `ball` 클로저 변수는 최초 생성된 일반 공만 가리킴 — 필살기로 교체 후에도 고정
- `ballRef.current`는 항상 현재 활성 공(big/small/normal)을 가리킴
- 이 수정으로 필살기 공의 범퍼 반발, 사운드, 점수, 죽음 판정 모두 정상 동작

---

### 2. AIPinball.jsx — AI beforeUpdate ball 참조 수정

**변경 전:**
```javascript
const ballPos = ball.position;
const ballVel = ball.velocity;
```

**변경 후:**
```javascript
const ballPos = ballRef.current.position;
const ballVel = ballRef.current.velocity;
```

**특징:**
- AI 플리퍼 판단 로직이 big_ball/small_ball 활성 시에도 올바른 공 위치를 추적

---

### 3. AIPinball.jsx — 공 초기 생성 위치 변경

**변경 전:**
```javascript
const ball = Bodies.circle(60, SHELF_Y - 220, 15, { ... });
```

**변경 후:**
```javascript
const ball = Bodies.circle(280, 700, 15, { ... });
```

---

### 4. AIPinball.jsx — 부활/재시작 위치 변경

**변경 전 (부활):**
```javascript
Body.setPosition(ball, { x: 662, y: 990 });
```

**변경 후 (부활):**
```javascript
Body.setPosition(activeBall, { x: 280, y: 700 });
```

**변경 전 (재시작):**
```javascript
Body.setPosition(ball, { x: 662, y: 990 });
```

**변경 후 (재시작):**
```javascript
Body.setPosition(ball, { x: 280, y: 700 });
```

**특징:**
- 초기·부활·재시작 위치를 x=280, y=700으로 통일 — 플레이어가 예측 가능한 위치에서 시작
- 부활은 `activeBall`로 필살기 공도 올바른 위치에 복귀

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- 필살기 발동 후 공이 죽음구역 진입 시 정상 목숨 감소 확인
- big_ball/small_ball AI 플리퍼 추적 정확도 확인