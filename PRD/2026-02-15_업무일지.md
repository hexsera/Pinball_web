# 업무일지

## 기본 정보
- 날짜: 2026-02-15 (일) 11:30~12:20
- 작성자: hexsera

## 진행 목표

`HeaderUserInfo.jsx`의 아바타 메뉴 dialog를 PRD 요구사항에 맞게 수정한다:
1. dialog 상단에 사용자 이메일 표시
2. dialog 오른쪽 상단에 닫기 버튼 추가
3. "프로필" → "계정설정"으로 변경, 클릭 시 `/dashboard` 계정 섹션으로 이동
4. "친구" MenuItem 추가, 클릭 시 `/dashboard` 친구 섹션으로 이동

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| PRD 요구사항 분석 및 계획 작성 | `PRD/유저 dialog 수정.md`, `PRD/HeaderUserInfo_dialog_수정_계획.md` | TDD 사이클 설계 포함 |
| [RED] 이메일 표시 테스트 작성 | `src/test/HeaderUserInfo.test.jsx` | 실패 확인 완료 |
| [RED] 닫기 버튼 테스트 작성 | `src/test/HeaderUserInfo.test.jsx` | 실패 확인 완료 |
| [RED] 계정설정 MenuItem 테스트 작성 | `src/test/HeaderUserInfo.test.jsx` | 실패 확인 완료 |
| [RED] 친구 MenuItem 테스트 작성 | `src/test/HeaderUserInfo.test.jsx` | 실패 확인 완료 |
| [GREEN] HeaderUserInfo 컴포넌트 수정 | `src/components/HeaderUserInfo.jsx` | 4개 기능 한 번에 구현 |
| [RED] Login email 저장 테스트 작성 | `src/test/Login.test.jsx` | 신규 파일, 실패 확인 완료 |
| [GREEN] Login 컴포넌트 email 저장 추가 | `src/pages/Login/Login.jsx` | 로그인 시 email 함께 저장 |
| Dashboard location.state 연동 | `src/pages/Dashboard/Dashboard.jsx` | navigate state로 섹션 초기화 |
| 전체 테스트 회귀 확인 | — | 11/11 통과 |

## 주요 변경 사항

### 1. HeaderUserInfo.jsx 수정

**변경 전:**
```jsx
import { Box, IconButton, Badge, Avatar, Typography, Button, Menu, MenuItem } from '@mui/material';
import { Notifications, Mail } from '@mui/icons-material';

// Menu 내부
<MenuItem>프로필</MenuItem>
<MenuItem onClick={handlePageNavigation}>페이지 이동</MenuItem>
<MenuItem onClick={() => AvatarLogoutButton()}>로그아웃</MenuItem>
```

**변경 후:**
```jsx
import { Box, IconButton, Badge, Avatar, Typography, Button, Menu, MenuItem, Divider } from '@mui/material';
import { Notifications, Mail, Close } from '@mui/icons-material';

// Menu 내부
<Box sx={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', px: 2, py: 1 }}>
  <Typography variant="body2" color="text.secondary">{user.email}</Typography>
  <IconButton aria-label="닫기" size="small" onClick={handleAvatarMenuClose}>
    <Close fontSize="small" />
  </IconButton>
</Box>
<Divider />
<MenuItem onClick={() => { navigate('/dashboard', { state: { section: '계정' } }); handleAvatarMenuClose(); }}>계정설정</MenuItem>
<MenuItem onClick={() => { navigate('/dashboard', { state: { section: '친구' } }); handleAvatarMenuClose(); }}>친구</MenuItem>
<MenuItem onClick={handlePageNavigation}>페이지 이동</MenuItem>
<MenuItem onClick={() => AvatarLogoutButton()}>로그아웃</MenuItem>
```

**특징:**
- 메뉴 상단에 로그인된 사용자의 이메일 표시 (비활성 영역)
- Close 아이콘 버튼으로 메뉴 닫기 가능
- "프로필" 대신 "계정설정"으로 변경, `navigate state`로 Dashboard 계정 섹션 직접 이동
- "친구" MenuItem 신규 추가, `navigate state`로 Dashboard 친구 섹션 직접 이동

---

### 2. Login.jsx 수정

**변경 전:**
```jsx
login({ id: response.data.user_id, name: response.data.nickname, role: response.data.role });
```

**변경 후:**
```jsx
login({ id: response.data.user_id, name: response.data.nickname, role: response.data.role, email: response.data.email });
```

**특징:**
- 백엔드 로그인 API는 이미 `email`을 반환하고 있었으나 프론트에서 저장하지 않았음
- `localStorage`의 `user` 객체에 `email` 추가 저장하여 HeaderUserInfo에서 이메일 표시 가능

---

### 3. Dashboard.jsx 수정

**변경 전:**
```jsx
import { useNavigate } from 'react-router-dom';

function Maindashboard() {
  const navigate = useNavigate();
  // ...
  const handleDrawerToggle = () => { ... };
```

**변경 후:**
```jsx
import { useNavigate, useLocation } from 'react-router-dom';

function Maindashboard() {
  const navigate = useNavigate();
  const location = useLocation();
  // ...
  const handleDrawerToggle = () => { ... };

  useEffect(() => {
    if (location.state?.section === '계정') {
      setShowUserInfo(true);
      setShowFriend(false);
    } else if (location.state?.section === '친구') {
      setShowFriend(true);
      setShowUserInfo(false);
    }
  }, [location.state]);
```

**특징:**
- `navigate('/dashboard', { state: { section: '계정' } })` 로 이동 시 자동으로 계정 섹션 활성화
- `navigate('/dashboard', { state: { section: '친구' } })` 로 이동 시 자동으로 친구 섹션 활성화
- URL에 노출되지 않는 `navigate state` 방식 사용

---

### 4. HeaderUserInfo.test.jsx 수정 (테스트 추가)

**추가된 테스트 (7개 신규):**
```
이메일 표시
  ✓ 아바타 메뉴를 열면 사용자 이메일이 표시된다

닫기 버튼
  ✓ 아바타 메뉴에 닫기 버튼이 있다
  ✓ 닫기 버튼 클릭 시 메뉴가 닫힌다

계정설정 이동
  ✓ 메뉴에 "계정설정" MenuItem이 존재한다
  ✓ "계정설정" 클릭 시 /dashboard로 이동하고 section이 "계정"이다

친구 이동
  ✓ 메뉴에 "친구" MenuItem이 존재한다
  ✓ "친구" 클릭 시 /dashboard로 이동하고 section이 "친구"이다
```

---

### 5. Login.test.jsx 신규 생성

**추가:**
```jsx
// src/test/Login.test.jsx (신규)
it('로그인 성공 시 localStorage에 email이 저장된다', async () => {
  // axios mock으로 로그인 응답에 email 포함
  // localStorage의 user.email이 저장되는지 검증
});
```

---

### 데이터 흐름

```
HeaderUserInfo (아바타 메뉴 클릭)
       │
       ├── "계정설정" 클릭
       │       │
       │       ▼
       │   navigate('/dashboard', { state: { section: '계정' } })
       │       │
       │       ▼
       │   Dashboard.jsx (useEffect → location.state 감지)
       │       │
       │       ▼
       │   setShowUserInfo(true) → UserInfo 컴포넌트 표시
       │
       └── "친구" 클릭
               │
               ▼
           navigate('/dashboard', { state: { section: '친구' } })
               │
               ▼
           Dashboard.jsx (useEffect → location.state 감지)
               │
               ▼
           setShowFriend(true) → FriendPage 컴포넌트 표시
```

## 결과
- 상태: 완료


## 배운 내용

## 다음 작업
- `FriendPage.test.jsx` — "친구" 메뉴 클릭 시 FriendPage 표시 테스트 기존 실패 (`setShowPinball` 미정의 버그) 수정

---

# 업무일지 (2차)

## 기본 정보
- 날짜: 2026-02-15 (일) 1:00~1:10
- 작성자: hexsera

## 진행 목표

`AdminStatisticsMain.jsx`의 API 호출 문제를 해결한다:
1. API 호출 주소 trailing slash 제거
2. Mock 데이터 관련 코드 전체 제거

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| API 경로 trailing slash 제거 | `src/pages/admin/AdminStatisticsMain.jsx` | 307 리다이렉트 방지 |
| `generateMockDailyData` 함수 제거 | `src/pages/admin/AdminStatisticsMain.jsx` | 개발용 임시 함수 삭제 |
| catch 블록 Mock 데이터 로직 제거 | `src/pages/admin/AdminStatisticsMain.jsx` | 에러 로그만 유지 |

## 주요 변경 사항

### 1. AdminStatisticsMain.jsx 수정

**변경 전:**
```jsx
// 임시 일별 데이터 생성 함수 (3개월 분량)
const generateMockDailyData = (startDate, endDate) => {
  const dailyData = [];
  // ... 랜덤 더미 데이터 생성 로직
  return dailyData;
};

const response = await axios.get('/api/v1/game_visits/', { ... });

} catch (error) {
  console.error('통계 데이터 로딩 실패:', error);
  const mockDailyData = generateMockDailyData(mockStartDate, mockEndDate);
  setDailyData(mockDailyData);
}
```

**변경 후:**
```jsx
const response = await axios.get('/api/v1/game_visits', { ... });

} catch (error) {
  console.error('통계 데이터 로딩 실패:', error);
}
```

**특징:**
- `CLAUDE.md`의 `redirect_slashes=False` 설정에 따라 trailing slash 제거로 307 리다이렉트 방지
- `generateMockDailyData` 함수(19줄) 전체 삭제
- `catch` 블록에서 Mock 데이터 생성 및 `setDailyData(mockDailyData)` 호출 제거
- API 정상 응답 처리(`setDailyData(dailyDataFromAPI.stats)`)는 기존과 동일하게 유지

### 데이터 흐름

```
AdminStatisticsMain (마운트)
       │
       ▼
GET /api/v1/game_visits?start_date=...&end_date=...
       │
       ▼
FastAPI: get_daily_visit_stats()
       │  날짜별 접속자 수 집계 (game_visits 테이블)
       ▼
{ stats: [{date, user_count}, ...], total_days, start_date, end_date }
       │
       ▼
setDailyData(response.data.stats)
       │
       ▼
LineChart (x축: date, y축: user_count)
```

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업

---

# 업무일지 (3차)

## 기본 정보
- 날짜: 2026-02-15 (일) 13:10~13:25
- 작성자: hexsera

## 진행 목표

어드민 페이지 사이드바에서 대시보드 항목을 제거하고, `/admin` 접속 시 회원관리 페이지를 바로 표시한다.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| PRD 작성 | `PRD/어드민 페이지 대시보드 숨기기prd.md` | 요구사항 문서 |
| 실행계획 작성 | `PRD/어드민-대시보드-숨기기-실행계획.md` | 구현 계획 |
| 사이드바 대시보드 항목 제거 | `src/pages/admin/AdminSidebar.jsx` | DashboardIcon import 포함 제거 |
| `/admin` 라우트 변경 | `src/App.jsx` | AdminPage → AdminUserPage |

## 주요 변경 사항

### 1. AdminSidebar.jsx 수정

**변경 전:**
```jsx
import { Dashboard as DashboardIcon, People, BarChart } from '@mui/icons-material';

const menuItems = [
  { text: '대시보드', icon: <DashboardIcon />, path: '/admin' },
  { text: '회원관리', icon: <People />, path: '/admin/users' },
  { text: '통계', icon: <BarChart />, path: '/admin/statistics' },
];
```

**변경 후:**
```jsx
import { People, BarChart } from '@mui/icons-material';

const menuItems = [
  { text: '회원관리', icon: <People />, path: '/admin' },
  { text: '통계', icon: <BarChart />, path: '/admin/statistics' },
];
```

**특징:**
- `DashboardIcon` import 제거로 불필요한 의존성 삭제
- 대시보드 menuItem 제거, 사이드바에 회원관리·통계 2개 항목만 표시
- 회원관리의 path를 `/admin/users` → `/admin`으로 변경하여 진입점과 일치

---

### 2. App.jsx 수정

**변경 전:**
```jsx
import { AdminPage, AdminUserPage, AdminStatisticsPage } from './pages/admin';

<Route path="/admin" element={<AdminPage/>} />
```

**변경 후:**
```jsx
import { AdminUserPage, AdminStatisticsPage } from './pages/admin';

<Route path="/admin" element={<AdminUserPage/>} />
```

**특징:**
- `AdminPage` import 제거
- `/admin` 라우트가 `AdminUserPage`를 렌더링하여 접속 즉시 회원관리 화면 표시

---

### 라우트 변경 구조

```
변경 전
/admin            → AdminPage (대시보드)
/admin/users      → AdminUserPage (회원관리)
/admin/statistics → AdminStatisticsPage (통계)

변경 후
/admin            → AdminUserPage (회원관리)
/admin/statistics → AdminStatisticsPage (통계)
```

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업

---

# 업무일지 (4차)

## 기본 정보
- 날짜: 2026-02-15 (일) 13:40~14:00
- 작성자: hexsera

## 진행 목표

`HeaderUserInfo.jsx`의 "페이지 이동" 메뉴 항목을 PRD 요구사항에 맞게 수정한다:
1. role이 'admin'인 사용자에게만 페이지 이동 항목 표시 (API 호출로 role 획득)
2. 어드민 페이지(`/admin`)에 있으면 "메인 페이지 이동", 아니면 "관리 페이지 이동"으로 문구 변경

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| PRD 분석 및 RED 계획 작성 | `PRD/TDD-RED-어드민페이지이동수정-2026-02-15.md` | 테스트 케이스 8개 설계 |
| [RED] 새 요구사항 테스트 8개 작성 | `src/test/HeaderUserInfo.test.jsx` | 6개 실패 확인 완료 |
| [GREEN] HeaderUserInfo 컴포넌트 수정 | `src/components/HeaderUserInfo.jsx` | API 호출 및 조건부 렌더링 구현 |
| [REFACTOR] 테스트 비동기 처리 개선 | `src/test/HeaderUserInfo.test.jsx` | act() 경고 제거 |
| GREEN 완료 보고서 작성 | `PRD/TDD-GREEN-어드민페이지이동수정-2026-02-15.md` | 15개 테스트 전체 통과 |

## 주요 변경 사항

### 1. HeaderUserInfo.jsx 수정

**변경 전:**
```jsx
import { useState } from 'react';
// axios import 없음

function HeaderUserInfo() {
  // role 관련 state 없음

  // 모든 로그인 사용자에게 "페이지 이동" 표시
  <MenuItem onClick={handlePageNavigation}>페이지 이동</MenuItem>
}
```

**변경 후:**
```jsx
import { useState, useEffect } from 'react';
import axios from 'axios';

function HeaderUserInfo() {
  const [role, setRole] = useState(null);

  useEffect(() => {
    if (isLoggedIn && user?.id) {
      axios.get(`/api/v1/users/${user.id}`)
        .then(res => setRole(res.data.role))
        .catch(() => setRole(null));
    }
  }, [isLoggedIn, user?.id]);

  const isAdmin = role === 'admin';
  const pageNavLabel = location.pathname.startsWith('/admin')
    ? '메인 페이지 이동'
    : '관리 페이지 이동';

  // admin에게만 조건부 렌더링
  {isAdmin && (
    <MenuItem onClick={handlePageNavigation}>{pageNavLabel}</MenuItem>
  )}
}
```

**특징:**
- `useEffect`로 컴포넌트 마운트 시 `GET /api/v1/users/{id}` 호출하여 최신 role 획득
- `isAdmin` 변수로 admin 여부 판단, 조건부 렌더링으로 일반 유저에게 항목 숨김
- 현재 경로에 따라 "메인 페이지 이동" / "관리 페이지 이동" 문구 동적 변경

---

### 2. HeaderUserInfo.test.jsx 수정

**추가된 테스트 (8개 신규):**
```
페이지 이동 항목 표시 여부 (role 기반)
  ✓ role이 admin인 사용자는 메뉴에 "관리 페이지 이동"이 표시된다
  ✓ role이 user인 사용자는 메뉴에 페이지 이동 항목이 표시되지 않는다
  ✓ role이 없는 사용자는 메뉴에 페이지 이동 항목이 표시되지 않는다

페이지 이동 문구 변경 (경로 기반)
  ✓ admin 유저가 /admin 경로에 있으면 "메인 페이지 이동"이 표시된다
  ✓ admin 유저가 /admin 경로가 아니면 "관리 페이지 이동"이 표시된다

API로 role 가져오기
  ✓ 컴포넌트 마운트 시 GET /api/v1/users/{id}를 호출한다

페이지 이동 내비게이션
  ✓ "관리 페이지 이동" 클릭 시 /admin으로 이동한다
  ✓ "메인 페이지 이동" 클릭 시 /로 이동한다
```

**REFACTOR - 비동기 처리 개선:**
- 기존 동기 테스트들에 `await waitFor(() => expect(axios.get).toHaveBeenCalled())` 추가
- 컴포넌트 마운트 후 axios 비동기 호출 완료를 기다린 뒤 UI 검증하도록 수정
- act() 경고 완전 제거

---

### 데이터 흐름

```
HeaderUserInfo 마운트
       │
       ▼
GET /api/v1/users/{user.id}
       │
       ├── role: 'admin' → isAdmin = true
       │       │
       │       ├── /admin 경로 → "메인 페이지 이동" 표시
       │       └── 그 외 경로  → "관리 페이지 이동" 표시
       │
       └── role: 'user' 또는 없음 → isAdmin = false
               │
               └── 페이지 이동 MenuItem 숨김
```

## 결과
- 상태: 완료
- 테스트: 15개 전체 통과, act() 경고 없음

## 배운 내용

## 다음 작업

# 업무일지

## 기본 정보
- 날짜: 2026-02-15 (일) 14:20~15:00
- 작성자: hexsera

## 진행 목표

HomePage 메인화면 UI 개선 — 헤더 인증 버튼 통합 및 반응형 레이아웃 적용

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| HeaderUserInfo에 색상 props 추가 | `frontend/src/components/HeaderUserInfo.jsx` | 기존 사용처 영향 없음 |
| HomePage 헤더 로그인/회원가입 버튼 → HeaderUserInfo 교체 | `frontend/src/pages/HomePage/HomePage.jsx` | |
| 헤더 타이틀 모바일 숨김, 게임하기 버튼 좌측 이동 및 contained 스타일 | `frontend/src/pages/HomePage/HomePage.jsx` | |
| Hero 섹션 반응형 처리 (여백, 문구 분기, CTA 버튼 숨김) | `frontend/src/pages/HomePage/HomePage.jsx` | |
| 모든 반응형 브레이크포인트 `md` → `sm` 변경 | `frontend/src/pages/HomePage/HomePage.jsx` | 600px 기준으로 전환 |

## 주요 변경 사항

### 1. HeaderUserInfo.jsx 수정

**변경 전:**
```jsx
function HeaderUserInfo() {
  // ...
  ) : (
    <>
      <Button variant="contained" onClick={() => navigate('/login')}>
        로그인
      </Button>
      <Button variant="outlined" onClick={() => navigate('/Register')}>
        회원가입
      </Button>
    </>
  )}
```

**변경 후:**
```jsx
function HeaderUserInfo({ buttonColor, buttonTextColor, outlinedBorderColor }) {
  // ...
  ) : (
    <>
      <Button
        variant="contained"
        onClick={() => navigate('/login')}
        sx={buttonColor ? { backgroundColor: buttonColor, color: buttonTextColor } : {}}
      >
        로그인
      </Button>
      <Button
        variant="outlined"
        onClick={() => navigate('/Register')}
        sx={buttonColor ? { color: buttonTextColor, borderColor: outlinedBorderColor } : {}}
      >
        회원가입
      </Button>
    </>
  )}
```

**특징:**
- `buttonColor`, `buttonTextColor`, `outlinedBorderColor` props를 추가하여 부모에서 색상 주입 가능
- props가 없으면 MUI 기본 색상을 유지하므로 기존 사용처(Dashboard 등) 영향 없음

---

### 2. HomePage.jsx — 헤더(Toolbar) 구조 변경

**변경 전:**
```jsx
<Toolbar sx={{ justifyContent: 'space-between' }}>
  <Typography variant="h6" sx={{ color: COLORS.text, fontWeight: 'bold' }}>
    🎯 HEXSERA PINBALL
  </Typography>
  <Box>
    <Button onClick={() => navigate('/pinball')} sx={{ color: COLORS.text }}>
      게임하기
    </Button>
    <Button onClick={() => navigate('/login')} variant="outlined"
      sx={{ color: COLORS.text, borderColor: COLORS.text, ml: 1 }}>
      로그인
    </Button>
    <Button onClick={() => navigate('/Register')} variant="contained"
      sx={{ backgroundColor: COLORS.primary, ml: 1 }}>
      회원가입
    </Button>
  </Box>
</Toolbar>
```

**변경 후:**
```jsx
<Toolbar sx={{ justifyContent: 'space-between' }}>
  {/* 좌측: 타이틀 + 게임하기 버튼 */}
  <Box sx={{ display: 'flex', alignItems: 'center', gap: 1 }}>
    <Typography
      variant="h6"
      sx={{
        color: COLORS.text,
        fontWeight: 'bold',
        fontSize: { xs: '0.9rem', sm: '1.25rem' },
        display: { xs: 'none', sm: 'block' },
      }}
    >
      HEXSERA PINBALL
    </Typography>
    <Button
      variant="contained"
      onClick={() => navigate('/pinball')}
      sx={{ backgroundColor: COLORS.primary, color: COLORS.text }}
    >
      게임하기
    </Button>
  </Box>
  {/* 우측: HeaderUserInfo */}
  <HeaderUserInfo
    buttonColor={COLORS.primary}
    buttonTextColor={COLORS.text}
    outlinedBorderColor={COLORS.text}
  />
</Toolbar>
```

**특징:**
- 로그인/회원가입 버튼 3개를 `HeaderUserInfo` 컴포넌트 1개로 대체
- "게임하기" 버튼을 우측에서 좌측(타이틀 오른쪽)으로 이동, `variant="contained"` 적용
- 타이틀은 `sm`(600px) 미만에서 숨김 처리

---

### 3. HomePage.jsx — Hero 섹션 반응형 처리

**변경 전:**
```jsx
<Grid container spacing={4} sx={{ mt: 16 }}>
  <Grid size={{ xs: 12, md: 8 }}>
    <Box sx={{ display: 'flex', flexDirection: 'column', gap: 3, pt: { md: 6 } }}>
      <Typography variant="h3" sx={{ color: COLORS.text, fontWeight: 'bold' }}>
        세상에서 가장 짜릿한<br />핀볼 게임을 경험하세요.
      </Typography>
      <Button variant="contained" size="large" onClick={() => navigate('/pinball')}
        sx={{ backgroundColor: COLORS.primary, width: 'fit-content', px: 4, py: 1.5 }}>
        ▶ 지금 바로 플레이하기
      </Button>
```

**변경 후:**
```jsx
<Grid container spacing={4} sx={{ mt: { xs: 8, sm: 16 } }}>
  <Grid size={{ xs: 12, md: 8 }}>
    <Box sx={{ display: 'flex', flexDirection: 'column', gap: 3, pt: { md: 6 } }}>
      <Typography variant="h3" sx={{ color: COLORS.text, fontWeight: 'bold' }}>
        <Box component="span" sx={{ display: { xs: 'none', sm: 'inline' } }}>
          세상에서 가장 짜릿한<br />핀볼 게임을 경험하세요.
        </Box>
        <Box component="span" sx={{ display: { xs: 'inline', sm: 'none' } }}>
          세상에서<br />가장 짜릿한<br />핀볼게임을<br />체험하세요
        </Box>
      </Typography>
      <Button variant="contained" size="large" onClick={() => navigate('/pinball')}
        sx={{ backgroundColor: COLORS.primary, width: 'fit-content', px: 4, py: 1.5 }}>
        ▶ 지금 바로 플레이하기
      </Button>
```

**특징:**
- Hero 상단 여백 `mt: { xs: 8, sm: 16 }` — 모바일에서 여백 축소
- 소개 문구를 두 `<Box component="span">`으로 분리하여 화면 크기에 따라 전환
- 모든 반응형 기준을 `md`(900px)에서 `sm`(600px)으로 통일

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- 없음

---

# 업무일지 (5차)

## 기본 정보
- 날짜: 2026-02-15 (일) 15:00~15:40
- 작성자: hexsera

## 진행 목표

`Login.jsx`와 `Register.jsx`의 색상 테마를 `HomePage.jsx`와 통일하고 Aurora 애니메이션 배경을 적용한다.

## 진행 내용

| 작업 | 수정 파일 | 비고 |
|------|----------|------|
| PRD 작성 | `PRD/로그인 배경 변경 PRD.md` | 요구사항 문서 |
| 실행계획 작성 | `PRD/로그인 배경 변경-실행계획.md` | 구현 계획 |
| Login.jsx Aurora 배경 및 dark 테마 적용 | `src/pages/Login/Login.jsx` | COLORS 정의, Aurora import 포함 |
| Login.jsx 로고 xs 숨김 처리 | `src/pages/Login/Login.jsx` | 모바일 대응 |
| Register.jsx Aurora 배경 및 dark 테마 적용 | `src/pages/Register/Register.jsx` | Login과 동일 테마, textFieldSx 공통 객체 추가 |

## 주요 변경 사항

### 1. Login.jsx 수정

**변경 전:**
```jsx
// import 없음 (Aurora)

return (
  <Box sx={{ minHeight: '100vh', backgroundColor: '#87CEEB', ... }}>
    <Container maxWidth="sm">
      <Paper sx={{ padding: 4, backgroundColor: 'white', borderRadius: 2 }}>
        <CardMedia sx={{ marginBottom: 2, objectFit: 'contain' }} />
        <Typography component="h1" variant="h5">로그인</Typography>
        <TextField fullWidth sx={{ mt: 1 }} ... />
        <Button variant="contained" sx={{ width: '300px' }}>로그인</Button>
        <Button variant="outlined" sx={{ width: '300px' }}>회원가입</Button>
      </Paper>
    </Container>
  </Box>
);
```

**변경 후:**
```jsx
import Aurora from '../../components/Aurora/Aurora';

const COLORS = {
  bg: '#0F172A',
  card: 'rgba(30, 41, 59, 0.85)',
  border: '#334155',
  text: '#F1F5F9',
  subText: '#94A3B8',
  primary: '#4F46E5',
};

return (
  <Box sx={{ position: 'relative', minHeight: '100vh', backgroundColor: COLORS.bg, ... }}>
    {/* Aurora 배경 (z-index: 0) */}
    <Box sx={{ position: 'absolute', inset: 0, zIndex: 0, overflow: 'hidden' }}>
      <Aurora colorStops={['#467ee5', '#7C3AED', '#908aff']} amplitude={1.2} speed={1.2} blend={0.35} />
    </Box>
    {/* 콘텐츠 (z-index: 1) */}
    <Box sx={{ position: 'relative', zIndex: 1, ... }}>
      <Paper sx={{ backgroundColor: COLORS.card, border: `1px solid ${COLORS.border}` }}>
        <CardMedia sx={{ display: { xs: 'none', sm: 'block' }, ... }} />
        <Typography sx={{ color: COLORS.text }}>로그인</Typography>
        <TextField sx={{ '& .MuiOutlinedInput-root': { color: COLORS.text, ... } }} />
        <Button sx={{ backgroundColor: COLORS.primary, color: COLORS.text }}>로그인</Button>
        <Button variant="outlined" sx={{ borderColor: COLORS.text, color: COLORS.text }}>회원가입</Button>
      </Paper>
    </Box>
  </Box>
);
```

**특징:**
- 배경색 `#87CEEB`(하늘색) → `#0F172A`(dark navy)로 변경
- Aurora WebGL 애니메이션 배경 레이어 추가 (z-index 0), 콘텐츠는 z-index 1로 분리
- Paper 카드를 반투명(`rgba(30, 41, 59, 0.85)`) dark 스타일로 변경, border 추가
- TextField 테두리·텍스트·placeholder 색상 dark 테마 적용
- 로고 이미지 xs 화면에서 숨김 처리 (`display: { xs: 'none', sm: 'block' }`)

---

### 2. Register.jsx 수정

**변경 전:**
```jsx
// Aurora import 없음

return (
  <Box sx={{ minHeight: '100vh', backgroundColor: '#87CEEB', ... }}>
    <Paper sx={{ backgroundColor: 'white' }}>
      <Typography component="h1" variant="h5">회원가입</Typography>
      <TextField sx={{ mt: 1 }} label="이메일" />
      <Button variant="contained" sx={{ width: '300px' }}>다음</Button>
    </Paper>
  </Box>
);
```

**변경 후:**
```jsx
import Aurora from '../../components/Aurora/Aurora';

const COLORS = { /* Login과 동일 */ };

const textFieldSx = {
  mt: 1,
  '& .MuiOutlinedInput-root': { color: COLORS.text, ... },
  '& .MuiInputLabel-root': { color: COLORS.subText },
  '& .MuiInputLabel-root.Mui-focused': { color: COLORS.primary },
};

return (
  <Box sx={{ position: 'relative', minHeight: '100vh', backgroundColor: COLORS.bg, ... }}>
    <Box sx={{ position: 'absolute', inset: 0, zIndex: 0, overflow: 'hidden' }}>
      <Aurora colorStops={['#467ee5', '#7C3AED', '#908aff']} amplitude={1.2} speed={1.2} blend={0.35} />
    </Box>
    <Box sx={{ position: 'relative', zIndex: 1, ... }}>
      <Paper sx={{ backgroundColor: COLORS.card, border: `1px solid ${COLORS.border}` }}>
        <CardMedia sx={{ display: { xs: 'none', sm: 'block' } }} />
        {step[registerStep]}
      </Paper>
    </Box>
  </Box>
);
```

**특징:**
- Login.jsx와 동일한 COLORS, Aurora 구조 적용
- Register에는 MUI `label` prop이 있어 `textFieldSx` 공통 객체에 `MuiInputLabel` 색상도 포함
- `textFieldSx` 객체를 컴포넌트 외부에 정의하여 3개 단계(이메일/비밀번호/생년월일)에서 재사용
- 로고 이미지 xs 숨김 처리 및 주석 처리된 로고 코드 정식 추가

---

### 페이지 구조 변경

```
변경 전
Login / Register
└── Box (backgroundColor: '#87CEEB')
    └── Paper (backgroundColor: 'white')
        └── 폼 콘텐츠

변경 후
Login / Register
└── Box (position: relative, backgroundColor: '#0F172A')
    ├── Box (position: absolute, z-index: 0)
    │   └── Aurora (WebGL 애니메이션 배경)
    └── Box (position: relative, z-index: 1)
        └── Paper (backgroundColor: rgba(30,41,59,0.85), border)
            └── 폼 콘텐츠 (dark 테마)
```

## 결과
- 상태: 완료

## 배운 내용

## 다음 작업
- 없음
