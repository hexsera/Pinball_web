## 목적
친구 추가 요청 api 엔드포인트를 생성한다.

## 이유
사용자간의 커뮤니케이션을 위해 친구 네트워크가 필요하기 때문에.

## 구조
/api/friend-requests 로 친구 추가 요청을 보낸다.

body 에 리퀘스트 스키마 id(친구 요청 id), requester_id(친구 요청 받는 id) 가 있어야한다.
테이블을 생성하지 않는다.
보안을 고려하지 않는다.
검증 로직을 고려하지 않는다.
api 내부 로직은 매우 간단하게 print("요청 받음, id -> requeter_id ") 형태로 구현한다. 